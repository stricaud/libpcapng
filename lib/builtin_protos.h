/* Auto-generated by embed_protos.py — do not edit. */
#ifndef BUILTIN_PROTOS_H
#define BUILTIN_PROTOS_H

static const char * const g_builtin_posa_protos[] = {
    /* 00-builtin-rules.posa */
    "# 00-builtin-rules.posa — dispatch bindings for the bundled decoders that ship\n# without their own `rule` lines. Kept in one file so the standard port /\n# ip.proto / ethertype bindings are easy to review in one place. Rules resolve\n# by protocol name at dispatch time, so load order does not matter.\n#\n# Intentionally NOT bound here (the built-in C dissector owns them):\n#   ethertype 0x0800 IPv4, 0x86DD IPv6, 0x0806 ARP, 0x8100 VLAN\n#   ip.proto  1 ICMP, 6 TCP, 17 UDP, 47 GRE\n# App-layer protocols (DNS, HTTP, TLS, SMB, MySQL, TFTP, DHCP, IGMP, RDP) carry\n# their own rules in their .posa files.\n\n# ── carried directly over IP (ip.proto) ─────────────────────────────────────\nrule ip.proto == 51  => AH\nrule ip.proto == 50  => ESP\nrule ip.proto == 33  => DCCP\nrule ip.proto == 46  => RSVP\nrule ip.proto == 58  => ICMPv6\nrule ip.proto == 88  => EIGRP\nrule ip.proto == 89  => OSPF\nrule ip.proto == 103 => PIM\nrule ip.proto == 112 => VRRP\nrule ip.proto == 115 => L2TP\nrule ip.proto == 132 => SCTP\n\n# ── UDP ports ────────────────────────────────────────────────────────────────\nrule udp.port == 88    => KRB5\nrule udp.port == 137   => NBNS\nrule udp.port == 319   => PTPv2\nrule udp.port == 320   => PTPv2\nrule udp.port == 500   => ISAKMP\nrule udp.port == 520   => RIPv2\nrule udp.port == 546   => DHCPv6\nrule udp.port == 547   => DHCPv6\nrule udp.port == 1701  => L2TP\nrule udp.port == 1985  => HSRP\nrule udp.port == 3478  => STUN\nrule udp.port == 3784  => BFD\nrule udp.port == 4500  => ISAKMP\nrule udp.port == 5683  => CoAP\nrule udp.port == 20000 => DNP3\nrule udp.port == 2055  => NetFlowV5\nrule udp.port == 9996  => NetFlowV5\nrule udp.port == 47808 => BACnet\n\n# ── TCP ports ────────────────────────────────────────────────────────────────\nrule tcp.port == 88    => KRB5\nrule tcp.port == 179   => BGP\nrule tcp.port == 502   => ModbusTCP\nrule tcp.port == 1080  => SOCKS5_HELLO\nrule tcp.port == 1883  => MQTT\nrule tcp.port == 5672  => AMQP\nrule tcp.port == 6633  => OpenFlow\nrule tcp.port == 6653  => OpenFlow\nrule tcp.port == 20000 => DNP3\n\n# ── content signatures (any port) ────────────────────────────────────────────\n# Recognise a protocol by what its payload starts with, not by port. These\n# target the built-in C dissectors, so they work on non-standard ports too.\n# SSH always opens with its ASCII version banner (\"SSH-2.0-…\", \"SSH-1.99-…\").\nrule tcp.content \"SSH-\" => SSH\n\n# ── below IP, at the ethernet layer (ethertype) ──────────────────────────────\nrule eth.type == 0x8847 => MPLS\nrule eth.type == 0x8848 => MPLS\nrule eth.type == 0x8863 => PPPoE\nrule eth.type == 0x8864 => PPPoE\nrule eth.type == 0x888e => EAPOL\nrule eth.type == 0x8809 => LACP\nrule eth.type == 0x0842 => WOL\n",
    /* activision.posa */
    "# activision.posa — Activision game traffic (Call of Duty, Warzone, etc.)\n#\n# Activision's UDP game protocol carries two variants, distinguished by the\n# first two bytes (big-endian magic):\n#\n#   0x0C02 / 0x0D02 — original variant; 0x0C02 is client→server, 0x0D02 is\n#                     server→client on the first packet.  Subsequent packets\n#                     use 0x29 (client) or 0x28 (server) as the first byte.\n#\n#   0x4600 / 0x4700 — newer protocol-level direction markers.\n#\n# Beyond the direction/type marker the payload is proprietary and largely\n# encrypted; only the marker byte is decodable.\n#\n# Minimum packet length: 18 bytes.  Detection requires >4 packets matching\n# the pattern before classification (nDPI heuristic; see activision.c).\n#\n# Runs on UDP, no fixed port — detected by packet content only.\n\nObject<main> ACTIVISION\n    abbrev \"activision\"\n    col \"Activision\"\n\n    uint8 packet_type \"Packet Type\" defaults(0x0C)\n        0x0C = \"Client Request (0x0C02 variant)\"\n        0x0D = \"Server Reply (0x0D02 variant)\"\n        0x29 = \"Client Subsequent\"\n        0x28 = \"Server Subsequent\"\n        0x46 = \"Client (0x4600 variant)\"\n        0x47 = \"Server (0x4700 variant)\"\n\n    payload body \"Body\"\n\n    info \"Activision type=0x%02x\" packet_type\n\nweak rule udp.content \"\\x0c\\x02\" => ACTIVISION\nweak rule udp.content \"\\x0d\\x02\" => ACTIVISION\nweak rule udp.content \"\\x46\\x00\" => ACTIVISION\nweak rule udp.content \"\\x47\\x00\" => ACTIVISION\n\ncolor activision.packet_type == 0x0C => black lightyellow\ncolor activision.packet_type == 0x0D => black lightblue\ncolor activision.packet_type == 0x46 => black lightyellow\ncolor activision.packet_type == 0x47 => black lightblue\ncolor activision => white darkblue\n",
    /* ah.posa */
    "# AH header (RFC 4302) — 12 bytes (standard, with 96-bit ICV)\n# IP protocol 51. payload_len in 32-bit words minus 2; value 4 = 12-byte fixed header + 96-bit ICV.\n# next_header is the protocol of the payload protected by AH.\nObject<main> AH\n    uint8  next_header defaults(59)\n        TCP    = 6\n        UDP    = 17\n        ICMP   = 1\n        ICMPV6 = 58\n        ESP    = 50\n        NONEXT = 59\n    uint8  payload_len defaults(4)\n    uint16 reserved defaults(0)\n    uint32 spi defaults(0)\n    uint32 seq defaults(1)\n",
    /* amqp.posa */
    "# amqp.posa — Advanced Message Queuing Protocol (versions 0-9-1 and 1.0)\n# AMQP 0-9-1 is the wire protocol used by RabbitMQ, Apache Qpid, and Azure Service Bus.\n# AMQP 1.0 is the ISO/IEC 19464:2014 standard adopted by ActiveMQ Artemis, Azure, etc.\n#\n# Standard TCP ports:\n#   5672  — plaintext AMQP\n#   5671  — TLS-wrapped AMQP (AMQPS)\n#\n# Connection lifecycle (0-9-1):\n#   1. Client sends 8-byte protocol header: \"AMQP\" + protocol_id + major + minor + revision\n#   2. Server replies with Connection.Start method frame (or its own protocol header)\n#   3. Both sides exchange method frames, content header frames, body frames, heartbeats\n#\n# Frame structure (AMQP 0-9-1, all integers big-endian):\n#   [0]    frame_type  uint8   1=METHOD 2=HEADER 3=BODY 4=HEARTBEAT\n#   [1-2]  channel     uint16  0 = connection-level, 1+ = session channels\n#   [3-6]  payload_size uint32 byte count of payload that follows\n#   [7..]  payload     varies  METHOD: class_id(2)+method_id(2)+args\n#                              HEADER: class_id(2)+weight(2)+body_size(8)+properties\n#                              BODY:   raw message content chunk\n#                              HEARTBEAT: empty (payload_size = 0)\n#   [end]  frame_end   uint8   must be 0xCE; mismatches indicate framing errors\n#\n# Method frame class IDs (0-9-1):\n#   10=CONNECTION  20=CHANNEL  30=EXCHANGE  40=QUEUE  50=BASIC  60=TX  90=CONFIRM\n#\n# AMQP 1.0 uses a different framing format (type codes, performatives, AMQP types);\n# this decoder recognises the 1.0 protocol header and labels the connection but does\n# not attempt to parse 1.0 frames, which require a type-code-driven decoder.\n\n# ── Protocol header group ─────────────────────────────────────────────────────\n# Dispatched on the first eight bytes; the two most common versions get their\n# own sub-object so the minor/revision display reads correctly for each.\n# A third catch-all sub-object matches any remaining \"AMQP\" prefix.\n\nObject<GROUP> AMQP_HEADER\n\n# ─── AMQP 0-9-1 protocol header ──────────────────────────────────────────────\n# Sent by the client immediately on TCP connect to negotiate protocol version.\n# The server responds with Connection.Start (method frame) on the same channel 0.\n\nObject<AMQP_HEADER> AMQP_091_HEADER\n    abbrev \"amqp\"\n    col    \"AMQP\"\n    starts \"AMQP\\x00\\x00\\x09\\x01\"\n\n    bytes<4> magic \"Protocol Magic\" defaults(\"AMQP\")\n    uint8 protocol_id \"Protocol ID\" defaults(0)\n        0 = \"AMQP\"\n        1 = \"TLS\"\n        2 = \"SASL\"\n        3 = \"SASL-AMQP\"\n    uint8 major \"Major Version\" defaults(0)\n    uint8 minor \"Minor Version\" defaults(9)\n    uint8 revision \"Revision\" defaults(1)\n\n    info \"AMQP 0-9-1 Header  protocol-id=%u  v%u.%u.%u\" protocol_id, major, minor, revision\n\n# ─── AMQP 1.0 protocol header ─────────────────────────────────────────────────\n# ISO/IEC 19464:2014.  Uses a completely different framing format from 0-9-1;\n# only the 8-byte handshake header is decoded structurally here.\n\nObject<AMQP_HEADER> AMQP_10_HEADER\n    abbrev \"amqp\"\n    col    \"AMQP\"\n    starts \"AMQP\\x00\\x01\\x00\\x00\"\n\n    bytes<4> magic \"Protocol Magic\" defaults(\"AMQP\")\n    uint8 protocol_id \"Protocol ID\" defaults(0)\n        0 = \"AMQP\"\n        1 = \"TLS\"\n        2 = \"SASL\"\n    uint8 major \"Major Version\" defaults(1)\n    uint8 minor \"Minor Version\" defaults(0)\n    uint8 revision \"Revision\" defaults(0)\n\n    info \"AMQP 1.0 Header  protocol-id=%u  v%u.%u.%u\" protocol_id, major, minor, revision\n\n# ─── Generic AMQP protocol header (unknown version) ───────────────────────────\n# Catches any AMQP version whose exact header bytes were not listed above.\n\nObject<AMQP_HEADER> AMQP_GENERIC_HEADER\n    abbrev \"amqp\"\n    col    \"AMQP\"\n    starts \"AMQP\"\n\n    bytes<4> magic \"Protocol Magic\" defaults(\"AMQP\")\n    uint8 protocol_id \"Protocol ID\" defaults(0)\n        0 = \"AMQP\"\n        1 = \"TLS\"\n        2 = \"SASL\"\n        3 = \"SASL-AMQP\"\n    uint8 major \"Major Version\" defaults(0)\n    uint8 minor \"Minor Version\" defaults(9)\n    uint8 revision \"Revision\" defaults(1)\n\n    info \"AMQP Header  protocol-id=%u  v%u.%u.%u\" protocol_id, major, minor, revision\n\n# ── AMQP 0-9-1 frame decoder ──────────────────────────────────────────────────\n# Decodes the repeating frame stream that follows the protocol header exchange.\n# All integers are in network byte order (big-endian); no little-endian fields.\n\nObject<main> AMQP\n    abbrev \"amqp\"\n    col    \"AMQP\"\n\n    # Frame type — upper 3 types are the data plane; HEARTBEAT is a keepalive.\n    uint8 frame_type \"Frame Type\" defaults(1)\n        1 = \"METHOD\"\n        2 = \"HEADER\"\n        3 = \"BODY\"\n        4 = \"HEARTBEAT\"\n\n    # Channel 0 is reserved for connection-level communication; channels 1–65535\n    # are session channels negotiated during Connection.Open / Channel.Open.\n    uint16 channel \"Channel\" defaults(0)\n    uint32 payload_size \"Payload Size\" defaults(0)\n\n    # ── METHOD frame payload ───────────────────────────────────────────────────\n    # Payload = class_id (2) + method_id (2) + method-specific arguments.\n    # The scope bounds class_id + method_id + method_args to exactly payload_size bytes.\n    when frame_type == 1:\n        scope payload_size\n            uint16 class_id \"Class ID\" defaults(10)\n                10  = \"CONNECTION\"\n                20  = \"CHANNEL\"\n                30  = \"EXCHANGE\"\n                40  = \"QUEUE\"\n                50  = \"BASIC\"\n                60  = \"TX\"\n                90  = \"CONFIRM\"\n\n            # method_id meaning depends on class_id (e.g. 10=start for CONNECTION,\n            # 10=open for CHANNEL).  The numeric value is always shown; a future\n            # when/class branch could decode it symbolically per class.\n            uint16 method_id \"Method ID\" defaults(10)\n\n            # Remaining bytes are class- and method-specific argument fields\n            # encoded in the AMQP type system (tables, short-strings, longs, etc.).\n            payload method_args \"Method Arguments\"\n\n    # ── HEADER (content header) frame payload ─────────────────────────────────\n    # Carries the class_id of the following body, the full body size across all\n    # BODY frames, and a property bitmask followed by present property values.\n    when frame_type == 2:\n        scope payload_size\n            uint16 class_id \"Class ID\" defaults(10)\n                10  = \"CONNECTION\"\n                20  = \"CHANNEL\"\n                30  = \"EXCHANGE\"\n                40  = \"QUEUE\"\n                50  = \"BASIC\"\n                60  = \"TX\"\n                90  = \"CONFIRM\"\n\n            # weight is always 0 in AMQP 0-9-1 (reserved for future use).\n            uint16 weight \"Weight\" defaults(0)\n            # body_size is the total byte count across ALL subsequent BODY frames\n            # for this message, allowing receivers to reassemble large messages.\n            uint64 body_size \"Body Size\" defaults(0)\n            # property flags (16-bit bitmask) and present property fields follow.\n            payload header_props \"Header Properties\"\n\n    # ── BODY frame payload ─────────────────────────────────────────────────────\n    # Raw application message bytes; one message may span multiple BODY frames.\n    # The total byte count across all BODY frames equals body_size from the\n    # preceding HEADER frame.\n    when frame_type == 3:\n        bytes[payload_size] body \"Message Body\"\n\n    # ── HEARTBEAT frame ────────────────────────────────────────────────────────\n    # payload_size is always 0; no payload bytes follow.  Sent periodically on\n    # channel 0 to keep the connection alive across idle periods or NAT timeouts.\n    # (No payload field needed — payload_size == 0 means nothing to decode.)\n\n    # Frame-end marker: 0xCE must terminate every frame.  A mismatch indicates\n    # framing corruption, a buggy broker, or a non-AMQP connection.\n    uint8 frame_end \"Frame End\" defaults(0xCE)\n\n    info \"AMQP %s  ch=%u  size=%u\" frame_type, channel, payload_size\n\n# ── Content detection rules (primary — port-independent) ──────────────────────\n# Exact 8-byte version strings provide the most reliable signatures; the shorter\n# \"AMQP\" rule is listed last as a broader fallback for unknown version headers.\n# Content rules always fire before port rules so the protocol is identified even\n# on non-standard ports.\n\nrule tcp.content \"AMQP\\x00\\x00\\x09\\x01\" => AMQP\nrule tcp.content \"AMQP\\x00\\x01\\x00\\x00\" => AMQP\nrule tcp.content \"AMQP\" => AMQP\n\n# ── Port rules (secondary — hint when content rules did not match) ─────────────\nrule tcp.port == 5672 => AMQP\nrule tcp.port == 5671 => AMQP\n\n# ── Coloring rules ─────────────────────────────────────────────────────────────\n\n# Framing errors — frame_end byte is not 0xCE.  Most urgent, listed first.\ncolor amqp.frame_end != 0xCE => white red\n\n# METHOD frames — client/server control exchange (requests and replies);\n# yellow signals active protocol negotiation (Connection.*, Channel.*, etc.).\ncolor amqp.frame_type == 1 => black lightyellow\n\n# HEADER (content header) frames — accompanies BASIC.Publish / BASIC.Deliver;\n# yellow shares the method palette because headers arrive with method frames.\ncolor amqp.frame_type == 2 => black lightyellow\n\n# BODY frames — application message payload; dark blue for bulk data plane traffic.\ncolor amqp.frame_type == 3 => white darkblue\n\n# HEARTBEAT frames — connection keepalive probes; gray (background, not data).\ncolor amqp.frame_type == 4 => black lightgray\n\n# Catch-all — any AMQP frame not matched above (e.g. unknown frame types).\ncolor amqp => white darkgray\n\n# ── Wireshark-compatible aliases ───────────────────────────────────────────────\n# Map the field names used in Wireshark's AMQP dissector to the POSA abbrevs so\n# that display filters written for Wireshark continue to work without modification.\n\n# Core frame header fields\nalias amqp.type          => amqp.frame_type\nalias amqp.channel       => amqp.channel\nalias amqp.payload.size  => amqp.payload_size\nalias amqp.frame_end     => amqp.frame_end\n\n# Method / content-header payload fields\nalias amqp.class         => amqp.class_id\nalias amqp.method        => amqp.method_id\nalias amqp.weight        => amqp.weight\nalias amqp.body_size     => amqp.body_size\n\n# Protocol header fields (version negotiation)\nalias amqp.protocol.id       => amqp.protocol_id\nalias amqp.protocol.version.major => amqp.major\nalias amqp.protocol.version.minor => amqp.minor\nalias amqp.protocol.version.revision => amqp.revision\n\n# Convenience macro aliases\nalias amqp.is_method     => amqp.frame_type == 1\nalias amqp.is_header     => amqp.frame_type == 2\nalias amqp.is_body       => amqp.frame_type == 3\nalias amqp.is_heartbeat  => amqp.frame_type == 4\nalias amqp.connection    => amqp.class_id == 10\nalias amqp.basic         => amqp.class_id == 50\n",
    /* bacnet.posa */
    "# BACnet/IP — BVLL header (ASHRAE 135)\n# UDP port 47808 (0xBAC0). bvlc_type is always 0x81 for BACnet/IP.\n# length includes the 4-byte BVLL header itself.\nObject<main> BACnet\n    uint8  bvlc_type defaults(0x81)\n    uint8  function defaults(0x0A)\n        RESULT                   = 0x00\n        WRITE_BDT                = 0x01\n        READ_BDT                 = 0x02\n        READ_BDT_ACK             = 0x03\n        FORWARDED_NPDU           = 0x04\n        REGISTER_FD              = 0x05\n        READ_FDT                 = 0x06\n        READ_FDT_ACK             = 0x07\n        DELETE_FDT_ENTRY         = 0x08\n        DISTRIBUTE_BCAST_TO_NET  = 0x09\n        ORIGINAL_UNICAST_NPDU    = 0x0A\n        ORIGINAL_BCAST_NPDU      = 0x0B\n    uint16 length defaults(4)\n",
    /* bfd.posa */
    "# BFD Control packet (RFC 5880) — 24-byte mandatory header\n# UDP port 3784 (single-hop) or 4784 (multi-hop).\n# vers_diag: Ver(3)=1 | Diag(5). sta_flags: State(2) | P|F|C|A|D|M flags.\n# Intervals are in microseconds. Auth section (if any) follows the mandatory header.\nObject<main> BFD\n    uint8  vers_diag defaults(0x20)\n        DIAG_NO_DIAG            = 0x20\n        DIAG_CTRL_DETECT_EXPIRY = 0x21\n        DIAG_ECHO_FAILED        = 0x22\n        DIAG_NEIGHBOR_SIGNALED  = 0x23\n        DIAG_FWD_PLANE_RESET    = 0x24\n        DIAG_PATH_DOWN          = 0x25\n        DIAG_CONCAT_PATH_DOWN   = 0x26\n        DIAG_ADMIN_DOWN         = 0x27\n        DIAG_REVERSE_CONCAT     = 0x28\n    uint8  sta_flags defaults(0xC0)\n        STATE_ADMIN_DOWN = 0x00\n        STATE_DOWN       = 0x40\n        STATE_INIT       = 0x80\n        STATE_UP         = 0xC0\n    uint8  detect_mult defaults(3)\n    uint8  length defaults(24)\n    uint32 my_discriminator defaults(1)\n    uint32 your_discriminator defaults(0)\n    uint32 desired_min_tx defaults(300000)\n    uint32 required_min_rx defaults(300000)\n    uint32 required_min_echo defaults(0)\n",
    /* bgp.posa */
    "# BGP — Border Gateway Protocol (RFC 4271)\n# Carried over TCP port 179. Every message starts with a 19-byte fixed header:\n# a 16-byte all-0xFF marker (framing/auth), a 2-byte big-endian total length,\n# and a 1-byte message type. The all-ones marker makes BGP uniquely detectable\n# on any port without relying on well-known port 179.\n#\n# Message types:\n#   1 OPEN          — session establishment: AS, router-ID, hold-timer, capabilities\n#   2 UPDATE        — route advertisement and/or withdrawal\n#   3 NOTIFICATION  — error report; session always closed after sending\n#   4 KEEPALIVE     — empty-body heartbeat (total length exactly 19)\n#   5 ROUTE_REFRESH — re-advertisement request for an AFI/SAFI (RFC 2918)\n\nObject<main> BGP\n    abbrev \"bgp\"\n    col \"BGP\"\n\n    # ── Fixed 19-byte message header ──────────────────────────────────────────\n    bytes<16> marker \"Marker\" defaults(\"\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\")\n    uint16    length \"Length\" defaults(19)\n    uint8     type \"Type\" defaults(4)\n        OPEN                 = 1\n        UPDATE               = 2\n        NOTIFICATION         = 3\n        KEEPALIVE            = 4\n        ROUTE_REFRESH        = 5\n        ROUTE_REFRESH_CISCO  = 128\n\n    # ── OPEN body (type == 1) ─────────────────────────────────────────────────\n    # Carries the speaker's BGP version (always 4), its 2-byte AS number\n    # (or 23456 AS_TRANS when a 4-byte AS is signalled via capability),\n    # the hold-time in seconds (0 or >= 3), and the BGP identifier (router-ID).\n    # Optional parameters (capability TLVs) follow as a length-prefixed block.\n    when type == 1:\n        uint8  version          \"Version\"                    defaults(4)\n        uint16 my_as            \"My AS\"                      defaults(65000)\n        uint16 hold_time        \"Hold Time\"                  defaults(180)\n        ip4    bgp_id           \"BGP Identifier\"\n        uint8  opt_param_length \"Optional Parameters Length\" defaults(0)\n        payload opt_params      \"Optional Parameters\"\n\n    # ── UPDATE body (type == 2) ───────────────────────────────────────────────\n    # Withdrawn routes come first as a length-prefixed list of IP prefixes.\n    # Path attributes follow in their own length-prefixed block (type-flags-len-value).\n    # Any remaining bytes after the path attribute block are NLRI prefixes\n    # being advertised for the implicit IPv4 unicast AFI/SAFI.\n    when type == 2:\n        uint16                        withdrawn_routes_length \"Withdrawn Routes Length\" defaults(0)\n        bytes[withdrawn_routes_length] withdrawn              \"Withdrawn Routes\"\n        uint16                        path_attr_length        \"Path Attributes Length\"  defaults(0)\n        bytes[path_attr_length]        path_attrs             \"Path Attributes\"\n        payload nlri                                          \"NLRI\"\n\n    # ── NOTIFICATION body (type == 3) ─────────────────────────────────────────\n    # Error code identifies the category; error subcode refines it.\n    # The optional data field carries diagnostic context (e.g. the offending header).\n    when type == 3:\n        uint8  error_code \"Error Code\" defaults(6)\n            MESSAGE_HEADER_ERROR = 1\n            OPEN_MESSAGE_ERROR   = 2\n            UPDATE_MESSAGE_ERROR = 3\n            HOLD_TIMER_EXPIRED   = 4\n            FSM_ERROR            = 5\n            CEASE                = 6\n        uint8  error_subcode \"Error Subcode\" defaults(0)\n        payload data         \"Data\"\n\n    # ── KEEPALIVE body (type == 4) — empty ───────────────────────────────────\n    # No fields after the header. Total message length is always 19 bytes.\n\n    # ── ROUTE-REFRESH body (type == 5, RFC 2918) ──────────────────────────────\n    # Requests the peer to re-advertise all routes for the specified AFI/SAFI.\n    when type == 5:\n        uint16 afi \"Address Family Identifier\" defaults(1)\n            IPv4 = 1\n            IPv6 = 2\n        uint8  res \"Reserved\" defaults(0)\n        uint8  safi \"Subsequent AFI\" defaults(1)\n            UNICAST   = 1\n            MULTICAST = 2\n            MPLS      = 4\n\n    # ── Info column ───────────────────────────────────────────────────────────\n    # type always expands (%s shows enum name). Conditional fields that are\n    # absent for a given message type expand to nothing; gaps close automatically.\n    info \"%s AS=%u BGP-ID=%s Err=%u/%u\" type, my_as, bgp_id, error_code, error_subcode\n\n\n# ── Detection rules ───────────────────────────────────────────────────────────\n# Content rule first: the 16-byte all-0xFF marker is unique to BGP and allows\n# detection on non-standard ports (route reflectors, multi-hop, lab setups).\nrule tcp.content \"\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\\xff\" => BGP\n# Standard BGP port (RFC 4271 §1). Listed after content rule per convention.\nrule tcp.port == 179 => BGP\n\n\n# ── Coloring rules ────────────────────────────────────────────────────────────\n# OPEN — session setup / capability negotiation (yellow: bidirectional handshake)\ncolor bgp.type == 1 => black lightyellow\n\n# UPDATE — route advertisement or withdrawal (green: reachability data plane)\ncolor bgp.type == 2 => black lightgreen\n\n# NOTIFICATION — protocol error; session teardown imminent (red: alert)\ncolor bgp.type == 3 => white red\n\n# KEEPALIVE — periodic heartbeat, no routing content (gray: management traffic)\ncolor bgp.type == 4 => black lightgray\n\n# ROUTE-REFRESH — capability exchange / re-advertisement request (blue: control)\ncolor bgp.type == 5 => white darkblue\n\n# Catch-all — any BGP frame not matched by a type-specific rule above\ncolor bgp => black lightgray\n\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\nalias bgp.type                       => bgp.type\nalias bgp.open.version               => bgp.version\nalias bgp.open.as                    => bgp.my_as\nalias bgp.open.holdtime              => bgp.hold_time\nalias bgp.open.identifier            => bgp.bgp_id\nalias bgp.open.opt_param_len         => bgp.opt_param_length\nalias bgp.update.withdrawn_length    => bgp.withdrawn_routes_length\nalias bgp.update.path_attr.length    => bgp.path_attr_length\nalias bgp.notification.error_code    => bgp.error_code\nalias bgp.notification.error_subcode => bgp.error_subcode\n",
    /* bitcoin.posa */
    "# bitcoin.posa — Bitcoin P2P network protocol\n#\n# Every Bitcoin message begins with a 24-byte header: a 4-byte network magic\n# (identifies mainnet vs testnet), a 12-byte null-padded ASCII command name,\n# a 4-byte little-endian payload length, and a 4-byte checksum (first 4 bytes\n# of SHA256(SHA256(payload))).  The payload follows immediately.\n#\n# Reference: https://developer.bitcoin.org/reference/p2p_networking.html\n#\n# Runs on TCP port 8333 (mainnet), 18333 (testnet), 18444 (regtest).\n\nLookup BitcoinCommands\n    \"version\"    = \"Version Handshake\"\n    \"verack\"     = \"Version Acknowledge\"\n    \"addr\"       = \"Address Advertisement\"\n    \"inv\"        = \"Inventory\"\n    \"getdata\"    = \"Get Data\"\n    \"notfound\"   = \"Not Found\"\n    \"getblocks\"  = \"Get Blocks\"\n    \"getheaders\" = \"Get Headers\"\n    \"tx\"         = \"Transaction\"\n    \"block\"      = \"Block\"\n    \"headers\"    = \"Block Headers\"\n    \"getaddr\"    = \"Get Addresses\"\n    \"mempool\"    = \"Mempool Request\"\n    \"ping\"       = \"Ping\"\n    \"pong\"       = \"Pong\"\n    \"reject\"     = \"Reject\"\n    \"filterload\" = \"Bloom Filter Load\"\n    \"filteradd\"  = \"Bloom Filter Add\"\n    \"filterclear\" = \"Bloom Filter Clear\"\n    \"merkleblock\" = \"Merkle Block\"\n    \"sendheaders\" = \"Send Headers (BIP130)\"\n    \"feefilter\"  = \"Fee Filter (BIP133)\"\n    \"sendcmpct\"  = \"Send Compact (BIP152)\"\n    \"cmpctblock\" = \"Compact Block (BIP152)\"\n    \"getblocktxn\" = \"Get Block Transactions\"\n    \"blocktxn\"   = \"Block Transactions\"\n\nObject<main> BITCOIN\n    abbrev \"bitcoin\"\n    col \"Bitcoin\"\n\n    uint32 magic hex \"Network Magic\" defaults(0xF9BEB4D9)\n        0xF9BEB4D9 = \"Mainnet\"\n        0xFABFB5DA = \"Testnet\"\n        0x0B110907 = \"Testnet3\"\n        0x0A03CF40 = \"Signet\"\n        0xF9BEB4FE = \"Namecoin\"\n\n    str<12> command lookup BitcoinCommands \"Command\" defaults(\"version\")\n\n    le_uint32 payload_length \"Payload Length\" defaults(0)\n    uint32 checksum hex \"Checksum\" defaults(0x5DF6E0E2)\n\n    bytes[payload_length] payload \"Payload\"\n\n    info \"%s magic=0x%08x len=%u\" command, magic, payload_length\n\nrule tcp.port == 8333  => BITCOIN\nrule tcp.port == 18333 => BITCOIN\nrule tcp.port == 18444 => BITCOIN\n\nrule tcp.content \"\\xf9\\xbe\\xb4\\xd9\" => BITCOIN\nrule tcp.content \"\\xfa\\xbf\\xb5\\xda\" => BITCOIN\nrule tcp.content \"\\x0b\\x11\\x09\\x07\" => BITCOIN\n\ncolor bitcoin.command == \"tx\"    => black lightgreen\ncolor bitcoin.command == \"block\" => black lightblue\ncolor bitcoin.magic == 0xF9BEB4D9 => white darkblue\ncolor bitcoin => white darkblue\n\nalias bitcoin.network => bitcoin.magic\nalias bitcoin.cmd     => bitcoin.command\n",
    /* bittorrent.posa */
    "# bittorrent.posa — BitTorrent Peer Wire Protocol (BEP-3)\n#\n# The TCP peer wire protocol opens every connection with a structured\n# handshake:\n#\n#   pstrlen   1 byte  — length of the protocol string, always 0x13 (19)\n#   pstr     19 bytes — \"BitTorrent protocol\"\n#   reserved  8 bytes — extension bits (BEP-10 extension protocol, Fast ext, etc.)\n#   info_hash 20 bytes — SHA1 of the torrent info dictionary\n#   peer_id   20 bytes — unique client identifier (20 ASCII/binary bytes)\n#\n# Total: 68 bytes for the handshake. After it both sides can send messages\n# using the BitTorrent message format: uint32_be length + uint8 type + payload.\n#\n# Extension bits (reserved field) encode capabilities:\n#   bit 20 (from the right) — Extension Protocol (BEP-10)\n#   bit 0                   — DHT Protocol (BEP-5)\n#\n# Reference: https://www.bittorrent.org/beps/bep_0003.html (BEP-3)\n#            https://www.bittorrent.org/beps/bep_0010.html (BEP-10)\n#\n# uTP (UDP-based transport, BEP-29) uses an entirely different header;\n# DHT and tracker traffic are also distinct. This decoder covers TCP only.\n\nObject<BITTORRENT> BT_HANDSHAKE\n    abbrev \"bittorrent\"\n    col \"BitTorrent\"\n    starts \"\\x13BitTorrent protocol\"\n\n    uint8    pstrlen      \"Protocol Name Length\" defaults(19)\n    str<19>  pstr         \"Protocol\"             defaults(\"BitTorrent protocol\")\n    bytes<8> reserved hex \"Reserved / Extension Bits\"\n    bytes<20> info_hash   \"Info Hash\"\n    bytes<20> peer_id     \"Peer ID\"\n\n    payload rest \"Post-Handshake\"\n\n    info \"BT handshake info_hash=%s\" info_hash\n\nObject<BITTORRENT> BT_MESSAGE\n    abbrev \"bittorrent\"\n    col \"BitTorrent\"\n\n    uint32 msg_length \"Message Length\" defaults(0)\n    when msg_length == 0:\n        # keep-alive: no type byte\n        payload rest \"Keep-Alive\"\n    else:\n        uint8 msg_type \"Message Type\" defaults(0)\n            0  = \"Choke\"\n            1  = \"Unchoke\"\n            2  = \"Interested\"\n            3  = \"Not Interested\"\n            4  = \"Have\"\n            5  = \"Bitfield\"\n            6  = \"Request\"\n            7  = \"Piece\"\n            8  = \"Cancel\"\n            9  = \"Port (DHT)\"\n            14 = \"Have All (Fast)\"\n            15 = \"Have None (Fast)\"\n            16 = \"Reject Request (Fast)\"\n            17 = \"Allowed Fast (Fast)\"\n            19 = \"Suggest Piece (Fast)\"\n            20 = \"Extended (BEP-10)\"\n\n        payload body \"Payload\"\n\n    info \"BT msg type=%u len=%u\" msg_type, msg_length\n\nObject<GROUP> BITTORRENT\n\nrule tcp.content \"\\x13BitTorrent protocol\" => BITTORRENT\n\ncolor bittorrent.msg_type == 7  => black lightgreen\ncolor bittorrent.msg_type == 6  => black lightblue\ncolor bittorrent.msg_type == 20 => black lightyellow\ncolor bittorrent.msg_type == 0 || bittorrent.msg_type == 1 => black lightgray\ncolor bittorrent => white darkblue\n\nalias bt.info_hash => bittorrent.info_hash\nalias bt.peer_id   => bittorrent.peer_id\n",
    /* capwap.posa */
    "# capwap.posa — CAPWAP (Control And Provisioning of Wireless Access Points)\n# RFC 5415 (CAPWAP Base), RFC 5416 (CAPWAP/802.11 Binding)\n# UDP 5246 (control), UDP 5247 (data)\n#\n# CAPWAP preamble (1 byte):\n#   bits[7:4]  version   = 0\n#   bits[3:0]  type      — 0=CAPWAP, 1=CAPWAP-DTLS-Header\n#\n# CAPWAP header (variable):\n#   bits[31:27] hlen    — header length in 32-bit words\n#   bits[26:22] rid     — radio ID\n#   bits[21:16] wbid    — wireless binding ID (1=802.11, 2=EPCGlobal)\n#   bits[15]    T       — type (0=data, 1=control)\n#   bits[14]    F       — fragment\n#   bits[13]    L       — last fragment\n#   bits[12]    W       — wireless specific info present\n#   bits[11]    M       — radio MAC present\n#   bits[10]    K       — keep-alive\n#   bits[9:7]   flags\n#   bits[6:0]   reserved\n#   uint16      fragment_id\n#   bits[15:3]  fragment_offset (>> 3)\n#   bits[2:0]   reserved\n\nLookup CapwapMessageTypes\n    1   = \"Discovery Request\"\n    2   = \"Discovery Response\"\n    3   = \"Join Request\"\n    4   = \"Join Response\"\n    5   = \"Configuration Status Request\"\n    6   = \"Configuration Status Response\"\n    7   = \"Configuration Update Request\"\n    8   = \"Configuration Update Response\"\n    9   = \"WTP Event Request\"\n    10  = \"WTP Event Response\"\n    11  = \"Change State Event Request\"\n    12  = \"Change State Event Response\"\n    13  = \"Echo Request\"\n    14  = \"Echo Response\"\n    15  = \"Image Information Request\"\n    16  = \"Image Information Response\"\n    17  = \"Reset Request\"\n    18  = \"Reset Response\"\n    19  = \"Primary Discovery Request\"\n    20  = \"Primary Discovery Response\"\n    21  = \"Data Transfer Request\"\n    22  = \"Data Transfer Response\"\n    23  = \"Clear Configuration Request\"\n    24  = \"Clear Configuration Response\"\n    25  = \"Station Configuration Request\"\n    26  = \"Station Configuration Response\"\n\nObject<main> CAPWAP\n    abbrev \"capwap\"\n    col \"CAPWAP\"\n\n    uint8  preamble    hex \"Preamble\" defaults(0)\n    bits preamble version 4 4 \"Version\"\n    bits preamble type    0 4 \"Type\"\n        CAPWAP_Plain = 0\n        CAPWAP_DTLS  = 1\n\n    uint32 capwap_header \"CAPWAP Header\" defaults(0x10018000)\n    bits capwap_header hlen  27 5 \"Header Length (words)\"\n    bits capwap_header rid   22 5 \"Radio ID\"\n    bits capwap_header wbid  16 6 \"Wireless Binding ID\"\n        IEEE_802_11 = 1\n        EPCGlobal   = 2\n    bits capwap_header T     15 1 \"Type\"\n        Data    = 0\n        Control = 1\n    bits capwap_header F     14 1 \"Fragment\"\n    bits capwap_header L     13 1 \"Last Fragment\"\n    bits capwap_header W     12 1 \"Wireless Specific Info\"\n    bits capwap_header M     11 1 \"Radio MAC\"\n    bits capwap_header K     10 1 \"Keep-Alive\"\n\n    uint16 fragment_id \"Fragment ID\" defaults(0)\n    uint16 fragment_offset_rsvd \"Fragment Offset/Reserved\" defaults(0)\n    bits fragment_offset_rsvd fragment_offset 3 13 \"Fragment Offset\"\n\n    when T == 1:\n        uint32 msg_element_length \"Control Header\" defaults(0x00010000)\n        bits msg_element_length msg_type 16 16 \"Message Type\"\n            lookup CapwapMessageTypes\n        bits msg_element_length seq_num 8 8 \"Sequence Number\"\n        payload message_elements \"Message Elements\"\n\n    else:\n        payload data_payload \"Data Payload\"\n\n    info \"CAPWAP %s\" T\n\n\nrule udp.port == 5246 => CAPWAP\nrule udp.port == 5247 => CAPWAP\n\ncolor capwap.T == 0 => black lightblue\ncolor capwap.T == 1 => black lightyellow\ncolor capwap        => white darkblue\n",
    /* cdp.posa */
    "# CDP header (Cisco Discovery Protocol, v1/v2) — 4 bytes\n# Sent to multicast 01:00:0c:cc:cc:cc via LLC/SNAP (OUI 00:00:0c, proto 0x2000).\n# ttl is how long a receiving device should hold CDP information (seconds).\n# Type-Length-Value records follow the 4-byte fixed header.\nObject<main> CDP\n    uint8  version defaults(2)\n    uint8  ttl defaults(180)\n    uint16 checksum defaults(0)\n",
    /* coap.posa */
    "# CoAP header (RFC 7252) — 4 bytes\n# UDP port 5683 (plain) or 5684 (DTLS). ver_type_tkl: Ver(2)=1 | T(2) | TKL(4).\n# T: 0=CON, 1=NON, 2=ACK, 3=RST. TKL = token length (0-8 bytes follow the header).\n# Default 0x40 = Ver=1, T=CON, TKL=0.\nObject<main> CoAP\n    uint8  ver_type_tkl defaults(0x40)\n    uint8  code defaults(0)\n        EMPTY                    = 0\n        GET                      = 1\n        POST                     = 2\n        PUT                      = 3\n        DELETE                   = 4\n        FETCH                    = 5\n        PATCH                    = 6\n        iPATCH                   = 7\n        CREATED                  = 0x41\n        DELETED                  = 0x42\n        VALID                    = 0x43\n        CHANGED                  = 0x44\n        CONTENT                  = 0x45\n        CONTINUE                 = 0x5F\n        BAD_REQUEST              = 0x80\n        UNAUTHORIZED             = 0x81\n        BAD_OPTION               = 0x82\n        FORBIDDEN                = 0x83\n        NOT_FOUND                = 0x84\n        METHOD_NOT_ALLOWED       = 0x85\n        NOT_ACCEPTABLE           = 0x86\n        REQUEST_ENTITY_INCOMPLETE = 0x88\n        CONFLICT                 = 0x89\n        PRECONDITION_FAILED      = 0x8C\n        REQUEST_TOO_LARGE        = 0x8D\n        UNSUPPORTED_CONTENT_FMT  = 0x8F\n        UNPROCESSABLE_ENTITY     = 0x96\n        TOO_MANY_REQUESTS        = 0x9D\n        INTERNAL_SERVER_ERROR    = 0xA0\n        NOT_IMPLEMENTED          = 0xA1\n        BAD_GATEWAY              = 0xA2\n        SERVICE_UNAVAILABLE      = 0xA3\n        GATEWAY_TIMEOUT          = 0xA4\n        PROXYING_NOT_SUPPORTED   = 0xA5\n    uint16 message_id defaults(0)\n",
    /* dccp.posa */
    "# DCCP header (RFC 4340) — 10 bytes (fixed part only)\n# IP protocol 33. offset = header length in 32-bit words (min 3 for short, 4 for long seqno).\n# res_type_x: Res(4)|Type(4)|X(1) — X=1 uses 6-byte sequence numbers (long form).\n# Type 0=REQUEST, 1=RESPONSE, 2=DATA, 3=ACK, 4=DATAACK, 5=CLOSEREQ, 6=CLOSE,\n#      7=RESET, 8=SYNC, 9=SYNCACK.\nObject<main> DCCP\n    uint16 sport defaults(0)\n    uint16 dport defaults(0)\n    uint8  offset defaults(4)\n    uint8  cscov defaults(0)\n    uint16 checksum defaults(0)\n    uint8  res_type_x defaults(0x00)\n        REQUEST  = 0x00\n        RESPONSE = 0x02\n        DATA     = 0x04\n        ACK      = 0x06\n        DATAACK  = 0x08\n        CLOSEREQ = 0x0A\n        CLOSE    = 0x0C\n        RESET    = 0x0E\n        SYNC     = 0x10\n        SYNCACK  = 0x12\n    uint8  reserved defaults(0)\n",
    /* dce_rpc.posa */
    "# dce_rpc.posa — DCE/RPC connection-oriented, as Microsoft MS-RPCE uses it\n# Reference: Open Group C706 (DCE 1.1: RPC) chapter 12; MS-RPCE\n# TCP 135 (endpoint mapper), TCP 593 (RPC over HTTP), and dynamic high ports;\n# also carried over SMB named pipes.\n#\n# DCE/RPC is the transport for most of what one Windows machine asks another to\n# do: create a service (SVCCTL), read the registry (WINREG), schedule a task\n# (ATSVC), enumerate accounts (SAMR), replicate the directory (DRSUAPI). Which\n# of those a connection is for is *not* in the request — it is agreed once, in\n# the BIND, by interface UUID, and every later request refers to it only by a\n# small context id.\n#\n# That makes the BIND the packet worth reading, and it is decoded in full below,\n# interface UUID resolved to a name. See the Limits section for what a\n# per-connection decoder cannot do without carrying state across packets.\n#\n# Fixed 16-octet header on every PDU:\n#\n#   +-----+-------+------+-------+------------+---------+---------+---------+\n#   | ver | minor | type | flags | data repr 4| frag  2 | auth  2 | call  4 |\n#   +-----+-------+------+-------+------------+---------+---------+---------+\n#\n# Everything after the header is in the byte order the *sender* chose, declared\n# in data_repr. Little-endian is what Windows sends and what every field below\n# assumes; a big-endian peer (rare, and generally non-Windows) would need the\n# le_ types swapped. data_repr.byteorder is decoded so you can see which it is.\n\n# ── Interface UUIDs ───────────────────────────────────────────────────────────\n# Extracted from Wireshark's 134 packet-dcerpc-*.c dissectors, which is the only\n# complete list of these in one place.\n#\n# The `uuid` field type matters here rather than being cosmetic. These UUIDs are\n# deliberately near-identical — LSARPC is …789ab and SAMR is …789ac, while\n# SPOOLSS shares LSARPC's last eight bytes and differs only in the first four.\n# Keying on any single 32- or 64-bit slice would conflate at least two of\n# SAMR, LSARPC and SPOOLSS, so the whole 128-bit value is matched as text.\nLookup DcerpcInterfaces\n    \"00000000-0000-0000-0000-000000000000\" = \"RCG\"\n    \"003fd39c-7feb-1bbc-bebe-02608c2ef4d2\" = \"ICL_RPC - DCE DFS ICL RPC\"\n    \"019ee420-682d-11c9-a607-08002b0dea7a\" = \"DTSSTIME_REQ - DCE Distributed Time Service Local Server\"\n    \"0d7c1e50-113a-11ca-b71f-08001e01dc6c\" = \"SECIDMAP - DCE Security ID Mapper\"\n    \"0eff23e6-555a-11cd-95bf-0800092784c3\" = \"rs_prop_attr - DCE/RPC Prop Attr\"\n    \"12345678-1234-1234-1234-abcdef123456\" = \"MISC\"\n    \"12345678-1234-abcd-ef00-0123456789ab\" = \"SPOOLSS - Microsoft Spool Subsystem\"\n    \"12345678-1234-abcd-ef00-01234567cffb\" = \"RPC_NETLOGON - Microsoft Network Logon\"\n    \"12345778-1234-abcd-ef00-0123456789ab\" = \"LSARPC - Local Security Authority\"\n    \"12345778-1234-abcd-ef00-0123456789ac\" = \"SAMR\"\n    \"1544f5e0-613c-11d1-93df-00c04fd7bd09\" = \"RFR - Exchange 2003 Directory Request For Response\"\n    \"1d193c08-000b-11ca-ba1d-02608c2ea96e\" = \"DCE/RPC BUTC\"\n    \"1ff70682-0a51-30e8-076d-740be8cee98b\" = \"ATSVC - Microsoft AT-Scheduler Service\"\n    \"257df1c9-c6d3-11ca-8554-08002b1c8f1f\" = \"CDS_CLERK - CDS Clerk Server Calls\"\n    \"2f5f6520-ca46-1067-b319-00dd010662da\" = \"TAPI - Microsoft Telephony API Service\"\n    \"3139a0e2-68da-11cd-91c7-080009242444\" = \"rs_pwd_mgmt - DCE/RPC Registry Password Management\"\n    \"333a2276-0000-0000-0d00-00809c000000\" = \"DCE/RPC Conversation Manager\"\n    \"333b33c3-0000-0000-0d00-008784000000\" = \"llb - DCE/RPC NCS 1.5.1 Local Location Broker\"\n    \"338cd001-2244-31f1-aaaa-900038001003\" = \"WINREG - Remote Registry Service\"\n    \"361993c0-b000-0000-0d00-008784000000\" = \"DCE/RPC RS_UNIX\"\n    \"367abb81-9844-35f1-ad32-98f038001003\" = \"SVCCTL - Service Control\"\n    \"3919286a-b10c-11d0-9ba8-00c04fd92ef5\" = \"DSSETUP - Active Directory Setup\"\n    \"47b33331-8000-0000-0d00-01dc6c000000\" = \"rdaclif - DCE/RPC Directory Acl Interface\"\n    \"4885772c-c6d3-11ca-84c6-08002b1c8f1f\" = \"cprpc_server - DNS Control Program Server\"\n    \"4b324fc8-1670-01d3-1278-5a47bf6ee188\" = \"SRVSVC - Server Service\"\n    \"4c878280-2000-0000-0d00-028714000000\" = \"DCE/RPC RS_ACCT\"\n    \"4c878280-3000-0000-0d00-028714000000\" = \"RS_PGO - DCE Name Service\"\n    \"4c878280-4000-0000-0d00-028714000000\" = \"RS_PLCY - RS Interface properties\"\n    \"4c878280-5000-0000-0d00-028714000000\" = \"DCE/RPC RS_MISC\"\n    \"4d37f2dd-ed43-0000-02c0-37cf1e000001\" = \"BOSSVR - DCE DFS Basic Overseer Server\"\n    \"4d37f2dd-ed43-0000-02c0-37cf1e001000\" = \"dce_update - DCE/RPC UpServer\"\n    \"4d37f2dd-ed43-0000-02c0-37cf2e000001\" = \"FLDB - DCE DFS Fileset Location Server\"\n    \"4d37f2dd-ed43-0002-02c0-37cf1e000000\" = \"UBIKDISK - DCE DFS FLDB UBIK TRANSFER\"\n    \"4d37f2dd-ed43-0003-02c0-37cf1e000000\" = \"DCE DFS FLDB UBIKVOTE\"\n    \"4d37f2dd-ed43-0004-02c0-37cf1e000000\" = \"FTServer Operations\"\n    \"4d37f2dd-ed43-0005-02c0-37cf1e000000\" = \"REP_PROC - DCE DFS Replication Server\"\n    \"4d37f2dd-ed93-0000-02c0-37cf1e000000\" = \"FILEEXP - DCE DFS File Exporter\"\n    \"4d37f2dd-ed96-0000-02c0-37cf1e000000\" = \"TKN4Int - DCE DFS Token Server\"\n    \"4da1c422-943d-11d1-acae-00c04fc2aa3f\" = \"TRKSVR - Microsoft Distributed Link Tracking Server Service\"\n    \"4fc742e0-4a10-11cf-8273-00aa004ae673\" = \"NETDFS - Settings for Microsoft Distributed File System\"\n    \"50abc2a4-574d-40b3-9d66-ee4fd5fba076\" = \"DNSSERVER - DNS Server\"\n    \"591d87d0-de64-11ca-a11c-08001e0394c7\" = \"rs_prop_acl - DCE/RPC Registry server propagation interface - ACLs\"\n    \"5a7b91f8-ff00-11d0-a9b2-00c04fb6e6fc\" = \"Microsoft Messenger Service\"\n    \"5b8c2fa8-b60b-11c9-be0f-08001e018fa0\" = \"RS_REPADM - Registry server administration operations.\"\n    \"5d978990-4851-11ca-9937-08001e039448\" = \"roverride - Remote Override interface\"\n    \"621dff68-3c39-4c6c-aae3-e68e2c6503ad\" = \"WZCSVC - Wireless Configuration Service\"\n    \"68097130-de43-11ca-a554-08001e0394c7\" = \"DCE/RPC RS_PROP_ACCT\"\n    \"6bffd098-a112-3610-9833-012892020162\" = \"RPC_BROWSER - RPC Browser\"\n    \"6bffd098-a112-3610-9833-46c3f87e345a\" = \"WKSSVC - Workstation Service\"\n    \"76f03f96-cdfd-44fc-a22c-64950a001209\" = \"IRemoteWinspool SubSystem\"\n    \"82273fdc-e32a-18c3-3f78-827929dc23ea\" = \"EVENTLOG - Event Logger\"\n    \"850446b0-e95b-11ca-ad90-08001e0145b1\" = \"RS_REPLIST - DCE/RPC Repserver Calls\"\n    \"86d35949-83c9-4044-b424-db363231fd0c\" = \"TaskSchedulerService - Microsoft Task Scheduler Service\"\n    \"885d85fb-c754-4062-a0e7-6872ce0064f4\" = \"MDSSVC - Spotlight metadata search service\"\n    \"894de0c0-0d55-11d3-a322-00c04fa321a1\" = \"INITSHUTDOWN - Init shutdown service\"\n    \"897e2e5f-93f3-4376-9c9c-fd2277495c27\" = \"FRSTRANS - File Replication Service DFS-R\"\n    \"8d9f4e40-a03d-11ce-8f69-08003e30051b\" = \"PNP - Microsoft Plug and Play service\"\n    \"8f09f000-b7ed-11ce-bbd2-00001a181cad\" = \"RRAS - Microsoft Routing and Remote Access Service\"\n    \"8f73de50-768c-11ca-bffc-08001e039431\" = \"KRB5RPC - DCE/RPC Kerberos V\"\n    \"9556dc99-828c-11cf-a37e-00aa003240c7\" = \"IWBEMSERVICES\"\n    \"9f6c78ef-fce5-42fa-abea-3e7df91921dc\" = \"IWBEMLOGINCLIENTIDEX\"\n    \"a35af600-9cf4-11cd-a076-08002b2bd711\" = \"SCARD_PACK\"\n    \"a4f1db00-ca47-1067-b31f-00dd010662da\" = \"MAPI\"\n    \"a71fc1e8-567f-11cb-98a0-08001e04de8c\" = \"RS_ATTR - Registry Server Attributes Manipulation Interface\"\n    \"a76e832a-10df-11cd-9056-080009242444\" = \"rsec_login - Remote sec_login preauth interface.\"\n    \"a8e0653c-2744-4389-a61d-7373df8b2292\" = \"FSRVP - File Server Remote VSS Protocol\"\n    \"afa8bd80-7d8a-11c9-bef4-08002b102989\" = \"MGMT - DCE/RPC Remote Management\"\n    \"b1e338f8-9533-11c9-a34a-08001e019c1e\" = \"rpriv - Privilege Server operations\"\n    \"b47c9460-567f-11cb-8c09-08001e04de8c\" = \"rs_attr_schema - DCE/RPC Registry Server Attributes Schema\"\n    \"b62dc198-dfd4-11ca-948f-08001e02594c\" = \"rs_repmgr - DCE/RPC Operations between registry server replicas\"\n    \"b97db8b2-4c63-11cf-bff6-08002be23f2f\" = \"Failover Cluster Management API (clusapi)\"\n    \"bfca1238-628a-11c9-a073-08002b0dea7a\" = \"DTSPROVIDER - DCE Distributed Time Service Provider\"\n    \"c23626e8-de34-11ca-8cbc-08001e0394c7\" = \"rs_prop_pgo - DCE/RPC Registry server propagation interface - PGO items\"\n    \"c681d488-d850-11d0-8c52-00c04fd90f7e\" = \"EFS\"\n    \"ccd8c074-d0e5-4a40-92b4-d074faa6ba28\" = \"SMB Witness Service\"\n    \"d049b186-814f-11d1-9a3c-00c04fc9b232\" = \"FRSAPI - Microsoft File Replication Service API\"\n    \"d46113d0-a848-11cb-b863-08001e046aa5\" = \"DCE/RPC RS_BIND\"\n    \"d4781cd6-e5d3-44df-ad94-930efe48a887\" = \"IWBEMLOGINCLIENTID\"\n    \"d5579459-8bca-11ca-b771-08002b1c8f1f\" = \"cds_solicit - DCE/RPC CDS Solicitation\"\n    \"e1af8308-5d1f-11c9-91a4-08002b14a0fa\" = \"EPM - DCE/RPC Endpoint Mapper\"\n    \"e3514235-4b06-11d1-ab04-00c04fc2dcd2\" = \"DRSUAPI - Active Directory Replication\"\n    \"e6ac5cb8-de3e-11ca-9376-08001e0394c7\" = \"rs_prop_plcy - DCE/RPC Registry server propagation interface - properties and policies\"\n    \"eb814e2a-0099-11ca-8678-02608c2ea96e\" = \"DCE/DFS BUDB\"\n    \"f309ad18-d86a-11d0-a075-00c04fb68820\" = \"IWBEMLEVEL1LOGIN\"\n    \"f5cc59b4-4264-101a-8c59-08002b2f8426\" = \"FRSRPC - File Replication Service\"\n    \"f5cc5a18-4264-101a-8c59-08002b2f8426\" = \"NSPI - Exchange 5.5 Name Service Provider\"\n\n# ── Transfer syntaxes ─────────────────────────────────────────────────────────\n# How the arguments are marshalled. NDR64 and the bind-time feature negotiation\n# UUID are MS-RPCE extensions; a client offering several lets the server pick.\nLookup DcerpcTransferSyntaxes\n    \"8a885d04-1ceb-11c9-9fe8-08002b104860\" = \"NDR32\"\n    \"71710533-beba-4937-8319-b5dbef9ccc36\" = \"NDR64\"\n    \"6cb71c2c-9812-4540-0300-000000000000\" = \"Bind Time Feature Negotiation\"\n\nLookup DcerpcAuthTypes\n    0   = \"None\"\n    1   = \"Kerberos 5\"\n    9   = \"SPNEGO\"\n    10  = \"NTLMSSP\"\n    14  = \"SCHANNEL SSP\"\n    16  = \"Kerberos SSP\"\n    17  = \"Distributed Password Authentication SSP\"\n    18  = \"MSN SSP\"\n    21  = \"Digest SSP\"\n    68  = \"NETLOGON Secure Channel\"\n    100 = \"MSMQ SSP\"\n\n# Only levels 5 and 6 sign or seal the stub data; 1-4 authenticate the bind and\n# then send every argument in clear.\nLookup DcerpcAuthLevels\n    1 = \"None\"\n    2 = \"Connect\"\n    3 = \"Call\"\n    4 = \"Packet\"\n    5 = \"Packet integrity\"\n    6 = \"Packet privacy\"\n\n# ── One presentation context offered in a BIND ────────────────────────────────\n# Defined before the object that `include`s it: include resolves in a single\n# forward pass, so the object has to already exist at that point.\nObject DcerpcCtxItem\n    le_uint16 ctx_id \"Context ID\" defaults(0)\n    uint8 num_trans_items \"Num Trans Items\" defaults(1)\n    uint8 ctx_reserved \"Reserved\" defaults(0)\n    # The interface the client wants to call, and which version of it.\n    uuid abstract_syntax lookup DcerpcInterfaces \"Abstract Syntax\"\n    # Remember which interface this context id means, for the life of the flow.\n    # Every Request afterwards names the interface only by this number.\n    bind interface[ctx_id] = abstract_syntax\n    le_uint16 if_ver \"Interface Ver\" defaults(0)\n    le_uint16 if_ver_minor \"Interface Ver Minor\" defaults(0)\n    repeat num_trans_items as transfer \"Transfer Syntaxes\"\n        uuid transfer_syntax lookup DcerpcTransferSyntaxes \"Transfer Syntax\"\n        le_uint32 transfer_ver \"Transfer Syntax Ver\" defaults(2)\n        label \"%s\" transfer_syntax\n\nObject<main> DCE_RPC\n    abbrev \"dcerpc\"\n    col \"DCE/RPC\"\n\n    uint8 version \"Version\" defaults(5)\n    uint8 minor_version \"Minor Version\" defaults(0)\n\n    # Connection-oriented PDU types. The connectionless (UDP) form reuses 0, 2\n    # and 3 for request/response/fault but assigns 1 and 4-10 to its own\n    # ping/working/nocall/fack machinery; those are deliberately not listed\n    # here, because this decoder is bound to TCP.\n    uint8 packet_type \"Packet Type\" defaults(11)\n        0  = \"Request\"\n        2  = \"Response\"\n        3  = \"Fault\"\n        11 = \"Bind\"\n        12 = \"Bind_ack\"\n        13 = \"Bind_nak\"\n        14 = \"Alter_context\"\n        15 = \"Alter_context_resp\"\n        16 = \"AUTH3\"\n        17 = \"Shutdown\"\n        18 = \"Co_cancel\"\n        19 = \"Orphaned\"\n        20 = \"RPC-over-HTTP RTS\"\n\n    uint8 flags hex \"Flags\" defaults(0x03)\n    bits flags first_frag 0 1 \"First Fragment\"\n    bits flags last_frag 1 1 \"Last Fragment\"\n    bits flags cancel_pend 2 1 \"Cancel Pending\"\n    bits flags reserved 3 1 \"Reserved\"\n    bits flags multiplex 4 1 \"Multiplex\"\n    bits flags did_not_exec 5 1 \"Did Not Execute\"\n    bits flags maybe 6 1 \"Maybe\"\n    bits flags object_uuid 7 1 \"Object UUID Present\"\n\n    # Byte order for everything that follows, plus character and float encoding.\n    uint8 drep0 hex \"Data Representation\" defaults(0x10)\n    bits drep0 byteorder 4 4 \"Byte order\"\n        0 = \"Big-endian\"\n        1 = \"Little-endian\"\n    bits drep0 character 0 4 \"Character\"\n        0 = \"ASCII\"\n        1 = \"EBCDIC\"\n    uint8 drep_float \"Float Representation\" defaults(0)\n        0 = \"IEEE\"\n        1 = \"VAX\"\n        2 = \"Cray\"\n        3 = \"IBM\"\n    uint16 drep_reserved hex \"Reserved\" defaults(0)\n\n    le_uint16 frag_length \"Fragment Length\" defaults(16)\n    # Non-zero means an authentication trailer sits at the very end of the PDU.\n    le_uint16 auth_length \"Auth Length\" defaults(0)\n    le_uint32 call_id hex \"Call ID\" defaults(1)\n\n    # ── Bind / Alter_context ──────────────────────────────────────────────────\n    # The packet that says what this connection is for. A client may offer\n    # several presentation contexts at once; each names an interface and the\n    # transfer syntaxes it can marshal with.\n    when packet_type == 11:\n        le_uint16 max_xmit_frag \"Max Xmit Frag\" defaults(4280)\n        le_uint16 max_recv_frag \"Max Recv Frag\" defaults(4280)\n        le_uint32 assoc_group \"Assoc Group\" defaults(0)\n        uint8 num_ctx_items \"Num Ctx Items\" defaults(1)\n        bytes<3> bind_reserved \"Reserved\"\n        repeat num_ctx_items as ctx_item \"Context Items\"\n            include DcerpcCtxItem\n            label \"Context %u: %s\" ctx_id, abstract_syntax\n\n    when packet_type == 14:\n        le_uint16 max_xmit_frag \"Max Xmit Frag\" defaults(4280)\n        le_uint16 max_recv_frag \"Max Recv Frag\" defaults(4280)\n        le_uint32 assoc_group \"Assoc Group\" defaults(0)\n        uint8 num_ctx_items \"Num Ctx Items\" defaults(1)\n        bytes<3> bind_reserved \"Reserved\"\n        repeat num_ctx_items as ctx_item \"Context Items\"\n            include DcerpcCtxItem\n            label \"Context %u: %s\" ctx_id, abstract_syntax\n\n    # ── Bind_ack / Alter_context_resp ─────────────────────────────────────────\n    # sec_addr is the named pipe the server wants to be called on. The result\n    # list that follows it is 4-byte aligned, and computing that padding needs\n    # arithmetic this grammar does not have — so the results are left as bytes\n    # rather than decoded from a guessed offset.\n    when packet_type == 12:\n        le_uint16 max_xmit_frag \"Max Xmit Frag\" defaults(4280)\n        le_uint16 max_recv_frag \"Max Recv Frag\" defaults(4280)\n        le_uint32 assoc_group \"Assoc Group\" defaults(0)\n        le_uint16 sec_addr_len \"Scndry Addr Len\" defaults(0)\n        str[sec_addr_len] sec_addr \"Scndry Addr\"\n        # The result list starts on the next 4-byte boundary after sec_addr.\n        seek (offset + 3) & ~3\n        uint8 num_results \"Num Results\" defaults(1)\n        bytes<3> results_reserved \"Reserved\"\n        repeat num_results as result \"Context Results\"\n            le_uint16 ack_result \"Result\" defaults(0)\n                0 = \"Acceptance\"\n                1 = \"User rejection\"\n                2 = \"Provider rejection\"\n                3 = \"Negotiate ACK\"\n            le_uint16 ack_reason \"Reason\" defaults(0)\n                0 = \"Reason not specified\"\n                1 = \"Abstract syntax not supported\"\n                2 = \"Proposed transfer syntaxes not supported\"\n                3 = \"Local limit exceeded\"\n            uuid ack_trans_id lookup DcerpcTransferSyntaxes \"Transfer Syntax\"\n            le_uint32 ack_trans_ver \"Transfer Syntax Ver\" defaults(2)\n            label \"%s\" ack_result\n\n    when packet_type == 15:\n        le_uint16 max_xmit_frag \"Max Xmit Frag\" defaults(4280)\n        le_uint16 max_recv_frag \"Max Recv Frag\" defaults(4280)\n        le_uint32 assoc_group \"Assoc Group\" defaults(0)\n        le_uint16 sec_addr_len \"Scndry Addr Len\" defaults(0)\n        str[sec_addr_len] sec_addr \"Scndry Addr\"\n        # The result list starts on the next 4-byte boundary after sec_addr.\n        seek (offset + 3) & ~3\n        uint8 num_results \"Num Results\" defaults(1)\n        bytes<3> results_reserved \"Reserved\"\n        repeat num_results as result \"Context Results\"\n            le_uint16 ack_result \"Result\" defaults(0)\n                0 = \"Acceptance\"\n                1 = \"User rejection\"\n                2 = \"Provider rejection\"\n                3 = \"Negotiate ACK\"\n            le_uint16 ack_reason \"Reason\" defaults(0)\n                0 = \"Reason not specified\"\n                1 = \"Abstract syntax not supported\"\n                2 = \"Proposed transfer syntaxes not supported\"\n                3 = \"Local limit exceeded\"\n            uuid ack_trans_id lookup DcerpcTransferSyntaxes \"Transfer Syntax\"\n            le_uint32 ack_trans_ver \"Transfer Syntax Ver\" defaults(2)\n            label \"%s\" ack_result\n\n    # ── Bind_nak ──────────────────────────────────────────────────────────────\n    # The bind was refused outright — no context was established.\n    when packet_type == 13:\n        le_uint16 reject_reason \"Reject Reason\" defaults(0)\n            0 = \"Reason not specified\"\n            1 = \"Temporary congestion\"\n            2 = \"Local limit exceeded\"\n            3 = \"Called paddr unknown\"\n            4 = \"Protocol version not supported\"\n            5 = \"Default context not supported\"\n            6 = \"User data not readable\"\n            7 = \"No PSAP available\"\n            8 = \"Authentication type not recognized\"\n            9 = \"Invalid checksum\"\n        uint8 num_protocols \"Number of Protocols\" defaults(0)\n        payload nak_versions \"Supported Versions\"\n\n    # ── Request ───────────────────────────────────────────────────────────────\n    # opnum selects the operation *within* the interface bound to ctx_id. What\n    # the pair means needs the BIND from earlier in the connection — see Limits.\n    when packet_type == 0:\n        le_uint32 alloc_hint \"Alloc hint\" defaults(0)\n        le_uint16 cn_ctx_id \"Context ID\" defaults(0)\n        le_uint16 opnum \"Opnum\" defaults(0)\n        recall interface[cn_ctx_id] as iface lookup DcerpcInterfaces \"Interface\"\n        payload stub_data \"Stub data\"\n\n    # ── Response ──────────────────────────────────────────────────────────────\n    when packet_type == 2:\n        le_uint32 alloc_hint \"Alloc hint\" defaults(0)\n        le_uint16 cn_ctx_id \"Context ID\" defaults(0)\n        uint8 cancel_count \"Cancel count\" defaults(0)\n        uint8 resp_reserved \"Reserved\" defaults(0)\n        recall interface[cn_ctx_id] as iface lookup DcerpcInterfaces \"Interface\"\n        payload stub_data \"Stub data\"\n\n    # ── Fault ─────────────────────────────────────────────────────────────────\n    # status is the RPC-level error; a call rejected for access reasons shows up\n    # here rather than as a response.\n    when packet_type == 3:\n        le_uint32 alloc_hint \"Alloc hint\" defaults(0)\n        le_uint16 cn_ctx_id \"Context ID\" defaults(0)\n        uint8 cancel_count \"Cancel count\" defaults(0)\n        uint8 fault_reserved \"Reserved\" defaults(0)\n        recall interface[cn_ctx_id] as iface lookup DcerpcInterfaces \"Interface\"\n        le_uint32 status hex \"Status\" defaults(0)\n            0x00000005 = \"nca_s_fault_access_denied\"\n            0x000006d3 = \"nca_s_unk_if\"\n            0x1c000001 = \"nca_s_fault_other\"\n            0x1c000006 = \"nca_s_fault_addr_error\"\n            0x1c00000d = \"nca_s_fault_context_mismatch\"\n            0x1c010002 = \"nca_s_op_rng_error\"\n            0x1c010003 = \"nca_s_unk_if (op)\"\n            0x1c010006 = \"nca_s_wrong_boot_time\"\n            0x1c01000b = \"nca_s_proto_error\"\n            0x1c010013 = \"nca_s_out_args_too_big\"\n            0x1c010014 = \"nca_s_server_too_busy\"\n        payload fault_stub \"Fault stub data\"\n\n    # ── AUTH3 and everything else ─────────────────────────────────────────────\n    else:\n        payload body \"Body\"\n\n    # ── Authentication trailer ────────────────────────────────────────────────\n    # Present on any PDU with auth_length > 0, and it sits at the very end:\n    # frag_length - auth_length - 8, the 8 being the trailer's own header.\n    # Reaching it needs arithmetic on two header fields, which is what the\n    # expression forms are for.\n    when auth_length >= 1:\n        seek frag_length - auth_length - 8\n        uint8 auth_type lookup DcerpcAuthTypes \"Auth type\" defaults(0)\n        uint8 auth_level lookup DcerpcAuthLevels \"Auth level\" defaults(0)\n        uint8 auth_pad_len \"Auth pad length\" defaults(0)\n        uint8 auth_rsrvd \"Auth Rsrvd\" defaults(0)\n        le_uint32 auth_ctx_id hex \"Auth Context ID\" defaults(0)\n        bytes[auth_length] auth_credentials \"Auth Credentials\"\n\n    info \"DCE/RPC %s call=%u %s\" packet_type, call_id, iface\n\n\n# ── Limits ────────────────────────────────────────────────────────────────────\n# A Request names its interface only by context id, so `bind` remembers what the\n# BIND agreed and `recall` reads it back — keyed by the flow's Community ID, so\n# it holds for the life of the connection and does not leak between flows. A\n# capture that starts after the BIND has nothing to recall; the field says so\n# rather than guessing, and a warning is raised.\n#\n# The opnum is decoded but not named. Turning \"SVCCTL opnum 12\" into\n# \"CreateServiceW\" needs a per-interface operation table — 134 of them in\n# Wireshark — which is a body of data to add, not a missing capability.\n#\n# The stub data itself is NDR-marshalled and interface-specific. Decoding it is\n# not a matter of adding fields here.\n\n\n# ── Binding ───────────────────────────────────────────────────────────────────\n# 135 is the endpoint mapper; 593 is RPC over HTTP. Real interface traffic moves\n# to a dynamically assigned high port that the mapper hands out, so content\n# detection earns its place here in a way it does not for most protocols.\n#\n# The signature is three octets, not two. Version 5 plus a minor of 0 or 1 is\n# only sixteen bits and would claim any stream that happened to begin 05 00 —\n# and content rules are tried before port rules, so a false match outranks the\n# real binding of whatever that stream was. Adding the PDU type rejects the\n# large majority of those: of 256 possible third octets only the handful below\n# are PDU types that open a connection or carry a call.\nrule tcp.content \"\\x05\\x00\\x0b\" => DCE_RPC\nrule tcp.content \"\\x05\\x00\\x0c\" => DCE_RPC\nrule tcp.content \"\\x05\\x00\\x00\" => DCE_RPC\nrule tcp.content \"\\x05\\x00\\x02\" => DCE_RPC\nrule tcp.content \"\\x05\\x00\\x03\" => DCE_RPC\nrule tcp.content \"\\x05\\x00\\x0e\" => DCE_RPC\nrule tcp.content \"\\x05\\x00\\x0f\" => DCE_RPC\nrule tcp.content \"\\x05\\x01\\x0b\" => DCE_RPC\nrule tcp.content \"\\x05\\x01\\x0c\" => DCE_RPC\nrule tcp.port == 135 => DCE_RPC\nrule tcp.port == 593 => DCE_RPC\n\n\n# ── Coloring Rules ────────────────────────────────────────────────────────────\n# Most specific first; first match wins.\n\n# Bind refused, or a call faulted — access denied lands here.\ncolor dcerpc.packet_type == 13 => yellow red\ncolor dcerpc.packet_type == 3 => white red\n\n# Binds: what the connection is being opened to do.\ncolor dcerpc.packet_type == 11 || dcerpc.packet_type == 14 => black lightyellow\n\n# Bind acknowledgements.\ncolor dcerpc.packet_type == 12 || dcerpc.packet_type == 15 => black lightgreen\n\n# Calls and their replies.\ncolor dcerpc.packet_type == 0 => black lightblue\ncolor dcerpc.packet_type == 2 => black lightcyan\n\n# Protocol catch-all.\ncolor dcerpc => white darkblue\n\n\n# ── Wireshark-Compatible Aliases ──────────────────────────────────────────────\nalias dcerpc.ver              => dcerpc.version\nalias dcerpc.ver_minor        => dcerpc.minor_version\nalias dcerpc.pkt_type         => dcerpc.packet_type\nalias dcerpc.cn_flags         => dcerpc.flags\nalias dcerpc.drep             => dcerpc.drep0\nalias dcerpc.drep.byteorder   => dcerpc.byteorder\nalias dcerpc.drep.character   => dcerpc.character\nalias dcerpc.cn_frag_len      => dcerpc.frag_length\nalias dcerpc.cn_auth_len      => dcerpc.auth_length\nalias dcerpc.cn_call_id       => dcerpc.call_id\nalias dcerpc.cn_max_xmit      => dcerpc.max_xmit_frag\nalias dcerpc.cn_max_recv      => dcerpc.max_recv_frag\nalias dcerpc.cn_assoc_group   => dcerpc.assoc_group\nalias dcerpc.cn_num_ctx_items => dcerpc.num_ctx_items\nalias dcerpc.cn_ctx_id        => dcerpc.cn_ctx_id\nalias dcerpc.cn_bind_if_id    => dcerpc.ctx_item.abstract_syntax\nalias dcerpc.cn_bind_trans_id => dcerpc.ctx_item.transfer.transfer_syntax\nalias dcerpc.cn_sec_addr_len  => dcerpc.sec_addr_len\nalias dcerpc.cn_sec_addr      => dcerpc.sec_addr\nalias dcerpc.cn_reject_reason => dcerpc.reject_reason\nalias dcerpc.cn_alloc_hint    => dcerpc.alloc_hint\nalias dcerpc.cn_cancel_count  => dcerpc.cancel_count\nalias dcerpc.cn_status        => dcerpc.status\nalias dcerpc.opnum            => dcerpc.opnum\n",
    /* dhcp.posa */
    "# dhcp.posa — DHCP / BOOTP (RFC 2131, 2132).\n#\n# A fixed BOOTP header, then a magic cookie, then options: a stream of\n# type-length-value records with no count in front of them. That is\n#\n#   repeat until end as option    records until the packet runs out\n#       scope opt_len             each option's value is opt_len bytes, and the\n#                                 scope ends there whether we decoded it or not —\n#                                 so an option this file has never heard of still\n#                                 leaves the walk on the right byte\n#       when opt_code == 53:      the ones worth naming get named…\n#       else:                     …and the rest still show their bytes\n#\n# Pad (0) and End (255) carry no length byte, hence the `when opt_code > 0` /\n# `when opt_code < 255` nesting around the length.\n\nObject<main> DHCP\n    abbrev \"dhcp\"\n    col \"DHCP\"\n\n    uint8 op \"Message Type\" defaults(1)\n        Boot Request = 1\n        Boot Reply = 2\n    uint8 htype \"Hardware Type\" defaults(1)\n        Ethernet = 1\n    uint8 hlen \"Hardware Address Length\" defaults(6)\n    uint8 hops \"Hops\" defaults(0)\n    uint32 xid hex \"Transaction ID\" defaults(0)\n    uint16 secs \"Seconds Elapsed\" defaults(0)\n    uint16 flags hex \"Flags\" defaults(0)\n        bits flags broadcast 15 1 \"Broadcast\"\n    ip4 ciaddr \"Client IP Address\"\n    ip4 yiaddr \"Your (client) IP Address\"\n    ip4 siaddr \"Next Server IP Address\"\n    ip4 giaddr \"Relay Agent IP Address\"\n    mac chaddr \"Client MAC Address\"\n    bytes<10> chaddr_pad \"Client Hardware Address Padding\"\n    bytes<64> sname \"Server Host Name\"\n    bytes<128> file \"Boot File Name\"\n    uint32 cookie hex \"Magic Cookie\" defaults(0x63825363)\n\n    repeat until end as option \"Options\"\n        label \"%s: %s%s%s\" opt_code, msg_type, opt_ip, opt_text\n        uint8 opt_code \"Option\" defaults(53)\n            Pad = 0\n            Subnet Mask = 1\n            Router = 3\n            Domain Name Server = 6\n            Host Name = 12\n            Domain Name = 15\n            Requested IP Address = 50\n            IP Address Lease Time = 51\n            DHCP Message Type = 53\n            Server Identifier = 54\n            Parameter Request List = 55\n            Maximum DHCP Message Size = 57\n            Renewal Time Value = 58\n            Rebinding Time Value = 59\n            Vendor Class Identifier = 60\n            Client Identifier = 61\n            End = 255\n        when opt_code > 0:\n            when opt_code < 255:\n                uint8 opt_len \"Length\" defaults(1)\n                scope opt_len\n                    when opt_code == 53:\n                        uint8 msg_type \"DHCP Message Type\" defaults(1)\n                            Discover = 1\n                            Offer = 2\n                            Request = 3\n                            Decline = 4\n                            ACK = 5\n                            NAK = 6\n                            Release = 7\n                            Inform = 8\n                    when opt_code == 1:\n                        ip4 opt_ip \"Subnet Mask\"\n                    when opt_code == 3:\n                        ip4 opt_ip \"Router\"\n                    when opt_code == 6:\n                        repeat until end as dns_server\n                            label \"DNS Server: %s\" opt_ip\n                            ip4 opt_ip \"Domain Name Server\"\n                    when opt_code == 50:\n                        ip4 opt_ip \"Requested IP Address\"\n                    when opt_code == 54:\n                        ip4 opt_ip \"Server Identifier\"\n                    when opt_code == 51:\n                        uint32 lease_time \"IP Address Lease Time\" defaults(86400)\n                    when opt_code == 12:\n                        str[opt_len] opt_text \"Host Name\"\n                    when opt_code == 15:\n                        str[opt_len] opt_text \"Domain Name\"\n                    when opt_code == 60:\n                        str[opt_len] opt_text \"Vendor Class Identifier\"\n                    when opt_code == 55:\n                        repeat until end as requested\n                            label \"Requested: %s\" param\n                            uint8 param \"Parameter\" defaults(1)\n                                Subnet Mask = 1\n                                Router = 3\n                                Domain Name Server = 6\n                                Host Name = 12\n                                Domain Name = 15\n                                Broadcast Address = 28\n                    else:\n                        payload opt_value \"Value\"\n\n    info \"DHCP %s - Transaction ID 0x%x\" msg_type, xid\n\nrule udp.port == 67 => DHCP\nrule udp.port == 68 => DHCP\n\ncolor dhcp => black lightblue\n",
    /* dhcpv6.posa */
    "# DHCPv6 header (RFC 8415) — 4 bytes\n# UDP port 546 (client) / 547 (server). transaction_id is a 3-byte random value.\n# Relay messages (RELAY_FORW/RELAY_REPL) use a different structure (hop-count + link/peer addr).\nObject<main> DHCPv6\n    uint8    msg_type defaults(1)\n        SOLICIT      = 1\n        ADVERTISE    = 2\n        REQUEST      = 3\n        CONFIRM      = 4\n        RENEW        = 5\n        REBIND       = 6\n        REPLY        = 7\n        RELEASE      = 8\n        DECLINE      = 9\n        RECONFIGURE  = 10\n        INFO_REQUEST = 11\n        RELAY_FORW   = 12\n        RELAY_REPL   = 13\n        LEASEQUERY   = 14\n        LEASEQUERY_REPLY  = 15\n        LEASEQUERY_DONE   = 16\n        LEASEQUERY_DATA         = 17\n        ACTIVELEASEQUERY        = 18\n        STARTTLS                = 19\n        BNDUPD                  = 25\n        BNDREPLY                = 26\n        POOLREQ                 = 27\n        POOLRESP                = 28\n        UPDREQ                  = 29\n        UPDREQALL               = 30\n        UPDDONE                 = 31\n        CONNECT                 = 32\n        CONNECTREPLY            = 33\n        DISCONNECT              = 34\n        STATE                   = 36\n        CONTACT                 = 37\n    bytes<3> transaction_id\n",
    /* diameter.posa */
    "# diameter.posa — Diameter Base Protocol (RFC 6733, supersedes RFC 3588)\n# TCP 3868 (plain) / TCP 5658 (TLS) / SCTP 3868\n#\n# Fixed 20-byte header:\n#   1  version        = 1\n#   3  message_length (total packet length including header)\n#   1  flags          (R=request, P=proxyable, E=error, T=retransmit)\n#   3  command_code   (24-bit)\n#   4  application_id\n#   4  hop_by_hop_id\n#   4  end_to_end_id\n# Variable-length AVP list follows.\n\nLookup DiameterCommandCodes\n    257  = \"Capabilities-Exchange (CER/CEA)\"\n    258  = \"Re-Auth (RAR/RAA)\"\n    260  = \"Accounting (ACR/ACA)\"\n    262  = \"Abort-Session (ASR/ASA)\"\n    264  = \"Session-Termination (STR/STA)\"\n    265  = \"Event-Record (ACR/ACA)\"\n    267  = \"Disconnect-Peer (DPR/DPA)\"\n    268  = \"Diameter-EAP (DER/DEA)\"\n    271  = \"Accounting (Accounting-Request)\"\n    272  = \"Credit-Control (CCR/CCA)\"\n    274  = \"Abort-Session\"\n    275  = \"Session-Termination\"\n    280  = \"Device-Watchdog (DWR/DWA)\"\n    282  = \"Disconnect-Peer\"\n    283  = \"Capabilities-Update (CUR/CUA)\"\n    300  = \"User-Authorization (UAR/UAA)\"\n    301  = \"Server-Assignment (SAR/SAA)\"\n    302  = \"Location-Info (LIR/LIA)\"\n    303  = \"Multimedia-Auth (MAR/MAA)\"\n    304  = \"Registration-Termination (RTR/RTA)\"\n    305  = \"Push-Profile (PPR/PPA)\"\n    306  = \"User-Data (UDR/UDA)\"\n    307  = \"Profile-Update (PUR/PUA)\"\n    308  = \"Subscribe-Notifications (SNR/SNA)\"\n    309  = \"Push-Notification (PNR/PNA)\"\n    310  = \"Bootstrapping-Info (BIR/BIA)\"\n    311  = \"Message-Process-Notification (MPR/MPA)\"\n    316  = \"Update-Location (ULR/ULA)\"\n    317  = \"Cancel-Location (CLR/CLA)\"\n    318  = \"Authentication-Information (AIR/AIA)\"\n    319  = \"Insert-Subscriber-Data (IDR/IDA)\"\n    320  = \"Delete-Subscriber-Data (DSR/DSA)\"\n    321  = \"Purge-UE (PUR/PUA)\"\n    322  = \"Reset (RSR/RSA)\"\n    323  = \"Notify (NOR/NOA)\"\n\nLookup DiameterApplicationIds\n    0    = \"Diameter Common Messages\"\n    1    = \"NASREQ\"\n    2    = \"Mobile IPv4\"\n    3    = \"Diameter Base Accounting\"\n    4    = \"Diameter Credit-Control\"\n    5    = \"Diameter EAP\"\n    6    = \"Diameter SIP\"\n    7    = \"Diameter Mobile IPv6 IKE\"\n    8    = \"Diameter Mobile IPv6 Auth\"\n    9    = \"Diameter QoS\"\n    16777216 = \"3GPP Cx\"\n    16777217 = \"3GPP Sh\"\n    16777218 = \"3GPP Re\"\n    16777219 = \"3GPP Wx\"\n    16777220 = \"3GPP Zn\"\n    16777221 = \"3GPP Zh\"\n    16777222 = \"3GPP Gq\"\n    16777224 = \"3GPP Gmb\"\n    16777225 = \"3GPP Gx\"\n    16777226 = \"3GPP Gx over Gy\"\n    16777229 = \"3GPP Rx\"\n    16777231 = \"3GPP S6a/S6d\"\n    16777236 = \"3GPP S13\"\n    16777238 = \"3GPP Rx\"\n    16777252 = \"3GPP S6b\"\n    16777264 = \"3GPP SWm\"\n    16777265 = \"3GPP SWx\"\n    16777266 = \"3GPP Gxx\"\n    16777267 = \"3GPP S9\"\n    4294967295 = \"Relay\"\n\nObject<main> Diameter\n    abbrev \"diameter\"\n    col \"Diameter\"\n\n    uint8  version \"Version\" defaults(1)\n    uint24 message_length \"Message Length\" defaults(20)\n    uint8  flags \"Flags\" defaults(0x80)\n\n    bits flags req        7 1 \"Request\"\n        Answer  = 0\n        Request = 1\n    bits flags proxyable  6 1 \"Proxyable\"\n    bits flags error      5 1 \"Error\"\n    bits flags retransmit 4 1 \"Potentially Re-transmitted Message\"\n\n    uint24 command_code lookup DiameterCommandCodes \"Command Code\" defaults(257)\n    uint32 application_id lookup DiameterApplicationIds \"Application-ID\" defaults(0)\n    uint32 hop_by_hop_id  hex \"Hop-by-Hop Identifier\" defaults(0)\n    uint32 end_to_end_id  hex \"End-to-End Identifier\" defaults(0)\n\n    payload avps \"AVPs\"\n\n    info \"%s %s app=%u\" command_code, req, application_id\n\n\nrule tcp.port == 3868 => Diameter\nrule tcp.port == 5658 => Diameter\n\ncolor diameter.flags & 0x20 == 0x20 => yellow red\ncolor diameter.req == 1             => black lightyellow\ncolor diameter.req == 0             => black lightgreen\ncolor diameter                      => white darkblue\n",
    /* dicom.posa */
    "# dicom.posa — DICOM (Digital Imaging and Communications in Medicine)\n# NEMA PS3.8 — Network Communication Support for Message Exchange.\n# TCP 104 (standard), TCP 11112 (alternative/TLS).\n#\n# Every PDU starts with a 1-byte PDU type, 1 byte reserved (0x00),\n# and a 4-byte big-endian length (total PDU size excluding the first 6 bytes).\n\nObject<main> DICOM\n    abbrev \"dicom\"\n    col \"DICOM\"\n\n    uint8  pdu_type \"PDU Type\" defaults(0x01)\n        A_ASSOCIATE_RQ  = 0x01\n        A_ASSOCIATE_AC  = 0x02\n        A_ASSOCIATE_RJ  = 0x03\n        P_DATA_TF       = 0x04\n        A_RELEASE_RQ    = 0x05\n        A_RELEASE_RP    = 0x06\n        A_ABORT         = 0x07\n    uint8  reserved \"Reserved\" defaults(0)\n    uint32 pdu_length \"PDU Length\" defaults(0)\n\n    # A-ASSOCIATE-RQ / A-ASSOCIATE-AC\n    when pdu_type == 0x01:\n        uint16 protocol_version hex \"Protocol Version\" defaults(0x0001)\n        uint16 reserved2                               defaults(0)\n        bytes<16> called_ae         \"Called AE Title\"  defaults(\"ANY-SCP\")\n        bytes<16> calling_ae        \"Calling AE Title\" defaults(\"ECHOSCU\")\n        bytes<32> reserved3\n        payload  variable_items     \"Variable Items\"\n\n    when pdu_type == 0x02:\n        uint16 protocol_version hex \"Protocol Version\" defaults(0x0001)\n        uint16 reserved2                               defaults(0)\n        bytes<16> called_ae         \"Called AE Title\"  defaults(\"ANY-SCP\")\n        bytes<16> calling_ae        \"Calling AE Title\" defaults(\"ECHOSCU\")\n        bytes<32> reserved3\n        payload  variable_items     \"Variable Items\"\n\n    # A-ASSOCIATE-RJ\n    when pdu_type == 0x03:\n        uint8  reserved2 defaults(0)\n        uint8  result \"Result\" defaults(1)\n            Rejected_Permanent = 1\n            Rejected_Transient = 2\n        uint8  source \"Source\" defaults(1)\n            DICOM_UL_Service_User     = 1\n            DICOM_UL_Service_Provider = 2\n            DICOM_UL_Service_Provider_Acse = 3\n        uint8  reason \"Reason/Diag\" defaults(1)\n\n    # P-DATA-TF\n    when pdu_type == 0x04:\n        payload pdv_list \"Presentation Data Value List\"\n\n    # A-ABORT\n    when pdu_type == 0x07:\n        uint8  reserved2 defaults(0)\n        uint8  reserved3 defaults(0)\n        uint8  source \"Source\" defaults(1)\n            Service_User     = 0\n            Service_Provider = 2\n        uint8  reason \"Reason/Diag\" defaults(1)\n\n    else:\n        payload body \"Body\"\n\n    info \"DICOM %s\" pdu_type\n\n\nrule tcp.port == 104   => DICOM\nrule tcp.port == 11112 => DICOM\n\n# A-ASSOCIATE-RQ is always the first PDU; version bytes 0x01 0x00 follow\nweak rule tcp.content \"\\x01\\x00\" => DICOM\n\ncolor dicom.pdu_type == 0x03 => white red\ncolor dicom.pdu_type == 0x07 => white red\ncolor dicom.pdu_type == 0x01 => black lightyellow\ncolor dicom.pdu_type == 0x02 => black lightgreen\ncolor dicom.pdu_type == 0x04 => black lightblue\ncolor dicom                  => white darkblue\n",
    /* discord.posa */
    "# discord.posa — Discord Voice UDP Protocol\n#\n# Discord voice traffic uses a custom UDP protocol derived from RTP.\n# Two identifiable packet types appear before voice data begins:\n#\n# 1) IP Discovery (type 0x00010046 = request, 0x00020046 = response):\n#    uint32  type         — 0x00010046 (request) or 0x00020046 (response)\n#    uint32  ssrc         — voice SSRC allocated by Discord server\n#    str<64> client_ip    — NUL-padded ASCII IP address\n#    uint16  port         — client's external UDP port\n#    Total: 74 bytes\n#\n# 2) Keep-Alive (magic 0x1337cafe):\n#    uint32  magic        — 0x1337cafe (identifies keep-alive)\n#    uint32  nonce        — monotonically increasing counter\n#    Total: 8 bytes\n#\n# After the discovery exchange, Discord sends RTP-like voice frames:\n#    uint8   flags        — 0x80 (RTP-like)\n#    uint8   payload_type — 0x78 (Discord Opus)\n#    uint16  sequence     — RTP sequence number\n#    uint32  timestamp    — RTP timestamp\n#    uint32  ssrc         — SSRC\n#    bytes   payload      — encrypted Opus audio (XSalsa20-Poly1305 or AEAD-AES256-GCM)\n#\n# Voice payload is encrypted, so only the headers are readable.\n#\n# Reference: https://discord.com/developers/docs/topics/voice-connections\n#\n# Discord uses dynamic UDP ports selected by the client; port ranges are\n# typically 50000-65535. Content rules are the primary detection mechanism.\n\nObject<DISCORD> DISCORD_IP_DISCOVERY\n    abbrev \"discord\"\n    col \"Discord\"\n\n    uint32 type hex \"Packet Type\" defaults(0x00010046)\n        0x00010046 = \"IP Discovery Request\"\n        0x00020046 = \"IP Discovery Response\"\n    uint32 ssrc hex \"SSRC\" defaults(0)\n    str<64> client_ip \"Client IP\"\n    uint16 port \"Client Port\" defaults(0)\n\n    info \"Discord IP discovery type=0x%08x ssrc=0x%08x\" type, ssrc\n\nObject<DISCORD> DISCORD_KEEPALIVE\n    abbrev \"discord\"\n    col \"Discord\"\n\n    uint32 magic hex \"Magic\" defaults(0x1337cafe)\n        0x1337cafe = \"Keep-Alive\"\n    uint32 nonce \"Nonce\" defaults(0)\n\n    info \"Discord keep-alive nonce=%u\" nonce\n\nObject<DISCORD> DISCORD_VOICE\n    abbrev \"discord\"\n    col \"Discord\"\n\n    uint8  flags        hex \"Flags\" defaults(0x80)\n    uint8  payload_type hex \"Payload Type\" defaults(0x78)\n        0x78 = \"Discord Opus\"\n    uint16 sequence \"Sequence\" defaults(0)\n    uint32 timestamp \"RTP Timestamp\" defaults(0)\n    uint32 ssrc         hex \"SSRC\" defaults(0)\n    payload encrypted_payload \"Encrypted Opus Payload\"\n\n    info \"Discord voice ssrc=0x%08x seq=%u\" ssrc, sequence\n\nObject<GROUP> DISCORD\n\nrule udp.content \"\\x00\\x01\\x00\\x46\" => DISCORD\nrule udp.content \"\\x00\\x02\\x00\\x46\" => DISCORD\nrule udp.content \"\\x13\\x37\\xca\\xfe\" => DISCORD\n\n# Voice RTP-like packets: flags=0x80, payload_type=0x78\nweak rule udp.content \"\\x80\\x78\" => DISCORD\n\ncolor discord.type == 0x00010046 => black lightyellow\ncolor discord.type == 0x00020046 => black lightgreen\ncolor discord.magic == 0x1337cafe => black lightgray\ncolor discord.payload_type == 0x78 => black lightblue\ncolor discord => white darkblue\n\nalias discord.ip_request   => discord.type == 0x00010046\nalias discord.ip_response  => discord.type == 0x00020046\nalias discord.keepalive    => discord.magic == 0x1337cafe\n",
    /* dnp3.posa */
    "# DNP3 — Distributed Network Protocol 3 (IEEE 1815 / IEC 60870-5)\n#\n# SCADA/ICS protocol used in electric grids, water treatment, oil & gas pipelines.\n# Transport: TCP or UDP port 20000. Detected by fixed magic bytes 0x05 0x64.\n#\n# Frame layers:\n#   Data Link Layer (DLL) — 10-byte header (6 bytes + 2-byte dest/src + 2-byte CRC)\n#   Transport Layer  (TL) — 1 byte: FIR | FIN | 6-bit sequence\n#   Application Layer (AL) — control byte + function code + data objects\n#\n# DLL control byte bit layout:\n#   bit 7   DIR  — 1 when frame originated from master (master→outstation)\n#   bit 6   PRM  — 1 for primary station frames\n#   bit 5   FCB (primary) or DFC (secondary, data flow control)\n#   bit 4   FCV (primary) or reserved (secondary)\n#   bits 3-0 DLL function code:\n#     Primary:   0=ResetLinkStates  1=ResetUserProcess  2=TestLinkStates\n#                3=UserData  4=UnconfirmedUserData  9=RequestLinkStatus\n#     Secondary: 0=ACK  1=NACK  11=LinkStatus  15=NotSupported\n#\n# DLL length field counts payload bytes in the remainder of the frame\n# excluding CRC bytes. Data payload bytes are grouped into 16-byte blocks\n# each followed by a 2-byte CRC-16/DNP; only the header CRC is decoded here.\n\nObject<main> DNP3\n    abbrev \"dnp3\"\n    col \"DNP3\"\n\n    # ── Data Link Layer Header (10 bytes) ─────────────────────────────────────────\n    uint8     start_lo \"Start Byte 0 (0x05)\" defaults(0x05)\n    uint8     start_hi \"Start Byte 1 (0x64)\" defaults(0x64)\n    uint8     length \"DLL Length\" defaults(5)\n    uint8     control \"DLL Control\" defaults(0x44)\n        bits control dir     7 1  \"DIR\"\n            0 = Outstation\n            1 = Master\n        bits control prm     6 1  \"PRM\"\n            0 = Secondary\n            1 = Primary\n        bits control fcb_dfc 5 1  \"FCB/DFC\"\n        bits control fcv     4 1  \"FCV\"\n        bits control dl_func 0 4  \"DLL Function Code\"\n            # Primary station (PRM=1) function codes\n            0  = ResetLinkStates\n            1  = ResetUserProcess\n            2  = TestLinkStates\n            3  = UserData\n            4  = UnconfirmedUserData\n            9  = RequestLinkStatus\n            # Secondary station (PRM=0): value 0=ACK, 1=NACK share codes 0 and 1\n            11 = LinkStatus\n            15 = NotSupported\n    le_uint16 destination \"Destination Address\" defaults(0)\n    le_uint16 source \"Source Address\" defaults(0)\n    uint16    header_crc \"Header CRC\" defaults(0)\n\n    # ── Transport Layer (1 byte, present when DLL carries user data) ──────────────\n    uint8 transport \"Transport Header\" defaults(0xC0)\n        bits transport tfir 7 1  \"FIR (First Fragment)\"\n        bits transport tfin 6 1  \"FIN (Final Fragment)\"\n        bits transport tseq 0 6  \"Transport Sequence\"\n\n    # ── Application Layer (present when DLL carries user data) ───────────────────\n    uint8 app_control \"AL Control\" defaults(0xC0)\n        bits app_control afir 7 1  \"FIR (First Fragment)\"\n        bits app_control afin 6 1  \"FIN (Final Fragment)\"\n        bits app_control acon 5 1  \"CON (Confirmation Required)\"\n        bits app_control auns 4 1  \"UNS (Unsolicited)\"\n        bits app_control aseq 0 4  \"AL Sequence\"\n    uint8 function_code \"AL Function Code\" defaults(0x01)\n        0x00 = Confirm\n        0x01 = Read\n        0x02 = Write\n        0x03 = Select\n        0x04 = Operate\n        0x05 = DirectOperate\n        0x06 = DirectOperateNR\n        0x07 = FreezeReplace\n        0x13 = ColdRestart\n        0x14 = WarmRestart\n        0x17 = InitializeApplication\n        0x1A = DisableUnsolicited\n        0x1B = EnableUnsolicited\n        0x1E = AssignClass\n        0x20 = DelayMeasurement\n        0x81 = Response\n        0x82 = UnsolicitedResponse\n    payload objects \"Objects\"\n\n    info \"[%s] %u→%u %s\" dir, source, destination, function_code\n\n\n# ── Content detection (primary, port-independent) ─────────────────────────────\n# Magic bytes 0x05 0x64 are the fixed DLL start sequence; reliable for detection\n# even on non-standard ports (e.g. tunnelled SCADA or alternate TCP ports).\nweak rule tcp.content \"\\x05\\x64\" => DNP3\nweak rule udp.content \"\\x05\\x64\" => DNP3\n\n# ── Port bindings (secondary hints) ──────────────────────────────────────────\nrule tcp.port == 20000 => DNP3\nrule udp.port == 20000 => DNP3\n\n\n# ── Coloring rules ────────────────────────────────────────────────────────────\n# Cold Restart / Warm Restart — potentially disruptive to field devices;\n# seeing these unexpectedly is a SCADA security/reliability concern.\ncolor dnp3.function_code == 0x13 || dnp3.function_code == 0x14 => white red\n\n# Select / Operate / Direct Operate / Direct Operate No Response —\n# control commands that actuate field equipment.\ncolor dnp3.function_code == 0x03 || dnp3.function_code == 0x04 || dnp3.function_code == 0x05 || dnp3.function_code == 0x06 => black lightyellow\n\n# Enable / Disable Unsolicited — configuration commands changing reporting behaviour.\ncolor dnp3.function_code == 0x1A || dnp3.function_code == 0x1B => black lightyellow\n\n# Solicited Response — standard outstation reply to a master request.\ncolor dnp3.function_code == 0x81 => black lightgreen\n\n# Unsolicited Response — outstation pushing data to master without a poll.\ncolor dnp3.function_code == 0x82 => white darkblue\n\n# Catch-all — Read, Write, Confirm, Assign Class and other management traffic.\ncolor dnp3 => white darkgray\n\n\n# ── Wireshark-compatible field aliases ────────────────────────────────────────\n# These map Wireshark's dnp3 dissector field paths to the names defined above\n# so saved display filters imported from Wireshark continue to work.\nalias dnp3.ctl.dir  => dnp3.dir\nalias dnp3.ctl.prm  => dnp3.prm\nalias dnp3.al.func  => dnp3.function_code\nalias dnp3.src      => dnp3.source\nalias dnp3.dst      => dnp3.destination\nalias dnp3.len      => dnp3.length\n",
    /* dns.posa */
    "# dns.posa — DNS / mDNS / LLMNR (RFC 1035, RFC 6762), the whole message.\n#\n# This is the decoder carcal uses for ports 53, 5353 and 5355 — there is no C\n# dissector behind it. Read it as the spec: a header, then four sections of\n# records, each section repeated as many times as its counter says.\n#\n# The pieces of posa that make a record protocol expressible:\n#\n#   repeat <count> as <item>   one subtree per record, count taken from a field\n#   repeat until end as <item> records until the enclosing scope runs out\n#   label \"...\" a, b           how to title a record's subtree, from its fields\n#   dnsname                    a name, following 0xc0 compression pointers\n#   bits <src> <n> <sh> <w>    a bitfield carved out of an already-parsed field\n#   scope <lenfield>           bound the next fields to <lenfield> bytes (rdata)\n#   include <Object>           inline another object's fields (the RR body,\n#                              shared by the answer/authority/additional sections)\n#\n# To change what you see: edit this file (Analyze ▸ Decoders ▸ DNS ▸ Enter) and\n# save — no rebuild. Add an RR type by adding a `when type == N:` block.\n\n# ── A resource record ────────────────────────────────────────────────────────\n# Name, type, class, TTL, then rdlength bytes of type-dependent data. `scope\n# rdlength` bounds the rdata so an unknown type still leaves the walk on the\n# right byte — the scope ends where rdlength says, decoded or not.\n#\n# Defined first because the three record sections below `include` it.\nObject<main> RR\n    abbrev \"dns\"\n\n    dnsname name \"Name\"\n    uint16 type \"Type\" defaults(1)\n        A = 1\n        NS = 2\n        CNAME = 5\n        SOA = 6\n        PTR = 12\n        HINFO = 13\n        MX = 15\n        TXT = 16\n        AAAA = 28\n        SRV = 33\n        OPT = 41\n        NSEC = 47\n        HTTPS = 65\n    uint16 class mask 0x7fff \"Class\" defaults(1)\n        IN = 1\n        CH = 3\n        ANY = 255\n    # mDNS reuses the top class bit to mean \"flush your cache for this name\"\n    bits class flush 15 1 \"Cache flush\"\n    uint32 ttl \"Time to live\" defaults(300)\n    uint16 rdlength \"Data length\" defaults(4)\n\n    scope rdlength\n        when type == 1:\n            ip4 rdata \"Address\"\n        when type == 28:\n            ip6 rdata \"Address\"\n        when type == 12:\n            dnsname rdata \"Domain name\"\n        when type == 5:\n            dnsname rdata \"CNAME\"\n        when type == 2:\n            dnsname rdata \"Name server\"\n        when type == 15:\n            uint16 preference \"Preference\" defaults(10)\n            dnsname rdata \"Mail exchange\"\n        when type == 33:\n            uint16 priority \"Priority\" defaults(0)\n            uint16 weight \"Weight\" defaults(0)\n            uint16 port \"Port\" defaults(0)\n            dnsname rdata \"Target\"\n        when type == 6:\n            dnsname rdata \"Primary name server\"\n            dnsname rname \"Responsible mailbox\"\n            uint32 serial \"Serial\" defaults(1)\n            uint32 refresh \"Refresh\" defaults(3600)\n            uint32 retry \"Retry\" defaults(600)\n            uint32 expire \"Expire\" defaults(604800)\n            uint32 minimum \"Minimum TTL\" defaults(300)\n        # TXT is a run of length-prefixed strings — \"model=J96AP\", \"osxvers=21\", …\n        # There is no count for them, so repeat until the rdata scope runs out.\n        when type == 16:\n            repeat until end as txt\n                label \"%s\" rdata\n                uint8 txtlen \"Length\" defaults(0)\n                str[txtlen] rdata \"Text\"\n        when type == 41:\n            payload rdata \"Options\"\n        when type == 47:\n            dnsname rdata \"Next domain name\"\n\nObject<main> DNS\n    abbrev \"dns\"\n    col \"DNS\"\n\n    uint16 id \"Transaction ID\" defaults(0)\n    uint16 flags \"Flags\" defaults(0x0100)\n        bits flags qr     15 1 \"Response\"\n            Query = 0\n            Response = 1\n        bits flags opcode 11 4 \"Opcode\"\n            QUERY = 0\n            IQUERY = 1\n            STATUS = 2\n            NOTIFY = 4\n            UPDATE = 5\n        bits flags aa     10 1 \"Authoritative\"\n        bits flags tc      9 1 \"Truncated\"\n        bits flags rd      8 1 \"Recursion desired\"\n        bits flags ra      7 1 \"Recursion available\"\n        bits flags rcode   0 4 \"Reply code\"\n            NoError = 0\n            FormErr = 1\n            ServFail = 2\n            NXDomain = 3\n            NotImp = 4\n            Refused = 5\n    uint16 questions \"Questions\" defaults(1)\n    uint16 answers \"Answer RRs\" defaults(0)\n    uint16 authority \"Authority RRs\" defaults(0)\n    uint16 additional \"Additional RRs\" defaults(0)\n\n    # ── Queries ───────────────────────────────────────────────────────────────\n    # A question is a name, a type, and a class. In mDNS the top class bit is the\n    # \"QU\" bit (send the reply unicast), so the class is masked and the bit shown\n    # on its own — that is what `mask` and `bits` are for.\n    repeat questions as query \"Queries\"\n        label \"%s: type %s, class %s\" qname, qtype, qclass\n        dnsname qname \"Name\"\n        uint16 qtype \"Type\" defaults(1)\n            A = 1\n            NS = 2\n            CNAME = 5\n            SOA = 6\n            PTR = 12\n            HINFO = 13\n            MX = 15\n            TXT = 16\n            AAAA = 28\n            SRV = 33\n            OPT = 41\n            NSEC = 47\n            HTTPS = 65\n            ANY = 255\n        uint16 qclass mask 0x7fff \"Class\" defaults(1)\n            IN = 1\n            CH = 3\n            NONE = 254\n            ANY = 255\n        bits qclass qu 15 1 \"QU (reply wanted unicast)\"\n            Multicast = 0\n            Unicast = 1\n\n    repeat answers as answer \"Answers\"\n        label \"%s: type %s, class %s, %s\" name, type, class, rdata\n        include RR\n\n    repeat authority as authority_rr \"Authoritative nameservers\"\n        label \"%s: type %s, class %s, %s\" name, type, class, rdata\n        include RR\n\n    repeat additional as additional_rr \"Additional records\"\n        label \"%s: type %s, class %s, %s\" name, type, class, rdata\n        include RR\n\n    # A query has questions and no records; a response is usually the reverse —\n    # name both, and the fields the packet does not carry expand to nothing.\n    info \"%s 0x%x %s %s %s %s\" qr, id, qtype, qname, type, rdata\n\nrule udp.port == 53   => DNS\nrule udp.port == 5353 => DNS\nrule udp.port == 5355 => DNS\n\ncolor dns.rcode > 0 => yellow red\ncolor dns           => white blue\n",
    /* dot1q.posa */
    "# 802.1Q VLAN tag (IEEE 802.1Q)\n# Sits between Ethernet header and payload; EtherType 0x8100 on the outer frame.\n# tci = PCP(3 bits) | DEI(1 bit) | VID(12 bits) — VID 0 means no VLAN.\nObject<main> Dot1Q\n    uint16 tci defaults(0)\n    uint16 etype defaults(0x0800)\n        IPV4       = 0x0800\n        IPV6       = 0x86DD\n        ARP        = 0x0806\n        MPLS       = 0x8847\n        MPLS_MCAST = 0x8848\n        VLAN       = 0x8100\n        QINQ       = 0x88A8\n",
    /* drda.posa */
    "# drda.posa — DRDA (Distributed Relational Database Architecture)\n# Used by IBM DB2. TCP 50000 (default), also 446, 447.\n# Reference: Open Group DRDA v4 specification.\n#\n# DDM (Distributed Data Management) frame header (10 bytes):\n#   2  length       — total length including this header\n#   1  magic        = 0xD0 (DDM magic byte at offset 2? Actually bytes[4])\n#   1  format       — format flags\n#   2  correlid     — correlation ID\n#   2  length2      — same as length (echoed)\n#   2  codepoint    — the DDM command/reply code\n#\n# Note: Wireshark identifies DRDA by bytes[4] == 0xD0 (the 5th byte of the\n# DDM header is the \"DSS Type and Chaining Bits\" magic).\n\nLookup DrdaCodepoints\n    0x1041 = \"EXCSAT (Exchange Server Attributes)\"\n    0x1403 = \"ACCSEC (Access Security)\"\n    0x14AC = \"SECCHK (Security Check)\"\n    0x107C = \"ACCRDB (Access Relational Database)\"\n    0x2001 = \"EXCSATRD (Exchange Server Attributes Reply)\"\n    0x14A3 = \"ACCSECRD (Access Security Reply Data)\"\n    0x1219 = \"SECCHKRM (Security Check Reply)\"\n    0x2004 = \"ACCRDBRM (Access RDB Reply)\"\n    0x1232 = \"RDBACCRM (RDB Access Reply)\"\n    0x220C = \"EXCSQLIMM (Execute Immediate SQL)\"\n    0x200A = \"EXCSQLSTR (Execute SQL String)\"\n    0x200B = \"EXCSQLSET (Execute SQL Set)\"\n    0x200F = \"OPNQRY (Open Query)\"\n    0x2417 = \"OPNQRYRM (Open Query Reply)\"\n    0x241D = \"QRYDSC (Query Descriptor)\"\n    0x241E = \"QRYDTA (Query Data)\"\n    0x2215 = \"CLSQRY (Close Query)\"\n    0x220A = \"PRPSQLSTT (Prepare SQL Statement)\"\n    0x2104 = \"BGNBND (Begin Binding)\"\n    0x2105 = \"BNDSQLSTT (Bind SQL Statement)\"\n    0x2113 = \"ENDBND (End Binding)\"\n    0x220D = \"EXCSQLSTT (Execute SQL Statement)\"\n    0x2202 = \"CNTQRY (Continue Query)\"\n    0x1232 = \"RDBUPDRM (RDB Update Reply)\"\n    0x2201 = \"SQLERRRM (SQL Error Reply)\"\n    0x2213 = \"RSLSETRM (Result Set Reply)\"\n    0x1403 = \"SQLSTT (SQL Statement)\"\n    0x2410 = \"SQLSTTRD (SQL Statement Returned Data)\"\n\nObject<main> DRDA\n    abbrev \"drda\"\n    col \"DRDA\"\n\n    uint16 length \"DDM Length\" defaults(10)\n    uint8  magic hex \"Magic\" defaults(0xD0)\n    uint8  format      hex \"Format\" defaults(0x10)\n\n    bits format chained   7 1 \"Chained\"\n    bits format continued 6 1 \"Continued\"\n    bits format dss_type  4 2 \"DSS Type\"\n        Request      = 1\n        Reply        = 2\n        Object       = 3\n        Correlation  = 4\n\n    uint16 correlid \"Correlation ID\" defaults(1)\n    uint16 length2 \"Length (echo)\" defaults(4)\n    uint16 codepoint lookup DrdaCodepoints \"Codepoint\" defaults(0x1041)\n\n    payload body \"DDM Object Data\"\n\n    info \"DRDA %s corr=%u\" codepoint, correlid\n\n\nrule tcp.port == 50000 => DRDA\nrule tcp.port == 446   => DRDA\nrule tcp.port == 447   => DRDA\n# DDM magic byte at offset 4\nweak rule tcp.content@4 \"\\xd0\" => DRDA\n\ncolor drda.dss_type == 2 => black lightgreen\ncolor drda.dss_type == 1 => black lightyellow\ncolor drda               => white darkblue\n",
    /* eap.posa */
    "# EAP header (RFC 3748) — 4 bytes\n# Carried over RADIUS (attribute 79), 802.1X (EtherType 0x888E), or PPP.\n# SUCCESS and FAILURE have no data field (length=4).\nObject<main> EAP\n    uint8  code defaults(1)\n        REQUEST  = 1\n        RESPONSE = 2\n        SUCCESS  = 3\n        FAILURE  = 4\n        INITIATE = 5\n        FINISH   = 6\n    uint8  identifier defaults(0)\n    uint16 length defaults(4)\n",
    /* eapol.posa */
    "# EAPOL header (IEEE 802.1X) — 4 bytes\n# EtherType 0x888E. length = byte count of the body that follows (0 for START/LOGOFF).\nObject<main> EAPOL\n    uint8  version defaults(1)\n    uint8  type defaults(0)\n        EAP_PACKET  = 0\n        START       = 1\n        LOGOFF      = 2\n        KEY         = 3\n        ASF_ALERT   = 4\n        MKA         = 5\n        ANNOUNCEMENT_GENERIC  = 6\n        ANNOUNCEMENT_SPECIFIC = 7\n        ANNOUNCEMENT_REQ      = 8\n    uint16 length defaults(0)\n",
    /* eapol_key.posa */
    "# EAPOL-Key frame (IEEE 802.1X / WPA/WPA2 4-way handshake) — 95 bytes\n# Carried inside EAPOL (type=KEY=3, EtherType 0x888E).\n# descriptor_type: 1=WPA, 2=RSN (WPA2/WPA3).\n# key_info bitmask (big-endian):\n#   bits 0-2: Key Descriptor Version (1=TKIP, 2=AES-CCMP, 3=AES-CCMP-256)\n#   bit  3:   Key Type (1=Pairwise, 0=Group)\n#   bit  6:   Install\n#   bit  7:   Key Ack\n#   bit  8:   Key MIC\n#   bit  9:   Secure\n#   bit 12:   Encrypted Key Data\n# 4-way handshake key_info values (WPA2/AES):\n#   M1 (AP→STA,  ANonce):       0x008A  Pairwise|Ack|Ver2\n#   M2 (STA→AP,  SNonce+MIC):   0x010A  Pairwise|MIC|Ver2\n#   M3 (AP→STA,  GTK+MIC+Enc):  0x13CA  Pairwise|Install|Ack|MIC|Secure|Enc|Ver2\n#   M4 (STA→AP,  MIC+Secure):   0x030A  Pairwise|MIC|Secure|Ver2\nObject<main> EAPOL_KEY\n    uint8     descriptor_type defaults(2)\n    uint16    key_info defaults(0x008A)\n    uint16    key_length defaults(16)\n    bytes<8>  replay_counter\n    bytes<32> key_nonce\n    bytes<16> key_iv\n    bytes<8>  key_rsc\n    bytes<8>  key_id\n    bytes<16> key_mic\n    uint16    key_data_length defaults(0)\n",
    /* eigrp.posa */
    "# EIGRP header (RFC 7868, Cisco EIGRP) — 20 bytes\n# IP protocol 88, multicast 224.0.0.10. TLVs follow the header.\n# version is always 2. sequence and ack are 0 for HELLO packets.\nObject<main> EIGRP\n    uint8  version defaults(2)\n    uint8  opcode defaults(5)\n        UPDATE    = 1\n        REQUEST   = 2\n        QUERY     = 3\n        REPLY     = 4\n        HELLO     = 5\n        PROBE     = 7\n        SIA_QUERY = 10\n        SIA_REPLY = 11\n    uint16 checksum defaults(0)\n    uint32 flags defaults(0)\n    uint32 sequence defaults(0)\n    uint32 ack defaults(0)\n    uint16 vrid defaults(0)\n    uint16 as_number defaults(0)\n",
    /* enip.posa */
    "# enip.posa — EtherNet/IP (Ethernet Industrial Protocol), IEC 61158-2 / ODVA CIP Vol. 2\n#\n# EtherNet/IP is the application-layer industrial automation protocol developed\n# by Allen-Bradley (Rockwell Automation) and standardised through ODVA.  It\n# wraps the Common Industrial Protocol (CIP) in an encapsulation layer that\n# runs over standard TCP and UDP, enabling PLCs, drives, I/O modules, and HMIs\n# to communicate on ordinary Ethernet infrastructure.\n#\n# Transport:\n#   TCP/44818 — Explicit Messaging (request/reply, CIP over TCP)\n#   UDP/44818 — I/O Messaging (implicit, time-critical cyclic data)\n#   UDP/2222  — I/O Messaging (alternate port used by some devices)\n#\n# Encapsulation Header (always 24 bytes, all multi-byte fields little-endian):\n#   [0-1]   command        LE uint16  identifies the encapsulation service\n#   [2-3]   length         LE uint16  byte count of data after this 24-byte header\n#   [4-7]   session_handle LE uint32  0 on RegisterSession request; target assigns\n#   [8-11]  status         LE uint32  0x00000000 = success in responses\n#   [12-19] sender_context bytes<8>   opaque bytes; echoed unchanged in reply\n#   [20-23] options        LE uint32  reserved, must be 0\n#   [24..]  data           varies     CPF items for SendRRData/SendUnitData; else empty\n#\n# Encapsulation Commands:\n#   0x0000  NOP              — no-operation; zero-length data\n#   0x0063  ListServices     — target returns list of supported services\n#   0x0064  ListIdentity     — target returns identity object data\n#   0x0065  RegisterSession  — establish a session (must be first exchange)\n#   0x0066  UnRegisterSession — close and release a session\n#   0x006F  SendUnitData     — connected explicit messaging (requires open connection)\n#   0x0070  SendRRData       — unconnected request/reply (most CIP messages)\n#\n# Common Packet Format (CPF) for SendRRData / SendUnitData:\n#   Interface Handle  LE uint32   0 = CIP; distinguishes CIP from other protocols\n#   Timeout           LE uint16   seconds; 0 in SendUnitData\n#   Item Count        LE uint16   number of CPF items following (typically 2)\n#   Items (repeated):\n#     Type ID         LE uint16   0x0000=Null, 0x00A1=ConnAddr, 0x00B1=ConnData,\n#                                 0x00B2=UnconnData, 0x8002=SeqAddr\n#     Length          LE uint16   byte count of item data\n#     Data            bytes       item payload (CIP request/reply or address)\n#\n# Status codes (encapsulation-level; CIP service error codes live inside CPF data):\n#   0x00000000  Success\n#   0x00000001  Invalid Command         — command code unknown\n#   0x00000002  Insufficient Memory     — no resources for session/buffer\n#   0x00000003  Incorrect Data          — malformed encapsulation data\n#   0x00000064  Invalid Session Handle  — session_handle does not match any session\n#   0x00000065  Invalid Length          — length field inconsistent with header\n#   0x00000069  Unsupported Protocol    — encapsulation protocol version not supported\n\nObject<main> EtherNetIP\n    abbrev \"enip\"\n    col \"EtherNet/IP\"\n\n    # Command — identifies the encapsulation service; sent in both directions.\n    # The target echoes the same command code in its response.\n    le_uint16 command [\"Command\"] defaults(0x0065)\n        NOP               = 0x0000\n        ListServices      = 0x0063\n        ListIdentity      = 0x0064\n        RegisterSession   = 0x0065\n        UnRegisterSession = 0x0066\n        SendUnitData      = 0x006F\n        SendRRData        = 0x0070\n\n    # Length — byte count of the encapsulation data that follows the 24-byte\n    # header.  0 for commands that carry no data (NOP, UnRegisterSession).\n    le_uint16 length [\"Data Length\"] defaults(0)\n\n    # Session Handle — assigned by the target in the RegisterSession reply;\n    # must appear in every subsequent command within that session.\n    # Originators send 0 in the RegisterSession request.\n    le_uint32 session_handle [\"Session Handle\"] defaults(0)\n\n    # Status — encapsulation-level result code.  Always 0 in requests.\n    # Non-zero in responses indicates the target rejected the command.\n    # CIP-level errors are carried inside the CPF item data, not here.\n    le_uint32 status [\"Status\"] defaults(0)\n        Success              = 0x00000000\n        InvalidCommand       = 0x00000001\n        InsufficientMemory   = 0x00000002\n        IncorrectData        = 0x00000003\n        InvalidSessionHandle = 0x00000064\n        InvalidLength        = 0x00000065\n        UnsupportedProtocol  = 0x00000069\n\n    # Sender Context — 8 opaque bytes chosen by the originator; the target\n    # copies them unchanged into the response, allowing request/reply matching.\n    bytes<8> sender_context [\"Sender Context\"]\n\n    # Options — reserved for future use; must be 0.\n    le_uint32 options [\"Options\"] defaults(0)\n\n    # ── Encapsulation data — presence and format depend on command ────────────\n\n    # SendRRData and SendUnitData both carry a CPF (Common Packet Format) payload\n    # consisting of an interface handle, timeout, and a list of typed items.\n    # The two most common items are:\n    #   Null/Address item — connection addressing (may be zero-length)\n    #   Data item         — the actual CIP request or reply bytes\n\n    when command == 0x0070:\n        # SendRRData — unconnected explicit messaging; most PCCC / CIP messages\n        # use this command.  Interface handle 0 selects the CIP encapsulator.\n        le_uint32 interface_handle [\"Interface Handle\"] defaults(0)\n            CIP = 0x00000000\n        le_uint16 timeout [\"Timeout (s)\"] defaults(0)\n        le_uint16 item_count [\"Item Count\"] defaults(2)\n        repeat item_count as cpf_item [\"CPF Item\"]\n            label \"Item 0x%x\" type_id\n            le_uint16 type_id [\"Item Type ID\"] defaults(0)\n                NullAddress     = 0x0000\n                ListIdentity    = 0x000C\n                ConnectedAddr   = 0x00A1\n                ConnectedData   = 0x00B1\n                UnconnectedData = 0x00B2\n                ListServices    = 0x0100\n                SockaddrO2T     = 0x8000\n                SockaddrT2O     = 0x8001\n                SeqAddrItem     = 0x8002\n            le_uint16 item_length [\"Item Length\"] defaults(0)\n            scope item_length\n                payload item_data [\"Item Data\"]\n\n    when command == 0x006F:\n        # SendUnitData — connected explicit messaging; a connection must have been\n        # established first (via ForwardOpen).  Timeout is 0 (connection manages\n        # timing); sequence address item carries the connection ID.\n        le_uint32 interface_handle [\"Interface Handle\"] defaults(0)\n            CIP = 0x00000000\n        le_uint16 timeout [\"Timeout (s)\"] defaults(0)\n        le_uint16 item_count [\"Item Count\"] defaults(2)\n        repeat item_count as cpf_item [\"CPF Item\"]\n            label \"Item 0x%x\" type_id\n            le_uint16 type_id [\"Item Type ID\"] defaults(0)\n                NullAddress     = 0x0000\n                ConnectedAddr   = 0x00A1\n                ConnectedData   = 0x00B1\n                UnconnectedData = 0x00B2\n                SeqAddrItem     = 0x8002\n            le_uint16 item_length [\"Item Length\"] defaults(0)\n            scope item_length\n                payload item_data [\"Item Data\"]\n\n    when command == 0x0064:\n        # ListIdentity — target returns identity objects describing itself.\n        # Used by network scanning tools and IO scanners to discover devices.\n        le_uint16 item_count [\"Item Count\"] defaults(2)\n        repeat item_count as id_item [\"Identity Item\"]\n            le_uint16 type_id [\"Item Type ID\"] defaults(0)\n            le_uint16 item_length [\"Item Length\"] defaults(0)\n            scope item_length\n                payload item_data [\"Item Data\"]\n\n    when command == 0x0063:\n        # ListServices — target returns the list of CIP services it supports.\n        # Typically returns one item describing the \"Communications\" service.\n        le_uint16 item_count [\"Item Count\"] defaults(2)\n        repeat item_count as svc_item [\"Service Item\"]\n            le_uint16 type_id [\"Item Type ID\"] defaults(0)\n            le_uint16 item_length [\"Item Length\"] defaults(0)\n            scope item_length\n                payload item_data [\"Item Data\"]\n\n    else:\n        # NOP, RegisterSession, UnRegisterSession, and future commands.\n        # RegisterSession data (LE): protocol version uint16 + option flags uint16.\n        payload data [\"Data\"]\n\n    info \"%s  Sess=0x%x  %s\" command, session_handle, status\n\n\n# ── Content Detection Rules (primary — port-independent) ──────────────────────\n#\n# EtherNet/IP command codes are little-endian uint16, so the first two bytes of\n# any encapsulation frame are the low byte then the high byte.  The six most\n# common commands all have a high byte of 0x00 and a distinct low byte:\n#\n#   RegisterSession  → \\x65\\x00\n#   SendRRData       → \\x70\\x00\n#   ListServices     → \\x63\\x00\n#   ListIdentity     → \\x64\\x00\n#   SendUnitData     → \\x6f\\x00\n#   UnRegisterSession→ \\x66\\x00\n#\n# These two-byte anchors are combined with the fact that the subsequent two bytes\n# (length) are also LE uint16, making false positives very unlikely in practice.\n# Content rules are listed before port rules so the protocol is recognised even\n# on non-standard ports used in some industrial networks.\n\nweak rule tcp.content \"\\x65\\x00\" => EtherNetIP\nweak rule tcp.content \"\\x70\\x00\" => EtherNetIP\nweak rule tcp.content \"\\x63\\x00\" => EtherNetIP\nweak rule tcp.content \"\\x64\\x00\" => EtherNetIP\nweak rule tcp.content \"\\x6f\\x00\" => EtherNetIP\nweak rule tcp.content \"\\x66\\x00\" => EtherNetIP\nweak rule udp.content@0 \"\\x6f\\x00\" => EtherNetIP\nweak rule udp.content@0 \"\\x63\\x00\" => EtherNetIP\nweak rule udp.content@0 \"\\x64\\x00\" => EtherNetIP\n\n# ── Port Rules (secondary — hint when content rules did not match) ─────────────\n# Standard ODVA-assigned ports for EtherNet/IP encapsulation.\nrule tcp.port == 44818 => EtherNetIP\nrule udp.port == 44818 => EtherNetIP\nrule udp.port == 2222  => EtherNetIP\n\n\n# ── Coloring Rules ─────────────────────────────────────────────────────────────\n# Most-specific rules first; first match wins.\n\n# Encapsulation-level errors — the target rejected the command entirely.\n# Red background makes these immediately visible during troubleshooting.\ncolor enip.status != 0x00000000 => white red\n\n# Session setup — RegisterSession request/reply pair.\n# Yellow: security-relevant session establishment, requires manual attention.\ncolor enip.command == 0x0065 => black lightyellow\n\n# Session teardown — UnRegisterSession.\n# Yellow: session lifecycle event, same palette as setup.\ncolor enip.command == 0x0066 => black lightyellow\n\n# Discovery — ListServices and ListIdentity.\n# Yellow: network scanning / device discovery, not application data.\ncolor enip.command == 0x0063 => black lightyellow\ncolor enip.command == 0x0064 => black lightyellow\n\n# Data exchange — SendRRData and SendUnitData carry the actual CIP payload.\n# Green: application-level I/O, the productive traffic in an industrial network.\ncolor enip.command == 0x0070 => black lightgreen\ncolor enip.command == 0x006F => black lightgreen\n\n# NOP — keepalive / padding frame.\ncolor enip.command == 0x0000 => black lightgray\n\n# Protocol catch-all — any EtherNet/IP frame not matched by a more specific rule.\ncolor enip => white darkgreen\n\n\n# ── Wireshark-Compatible Aliases ───────────────────────────────────────────────\n# Map field names from Wireshark's enip dissector (as of Wireshark 4.x) to the\n# POSA abbrevs so that existing display filters work without modification.\n\n# Core encapsulation header\nalias enip.command        => enip.command\nalias enip.length         => enip.length\nalias enip.session        => enip.session_handle\nalias enip.status         => enip.status\nalias enip.sendercontext  => enip.sender_context\nalias enip.options        => enip.options\n\n# CPF fields (inside SendRRData / SendUnitData)\nalias enip.cpf.count      => enip.item_count\nalias enip.cpf.typeid     => enip.type_id\nalias enip.cpf.length     => enip.item_length\nalias enip.cpf.cdi.seqcnt => enip.type_id\n\n# Convenience macro aliases\nalias enip.request        => enip.session_handle == 0\nalias enip.response       => enip.session_handle != 0\nalias enip.error          => enip.status != 0\n",
    /* esp.posa */
    "# ESP header (RFC 4303) — 8 bytes\n# IP protocol 50. Encrypted payload, padding, pad_len, next_header, and ICV follow.\n# SPI=0 is reserved; seq starts at 1 and must not wrap without rekeying.\nObject<main> ESP\n    uint32 spi defaults(0)\n    uint32 seq defaults(1)\n",
    /* ftp.posa */
    "# ftp.posa — File Transfer Protocol (RFC 959)\n#\n# FTP is a line-oriented text protocol.  The control connection uses TCP port 21.\n# Active-mode data transfers use TCP port 20; passive-mode data uses an ephemeral\n# port announced in the server's 227 (PASV) or 229 (EPSV) reply.\n#\n# Three object classes share the FTP group:\n#\n#   FTP_RESPONSE       — server → client; opens with a 3-digit ASCII reply code,\n#                        then either ' ' (final line) or '-' (multi-line continuation),\n#                        then human-readable text, then CRLF.\n#                        The code is read as bytes<3> so both \"220 \" and \"220-\"\n#                        parse cleanly — the separator byte is the 4th byte.\n#\n#   FTP_COMMAND        — client → server; verb, a space, an argument, CRLF.\n#                        Covers every RFC 959 command that requires an argument\n#                        (USER, PASS, RETR, STOR, CWD, …) plus the optional-argument\n#                        commands when they are followed by a space (LIST /dir, STAT\n#                        /path, …).  The USER command additionally exposes a named\n#                        `username` field for direct filter access.\n#\n#   FTP_COMMAND_BARE   — client → server; verb, CRLF, no argument.\n#                        Covers PASV, QUIT, ABOR, CDUP, PWD, SYST, NOOP, FEAT, and\n#                        the no-argument forms of LIST/NLST/STAT/HELP.  Separating\n#                        these ensures the command field contains exactly the verb\n#                        without an embedded CRLF.\n#\n# Content detection fires on the server greeting and the most common client verbs.\n# Port rules for 21 and 20 act as secondary hints.\n\nObject<GROUP> FTP\n\n# ── FTP response codes (RFC 959 + extensions) ────────────────────────────────\nLookup FtpResponseCodes\n    \"110\" = \"Restart Marker Reply\"\n    \"120\" = \"Service Ready In nnn Minutes\"\n    \"125\" = \"Data Connection Already Open\"\n    \"150\" = \"File Status OK; Opening Data Connection\"\n    \"200\" = \"Command OK\"\n    \"202\" = \"Command Not Implemented (superfluous)\"\n    \"211\" = \"System Status\"\n    \"212\" = \"Directory Status\"\n    \"213\" = \"File Status\"\n    \"214\" = \"Help Message\"\n    \"215\" = \"System Type\"\n    \"220\" = \"Service Ready\"\n    \"221\" = \"Service Closing Control Connection\"\n    \"225\" = \"Data Connection Open; No Transfer In Progress\"\n    \"226\" = \"Closing Data Connection; Transfer Complete\"\n    \"227\" = \"Entering Passive Mode\"\n    \"228\" = \"Entering Long Passive Mode\"\n    \"229\" = \"Entering Extended Passive Mode\"\n    \"230\" = \"User Logged In\"\n    \"231\" = \"User Logged Out; Service Terminated\"\n    \"232\" = \"Logout Command Noted\"\n    \"234\" = \"AUTH TLS/SSL Accepted\"\n    \"250\" = \"Requested File Action OK\"\n    \"257\" = \"Pathname Created\"\n    \"331\" = \"Username OK, Need Password\"\n    \"332\" = \"Need Account for Login\"\n    \"350\" = \"Requested File Action Pending\"\n    \"421\" = \"Service Not Available; Closing Connection\"\n    \"425\" = \"Can't Open Data Connection\"\n    \"426\" = \"Connection Closed; Transfer Aborted\"\n    \"430\" = \"Invalid Username or Password\"\n    \"431\" = \"Need Unavailable Resource to Process Security\"\n    \"450\" = \"Requested File Action Not Taken (file busy)\"\n    \"451\" = \"Requested Action Aborted: Local Error\"\n    \"452\" = \"Requested Action Not Taken: Insufficient Storage\"\n    \"500\" = \"Syntax Error: Command Unrecognized\"\n    \"501\" = \"Syntax Error in Parameters or Arguments\"\n    \"502\" = \"Command Not Implemented\"\n    \"503\" = \"Bad Sequence of Commands\"\n    \"504\" = \"Command Not Implemented for That Parameter\"\n    \"521\" = \"Supported Address Families are (f,f)\"\n    \"522\" = \"Protocol Not Supported\"\n    \"530\" = \"Not Logged In\"\n    \"532\" = \"Need Account for Storing Files\"\n    \"550\" = \"Requested Action Not Taken: File Unavailable\"\n    \"551\" = \"Requested Action Aborted: Page Type Unknown\"\n    \"552\" = \"Requested File Action Aborted: Exceeded Storage Allocation\"\n    \"553\" = \"Requested Action Not Taken: File Name Not Allowed\"\n\n# ─── Server → client: 3-digit code, separator byte, text, CRLF ──────────────\nObject<FTP> FTP_RESPONSE\n    abbrev \"ftp\"\n    col \"FTP\"\n    starts \"1\" \"2\" \"3\" \"4\" \"5\"\n\n    # str<3> so enum_name_str() can resolve the code to a label\n    str<3> code lookup FtpResponseCodes \"Response Code\" defaults(\"220\")\n\n    # ' ' = final reply line; '-' = more lines follow with same code\n    str<1> continuation \"Continuation\" defaults(\" \")\n        \" \" = \"Final\"\n        \"-\" = \"Multi-line\"\n\n    string message until \"\\r\\n\" \"Reply Text\" defaults(\"Service ready\")\n\n    info \"%s %s\" code, message\n\n# ─── Client → server: commands that carry an argument ────────────────────────\nObject<FTP> FTP_COMMAND\n    abbrev \"ftp\"\n    col \"FTP\"\n    starts \"USER \" \"PASS \" \"ACCT \" \"CWD \"  \"PORT \" \"EPRT \" \"TYPE \" \"STRU \"\n           \"MODE \" \"RETR \" \"STOR \" \"APPE \" \"ALLO \" \"REST \" \"RNFR \" \"RNTO \"\n           \"DELE \" \"RMD \"  \"MKD \"  \"SITE \" \"OPTS \" \"MLST \" \"MLSD \" \"SIZE \"\n           \"MDTM \" \"AUTH \" \"ADAT \" \"PROT \" \"PBSZ \" \"MIC \"  \"CONF \" \"ENC \"\n           \"LIST \" \"NLST \" \"STAT \" \"HELP \"\n\n    string command until \" \" \"Command\" defaults(\"USER\")\n\n    # Expose the username separately on USER so capture filters can name it directly.\n    # All other arguments (password, path, PORT spec, …) land in the generic field.\n    when command == \"USER\":\n        string username until \"\\r\\n\" \"Username\" defaults(\"anonymous\")\n    else:\n        string argument until \"\\r\\n\" \"Argument\"\n\n    # Gap auto-close: only whichever of username/argument exists is printed\n    info \"%s %s%s\" command, username, argument\n\n# ─── Client → server: commands that take NO argument ─────────────────────────\n# These commands are followed directly by CRLF; reading until \"\\r\\n\" gives the\n# clean verb without an embedded newline in the command field.\nObject<FTP> FTP_COMMAND_BARE\n    abbrev \"ftp\"\n    col \"FTP\"\n    starts \"PASV\\r\" \"EPSV\\r\" \"STOU\\r\" \"ABOR\\r\" \"CDUP\\r\" \"QUIT\\r\" \"REIN\\r\"\n           \"PWD\\r\"  \"SYST\\r\" \"NOOP\\r\" \"FEAT\\r\" \"CCC\\r\"\n           \"LIST\\r\" \"NLST\\r\" \"STAT\\r\" \"HELP\\r\"\n\n    string command until \"\\r\\n\" \"Command\" defaults(\"NOOP\")\n\n    info \"%s\" command\n\n# ─── Content detection (primary — port-independent) ───────────────────────────\n# \"220 \" and \"220-\" both identify a server greeting; a bare \"220\" prefix would\n# also match mid-stream data, so the space/hyphen is always included.\n# Client verb rules keep their trailing space so partial collisions with URI\n# paths or body text cannot trigger a false decode.\nrule tcp.content \"220 \"  => FTP\nrule tcp.content \"220-\"  => FTP\nrule tcp.content \"USER \" => FTP\nrule tcp.content \"PASS \" => FTP\nrule tcp.content \"PASV\"  => FTP\nrule tcp.content \"RETR \" => FTP\nrule tcp.content \"STOR \" => FTP\nrule tcp.content \"LIST\"  => FTP\nrule tcp.content \"QUIT\"  => FTP\n\n# ─── Port bindings (secondary — hints when content is ambiguous) ───────────────\nrule tcp.port == 21 => FTP\nrule tcp.port == 20 => FTP\n\n# ─── Coloring (first-match wins; most-specific rules go first) ────────────────\n\n# Positive completion (2xx) — green: session milestones and successful transfers\ncolor tcp.content \"200 \" => black lightgreen   # Command OK (e.g. TYPE response)\ncolor tcp.content \"220 \" => black lightgreen   # Service ready / greeting\ncolor tcp.content \"220-\" => black lightgreen   # Greeting, multi-line\ncolor tcp.content \"221 \" => black lightgreen   # Goodbye\ncolor tcp.content \"225 \" => black lightgreen   # Data connection already open\ncolor tcp.content \"226 \" => black lightgreen   # Transfer complete\ncolor tcp.content \"227 \" => black lightgreen   # Entering passive mode (h,h,h,h,p,p)\ncolor tcp.content \"228 \" => black lightgreen   # Entering long passive mode\ncolor tcp.content \"229 \" => black lightgreen   # Entering extended passive mode (EPSV)\ncolor tcp.content \"230 \" => black lightgreen   # User logged in\ncolor tcp.content \"250 \" => black lightgreen   # Requested file action OK\ncolor tcp.content \"257 \" => black lightgreen   # Pathname created (MKD/PWD reply)\n\n# Positive preliminary (1xx) — light blue: transfer initiated, not yet complete\ncolor tcp.content \"125 \" => black lightblue    # Data connection already open; starting\ncolor tcp.content \"150 \" => black lightblue    # Opening data connection\n\n# Positive intermediate (3xx) — yellow: more information needed before completion\ncolor tcp.content \"331 \" => black lightyellow  # Username OK, need password\ncolor tcp.content \"332 \" => black lightyellow  # Need account for login\ncolor tcp.content \"350 \" => black lightyellow  # File action pending further info\n\n# Transient negative (4xx) — orange: temporary failure; client may retry\ncolor tcp.content \"421 \" => black orange       # Service not available; closing connection\ncolor tcp.content \"425 \" => black orange       # Can't open data connection\ncolor tcp.content \"426 \" => black orange       # Connection closed; transfer aborted\ncolor tcp.content \"430 \" => black orange       # Invalid username or password\ncolor tcp.content \"450 \" => black orange       # Requested file action not taken (file busy)\ncolor tcp.content \"451 \" => black orange       # Action aborted: local error in processing\ncolor tcp.content \"452 \" => black orange       # Action not taken: insufficient storage\n\n# Permanent negative (5xx) — red: action will not succeed without change\ncolor tcp.content \"500 \" => white red          # Syntax error, command unrecognized\ncolor tcp.content \"501 \" => white red          # Syntax error in parameters or arguments\ncolor tcp.content \"502 \" => white red          # Command not implemented\ncolor tcp.content \"503 \" => white red          # Bad sequence of commands\ncolor tcp.content \"504 \" => white red          # Command parameter not implemented\ncolor tcp.content \"530 \" => white red          # Not logged in\ncolor tcp.content \"532 \" => white red          # Need account for storing files\ncolor tcp.content \"550 \" => white red          # File unavailable (not found, no access)\ncolor tcp.content \"551 \" => white red          # Page type unknown\ncolor tcp.content \"552 \" => white red          # Exceeded storage allocation\ncolor tcp.content \"553 \" => white red          # File name not allowed\n\n# Client commands (both FTP_COMMAND and FTP_COMMAND_BARE) — light yellow\ncolor ftp.command => black lightyellow\n\n# Catch-all: unclassified FTP frames (e.g. active data-channel segments)\ncolor ftp => white darkblue\n\n# ─── Wireshark-compatible field aliases ───────────────────────────────────────\n# Map Wireshark's built-in ftp dissector field names to the abbrevs used here\n# so that display filters written against a Wireshark capture continue to work.\nalias ftp.response.code    => ftp.code\nalias ftp.response.arg     => ftp.message\nalias ftp.request.command  => ftp.command\nalias ftp.request.arg      => ftp.argument ftp.username\nalias ftp.username         => ftp.username\n# The passive-address string lives in the message field of a 227/229 reply\nalias ftp.passive.address  => ftp.message\n",
    /* git.posa */
    "# git.posa — Git Smart Protocol (git upload-pack / receive-pack)\n#\n# Git's \"smart\" wire protocol (v1 and v2) over TCP uses pkt-line encoding:\n# every line is prefixed with a 4-hex-digit length (including the 4-byte\n# prefix itself). A flush packet is 0000. A delimiter packet is 0001 (v2).\n#\n# Session flow (fetch, protocol v1):\n#   Server sends: reference advertisement (pkt-lines listing refs)\n#   Client sends: want/have lines then \"done\"\n#   Server sends: PACK data\n#\n# Protocol v2 (Git 2.26+):\n#   Client sends capability advertisement, server responds with capabilities.\n#   Implemented as structured command dispatch.\n#\n# The TCP session starts with the server's first pkt-line advertisement.\n# Common first bytes: \"00\" (pkt-line length in hex) or \"git-upload-pack\"\n# if the client is initiating.\n#\n# Reference: https://git-scm.com/docs/protocol-v2\n#            https://git-scm.com/docs/pack-protocol\n# Default TCP port: 9418\n\nObject<GIT> GIT_UPLOAD_PACK\n    abbrev \"git\"\n    col \"Git\"\n    starts \"git-upload-pack\"\n\n    string service until \"\\x00\\r\\n\" \"Service\" defaults(\"git-upload-pack\")\n    payload rest \"Advertisement\"\n\n    info \"Git %s\" service\n\nObject<GIT> GIT_PKTLINE\n    abbrev \"git\"\n    col \"Git\"\n\n    str<4>  pkt_len  \"Packet Length (hex)\" defaults(\"0000\")\n    payload pkt_data \"Packet Data\"\n\n    info \"Git pkt-line len=%s\" pkt_len\n\nObject<GROUP> GIT\n\nrule tcp.port == 9418 => GIT\n\nrule tcp.content \"git-upload-pack\"   => GIT\nrule tcp.content \"git-receive-pack\"  => GIT\nrule tcp.content \"git-upload-archive\" => GIT\n\ncolor git => white darkblue\n",
    /* gtp.posa */
    "# gtp.posa — GPRS Tunnelling Protocol (GTPv1 / GTPv2)\n#\n# GTP tunnels IP traffic between mobile network nodes. Two versions share the\n# same port range:\n#\n#   GTPv1 (flags bits 7-5 = 001) — used for GTP-U (user data) on UDP/2152\n#   and GTP-C (control) on UDP/2123. A 4-byte common header is followed by a\n#   4-byte TEID (Tunnel Endpoint ID), then optional sequence/N-PDU/ext fields.\n#\n#   GTPv2 (flags bits 7-5 = 010) — used for GTP-C v2 (S11/S5/S8 interfaces)\n#   on UDP/2123. A 4-byte common header is followed by an optional 4-byte TEID\n#   and a 3-byte sequence number.\n#\n# Reference: 3GPP TS 29.274 (GTPv2-C), 3GPP TS 29.281 (GTPv1-U)\n#\n# GTP-U:  UDP 2152  (user plane tunneling)\n# GTP-C:  UDP 2123  (control plane, v1 and v2)\n# GTP-PRIME: UDP 3386 (charging gateway)\n\nLookup GtpV1MessageTypes\n    1   = \"Echo Request\"\n    2   = \"Echo Response\"\n    3   = \"Version Not Supported\"\n    4   = \"Node Alive Request\"\n    5   = \"Node Alive Response\"\n    6   = \"Redirection Request\"\n    7   = \"Redirection Response\"\n    16  = \"Create PDP Context Request\"\n    17  = \"Create PDP Context Response\"\n    18  = \"Update PDP Context Request\"\n    19  = \"Update PDP Context Response\"\n    20  = \"Delete PDP Context Request\"\n    21  = \"Delete PDP Context Response\"\n    22  = \"Initiate PDP Context Activation Request\"\n    23  = \"Initiate PDP Context Activation Response\"\n    24  = \"Delete AA PDP Context Request\"\n    25  = \"Delete AA PDP Context Response\"\n    26  = \"Error Indication\"\n    27  = \"PDU Notification Request\"\n    28  = \"PDU Notification Response\"\n    29  = \"PDU Notification Reject Request\"\n    30  = \"PDU Notification Reject Response\"\n    31  = \"Supported Extension Header Notification\"\n    32  = \"Send Routing Info for GPRS Request\"\n    33  = \"Send Routing Info for GPRS Response\"\n    34  = \"Failure Report Request\"\n    35  = \"Failure Report Response\"\n    36  = \"Note MS GPRS Present Request\"\n    37  = \"Note MS GPRS Present Response\"\n    48  = \"Identification Request\"\n    49  = \"Identification Response\"\n    50  = \"SGSN Context Request\"\n    51  = \"SGSN Context Response\"\n    52  = \"SGSN Context Acknowledgement\"\n    53  = \"Forward Relocation Request\"\n    54  = \"Forward Relocation Response\"\n    55  = \"Forward Relocation Complete\"\n    56  = \"Relocation Cancel Request\"\n    57  = \"Relocation Cancel Response\"\n    58  = \"Forward SRNS Context\"\n    59  = \"Forward Relocation Complete Acknowledge\"\n    60  = \"Forward SRNS Context Acknowledge\"\n    61  = \"UE Registration Query Request\"\n    62  = \"UE Registration Query Response\"\n    70  = \"RAN Information Relay\"\n    240 = \"Data Record Transfer Request\"\n    241 = \"Data Record Transfer Response\"\n    254 = \"End Marker\"\n    255 = \"G-PDU (User Data)\"\n\nLookup GtpV2MessageTypes\n    1   = \"Echo Request\"\n    2   = \"Echo Response\"\n    3   = \"Version Not Supported Indication\"\n    25  = \"Identification Request\"\n    26  = \"Identification Response\"\n    27  = \"Context Request\"\n    28  = \"Context Response\"\n    29  = \"Context Acknowledge\"\n    30  = \"Forward Relocation Request\"\n    31  = \"Forward Relocation Response\"\n    32  = \"Create Session Request\"\n    33  = \"Create Session Response\"\n    34  = \"Modify Bearer Request\"\n    35  = \"Modify Bearer Response\"\n    36  = \"Delete Session Request\"\n    37  = \"Delete Session Response\"\n    38  = \"Change Notification Request\"\n    39  = \"Change Notification Response\"\n    40  = \"Remote UE Report Notification\"\n    41  = \"Remote UE Report Acknowledge\"\n    64  = \"Modify Bearer Command\"\n    65  = \"Modify Bearer Failure Indication\"\n    66  = \"Delete Bearer Command\"\n    67  = \"Delete Bearer Failure Indication\"\n    68  = \"Bearer Resource Command\"\n    69  = \"Bearer Resource Failure Indication\"\n    70  = \"Downlink Data Notification Failure Indication\"\n    71  = \"Trace Session Activation\"\n    72  = \"Trace Session Deactivation\"\n    73  = \"Stop Paging Indication\"\n    95  = \"Create Bearer Request\"\n    96  = \"Create Bearer Response\"\n    97  = \"Update Bearer Request\"\n    98  = \"Update Bearer Response\"\n    99  = \"Delete Bearer Request\"\n    100 = \"Delete Bearer Response\"\n    101 = \"Delete PDN Connection Set Request\"\n    102 = \"Delete PDN Connection Set Response\"\n    128 = \"Identification Request (Rel 11+)\"\n    129 = \"Identification Response (Rel 11+)\"\n    130 = \"Context Request (Rel 11+)\"\n    131 = \"Context Response (Rel 11+)\"\n    132 = \"Context Acknowledge (Rel 11+)\"\n    133 = \"Forward Relocation Request (Rel 11+)\"\n    134 = \"Forward Relocation Response (Rel 11+)\"\n    135 = \"Forward Relocation Complete Notification\"\n    136 = \"Forward Relocation Complete Acknowledge\"\n    149 = \"Detach Notification\"\n    150 = \"Detach Acknowledge\"\n    151 = \"CS Paging Indication\"\n    152 = \"RAN Information Relay\"\n    153 = \"Alert MME Notification\"\n    154 = \"Alert MME Acknowledge\"\n    155 = \"UE Activity Notification\"\n    156 = \"UE Activity Acknowledge\"\n    160 = \"Create Forwarding Tunnel Request\"\n    161 = \"Create Forwarding Tunnel Response\"\n    162 = \"Suspend Notification\"\n    163 = \"Suspend Acknowledge\"\n    164 = \"Resume Notification\"\n    165 = \"Resume Acknowledge\"\n    166 = \"Create Indirect Data Forwarding Tunnel Request\"\n    167 = \"Create Indirect Data Forwarding Tunnel Response\"\n    168 = \"Delete Indirect Data Forwarding Tunnel Request\"\n    169 = \"Delete Indirect Data Forwarding Tunnel Response\"\n    170 = \"Release Access Bearers Request\"\n    171 = \"Release Access Bearers Response\"\n    176 = \"Downlink Data Notification\"\n    177 = \"Downlink Data Notification Acknowledge\"\n    179 = \"PGW Restart Notification\"\n    180 = \"PGW Restart Notification Acknowledge\"\n    200 = \"Update PDN Connection Set Request\"\n    201 = \"Update PDN Connection Set Response\"\n    211 = \"Modify Access Bearers Request\"\n    212 = \"Modify Access Bearers Response\"\n\nObject<GTP> GTP_V1\n    abbrev \"gtp\"\n    col \"GTP\"\n\n    uint8 flags hex \"Flags\" defaults(0x30)\n        0x20 = \"GTPv1, GTP-U\"\n        0x22 = \"GTPv1, GTP-U, seq\"\n        0x30 = \"GTPv1, GTP-Prime\"\n        0x32 = \"GTPv1, seq\"\n\n    uint8 message_type lookup GtpV1MessageTypes \"Message Type\" defaults(255)\n    uint16 total_length \"Length\" defaults(0)\n    uint32 teid \"TEID\" defaults(0)\n\n    when flags == 0x22:\n        uint16 sequence_num \"Sequence Number\" defaults(0)\n        uint8 npdu_number \"N-PDU Number\" defaults(0)\n        uint8 next_ext_type \"Next Extension Header Type\" defaults(0)\n        payload body \"Payload\"\n    else:\n        payload body \"Payload\"\n\n    info \"GTPv1 type=%u TEID=0x%08x\" message_type, teid\n\nObject<GTP> GTP_V2\n    abbrev \"gtp\"\n    col \"GTP\"\n\n    uint8 flags hex \"Flags\" defaults(0x48)\n        0x40 = \"GTPv2, no TEID\"\n        0x48 = \"GTPv2, with TEID\"\n\n    uint8 message_type lookup GtpV2MessageTypes \"Message Type\" defaults(1)\n    uint16 message_length \"Message Length\" defaults(0)\n\n    when flags == 0x48:\n        uint32 teid \"TEID\" defaults(0)\n        uint8 seq1 \"Seq High\" defaults(0)\n        uint8 seq2 \"Seq Mid\" defaults(0)\n        uint8 seq3 \"Seq Low\" defaults(0)\n        uint8 spare \"Spare\" defaults(0)\n        payload body \"IEs\"\n    else:\n        uint8 seq1 \"Seq High\" defaults(0)\n        uint8 seq2 \"Seq Mid\" defaults(0)\n        uint8 seq3 \"Seq Low\" defaults(0)\n        uint8 spare \"Spare\" defaults(0)\n        payload body \"IEs\"\n\n    info \"GTPv2 type=%u\" message_type\n\nObject<GROUP> GTP\n\nrule udp.port == 2152 => GTP\nrule udp.port == 2123 => GTP\nrule udp.port == 3386 => GTP\n\nweak rule udp.content \"\\x20\\xff\" => GTP\nweak rule udp.content \"\\x32\\xff\" => GTP\nweak rule udp.content \"\\x32\\x01\" => GTP\nweak rule udp.content \"\\x32\\x02\" => GTP\nweak rule udp.content \"\\x40\\x20\" => GTP\nweak rule udp.content \"\\x48\\x20\" => GTP\n\ncolor gtp.message_type == 255 => black lightblue\ncolor gtp.message_type == 1 || gtp.message_type == 2 => black lightgray\ncolor gtp => white darkblue\n\nalias gtp.v1.type => gtp.message_type\nalias gtp.v2.type => gtp.message_type\n",
    /* hl7.posa */
    "# hl7.posa — HL7 v2.x MLLP (Minimal Lower Layer Protocol)\n# RFC-level: HL7 v2.6 Chapter 2, Appendix C (Lower Layer Protocol)\n# TCP 2575 (standard), also seen on 2576, 6661, 4000.\n#\n# MLLP framing:\n#   0x0B               — Start Block (VT, vertical tab)\n#   <HL7 message>      — HL7 v2.x pipe-delimited text\n#   0x1C               — End Block (FS, file separator)\n#   0x0D               — Carriage Return (mandatory after FS)\n#\n# The HL7 message itself starts with \"MSH|^~\\&|\" (mandatory segment header).\n# Segments are \\r-delimited; fields are |-delimited.\n# Common message types (MSH-9):\n#   ADT  — Admit/Discharge/Transfer\n#   ORM  — Order Message\n#   ORU  — Observation Result Unsolicited\n#   ACK  — Acknowledgement\n#   QRY  — Query\n#   RDE  — Pharmacy/Treatment Encoded Order\n\nObject<main> HL7_MLLP\n    abbrev \"hl7\"\n    col \"HL7\"\n\n    uint8  start_block  hex      \"Start Block (VT)\" defaults(0x0B)\n    string  message until \"\\x1c\" \"HL7 Message\"\n    uint8  end_block  hex        \"End Block (FS)\"   defaults(0x1C)\n    uint8  cr  hex               \"Carriage Return\"  defaults(0x0D)\n\n    info \"HL7 %s\" message\n\n\nrule tcp.port == 2575 => HL7_MLLP\nrule tcp.port == 2576 => HL7_MLLP\nrule tcp.content \"\\x0bMSH\" => HL7_MLLP\n\ncolor hl7 => black lightgreen\n",
    /* hpfeeds.posa */
    "# hpfeeds.posa — HPFEEDS, the Honeynet Project's authenticated data feed\n# Reference: https://hpfeeds.org/wire-protocol — TCP, conventionally port 10000\n#\n# hpfeeds is the publish/subscribe bus honeypots use to ship what they catch to\n# a central broker: a honeypot authenticates, then publishes malware samples,\n# scan events and session transcripts onto named channels that analysis tools\n# subscribe to.\n#\n# Every PDU is length-prefixed, so a reader never has to scan for a delimiter:\n#\n#   +----------+--------+---------------------------+\n#   | msglen 4 | op 1   | opcode-specific body      |\n#   +----------+--------+---------------------------+\n#\n# msglen counts the whole PDU including these five octets.\n#\n# Authentication is challenge-response and never sends the shared key: the\n# broker's Info PDU carries a nonce, and the client's Auth PDU answers with\n# sha1(nonce + authkey).  A capture therefore shows the identity but not the\n# secret — the `secret` field below is that digest, not the key.\n\nObject<main> HPFeeds\n    abbrev \"hpfeeds\"\n    col \"hpfeeds\"\n\n    uint32 msglen \"Message Length\" defaults(5)\n    uint8 opcode \"Opcode\" defaults(1)\n        0 = \"Error\"\n        1 = \"Info\"\n        2 = \"Auth\"\n        3 = \"Publish\"\n        4 = \"Subscribe\"\n\n    # ── Error: broker rejecting something, as free text ───────────────────────\n    when opcode == 0:\n        payload errmsg \"Error message\"\n\n    # ── Info: broker greeting — its name and the auth challenge ───────────────\n    when opcode == 1:\n        uint8 server_len \"Server length\" defaults(0)\n        str[server_len] server \"Server\"\n        payload nonce \"Nonce\"\n\n    # ── Auth: client identity and sha1(nonce + authkey) ───────────────────────\n    when opcode == 2:\n        uint8 ident_len \"Ident length\" defaults(0)\n        str[ident_len] ident \"Ident\"\n        payload secret \"Secret\"\n\n    # ── Publish: identity, channel, then the published data ───────────────────\n    # This is the bulk of a live feed; the payload is whatever the honeypot\n    # decided to ship — often JSON, sometimes a raw binary sample.\n    when opcode == 3:\n        uint8 ident_len \"Ident length\" defaults(0)\n        str[ident_len] ident \"Ident\"\n        uint8 channel_len \"Channel length\" defaults(0)\n        str[channel_len] channel \"Channel\"\n        payload data \"Payload\"\n\n    # ── Subscribe: identity, then the channel being joined ────────────────────\n    when opcode == 4:\n        uint8 ident_len \"Ident length\" defaults(0)\n        str[ident_len] ident \"Ident\"\n        payload channel \"Channel\"\n\n    info \"hpfeeds %s %s %s\" opcode, ident, channel\n\n\n# ── Port Rule ─────────────────────────────────────────────────────────────────\n# hpfeeds has no IANA assignment; 10000 is the conventional broker port. There \n# is no content signature to match on — the first octet of every PDU is the high \n# byte of a length.\nrule tcp.port == 10000 => HPFeeds\n\n\n# ── Coloring Rules ────────────────────────────────────────────────────────────\n# Most specific first; first match wins.\n\n# Errors — a rejected auth or a bad subscribe; the reason a feed goes quiet.\ncolor hpfeeds.opcode == 0 => white red\n\n# Authentication exchange.\ncolor hpfeeds.opcode == 1 || hpfeeds.opcode == 2 => black lightyellow\n\n# Subscribe — a consumer joining a channel.\ncolor hpfeeds.opcode == 4 => black lightblue\n\n# Protocol catch-all — Publish, the steady-state traffic.\ncolor hpfeeds => black lightgreen\n\n\n# ── Wireshark-Compatible Aliases ──────────────────────────────────────────────\nalias hpfeeds.payload => hpfeeds.data\n",
    /* hsr.posa */
    "# hsr.posa — HSR, High-availability Seamless Redundancy\n# Reference: IEC 62439-3 Clause 5\n# Ethertype 0x892F\n#\n# HSR gives substation and factory Ethernet zero-downtime redundancy without a\n# reconvergence protocol: a node on an HSR ring sends *two copies* of every\n# frame, one in each direction around the ring, each tagged with the same\n# sequence number.  The receiver takes whichever arrives first and discards the\n# duplicate.  A single break anywhere in the ring costs no frames at all —\n# which is why it is used for IEC 61850 GOOSE and sampled-value traffic where\n# a protection relay cannot wait for spanning tree.\n#\n# The 6-octet HSR tag sits directly after the source MAC, in place of the\n# ethertype, and carries the original ethertype at its end:\n#\n#   +-------------+-------------+-------------+\n#   | path+LSDU 2 | seq. nr. 2  | ethertype 2 |\n#   +-------------+-------------+-------------+\n#\n# The duplicate pair is identified by (source MAC, sequence number); the lane\n# ID bit says which way round the ring this copy travelled.\n\nObject<main> HSR\n    abbrev \"hsr\"\n    col \"HSR\"\n\n    uint16 path_lsdu hex \"Path / LSDU Size\" defaults(0)\n    # The 4-bit path field is read two ways: as one value, and split into the\n    # 3-bit network ID and the 1-bit lane ID that HSR actually uses.\n    bits path_lsdu path 12 4 \"Path\"\n    bits path_lsdu netid 13 3 \"Network id\"\n    bits path_lsdu laneid 12 1 \"Lane id\"\n        0 = \"Lane A\"\n        1 = \"Lane B\"\n    # Size of the frame from here to the end of the payload.\n    bits path_lsdu lsdu_size 0 12 \"LSDU size\"\n\n    # Incremented once per frame *pair*; both copies carry the same value,\n    # which is what lets the receiver discard the second one.\n    uint16 sequence_nr \"Sequence number\" defaults(0)\n\n    # The ethertype of the frame HSR is carrying — 0x88B8 GOOSE, 0x88BA\n    # sampled values, 0x0800 IPv4.\n    uint16 type hex \"Type\" defaults(0x0800)\n\n    payload data \"Payload\"\n\n    info \"HSR lane=%s seq=%u type=0x%x\" laneid, sequence_nr, type\n\n\n# ── Ethertype Rule ────────────────────────────────────────────────────────────\nrule eth.type == 0x892f => HSR\n\n\n# ── Coloring Rules ────────────────────────────────────────────────────────────\n# Most specific first; first match wins.\n\n# GOOSE — IEC 61850 protection trips and interlocking, the most time-critical\n# traffic on a substation network.\ncolor hsr.type == 0x88b8 => white red\n\n# Sampled values — continuous current/voltage measurement streams.\ncolor hsr.type == 0x88ba => black lightyellow\n\n# The two ring directions, so a missing lane is visible at a glance.\ncolor hsr.laneid == 0 => black lightgreen\ncolor hsr.laneid == 1 => black lightblue\n\n# Protocol catch-all.\ncolor hsr => black lightgray\n",
    /* hsrp.posa */
    "# HSRPv1 header (RFC 2281) — 20 bytes\n# UDP port 1985, multicast 224.0.0.2. auth_data is an 8-byte plaintext password (zero-padded).\n# Default priority 100; higher wins. Active router sends HELLO every helo_time seconds.\nObject<main> HSRP\n    uint8    version defaults(0)\n    uint8    op_code defaults(0)\n        HELLO  = 0\n        COUP   = 1\n        RESIGN = 2\n    uint8    state defaults(0)\n        INITIAL = 0\n        LEARN   = 1\n        LISTEN  = 2\n        SPEAK   = 4\n        STANDBY = 8\n        ACTIVE  = 16\n    uint8    helo_time defaults(3)\n    uint8    hold_time defaults(10)\n    uint8    priority defaults(100)\n    uint8    group defaults(0)\n    uint8    reserved defaults(0)\n    bytes<8> auth_data\n    ip4      virtual_ip defaults(0.0.0.0)\n",
    /* http.posa */
    "# http.posa — HTTP/1.x (RFC 9112).\n#\n# HTTP has no lengths in front of anything: it is lines of text ending in CRLF,\n# then a blank line, then the body. Two posa constructs cover that shape:\n#\n#   string <name> until \"<delim>\"      one delimited token\n#   repeat until \"<delim>\" as <item>   records until those bytes come next —\n#                                      here, header lines until the blank line\n#\n# A request and a response differ only in their first line, so they are two\n# objects in an Object<HTTP> group, told apart by their first field: a response\n# starts with \"HTTP\", a request with a method. The group is dispatched on the\n# first field's value, so \"HTTP\" as a uint32 (0x48545450) selects the response.\n#\n# Not covered (add it here, no rebuild): chunked transfer-encoding is shown as\n# the raw body, and the body is not decompressed.\n\n# ── HTTP status codes (IANA registry) ────────────────────────────────────────\nLookup HttpStatusCodes\n    # 1xx Informational\n    \"100\" = \"Continue\"\n    \"101\" = \"Switching Protocols\"\n    \"102\" = \"Processing\"\n    \"103\" = \"Early Hints\"\n    # 2xx Success\n    \"200\" = \"OK\"\n    \"201\" = \"Created\"\n    \"202\" = \"Accepted\"\n    \"203\" = \"Non-Authoritative Information\"\n    \"204\" = \"No Content\"\n    \"205\" = \"Reset Content\"\n    \"206\" = \"Partial Content\"\n    \"207\" = \"Multi-Status\"\n    \"208\" = \"Already Reported\"\n    \"226\" = \"IM Used\"\n    # 3xx Redirection\n    \"300\" = \"Multiple Choices\"\n    \"301\" = \"Moved Permanently\"\n    \"302\" = \"Found\"\n    \"303\" = \"See Other\"\n    \"304\" = \"Not Modified\"\n    \"305\" = \"Use Proxy\"\n    \"307\" = \"Temporary Redirect\"\n    \"308\" = \"Permanent Redirect\"\n    # 4xx Client Error\n    \"400\" = \"Bad Request\"\n    \"401\" = \"Unauthorized\"\n    \"402\" = \"Payment Required\"\n    \"403\" = \"Forbidden\"\n    \"404\" = \"Not Found\"\n    \"405\" = \"Method Not Allowed\"\n    \"406\" = \"Not Acceptable\"\n    \"407\" = \"Proxy Authentication Required\"\n    \"408\" = \"Request Timeout\"\n    \"409\" = \"Conflict\"\n    \"410\" = \"Gone\"\n    \"411\" = \"Length Required\"\n    \"412\" = \"Precondition Failed\"\n    \"413\" = \"Content Too Large\"\n    \"414\" = \"URI Too Long\"\n    \"415\" = \"Unsupported Media Type\"\n    \"416\" = \"Range Not Satisfiable\"\n    \"417\" = \"Expectation Failed\"\n    \"418\" = \"I'm a Teapot\"\n    \"421\" = \"Misdirected Request\"\n    \"422\" = \"Unprocessable Content\"\n    \"423\" = \"Locked\"\n    \"424\" = \"Failed Dependency\"\n    \"425\" = \"Too Early\"\n    \"426\" = \"Upgrade Required\"\n    \"428\" = \"Precondition Required\"\n    \"429\" = \"Too Many Requests\"\n    \"431\" = \"Request Header Fields Too Large\"\n    \"451\" = \"Unavailable For Legal Reasons\"\n    # 5xx Server Error\n    \"500\" = \"Internal Server Error\"\n    \"501\" = \"Not Implemented\"\n    \"502\" = \"Bad Gateway\"\n    \"503\" = \"Service Unavailable\"\n    \"504\" = \"Gateway Timeout\"\n    \"505\" = \"HTTP Version Not Supported\"\n    \"506\" = \"Variant Also Negotiates\"\n    \"507\" = \"Insufficient Storage\"\n    \"508\" = \"Loop Detected\"\n    \"510\" = \"Not Extended\"\n    \"511\" = \"Network Authentication Required\"\n\nObject<HTTP> HTTP_RESPONSE\n    abbrev \"http\"\n    col \"HTTP\"\n\n    # the group is dispatched on the first field's value, so a response is\n    # recognised by its \"HTTP\" magic — then `seek 0` rewinds over it, and the\n    # status line is read as the text it actually is\n    uint32 magic hex \"Magic\" defaults(0x48545450)\n    seek 0\n    string version until \" \" \"Version\" defaults(\"HTTP/1.1\")\n    string status_code until \" \" lookup HttpStatusCodes \"Status Code\" defaults(\"200\")\n    string reason until \"\\r\\n\" \"Reason Phrase\" defaults(\"OK\")\n    kvblock headers \"Headers\"\n    payload body \"Body\"\n\n    info \"%s %s\" status_code, reason\n\nObject<HTTP> HTTP_REQUEST\n    abbrev \"http\"\n    col \"HTTP\"\n    # Only decode as a request when the payload begins with a real method — mid-\n    # stream body segments (e.g. JPEG bytes of a 200 OK) start with neither this\n    # nor the \"HTTP\" response magic, so they fall through to TCP data instead of\n    # being mis-parsed as a request line.\n    starts \"GET \" \"POST \" \"PUT \" \"HEAD \" \"DELETE \" \"OPTIONS \" \"TRACE \" \"CONNECT \" \"PATCH \"\n\n    string method until \" \" \"Method\" defaults(\"GET\")\n    string uri until \" \" \"Request URI\" defaults(\"/\")\n    string version until \"\\r\\n\" \"Version\" defaults(\"HTTP/1.1\")\n    kvblock headers \"Headers\"\n    payload body \"Body\"\n\n    info \"%s %s %s\" method, uri, version\n\nrule tcp.port == 80   => HTTP\nrule tcp.port == 8080 => HTTP\nrule tcp.port == 8000 => HTTP\nrule tcp.port == 8888 => HTTP\nrule tcp.port == 3128 => HTTP\n\n# Content signatures — recognise HTTP on any port, not only the ones above.\n# Tried only when no port rule matched; the group then tells request from\n# response as usual. \"HTTP/1.\" (a status line) is very specific; the method\n# signatures keep their trailing space so \"GETTER\"-style false hits can't occur.\nrule tcp.content \"HTTP/1.\"  => HTTP\nrule tcp.content \"GET \"     => HTTP\nrule tcp.content \"POST \"    => HTTP\nrule tcp.content \"HEAD \"    => HTTP\nrule tcp.content \"PUT \"     => HTTP\nrule tcp.content \"DELETE \"  => HTTP\nrule tcp.content \"OPTIONS \" => HTTP\nrule tcp.content \"PATCH \"   => HTTP\n\n# Wireshark-compatible filter names → the abbrevs this decoder emits. Field\n# synonyms (single target) work in comparisons too; `http.request` selects the\n# request-only method field, `http.response` the response-only status field.\nalias http.request        => http.method\nalias http.request.method => http.method\nalias http.request.uri    => http.uri\nalias http.response       => http.status_code\nalias http.response.code  => http.status_code\nalias http.response.phrase => http.reason\n# A macro alias — a named expression reusable as a filter term.\nalias http.error          => http.response.code >= 400\n\n# Wireshark paints HTTP light green; match that instead of grey.\ncolor http => black lightgreen\n",
    /* http2.posa */
    "# HTTP/2 protocol decoder — RFC 7540\n# Detection: client connection preface \"PRI * HTTP/2.0\\r\\n\\r\\nSM\\r\\n\\r\\n\" (24 bytes) at TCP offset 0.\n# Frame format: 3-byte length + 1-byte type + 1-byte flags + 4-byte stream_id + payload.\n\n# ── A single frame ───────────────────────────────────────────────────────────\n# Defined first because the frame loop below `include`s it: include resolves in\n# a single forward pass, so the object has to already exist at that point.\nObject HTTP2Frame\n    uint24 length \"Frame Length\" defaults(0)\n    uint8 type \"Frame Type\" defaults(4)\n        DATA          = 0\n        HEADERS       = 1\n        PRIORITY      = 2\n        RST_STREAM    = 3\n        SETTINGS      = 4\n        PUSH_PROMISE  = 5\n        PING          = 6\n        GOAWAY        = 7\n        WINDOW_UPDATE = 8\n        CONTINUATION  = 9\n    uint8 flags \"Flags\" defaults(0)\n    bits flags end_stream   0 1 \"End Stream / ACK\"\n    bits flags end_headers  2 1 \"End Headers\"\n    bits flags padded       3 1 \"Padded\"\n    bits flags priority_bit 5 1 \"Priority\"\n    uint32 stream_id \"Stream Identifier\" defaults(0)\n    bytes[length] payload \"Payload\"\n\n# ── The connection ───────────────────────────────────────────────────────────\n# The client preface, then frames until the segment runs out.\nObject<main> HTTP2\n    abbrev \"http2\"\n    col \"HTTP2\"\n\n    bytes<24> preface \"Connection Preface\" defaults(\"PRI * HTTP/2.0\\r\\n\\r\\nSM\\r\\n\\r\\n\")\n\n    repeat until end as frame \"Frame\"\n        include HTTP2Frame\n        label \"%s stream=%u\" type, stream_id\n\n    info \"HTTP/2 %s stream=%u\" type, stream_id\n\n# Content detection — primary, port-independent\nrule tcp.content \"PRI * HTTP/2.0\\r\\n\\r\\nSM\\r\\n\\r\\n\" => HTTP2\n\n# Port hints — secondary\nrule tcp.port == 443 => HTTP2\nrule tcp.port == 80  => HTTP2\n\n# Coloring rules — most specific first\ncolor http2.type == 4 => black lightgray\ncolor http2.type == 6 => black lightgray\ncolor http2.type == 0 => black lightgreen\ncolor http2.type == 1 => black lightyellow\ncolor http2           => white darkblue\n\n# Wireshark-compatible field aliases\nalias http2.frame.type => http2.type\nalias http2.stream_id  => http2.stream_id\n",
    /* iax.posa */
    "# iax.posa — IAX2 (Inter-Asterisk eXchange protocol v2, RFC 5456)\n# UDP 4569. IAX2 carries VoIP signaling and media in a unified binary stream.\n#\n# Two frame types:\n#   Full frames  — first bit = 1; 12-byte header; carry signaling\n#   Mini frames  — first bit = 0; 4-byte header; carry compressed audio\n#\n# Full frame header (12 bytes):\n#   bits[15:15] F       = 1 (full frame flag)\n#   bits[14:0]  src_call — source call number\n#   bits[15:15] R       = retransmit flag\n#   bits[14:0]  dst_call — destination call number\n#   uint32      timestamp (ms since session start)\n#   uint8       outbound_seqno\n#   uint8       inbound_seqno\n#   uint8       frame_type\n#   uint8       subclass (compressed uint: if & 0x80, value = 1 << (subclass & 0x3F))\n#\n# Mini frame header (4 bytes):\n#   bits[15:15] F       = 0 (mini frame flag)\n#   bits[14:0]  src_call\n#   uint16      timestamp (low 16 bits)\n\nLookup IaxFrameTypes\n    1   = \"DTMF\"\n    2   = \"Voice\"\n    3   = \"Video\"\n    4   = \"Control\"\n    5   = \"Null\"\n    6   = \"IAX\"\n    7   = \"Text\"\n    8   = \"Image\"\n    9   = \"HTML\"\n    10  = \"CNG\"\n    11  = \"Voice (modem passthrough)\"\n    12  = \"T38\"\n\nLookup IaxControlSubclass\n    1   = \"Hangup\"\n    2   = \"Ring\"\n    3   = \"Ringing\"\n    4   = \"Answer\"\n    5   = \"Busy\"\n    6   = \"Tkoffhk\"\n    7   = \"Offhook\"\n    8   = \"Congestion\"\n    9   = \"Flash Hook\"\n    10  = \"Wink\"\n    11  = \"Option\"\n    12  = \"Key Radio\"\n    13  = \"Unkey Radio\"\n    14  = \"Call Progress\"\n    15  = \"Call Proceeding\"\n    16  = \"Hold\"\n    17  = \"Unhold\"\n    18  = \"Vidupdate\"\n    19  = \"T38 Progress\"\n    20  = \"Connected Line\"\n    21  = \"Redirecting\"\n    22  = \"CC Control\"\n    23  = \"Srcupdate\"\n    24  = \"Stop Sounds\"\n\nLookup IaxSubclass\n    1   = \"NEW\"\n    2   = \"PING\"\n    3   = \"PONG\"\n    4   = \"ACK\"\n    5   = \"HANGUP\"\n    6   = \"REJECT\"\n    7   = \"ACCEPT\"\n    8   = \"AUTHREQ\"\n    9   = \"AUTHREP\"\n    10  = \"INVAL\"\n    11  = \"LAGRQ\"\n    12  = \"LAGRP\"\n    13  = \"REGREQ\"\n    14  = \"REGAUTH\"\n    15  = \"REGACK\"\n    16  = \"REGREJ\"\n    17  = \"REGREL\"\n    18  = \"VNAK\"\n    19  = \"DPREQ\"\n    20  = \"DPREP\"\n    21  = \"DIAL\"\n    22  = \"TXREQ\"\n    23  = \"TXCNT\"\n    24  = \"TXACC\"\n    25  = \"TXREADY\"\n    26  = \"TXREL\"\n    27  = \"TXREJ\"\n    28  = \"QUELCH\"\n    29  = \"UNQUELCH\"\n    30  = \"POKE\"\n    31  = \"MWI\"\n    32  = \"UNSUPPORT\"\n    33  = \"TRANSFER\"\n    34  = \"PROVISION\"\n    35  = \"FWDOWNL\"\n    36  = \"FWDATA\"\n    37  = \"TXMEDIA\"\n    38  = \"RTKEY\"\n    39  = \"CALLTOKEN\"\n\nObject<IAX2> IAX2_FULL\n    abbrev \"iax2\"\n    col \"IAX2\"\n\n    uint16 src_call  hex \"Source Call (F=1)\" defaults(0)\n    uint16 dst_call  hex \"Dest Call\" defaults(0)\n    uint32 timestamp \"Timestamp (ms)\" defaults(0)\n    uint8  oseqno \"Outbound Seq No\" defaults(0)\n    uint8  iseqno \"Inbound Seq No\" defaults(0)\n    uint8  frame_type lookup IaxFrameTypes \"Frame Type\" defaults(6)\n    uint8  subclass \"Subclass\" defaults(1)\n\n    when frame_type == 6:\n        payload ie_list \"Information Elements\"\n\n    else:\n        payload frame_data \"Frame Data\"\n\n    info \"IAX2 %s\" frame_type\n\nObject<IAX2> IAX2_MINI\n    abbrev \"iax2\"\n    col \"IAX2\"\n\n    uint16 src_call  hex \"Source Call (F=0)\"          defaults(0)\n    uint16 mini_ts       \"Mini Timestamp (ms low 16)\" defaults(0)\n    payload  samples     \"Voice Samples\"\n\n    info \"IAX2 mini voice\"\n\nObject<GROUP> IAX2\n\nrule udp.port == 4569 => IAX2\n\ncolor iax2.frame_type == 5 => white red\ncolor iax2.frame_type == 6 => black lightyellow\ncolor iax2.frame_type == 2 => black lightblue\ncolor iax2.frame_type == 4 => black lightgreen\ncolor iax2                 => white darkblue\n",
    /* icmpv6.posa */
    "# ICMPv6 header (RFC 4443) — 8 bytes\n# Carried over IPv6 next_header=58. checksum covers IPv6 pseudo-header + ICMPv6 message.\n# data field interpretation is type-specific: echo id+seq, ND target address, etc.\nObject<main> ICMPv6\n    uint8  type defaults(128)\n        DST_UNREACH      = 1\n        PKT_TOO_BIG      = 2\n        TIME_EXCEEDED    = 3\n        PARAM_PROBLEM    = 4\n        ECHO_REQUEST     = 128\n        ECHO_REPLY       = 129\n        MLD_QUERY        = 130\n        MLD_REPORT       = 131\n        MLD_DONE         = 132\n        ROUTER_SOLICIT   = 133\n        ROUTER_ADVERT    = 134\n        NEIGHBOR_SOLICIT = 135\n        NEIGHBOR_ADVERT  = 136\n        REDIRECT         = 137\n        MLDV2_REPORT     = 143\n    uint8  code defaults(0)\n    uint16 checksum defaults(0)\n    uint32 data defaults(0)\n",
    /* iec104.posa */
    "# IEC 60870-5-104 (IEC-104) — Telecontrol Equipment and Systems\n# TCP companion standard to IEC 60870-5-101 for SCADA / EMS power-system telecontrol.\n# Reference: IEC 60870-5-104:2006 (Edition 2)\n# TCP Port: 2404\n#\n# Every APDU begins with a fixed start byte 0x68 followed by the APDU length,\n# then a 4-byte control field whose first byte selects the frame format:\n#\n#   ctrl0 bits 1-0 = 00 or 10  →  I-format: information transfer, carries ASDU\n#   ctrl0 bits 1-0 = 01        →  S-format: supervisory, RSN acknowledgement only\n#   ctrl0 bits 1-0 = 11        →  U-format: unnumbered (STARTDT / STOPDT / TESTFR)\n#\n# I-format send sequence number (SSN): bits 7-1 of ctrl0 give SSN[6:0],\n#   ctrl1 gives SSN[14:7]; combined 15-bit value = ssn_lo | (ssn_hi << 7).\n# Receive sequence number (RSN) for I and S: ctrl2[7:1] and ctrl3[7:0] as 15 bits.\n#\n# ASDU structure (I-format only, starts at byte offset 6):\n#   type_id   — data type identifier (1=SPS, 13=MvFl, 45=SC, 100=IC, ...)\n#   sq_num    — bit7: SQ flag; bits 6-0: number of information objects\n#   cause     — LE uint16: bits 5-0 COT, bit6 P/N, bit7 T, bits 15-8 originator\n#   common_addr — LE uint16: ASDU common address (station address)\n#   objects   — variable-length information object payload\n\nObject<main> IEC104\n    abbrev \"iec104\"\n    col \"IEC-104\"\n\n    uint8 start_byte \"Start (0x68)\" defaults(0x68)\n    uint8 apdu_length \"APDU Length\" defaults(4)\n\n    # ── Control field byte 0 — determines frame format ──────────────────────────\n    uint8 ctrl0 \"Control Byte 0\" defaults(0x43)\n    bits ctrl0 format_type 0 2       \"Format\"\n        0 = \"I-format\"\n        1 = \"S-format\"\n        2 = \"I-format\"\n        3 = \"U-format\"\n\n    # ── U-format: unnumbered control functions ───────────────────────────────────\n    # ctrl0 = UType | 0x03; ctrl1..ctrl3 are always 0x00.\n    # UType occupies bits 7-2; the two LSBs are the 0b11 format indicator.\n    when format_type == 3:\n        bits ctrl0 utype 2 6 \"U-Function\"\n            1  = \"STARTDT act\"\n            2  = \"STARTDT con\"\n            4  = \"STOPDT act\"\n            8  = \"STOPDT con\"\n            16 = \"TESTFR act\"\n            32 = \"TESTFR con\"\n        uint8 ctrl1 \"Control Byte 1\" defaults(0)\n        uint8 ctrl2 \"Control Byte 2\" defaults(0)\n        uint8 ctrl3 \"Control Byte 3\" defaults(0)\n\n    # ── S-format: supervisory frame — receive-sequence acknowledgement only ──────\n    # ctrl0 = 0x01, ctrl1 = 0x00; ctrl2+ctrl3 carry the 15-bit RSN.\n    when format_type == 1:\n        uint8 ctrl1 \"Control Byte 1\" defaults(0)\n        le_uint16 rsn_word \"RSN Word\" defaults(0)\n        bits rsn_word recv_seq 1 15    \"Receive Sequence Number\"\n\n    # ── I-format: information transfer — carries an ASDU ────────────────────────\n    # SSN = ssn_lo | (ssn_hi << 7) gives the 15-bit send sequence number.\n    # RSN is encoded identically to S-format in ctrl2+ctrl3.\n    else:\n        bits ctrl0 ssn_lo 1 7          \"SSN Low (bits 0-6)\"\n        uint8 ssn_hi \"SSN High (bits 7-14)\" defaults(0)\n        le_uint16 rsn_word \"RSN Word\" defaults(0)\n        bits rsn_word recv_seq 1 15    \"Receive Sequence Number\"\n\n        # ── ASDU: Application Service Data Unit ──────────────────────────────────\n        uint8 type_id \"Type ID\" defaults(1)\n            1   = \"Single-Point Information (SPS)\"\n            3   = \"Double-Point Information (DPS)\"\n            5   = \"Step Position Information\"\n            7   = \"Bitstring of 32 Bits\"\n            9   = \"Measured Value, Normalized\"\n            11  = \"Measured Value, Scaled\"\n            13  = \"Measured Value, Float\"\n            15  = \"Integrated Totals\"\n            20  = \"Packed Single-Point with SCD\"\n            21  = \"Normalized Value without Quality\"\n            30  = \"Single-Point Info with Time\"\n            31  = \"Double-Point Info with Time\"\n            34  = \"Step Position with Time\"\n            35  = \"Bitstring of 32 Bits with Time\"\n            36  = \"Measured Value, Float with Time\"\n            37  = \"Integrated Totals with Time\"\n            45  = \"Single Command (SC)\"\n            46  = \"Double Command (DC)\"\n            47  = \"Regulating Step Command\"\n            48  = \"Set-Point, Normalized\"\n            49  = \"Set-Point, Scaled\"\n            50  = \"Set-Point, Float\"\n            51  = \"Bitstring 32-bit Command\"\n            58  = \"Single Command with Time\"\n            59  = \"Double Command with Time\"\n            60  = \"Regulating Step Command with Time\"\n            61  = \"Set-Point, Normalized with Time\"\n            62  = \"Set-Point, Scaled with Time\"\n            63  = \"Set-Point, Float with Time\"\n            70  = \"End of Initialization\"\n            100 = \"Interrogation Command\"\n            101 = \"Counter Interrogation Command\"\n            102 = \"Read Command\"\n            103 = \"Clock Synchronization\"\n            104 = \"Test Command\"\n            105 = \"Reset Process Command\"\n            106 = \"Delay Acquisition Command\"\n            107 = \"Test Command with Time\"\n            110 = \"Parameter, Normalized\"\n            111 = \"Parameter, Scaled\"\n            112 = \"Parameter, Float\"\n            113 = \"Parameter Activation\"\n            120 = \"File Ready\"\n            121 = \"Section Ready\"\n            122 = \"Call Directory / Select File\"\n            123 = \"Last Section / Segment\"\n            124 = \"Ack File / Section\"\n            125 = \"Segment\"\n            126 = \"Directory\"\n\n        uint8 sq_num \"Structure Qualifier\" defaults(1)\n        bits sq_num sq_flag 7 1        \"SQ\"\n            0 = \"Discrete Objects\"\n            1 = \"Sequential Elements\"\n        bits sq_num num_objects 0 7    \"Number of Objects\"\n\n        # cause: LE uint16 — bits 5-0 = COT, bit6 = P/N, bit7 = T, bits 15-8 = OA\n        le_uint16 cause \"Cause of Transmission\" defaults(3)\n        bits cause cot_code 0 6        \"COT Code\"\n            1  = \"Periodic/Cyclic\"\n            2  = \"Background Scan\"\n            3  = \"Spontaneous\"\n            4  = \"Initialized\"\n            5  = \"Interrogation\"\n            6  = \"Activation\"\n            7  = \"Activation Confirmation\"\n            8  = \"Deactivation\"\n            9  = \"Deactivation Confirmation\"\n            10 = \"Activation Termination\"\n            11 = \"Return Info (Remote)\"\n            12 = \"Return Info (Local)\"\n            13 = \"File Transfer\"\n            20 = \"Interrogated (Global)\"\n            21 = \"Interrogated, Group 1\"\n            22 = \"Interrogated, Group 2\"\n            23 = \"Interrogated, Group 3\"\n            24 = \"Interrogated, Group 4\"\n            25 = \"Interrogated, Group 5\"\n            26 = \"Interrogated, Group 6\"\n            27 = \"Interrogated, Group 7\"\n            28 = \"Interrogated, Group 8\"\n            29 = \"Interrogated, Group 9\"\n            30 = \"Interrogated, Group 10\"\n            31 = \"Interrogated, Group 11\"\n            32 = \"Interrogated, Group 12\"\n            33 = \"Interrogated, Group 13\"\n            34 = \"Interrogated, Group 14\"\n            35 = \"Interrogated, Group 15\"\n            36 = \"Interrogated, Group 16\"\n            37 = \"Counter Request (General)\"\n            38 = \"Counter, Group 1\"\n            39 = \"Counter, Group 2\"\n            40 = \"Counter, Group 3\"\n            41 = \"Counter, Group 4\"\n            44 = \"Unknown Type ID\"\n            45 = \"Unknown COT\"\n            46 = \"Unknown ASDU Address\"\n            47 = \"Unknown IOA\"\n        bits cause pn_flag 6 1         \"P/N\"\n            0 = \"Positive\"\n            1 = \"Negative\"\n        bits cause test_flag 7 1       \"Test\"\n            0 = \"Live\"\n            1 = \"Test\"\n        bits cause orig_addr 8 8       \"Originator Address\"\n\n        le_uint16 common_addr \"ASDU Common Address\" defaults(1)\n\n        payload objects \"Information Objects\"\n\n    info \"IEC-104 %s %s COT=%s Addr=%u\" utype, type_id, cot_code, common_addr\n\n\n# ── Content Detection Rules (primary — port-independent) ──────────────────────\n# 0x68 is the fixed IEC 60870-5 start byte; reliable on non-standard ports\n# (e.g. tunnelled SCADA links or alternate TCP ports in substation networks).\n# Listed before the port rule so content detection fires first.\nweak rule tcp.content \"\\x68\" => IEC104\n\n# ── Port Rule (secondary — standard IANA assignment) ──────────────────────────\nrule tcp.port == 2404 => IEC104\n\n\n# ── Coloring Rules ─────────────────────────────────────────────────────────────\n# Most-specific rules first; first match wins.\n\n# STARTDT / STOPDT — data transfer session control; operationally significant\ncolor iec104.utype == 1 || iec104.utype == 2 || iec104.utype == 4 || iec104.utype == 8 => black yellow\n\n# TESTFR — keepalive probes; low-priority background traffic\ncolor iec104.utype == 16 || iec104.utype == 32 => white darkgray\n\n# Command type-IDs: SC, DC, SC+T, DC+T — actuating field devices; highest concern\ncolor iec104.type_id == 45 || iec104.type_id == 46 || iec104.type_id == 58 || iec104.type_id == 59 => white red\n\n# Status information: single-point, double-point, and time-tagged variants\ncolor iec104.type_id == 1 || iec104.type_id == 3 || iec104.type_id == 30 || iec104.type_id == 31 => black lightgreen\n\n# Measurement values: normalized, scaled, float — analog telemetry\ncolor iec104.type_id == 9 || iec104.type_id == 11 || iec104.type_id == 13 || iec104.type_id == 36 => white darkblue\n\n# Protocol catch-all — interrogation, clock sync, counter, file transfer, S-frames\ncolor iec104 => black lightgray\n\n\n# ── Wireshark-Compatible Aliases ───────────────────────────────────────────────\n# Map Wireshark's iec104 dissector field paths to POSA names so that existing\n# display filters and coloring rules continue to work without modification.\n\n# iec104.type_id is already the canonical name — no remapping required\n# The Wireshark \"cot\" field covers the full 2-byte cause word; map to our field\nalias iec104.cot        => iec104.cause\n# Wireshark uses \"asdu_addr\" for the common address field\nalias iec104.asdu_addr  => iec104.common_addr\n",
    /* igmp.posa */
    "# igmp.posa — IGMP v1/v2/v3 (RFC 1112, 2236, 3376).\n#\n# Bound by IP protocol number, not a port:  rule ip.proto == 2 => IGMP\n#\n# The v3 membership report (type 0x22) is a different shape from the queries and\n# the v1/v2 reports: no group address in the header, just a count of the group\n# records that follow, each with its own list of source addresses. That is two\n# nested `repeat`s — the outer one counted by a header field, the inner by a\n# field of the record it is inside.\n#\n# Every field a posa decoder produces carries its byte range, so selecting a row\n# in the details pane highlights the right bytes in the hex pane. That is free\n# here; in hand-written C it is a call you can forget to make.\n\nObject<main> IGMP\n    abbrev \"igmp\"\n    col \"IGMP\"\n\n    uint8 type \"Type\" defaults(0x11)\n        Membership Query = 0x11\n        IGMPv1 Membership Report = 0x12\n        IGMPv2 Membership Report = 0x16\n        Leave Group = 0x17\n        IGMPv3 Membership Report = 0x22\n    uint8 max_resp \"Max Resp Time\" defaults(100)\n    uint16 checksum \"Checksum\" defaults(0)\n\n    # v3 membership report: reserved, then N group records\n    when type == 0x22:\n        uint16 reserved \"Reserved\" defaults(0)\n        uint16 num_records \"Num Group Records\" defaults(1)\n        repeat num_records as record \"Group Records\"\n            label \"%s: %s (%u source(s))\" record_type, maddr, num_sources\n            uint8 record_type \"Record Type\" defaults(1)\n                MODE_IS_INCLUDE = 1\n                MODE_IS_EXCLUDE = 2\n                CHANGE_TO_INCLUDE = 3\n                CHANGE_TO_EXCLUDE = 4\n                ALLOW_NEW_SOURCES = 5\n                BLOCK_OLD_SOURCES = 6\n            uint8 aux_len \"Aux Data Length\" defaults(0)\n            uint16 num_sources \"Num Sources\" defaults(0)\n            ip4 maddr \"Multicast Address\"\n            repeat num_sources as source \"Source Addresses\"\n                label \"Source: %s\" saddr\n                ip4 saddr \"Source Address\"\n\n    # everything else (queries, v1/v2 reports, leave) carries a group address\n    when type != 0x22:\n        ip4 maddr \"Multicast Address\"\n\n    info \"%s %s\" type, maddr\n\nrule ip.proto == 2 => IGMP\n\ncolor igmp => black lightgreen\n",
    /* ikev2.posa */
    "# IKEv2 header (RFC 7296) — 28 bytes\n# UDP port 500 (or 4500 for NAT-T). version 0x20 = major 2, minor 0.\n# flags: 0x08 = Initiator, 0x20 = Response, 0x28 = both.\nObject<main> IKEv2\n    bytes<8> initiator_spi\n    bytes<8> responder_spi\n    uint8    next_payload defaults(0)\n    uint8    version defaults(0x20)\n    uint8    exchange_type defaults(34)\n        IKE_SA_INIT     = 34\n        IKE_AUTH        = 35\n        CREATE_CHILD_SA = 36\n        INFORMATIONAL   = 37\n    uint8    flags defaults(0x08)\n    uint32   message_id defaults(0)\n    uint32   length defaults(28)\n",
    /* imap.posa */
    "# ── IMAP server response keywords (RFC 3501 + common extensions) ─────────────\nLookup ImapServerKeywords\n    \"OK\"           = \"OK (success)\"\n    \"NO\"           = \"NO (failure)\"\n    \"BAD\"          = \"BAD (protocol error)\"\n    \"BYE\"          = \"BYE (server farewell)\"\n    \"PREAUTH\"      = \"Pre-Authenticated\"\n    \"CAPABILITY\"   = \"Capability List\"\n    \"FLAGS\"        = \"Flags\"\n    \"EXISTS\"       = \"Mailbox Message Count\"\n    \"RECENT\"       = \"Recent Messages\"\n    \"EXPUNGE\"      = \"Message Expunged\"\n    \"FETCH\"        = \"Fetch Data\"\n    \"SEARCH\"       = \"Search Results\"\n    \"STATUS\"       = \"Mailbox Status\"\n    \"LIST\"         = \"List Result\"\n    \"LSUB\"         = \"Subscribed Mailbox\"\n    \"NAMESPACE\"    = \"Namespace\"\n    \"SORT\"         = \"Sort Results\"\n    \"THREAD\"       = \"Thread Results\"\n    \"UNSEEN\"       = \"Unseen Message Sequence\"\n    \"PERMANENTFLAGS\" = \"Permanent Flags\"\n    \"UIDNEXT\"      = \"Next UID\"\n    \"UIDVALIDITY\"  = \"UID Validity\"\n    \"HIGHESTMODSEQ\" = \"Highest Modification Sequence\"\n    \"QUOTA\"        = \"Quota\"\n    \"QUOTAROOT\"    = \"Quota Root\"\n    \"ACL\"          = \"Access Control List\"\n    \"MYRIGHTS\"     = \"My Rights\"\n    \"LISTRIGHTS\"   = \"List Rights\"\n    \"ID\"           = \"Server Identity\"\n    \"ENABLED\"      = \"Enabled Extensions\"\n    \"IDLE\"         = \"Idle\"\n    \"VANISHED\"     = \"Vanished UIDs\"\n    \"METADATA\"     = \"Metadata\"\n\n# ── IMAP client commands (RFC 3501 + common extensions) ──────────────────────\nLookup ImapCommands\n    \"CAPABILITY\"   = \"Capability Request\"\n    \"NOOP\"         = \"No-Op\"\n    \"LOGOUT\"       = \"Logout\"\n    \"AUTHENTICATE\" = \"Authenticate (SASL)\"\n    \"LOGIN\"        = \"Login\"\n    \"STARTTLS\"     = \"Start TLS\"\n    \"SELECT\"       = \"Select Mailbox\"\n    \"EXAMINE\"      = \"Examine Mailbox (read-only)\"\n    \"CREATE\"       = \"Create Mailbox\"\n    \"DELETE\"       = \"Delete Mailbox\"\n    \"RENAME\"       = \"Rename Mailbox\"\n    \"SUBSCRIBE\"    = \"Subscribe to Mailbox\"\n    \"UNSUBSCRIBE\"  = \"Unsubscribe from Mailbox\"\n    \"LIST\"         = \"List Mailboxes\"\n    \"LSUB\"         = \"List Subscribed Mailboxes\"\n    \"STATUS\"       = \"Mailbox Status\"\n    \"APPEND\"       = \"Append Message\"\n    \"CHECK\"        = \"Checkpoint\"\n    \"CLOSE\"        = \"Close Mailbox\"\n    \"EXPUNGE\"      = \"Expunge Deleted Messages\"\n    \"SEARCH\"       = \"Search Messages\"\n    \"FETCH\"        = \"Fetch Message Data\"\n    \"STORE\"        = \"Store Message Flags\"\n    \"COPY\"         = \"Copy Messages\"\n    \"UID\"          = \"UID Command Prefix\"\n    \"NAMESPACE\"    = \"Namespace\"\n    \"IDLE\"         = \"Idle (RFC 2177)\"\n    \"ENABLE\"       = \"Enable Extension\"\n    \"GETQUOTA\"     = \"Get Quota\"\n    \"SETQUOTA\"     = \"Set Quota\"\n    \"GETACL\"       = \"Get ACL\"\n    \"SETACL\"       = \"Set ACL\"\n    \"DELETEACL\"    = \"Delete ACL\"\n    \"MYRIGHTS\"     = \"My Rights\"\n    \"OK\"           = \"Tagged OK (success)\"\n    \"NO\"           = \"Tagged NO (failure)\"\n    \"BAD\"          = \"Tagged BAD (protocol error)\"\n\n# imap.posa — Internet Message Access Protocol (RFC 3501)\n#\n# IMAP is a line-based text protocol for remote mailbox access.\n# Each PDU is one CR LF-terminated line.  Three line shapes share the wire:\n#\n#   *  KEYWORD data CRLF   untagged server response — status, data, or greeting\n#   +  text CRLF           server continuation request (literal / auth data needed)\n#   TAG COMMAND args CRLF  tagged client command or tagged server status (OK/NO/BAD)\n#\n# For untagged messages the first token after * is either a keyword (OK, BYE,\n# CAPABILITY, FLAGS, SEARCH, LIST, NAMESPACE, …) or a decimal sequence number\n# (EXISTS, RECENT, EXPUNGE, FETCH use  \"* n KEYWORD data\"  on the wire).\n# In the second form the `keyword` field captures the sequence number, and the\n# actual keyword plus its attributes appear in `data`; content-based color rules\n# below handle that case independently of the field value.\n#\n# Runs on TCP 143 (plain IMAP) and TCP 993 (IMAPS — TLS-wrapped).  IMAPS packets\n# are opaque to content rules; port 993 is the secondary hint that keeps the\n# stream labelled.  Plain IMAP on non-standard ports is detected via the content\n# rules first.\n\nObject<main> IMAP\n    abbrev \"imap\"\n    col \"IMAP\"\n\n    # The first whitespace-delimited token identifies the line type.\n    # Tagged lines have a variable-length client-chosen tag (\"A001\", \"1\", etc.)\n    # so there is no fixed prefix to dispatch on; the when/else chain below\n    # handles all three variants after reading this shared token.\n    string tag until \" \" \"Tag\" defaults(\"a001\")\n\n    when tag == \"*\":\n        # Untagged server response: \"* KEYWORD data\\r\\n\"\n        # When a decimal sequence number precedes the keyword (EXISTS, RECENT,\n        # EXPUNGE, FETCH), `keyword` holds the number and `data` holds the rest.\n        string keyword until \" \\r\" lookup ImapServerKeywords \"Keyword / Seq\" defaults(\"OK\")\n        string data until \"\\r\\n\" \"Response Data\" defaults(\"IMAP4rev1 Service Ready\")\n    else:\n        when tag == \"+\":\n            # Server continuation request: \"+ text\\r\\n\"\n            # Server is waiting for a client literal or the next AUTHENTICATE exchange.\n            string continuation until \"\\r\\n\" \"Continuation Text\"\n        else:\n            # Tagged line: \"A001 COMMAND-OR-STATUS args\\r\\n\"\n            # The `until \" \\r\"` delimiter set lets the field terminate at a space\n            # (command with arguments) or at CR (command with no arguments, e.g. NOOP).\n            string command until \" \\r\" lookup ImapCommands \"Command\" defaults(\"LOGIN\")\n            string arguments until \"\\r\\n\" \"Arguments\"\n\n    # info: missing fields collapse — each branch produces a tidy summary:\n    #   untagged   \"* OK [CAPABILITY IMAP4rev1] Dovecot ready\"\n    #   seq-prefix \"* 3 FETCH (FLAGS (\\Seen))\"\n    #   tagged cmd \"A001 LOGIN user@example.com secret\"\n    #   tagged ok  \"A001 OK [CAPABILITY IMAP4rev1] Logged in\"\n    #   continu    \"+ go ahead\"\n    info \"%s %s %s %s %s %s\" tag, keyword, command, data, arguments, continuation\n\n\n# ── Content detection (primary — fires on any port) ───────────────────────────\n# \"* OK\" and \"* BYE\" cover the server greeting and sign-off; \"* PREAUTH\" covers\n# pre-authenticated server greetings.  \"+ \" catches AUTHENTICATE exchanges.\n# \"A001 \" is the most common tagged-command prefix seen in the wild.\n\nrule tcp.content \"* OK\"      => IMAP\nrule tcp.content \"* BYE\"     => IMAP\nrule tcp.content \"* NO\"      => IMAP\nrule tcp.content \"* BAD\"     => IMAP\nrule tcp.content \"* PREAUTH\" => IMAP\nweak rule tcp.content \"+ \"        => IMAP\nrule tcp.content \"A001 \"     => IMAP\n\n# ── Port bindings (secondary — used when content rules did not match) ─────────\n# TCP 143 — plain IMAP.  TCP 993 — IMAPS (TLS-wrapped, content opaque).\n\nrule tcp.port == 143 => IMAP\nrule tcp.port == 993 => IMAP\n\n# ── Coloring rules (most-specific first) ──────────────────────────────────────\n\n# FETCH and mailbox-data responses — green (desired mail data arriving)\ncolor imap.keyword == \"FETCH\"     => black lightgreen\ncolor imap.keyword == \"SEARCH\"    => black lightgreen\ncolor imap.keyword == \"FLAGS\"     => black lightgreen\ncolor imap.keyword == \"STATUS\"    => black lightgreen\ncolor imap.keyword == \"LIST\"      => black lightgreen\ncolor imap.keyword == \"LSUB\"      => black lightgreen\ncolor imap.keyword == \"NAMESPACE\" => black lightgreen\ncolor imap.keyword == \"SORT\"      => black lightgreen\ncolor imap.keyword == \"THREAD\"    => black lightgreen\n\n# Sequence-number-prefixed messages (* n KEYWORD): use content matching because\n# imap.keyword holds the number rather than the response word in these cases.\ncolor tcp.content \" FETCH (\"      => black lightgreen\ncolor tcp.content \" EXISTS\\r\\n\"   => black lightgreen\ncolor tcp.content \" RECENT\\r\\n\"   => black lightgreen\ncolor tcp.content \" EXPUNGE\\r\\n\"  => black lightgray\n\n# Tagged OK and server greeting / PREAUTH — dark blue (session established)\ncolor imap.command == \"OK\"        => white darkblue\ncolor imap.keyword == \"OK\"        => white darkblue\ncolor imap.keyword == \"PREAUTH\"   => white darkblue\n\n# Error and disconnect responses — white on red (alert)\ncolor imap.command == \"NO\"        => white red\ncolor imap.command == \"BAD\"       => white red\ncolor imap.keyword == \"NO\"        => white red\ncolor imap.keyword == \"BAD\"       => white red\ncolor imap.keyword == \"BYE\"       => white red\n\n# Authentication and security-sensitive client commands — yellow\ncolor imap.command == \"LOGIN\"      => black lightyellow\ncolor imap.command == \"AUTHENTICATE\" => black lightyellow\ncolor imap.command == \"STARTTLS\"   => black lightyellow\n\n# Server continuation (waiting for literal data or auth step) — yellow\ncolor imap.tag == \"+\"              => black lightyellow\n\n# CAPABILITY advertisement — light blue (informational)\ncolor imap.keyword == \"CAPABILITY\" => black lightblue\n\n# Catch-all: any IMAP frame not matched above\ncolor imap                         => white darkblue\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# Map Wireshark IMAP dissector field names to the corresponding POSA fields so\n# that display filters written for Wireshark continue to work unchanged.\n\nalias imap.tag             => imap.tag\nalias imap.command         => imap.command\nalias imap.response        => imap.keyword\nalias imap.capability      => imap.data\nalias imap.request.command => imap.command\nalias imap.request.tag     => imap.tag\nalias imap.response.tag    => imap.tag\nalias imap.response.status => imap.command\nalias imap.response.code   => imap.data\nalias imap.flags           => imap.data\nalias imap.search.results  => imap.data\n\n# Convenience macro aliases for common filter expressions.\nalias imap.is_request      => imap.command\nalias imap.is_response     => imap.keyword\nalias imap.error           => imap.command == \"NO\" || imap.command == \"BAD\" || imap.keyword == \"NO\" || imap.keyword == \"BAD\"\nalias imap.auth            => imap.command == \"LOGIN\" || imap.command == \"AUTHENTICATE\"\nalias imap.encrypted       => imap.command == \"STARTTLS\" || imap.keyword == \"PREAUTH\"\n",
    /* ipv6.posa */
    "# IPv6 fixed header (RFC 8200) — 40 bytes\n# ver_tc_fl: version(4)=6 | traffic-class(8) | flow-label(20). Default 0x60000000.\n# next_header uses the same protocol numbers as IPv4 proto field.\n# src and dst are 16-byte big-endian addresses.\nObject<main> IPv6\n    uint32    ver_tc_fl defaults(0x60000000)\n    uint16    payload_len defaults(0)\n    uint8     next_header defaults(59)\n        HOPBYHOP = 0\n        ICMP     = 1\n        TCP      = 6\n        UDP      = 17\n        ROUTING  = 43\n        FRAGMENT = 44\n        ESP      = 50\n        AH       = 51\n        ICMPV6   = 58\n        NONEXT   = 59\n        DSTOPTS  = 60\n        SCTP     = 132\n    uint8     hop_limit defaults(64)\n    bytes<16> src\n    bytes<16> dst\n",
    /* irc.posa */
    "# irc.posa — Internet Relay Chat — RFC 2812 / RFC 1459\n#\n# IRC is a line-based ASCII plaintext protocol. Each message ends with CR LF.\n# Wire format:  [':' prefix SP] command [SP params] CR LF\n#   prefix  — :nick!user@host  or  :servername\n#   command — text verb (NICK, JOIN, PRIVMSG, …) or 3-digit numeric reply\n#   params  — space-separated list; the last parameter may be prefixed with ':'\n#             to allow embedded spaces (\"trailing parameter\")\n#\n# Two variants are parsed as a group:\n#   IRC_SERVER  — any message whose first byte is ':' (0x3A), which means it\n#                 carries a prefix. These are always server-originated.\n#   IRC_CLIENT  — messages beginning with a known command word (no prefix).\n#                 Also catches the handful of server messages sent without a\n#                 prefix (PING, ERROR).\n#\n# Fields exposed (both variants share the `irc` abbrev):\n#   irc.prefix   — the full prefix token including the leading ':', e.g.\n#                  \":nick!user@host\" or \":irc.example.com\"\n#   irc.command  — the command verb or numeric code, e.g. \"PRIVMSG\" or \"433\"\n#   irc.params   — everything after the command up to CR LF, e.g.\n#                  \"#general :Hello, world!\"\n#\n# What is not decoded (add it here, no rebuild needed):\n#   • The prefix is not split into nick / user / host sub-fields.\n#   • The trailing parameter is not stripped of its leading ':'.\n#   • CTCP messages embedded in PRIVMSG are not decoded.\n\nObject<IRC> IRC_SERVER\n    abbrev \"irc\"\n    col \"IRC\"\n\n    # Server messages always start with ':' (0x3A). Read that byte as a\n    # discriminator so the group engine selects this variant; then rewind to\n    # position 0 so the prefix string field captures the ':' as well.\n    uint8 colon \"Colon\" defaults(0x3A)\n    seek 0\n    string prefix  until \" \" \"Prefix\" defaults(\"irc.example.net\")\n    string command until \" \\r\" \"Command\" defaults(\"PRIVMSG\")\n    string params  until \"\\r\\n\" \"Parameters\"\n\n    info \"%s %s\" command, params\n\nObject<IRC> IRC_CLIENT\n    abbrev \"irc\"\n    col \"IRC\"\n\n    # Limit decoding to packets that begin with a known IRC command so that\n    # mid-stream TCP segments carrying bulk PRIVMSG data are not mis-parsed.\n    # Commands that can appear without a trailing space (QUIT, LIST, etc.) are\n    # listed without the space so they match the bare form as well.\n    starts \"NICK \" \"USER \" \"PASS \" \"JOIN \" \"PART \" \"QUIT\" \"PRIVMSG \" \"NOTICE \"\n           \"PING \" \"PONG \" \"MODE \" \"TOPIC \" \"KICK \" \"INVITE \" \"WHOIS \" \"WHO \"\n           \"LIST\" \"NAMES\" \"OPER \" \"AWAY\" \"MOTD\" \"REHASH\" \"DIE\" \"RESTART\"\n           \"SUMMON \" \"USERS\" \"WALLOPS \" \"USERHOST \" \"ISON \" \"LUSERS\" \"VERSION\"\n           \"STATS \" \"LINKS\" \"TIME\" \"CONNECT \" \"TRACE \" \"ADMIN\" \"INFO\"\n           \"CAP \" \"AUTHENTICATE \" \"ERROR \"\n\n    string command until \" \\r\" \"Command\" defaults(\"NICK\")\n    string params  until \"\\r\\n\" \"Parameters\"\n\n    info \"%s %s\" command, params\n\n# ─── Content detection (primary — catches non-standard ports) ─────────────────\n# These are checked before port rules and fire on any TCP stream whose payload\n# begins with a recognisable IRC command or server reply prefix.\n\nrule tcp.content \"NICK \"    => IRC\nrule tcp.content \"USER \"    => IRC\nrule tcp.content \"PASS \"    => IRC\nrule tcp.content \"JOIN \"    => IRC\nrule tcp.content \"PING \"    => IRC\nrule tcp.content \"PRIVMSG \" => IRC\nrule tcp.content \"NOTICE \"  => IRC\nrule tcp.content \":001 \"    => IRC\nrule tcp.content \":PING \"   => IRC\nrule tcp.content \"CAP \"     => IRC\n\n# ─── Port bindings (secondary hints) ─────────────────────────────────────────\n# Standard IRC (plain) and IRC-over-TLS. Port rules are checked only when no\n# content rule matched; they keep the decoder active for the whole TCP stream.\n\nrule tcp.port == 6667 => IRC\nrule tcp.port == 6697 => IRC\n\n# ─── Coloring ─────────────────────────────────────────────────────────────────\n# First match wins. More specific patterns come first; the catch-all is last.\n\n# Chat and direct messaging — green (response/data convention)\ncolor irc.command == \"PRIVMSG\" => black lightgreen\ncolor irc.command == \"NOTICE\"  => black lightblue\n\n# Channel lifecycle — yellow (request convention)\ncolor irc.command == \"JOIN\"    => black lightyellow\ncolor irc.command == \"PART\"    => black lightyellow\ncolor irc.command == \"QUIT\"    => black lightyellow\ncolor irc.command == \"INVITE\"  => black lightyellow\ncolor irc.command == \"KICK\"    => yellow red\n\n# State changes (nick, mode, topic)\ncolor irc.command == \"NICK\"    => black lightcyan\ncolor irc.command == \"MODE\"    => black lightcyan\ncolor irc.command == \"TOPIC\"   => black lightcyan\n\n# Keepalives — muted gray\ncolor irc.command == \"PING\"    => white darkgray\ncolor irc.command == \"PONG\"    => white darkgray\n\n# Fatal error\ncolor irc.command == \"ERROR\"   => white red\n\n# ── 4xx numeric error replies ─────────────────────────────────────────────────\ncolor irc.command == \"401\"     => white red\ncolor irc.command == \"402\"     => white red\ncolor irc.command == \"403\"     => white red\ncolor irc.command == \"404\"     => white red\ncolor irc.command == \"405\"     => white red\ncolor irc.command == \"406\"     => white red\ncolor irc.command == \"407\"     => white red\ncolor irc.command == \"408\"     => white red\ncolor irc.command == \"409\"     => white red\ncolor irc.command == \"411\"     => white red\ncolor irc.command == \"412\"     => white red\ncolor irc.command == \"413\"     => white red\ncolor irc.command == \"414\"     => white red\ncolor irc.command == \"421\"     => white red\ncolor irc.command == \"422\"     => white red\ncolor irc.command == \"423\"     => white red\ncolor irc.command == \"424\"     => white red\ncolor irc.command == \"431\"     => white red\ncolor irc.command == \"432\"     => white red\ncolor irc.command == \"433\"     => white red\ncolor irc.command == \"436\"     => white red\ncolor irc.command == \"437\"     => white red\ncolor irc.command == \"441\"     => white red\ncolor irc.command == \"442\"     => white red\ncolor irc.command == \"443\"     => white red\ncolor irc.command == \"444\"     => white red\ncolor irc.command == \"445\"     => white red\ncolor irc.command == \"446\"     => white red\ncolor irc.command == \"451\"     => white red\ncolor irc.command == \"461\"     => white red\ncolor irc.command == \"462\"     => white red\ncolor irc.command == \"463\"     => white red\ncolor irc.command == \"464\"     => white red\ncolor irc.command == \"465\"     => white red\ncolor irc.command == \"466\"     => white red\ncolor irc.command == \"467\"     => white red\ncolor irc.command == \"471\"     => white red\ncolor irc.command == \"472\"     => white red\ncolor irc.command == \"473\"     => white red\ncolor irc.command == \"474\"     => white red\ncolor irc.command == \"475\"     => white red\ncolor irc.command == \"476\"     => white red\ncolor irc.command == \"477\"     => white red\ncolor irc.command == \"481\"     => white red\ncolor irc.command == \"482\"     => white red\ncolor irc.command == \"483\"     => white red\ncolor irc.command == \"484\"     => white red\ncolor irc.command == \"491\"     => white red\n\n# ── Server welcome / registration (001–005) — dark blue ──────────────────────\ncolor irc.command == \"001\"     => white darkblue\ncolor irc.command == \"002\"     => white darkblue\ncolor irc.command == \"003\"     => white darkblue\ncolor irc.command == \"004\"     => white darkblue\ncolor irc.command == \"005\"     => white darkblue\n\n# ── MOTD lines (372–376) — muted gray ────────────────────────────────────────\ncolor irc.command == \"375\"     => white darkgray\ncolor irc.command == \"372\"     => white darkgray\ncolor irc.command == \"376\"     => white darkgray\n\n# ── WHOIS / WHO / NAMES / LIST replies (3xx) — light gray ────────────────────\ncolor irc.command == \"311\"     => black lightgray\ncolor irc.command == \"312\"     => black lightgray\ncolor irc.command == \"313\"     => black lightgray\ncolor irc.command == \"314\"     => black lightgray\ncolor irc.command == \"315\"     => black lightgray\ncolor irc.command == \"317\"     => black lightgray\ncolor irc.command == \"318\"     => black lightgray\ncolor irc.command == \"319\"     => black lightgray\ncolor irc.command == \"321\"     => black lightgray\ncolor irc.command == \"322\"     => black lightgray\ncolor irc.command == \"323\"     => black lightgray\ncolor irc.command == \"324\"     => black lightgray\ncolor irc.command == \"329\"     => black lightgray\ncolor irc.command == \"332\"     => black lightgray\ncolor irc.command == \"333\"     => black lightgray\ncolor irc.command == \"341\"     => black lightgray\ncolor irc.command == \"352\"     => black lightgray\ncolor irc.command == \"353\"     => black lightgray\ncolor irc.command == \"366\"     => black lightgray\ncolor irc.command == \"367\"     => black lightgray\ncolor irc.command == \"368\"     => black lightgray\n\n# ── Catch-all: any IRC packet not already coloured above ─────────────────────\n# Prefixed server messages get dark blue; unrecognised client commands get gray.\ncolor irc.prefix != \"\"         => white darkblue\ncolor irc                      => white darkgray\n\n# ─── Wireshark-compatible aliases ─────────────────────────────────────────────\n# Map names a Wireshark IRC dissector would emit to the fields this decoder\n# produces, so display-filter expressions written for Wireshark keep working.\n\nalias irc.request.command  => irc.command\nalias irc.response.command => irc.command\nalias irc.request.trailer  => irc.params\nalias irc.response.trailer => irc.params\nalias irc.nick             => irc.prefix\nalias irc.message          => irc.params\n\n# Convenience macro aliases for common filter expressions.\nalias irc.error            => irc.command >= \"400\" && irc.command < \"500\"\nalias irc.numeric          => irc.command >= \"001\" && irc.command < \"600\"\nalias irc.chat             => irc.command == \"PRIVMSG\" || irc.command == \"NOTICE\"\n",
    /* isakmp.posa */
    "# ISAKMP header (RFC 2408, IKEv1) — 28 bytes\n# UDP port 500. version 0x10 = major 1 / minor 0. Distinguishable from IKEv2 by version field.\n# IDENTITY_PROTECT (main mode) and AGGRESSIVE are the two common exchange types.\nObject<main> ISAKMP\n    bytes<8> initiator_cookie\n    bytes<8> responder_cookie\n    uint8    next_payload defaults(0)\n    uint8    version defaults(0x10)\n    uint8    exchange_type defaults(2)\n        NONE             = 0\n        BASE             = 1\n        IDENTITY_PROTECT = 2\n        AUTH_ONLY        = 3\n        AGGRESSIVE       = 4\n        INFORMATIONAL    = 5\n        QUICK_MODE       = 32\n        NEW_GROUP_MODE   = 33\n    uint8    flags defaults(0)\n    uint32   message_id defaults(0)\n    uint32   length defaults(28)\n",
    /* kafka.posa */
    "# kafka.posa — Apache Kafka Wire Protocol\n#\n# Kafka's binary protocol is length-prefixed and big-endian throughout.\n# Every request begins with a 4-byte frame length (not counting these 4 bytes\n# themselves), followed by the request header: api_key, api_version,\n# correlation_id, and a client_id (uint16 length-prefixed string).\n#\n# Responses share the same framing (4-byte length + 4-byte correlation_id)\n# but have no api_key — they're matched to the request by correlation_id.\n#\n# Reference: https://kafka.apache.org/protocol\n#\n# Runs on TCP port 9092 (default plaintext), 9093 (TLS).\n\nLookup KafkaApiKeys\n    0  = \"Produce\"\n    1  = \"Fetch\"\n    2  = \"ListOffsets\"\n    3  = \"Metadata\"\n    4  = \"LeaderAndIsr\"\n    5  = \"StopReplica\"\n    6  = \"UpdateMetadata\"\n    7  = \"ControlledShutdown\"\n    8  = \"OffsetCommit\"\n    9  = \"OffsetFetch\"\n    10 = \"FindCoordinator\"\n    11 = \"JoinGroup\"\n    12 = \"Heartbeat\"\n    13 = \"LeaveGroup\"\n    14 = \"SyncGroup\"\n    15 = \"DescribeGroups\"\n    16 = \"ListGroups\"\n    17 = \"SaslHandshake\"\n    18 = \"ApiVersions\"\n    19 = \"CreateTopics\"\n    20 = \"DeleteTopics\"\n    21 = \"DeleteRecords\"\n    22 = \"InitProducerId\"\n    23 = \"OffsetForLeaderEpoch\"\n    24 = \"AddPartitionsToTxn\"\n    25 = \"AddOffsetsToTxn\"\n    26 = \"EndTxn\"\n    27 = \"WriteTxnMarkers\"\n    28 = \"TxnOffsetCommit\"\n    29 = \"DescribeAcls\"\n    30 = \"CreateAcls\"\n    31 = \"DeleteAcls\"\n    32 = \"DescribeConfigs\"\n    33 = \"AlterConfigs\"\n    34 = \"AlterReplicaLogDirs\"\n    35 = \"DescribeLogDirs\"\n    36 = \"SaslAuthenticate\"\n    37 = \"CreatePartitions\"\n    38 = \"CreateDelegationToken\"\n    39 = \"RenewDelegationToken\"\n    40 = \"ExpireDelegationToken\"\n    41 = \"DescribeDelegationToken\"\n    42 = \"DeleteGroups\"\n    43 = \"ElectLeaders\"\n    44 = \"IncrementalAlterConfigs\"\n    45 = \"AlterPartitionReassignments\"\n    46 = \"ListPartitionReassignments\"\n    47 = \"OffsetDelete\"\n    48 = \"DescribeClientQuotas\"\n    49 = \"AlterClientQuotas\"\n    50 = \"DescribeUserScramCredentials\"\n    51 = \"AlterUserScramCredentials\"\n    56 = \"AlterPartition\"\n    57 = \"UpdateFeatures\"\n    60 = \"DescribeCluster\"\n    61 = \"DescribeProducers\"\n    65 = \"DescribeTransactions\"\n    66 = \"ListTransactions\"\n    67 = \"AllocateProducerIds\"\n    68 = \"ConsumerGroupHeartbeat\"\n    69 = \"ConsumerGroupDescribe\"\n    71 = \"GetTelemetrySubscriptions\"\n    72 = \"PushTelemetry\"\n    74 = \"ListClientMetrics\"\n\nObject<KAFKA> KAFKA_REQUEST\n    abbrev \"kafka\"\n    col \"Kafka\"\n\n    uint32 message_length \"Frame Length\" defaults(0)\n    uint16 api_key lookup KafkaApiKeys \"API Key\" defaults(18)\n    uint16 api_version \"API Version\" defaults(0)\n    uint32 correlation_id \"Correlation ID\" defaults(1)\n    uint16 client_id_length \"Client ID Length\" defaults(0)\n    str[client_id_length] client_id \"Client ID\"\n\n    payload body \"Request Body\"\n\n    info \"%s v%u corr=%u\" api_key, api_version, correlation_id\n\nObject<KAFKA> KAFKA_RESPONSE\n    abbrev \"kafka\"\n    col \"Kafka\"\n\n    uint32 message_length \"Frame Length\" defaults(0)\n    uint32 correlation_id \"Correlation ID\" defaults(1)\n\n    payload body \"Response Body\"\n\n    info \"Response corr=%u\" correlation_id\n\nObject<GROUP> KAFKA\n\nrule tcp.port == 9092 => KAFKA\nrule tcp.port == 9093 => KAFKA\n\ncolor kafka.api_key == 0  => black lightyellow\ncolor kafka.api_key == 1  => black lightgreen\ncolor kafka.api_key == 18 => black lightgray\ncolor kafka => white darkblue\n\nalias kafka.request.api  => kafka.api_key\nalias kafka.request.corr => kafka.correlation_id\n",
    /* kerberos.posa */
    "# kerberos.posa — Kerberos 5 protocol (RFC 4120)\n#\n# Transport: UDP port 88 (no record mark).  Use UDP to avoid Wireshark's\n#   TCP reassembly \"Unreassembled fragment\" notes.\n#\n# Two protocol objects:\n#   KRB5     — request messages (AS-REQ, TGS-REQ): pvno=[1], msg-type=[2]\n#   KRB5_REP — reply/error messages (AS-REP, TGS-REP, AP-*, KRB-ERROR):\n#              pvno=[0], msg-type=[1]  (RFC 4120 §5.4.2, §5.9.1)\n#\n# Encoding:\n#   body_len=0x0c (12) and seq_len=0x0a (10) are the correct DER lengths\n#   for the 12-byte SEQUENCE content and 10-byte field content respectively.\n#   With correct lengths Wireshark parses pvno+msg-type cleanly and stops\n#   at end-of-sequence without crashing.\n#\n# Usage examples:\n#   # AS-REQ (client → KDC)\n#   IP(src=\"10.0.1.1\",dst=\"10.0.0.1\")/UDP(sport=52001,dport=88)/KRB5(app_tag=AS_REQ,msg_type=AS_REQUEST)\n#\n#   # AS-REP (KDC → client)\n#   IP(src=\"10.0.0.1\",dst=\"10.0.1.1\")/UDP(sport=88,dport=52001)/KRB5_REP(app_tag=AS_REP,msg_type=AS_REPLY)\n#\n#   # KRB-ERROR (pre-authentication required)\n#   IP(src=\"10.0.0.1\",dst=\"10.0.1.1\")/UDP(sport=88,dport=52001)/KRB5_REP(app_tag=KRB_ERROR,msg_type=KRB_ERROR_VAL)\n\n# ─── Request messages (AS-REQ, TGS-REQ): pvno=[1], msg-type=[2] ─────────────\nObject<main> KRB5\n\n    # ── ASN.1 APPLICATION tag ────────────────────────────────────────────────\n    uint8 app_tag defaults(0x6a)\n        AS_REQ    = 0x6a   # [APPLICATION 10] Authentication Service Request\n        TGS_REQ   = 0x6c   # [APPLICATION 12] Ticket-Granting Service Request\n        AP_REQ    = 0x6e   # [APPLICATION 14] Application Request\n\n    # ── Outer BER/DER length ─────────────────────────────────────────────────\n    # 12 bytes follow: seq_tag(1) + seq_len(1) + ctx1(5) + ctx2(5)\n    uint8 body_len defaults(0x0c)\n\n    # ── SEQUENCE wrapper ─────────────────────────────────────────────────────\n    uint8 seq_tag defaults(0x30)\n    # 10 bytes: ctx1(5) + ctx2(5)\n    uint8 seq_len defaults(0x0a)\n\n    # ── [1] pvno — Kerberos protocol version (always 5) ─────────────────────\n    uint8 ctx1_tag defaults(0xa1)   # CONTEXT [1] CONSTRUCTED\n    uint8 ctx1_len defaults(0x03)\n    uint8 pvno_tag defaults(0x02)   # UNIVERSAL INTEGER\n    uint8 pvno_len defaults(0x01)\n    uint8 pvno defaults(5)\n\n    # ── [2] msg-type ─────────────────────────────────────────────────────────\n    uint8 ctx2_tag defaults(0xa2)   # CONTEXT [2] CONSTRUCTED\n    uint8 ctx2_len defaults(0x03)\n    uint8 mtype_tag defaults(0x02)   # UNIVERSAL INTEGER\n    uint8 mtype_len defaults(0x01)\n    uint8 msg_type defaults(10)\n        AS_REQUEST    = 10   # Authentication Service Request\n        TGS_REQUEST   = 12   # Ticket-Granting Service Request\n        AP_REQUEST    = 14   # Application authentication request\n\n    payload body\n\n\n# ─── Reply/error messages (AS-REP, TGS-REP, KRB-ERROR): pvno=[0], msg-type=[1]\nObject<main> KRB5_REP\n\n    # ── ASN.1 APPLICATION tag ────────────────────────────────────────────────\n    uint8 app_tag defaults(0x6b)\n        AS_REP    = 0x6b   # [APPLICATION 11] Authentication Service Reply\n        TGS_REP   = 0x6d   # [APPLICATION 13] Ticket-Granting Service Reply\n        AP_REP    = 0x6f   # [APPLICATION 15] Application Reply\n        KRB_SAFE  = 0x74   # [APPLICATION 20] Safe (integrity-protected) message\n        KRB_PRIV  = 0x75   # [APPLICATION 21] Private (encrypted) message\n        KRB_CRED  = 0x76   # [APPLICATION 22] Credential forwarding\n        KRB_ERROR = 0x7e   # [APPLICATION 30] Error reply\n\n    # ── Outer BER/DER length ─────────────────────────────────────────────────\n    uint8 body_len defaults(0x0c)\n\n    # ── SEQUENCE wrapper ─────────────────────────────────────────────────────\n    uint8 seq_tag defaults(0x30)\n    uint8 seq_len defaults(0x0a)\n\n    # ── [0] pvno — Kerberos protocol version (always 5) ─────────────────────\n    # REP/ERROR types use CONTEXT[0] for pvno (RFC 4120 §5.4.2, §5.9.1)\n    uint8 ctx1_tag defaults(0xa0)   # CONTEXT [0] CONSTRUCTED\n    uint8 ctx1_len defaults(0x03)\n    uint8 pvno_tag defaults(0x02)   # UNIVERSAL INTEGER\n    uint8 pvno_len defaults(0x01)\n    uint8 pvno defaults(5)\n\n    # ── [1] msg-type ─────────────────────────────────────────────────────────\n    uint8 ctx2_tag defaults(0xa1)   # CONTEXT [1] CONSTRUCTED\n    uint8 ctx2_len defaults(0x03)\n    uint8 mtype_tag defaults(0x02)   # UNIVERSAL INTEGER\n    uint8 mtype_len defaults(0x01)\n    uint8 msg_type defaults(11)\n        AS_REPLY      = 11   # Authentication Service Reply\n        TGS_REPLY     = 13   # Ticket-Granting Service Reply\n        AP_REPLY      = 15   # Application authentication reply\n        KRB_SAFE_VAL  = 20   # Integrity-protected application message\n        KRB_PRIV_VAL  = 21   # Encrypted application message\n        KRB_CRED_VAL  = 22   # Credential forwarding message\n        KRB_ERROR_VAL = 30   # Error from KDC or application server\n\n    payload body\n",
    /* knxnetip.posa */
    "# knxnetip.posa — KNXnet/IP building-automation protocol over IP\n# Reference: KNX Association — KNXnet/IP Core specification (03_08_02)\n#            ISO 22510 / IEC 14543-3-x\n#\n# KNXnet/IP encapsulates KNX telegrams (cEMI frames) in UDP or TCP datagrams\n# for transport across standard IP networks.  Every frame starts with a fixed\n# 6-byte header: header_length=0x06, version=0x10, 16-bit service type, 16-bit\n# total length.  The two-byte prefix \"\\x06\\x10\" is used for content detection.\n#\n# Service families decoded here:\n#   0x02xx  Core   — device discovery, connection setup, state, disconnect\n#   0x03xx  Device Management — configuration of KNX devices\n#   0x04xx  Tunnelling — unicast delivery of cEMI frames (most common)\n#   0x05xx  Routing   — multicast delivery of cEMI frames\n\nObject<main> KNXnetIP\n    abbrev \"knxip\"\n    col \"KNXnet/IP\"\n\n    # ── Fixed 6-byte KNXnet/IP Header ────────────────────────────────────────\n    uint8  header_length [\"Header Length\"] defaults(6)\n    uint8  version [\"Version\"] defaults(0x10)\n        0x10 = KNXnet/IP 1.0\n    uint16 service_type [\"Service Type\"] defaults(0x0420)\n        0x0201 = SEARCH_REQUEST\n        0x0202 = SEARCH_RESPONSE\n        0x0203 = DESCRIPTION_REQUEST\n        0x0204 = DESCRIPTION_RESPONSE\n        0x0205 = CONNECT_REQUEST\n        0x0206 = CONNECT_RESPONSE\n        0x0207 = CONNECTIONSTATE_REQUEST\n        0x0208 = CONNECTIONSTATE_RESPONSE\n        0x0209 = DISCONNECT_REQUEST\n        0x020A = DISCONNECT_RESPONSE\n        0x0310 = CONFIGURATION_REQUEST\n        0x0311 = CONFIGURATION_ACK\n        0x0420 = TUNNELLING_REQUEST\n        0x0421 = TUNNELLING_ACK\n        0x0530 = ROUTING_INDICATION\n        0x0531 = ROUTING_LOST_MESSAGE\n        0x0532 = ROUTING_BUSY\n    uint16 total_length [\"Total Length\"] defaults(6)\n\n    # ── TUNNELLING_REQUEST (0x0420) ───────────────────────────────────────────\n    # The tunnelling request is the most common KNXnet/IP message type.  After\n    # the 6-byte header comes a 4-byte Connection Header structure followed by\n    # a cEMI (Common External Message Interface) frame carrying the raw KNX\n    # telegram.  The sequence counter is echoed in the corresponding ACK.\n    when service_type == 0x0420:\n        uint8  conn_header_len [\"Conn Header Length\"] defaults(4)\n        uint8  channel_id [\"Channel ID\"] defaults(1)\n        uint8  seq_counter [\"Sequence Counter\"] defaults(0)\n        uint8  reserved [\"Reserved\"] defaults(0)\n        # cEMI frame — message code selects the KNX telegram direction\n        uint8  cemi_msg_code [\"cEMI Message Code\"] defaults(0x11)\n            0x11 = L_data.req\n            0x2E = L_data.con\n            0x29 = L_data.ind\n        payload cemi_data [\"cEMI Data\"]\n\n    # ── TUNNELLING_ACK (0x0421) ───────────────────────────────────────────────\n    # ACK carries the same 4-byte connection header as the request; the echoed\n    # sequence counter lets the sender correlate with the original request.\n    when service_type == 0x0421:\n        uint8  conn_header_len [\"Conn Header Length\"] defaults(4)\n        uint8  channel_id [\"Channel ID\"] defaults(1)\n        uint8  seq_counter [\"Sequence Counter\"] defaults(0)\n        uint8  status [\"Status\"] defaults(0)\n            0x00 = No Error\n            0x04 = Tunnelling Error\n\n    # ── All other service types ───────────────────────────────────────────────\n    # SEARCH, DESCRIPTION, CONNECT, CONNECTIONSTATE, DISCONNECT, CONFIGURATION,\n    # and ROUTING messages are captured verbatim for inspection.\n    else:\n        payload data [\"Data\"]\n\n    info \"%s seq=%u\" service_type, seq_counter\n\n\n# ── Content Detection Rules (primary — port-independent) ──────────────────────\n#\n# The two-byte fixed prefix \"\\x06\\x10\" (header_length=6, version=1.0) is present\n# in every valid KNXnet/IP frame, in both UDP and TCP streams.  Matching these\n# two bytes is sufficient for reliable heuristic detection on non-standard ports.\nweak rule udp.content \"\\x06\\x10\" => KNXnetIP\nweak rule tcp.content \"\\x06\\x10\" => KNXnetIP\n\n# ── Port Rules (secondary hints) ──────────────────────────────────────────────\n# IANA-assigned port 3671 for both UDP (default, used for routing multicast on\n# 224.0.23.12) and TCP (optional, used for tunnelling when UDP is not available).\nrule udp.port == 3671 => KNXnetIP\nrule tcp.port == 3671 => KNXnetIP\n\n\n# ── Coloring Rules ────────────────────────────────────────────────────────────\n# Most-specific first; first match wins.\n\n# Disconnect — red: session teardown, potential connectivity loss\ncolor knxip.service_type == 0x0209 or knxip.service_type == 0x020A => white red\n\n# Tunnelling — green: cEMI frames carrying live KNX telegrams (data plane)\ncolor knxip.service_type == 0x0420 or knxip.service_type == 0x0421 => black lightgreen\n\n# Routing — blue: multicast cEMI indication and router status messages\ncolor knxip.service_type == 0x0530 or knxip.service_type == 0x0531 or knxip.service_type == 0x0532 => black lightblue\n\n# Search — yellow: device discovery on the network\ncolor knxip.service_type == 0x0201 or knxip.service_type == 0x0202 => black lightyellow\n\n# Connection setup — yellow for requests, green for responses\ncolor knxip.service_type == 0x0203 or knxip.service_type == 0x0205 or knxip.service_type == 0x0207 => black lightyellow\ncolor knxip.service_type == 0x0204 or knxip.service_type == 0x0206 or knxip.service_type == 0x0208 => black lightgreen\n\n# Configuration — gray: device management background traffic\ncolor knxip.service_type == 0x0310 or knxip.service_type == 0x0311 => black lightgray\n\n# Protocol catch-all — any KNXnet/IP frame not matched above\ncolor knxip => white darkgreen\n\n\n# ── Wireshark-Compatible Aliases ──────────────────────────────────────────────\n# Map field names used by the Wireshark knxip dissector so that existing display\n# filters and coloring rules written for Wireshark continue to work unchanged.\n\nalias knxip.service_type  => knxip.service_type\nalias knxip.seq           => knxip.seq_counter\nalias knxip.cemi.msg_code => knxip.cemi_msg_code\n",
    /* l2tp.posa */
    "# L2TPv2 header (RFC 2661) — simplified data message form (8 bytes with ns/nr)\n# UDP port 1701. flags 0x0002 = L-bit (length present); 0x0802 = L+S-bits (seq present).\n# Control messages set flags 0xC802 (L+T+S). Session data typically uses 0x0002.\nObject<main> L2TP\n    uint16 flags defaults(0x0002)\n        DATA_MSG    = 0x0002\n        CTRL_MSG    = 0xC802\n    uint16 length defaults(12)\n    uint16 tunnel_id defaults(0)\n    uint16 session_id defaults(0)\n    uint16 ns defaults(0)\n    uint16 nr defaults(0)\n",
    /* lacp.posa */
    "# LACP PDU (IEEE 802.3ad / 802.1AX) — subtype + version + actor TLV (22 bytes)\n# EtherType 0x8809 (Slow Protocols), sent to 01:80:c2:00:00:02.\n# actor_state is a bitmask; common values: 0x47=DEFAULTED|AGGREGATION|TIMEOUT|ACTIVITY,\n# 0x3D=DISTRIBUTING|COLLECTING|SYNCHRONIZATION|AGGREGATION|ACTIVITY (fully up).\nObject<main> LACP\n    uint8    subtype defaults(0x01)\n        LACP   = 0x01\n        MARKER = 0x02\n    uint8    version defaults(0x01)\n    uint8    actor_type defaults(0x01)\n    uint8    actor_len defaults(0x14)\n    uint16   actor_sys_priority defaults(32768)\n    bytes<6> actor_sys_id\n    uint16   actor_key defaults(0)\n    uint16   actor_port_priority defaults(32768)\n    uint16   actor_port defaults(1)\n    uint8    actor_state defaults(0x47)\n        LACP_ACTIVITY    = 0x01\n        LACP_TIMEOUT     = 0x02\n        AGGREGATION      = 0x04\n        SYNCHRONIZATION  = 0x08\n        COLLECTING       = 0x10\n        DISTRIBUTING     = 0x20\n        DEFAULTED        = 0x40\n        EXPIRED          = 0x80\n    bytes<3> actor_reserved\n",
    /* ldap.posa */
    "# LDAP (Lightweight Directory Access Protocol) — RFC 4511\n# BER/ASN.1 encoding over TCP port 389 (plain) or 636 (LDAPS/TLS-wrapped).\n#\n# Every LDAP message is an ASN.1 LDAPMessage SEQUENCE:\n#\n#   LDAPMessage ::= SEQUENCE {        tag 0x30\n#       messageID   INTEGER,          tag 0x02 + 1-byte len + value bytes\n#       protocolOp  CHOICE { ... },   Application-class tag 0x60–0x79\n#       controls   [0] OPTIONAL }\n#\n# BER length is simplified to short form (1 byte, value 0–127). Messages\n# larger than 127 bytes use the long form (first byte = 0x80|numBytes) but\n# the operation tag is still decoded from the known offset that follows the\n# messageID TLV, so field alignment is preserved for typical LDAP traffic.\n#\n# LDAPS on port 636 wraps LDAP inside a TLS session; the TLS dissector will\n# handle those packets first, so the content rule (0x30) only fires on port\n# 389 plain-text sessions. The port-636 rule remains as a label hint.\n\nObject<main> LDAP\n    abbrev \"ldap\"\n    col \"LDAP\"\n\n    # ── Outer SEQUENCE wrapper ────────────────────────────────────────────────\n    # 0x30 = universal constructed tag 16 (SEQUENCE)\n    uint8 sequence_tag hex \"Sequence Tag\" defaults(0x30)\n\n    # Short-form BER length (1 byte). Covers messages up to 127 bytes.\n    # Long-form lengths (first byte 0x81 or 0x82) will misparse here but the\n    # operation tag is still visible after the messageID TLV at a fixed offset.\n    uint8 sequence_len \"Sequence Length\" defaults(0)\n\n    # ── MessageID — ASN.1 INTEGER ─────────────────────────────────────────────\n    # 0x02 = universal primitive tag 2 (INTEGER)\n    uint8 msg_id_tag hex \"Message ID Tag\" defaults(0x02)\n    uint8 msg_id_len \"Message ID Length\" defaults(1)\n    bytes[msg_id_len] message_id hex \"Message ID\"\n\n    # ── ProtocolOp — Application-class CHOICE ─────────────────────────────────\n    # Each operation is encoded as a constructed Application tag (0x60–0x79).\n    # The tag value alone identifies the operation type; the body varies per op.\n    uint8 op_tag hex \"Operation\" defaults(0x60)\n        BindRequest           = 0x60\n        BindResponse          = 0x61\n        UnbindRequest         = 0x62\n        SearchRequest         = 0x63\n        SearchResultEntry     = 0x64\n        SearchResultDone      = 0x65\n        ModifyRequest         = 0x66\n        ModifyResponse        = 0x67\n        AddRequest            = 0x68\n        AddResponse           = 0x69\n        DelRequest            = 0x6A\n        DelResponse           = 0x6B\n        ModifyDNRequest       = 0x6C\n        ModifyDNResponse      = 0x6D\n        CompareRequest        = 0x6E\n        CompareResponse       = 0x6F\n        AbandonRequest        = 0x70\n        SearchResultReference = 0x73\n        ExtendedRequest       = 0x77\n        ExtendedResponse      = 0x78\n        IntermediateResponse  = 0x79\n\n    # Remainder of the operation body — BER-structured, varies per op type\n    payload\n\n    info \"id=%s %s\" message_id, op_tag\n\n# ── Content detection ─────────────────────────────────────────────────────────\n# Every LDAP message begins with the ASN.1 SEQUENCE tag 0x30. This is a\n# common byte (e.g. TLS also uses it in some contexts), so combine with the\n# port rules below rather than relying on this alone.\nweak rule tcp.content \"\\x30\" => LDAP\n\n# ── Port bindings ─────────────────────────────────────────────────────────────\n# Port rules are secondary: the content rule fires first on non-standard ports.\n# Port 636 (LDAPS) is TLS-wrapped; the 0x30 content rule will not match there\n# (a TLS ClientHello starts with 0x16), so the port rule is the sole trigger.\nrule tcp.port == 389 => LDAP\nrule tcp.port == 636 => LDAP\n\n# ── Coloring rules ────────────────────────────────────────────────────────────\n# Bind and authentication operations — yellow (security-sensitive)\ncolor ldap.op_tag == 0x60 => black yellow\ncolor ldap.op_tag == 0x61 => black yellow\ncolor ldap.op_tag == 0x62 => black yellow\n\n# Search operations (request, entry results, done, referral) — green\ncolor ldap.op_tag == 0x63 => black lightgreen\ncolor ldap.op_tag == 0x64 => black lightgreen\ncolor ldap.op_tag == 0x65 => black lightgreen\ncolor ldap.op_tag == 0x73 => black lightgreen\n\n# Modify / Add / Delete / ModifyDN / Compare requests — lightyellow\ncolor ldap.op_tag == 0x66 => black lightyellow\ncolor ldap.op_tag == 0x68 => black lightyellow\ncolor ldap.op_tag == 0x6A => black lightyellow\ncolor ldap.op_tag == 0x6C => black lightyellow\ncolor ldap.op_tag == 0x6E => black lightyellow\ncolor ldap.op_tag == 0x70 => black lightyellow\n\n# Modify / Add / Delete / ModifyDN / Compare responses — lightcyan\ncolor ldap.op_tag == 0x67 => black lightcyan\ncolor ldap.op_tag == 0x69 => black lightcyan\ncolor ldap.op_tag == 0x6B => black lightcyan\ncolor ldap.op_tag == 0x6D => black lightcyan\ncolor ldap.op_tag == 0x6F => black lightcyan\n\n# Extended / Intermediate operations — darkblue (tunneled/special)\ncolor ldap.op_tag == 0x77 => white darkblue\ncolor ldap.op_tag == 0x78 => white darkblue\ncolor ldap.op_tag == 0x79 => white darkblue\n\n# Catch-all — unclassified LDAP traffic\ncolor ldap => white darkgray\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\nalias ldap.messageID => ldap.message_id\nalias ldap.operation => ldap.op_tag\n",
    /* llc.posa */
    "# LLC header (IEEE 802.2) — 3 bytes\n# Appears after 802.3 Ethernet (Dot3), 802.11 WiFi data frames, and Token Ring.\n# ctrl=0x03 = UI (unnumbered information), the most common form.\nObject<main> LLC\n    uint8 dsap defaults(0x42)\n        NULL_LSAP = 0x00\n        STP       = 0x42\n        SNAP      = 0xAA\n        IPX       = 0xE0\n        NETBIOS   = 0xF0\n        GLOBAL    = 0xFF\n    uint8 ssap defaults(0x42)\n    uint8 ctrl defaults(0x03)\n",
    /* mdns.posa */
    "# mdns.posa — Multicast DNS (RFC 6762) and DNS-SD (RFC 6763)\n#\n# mDNS carries the standard DNS wire format (RFC 1035) on UDP port 5353,\n# sent to the multicast group 224.0.0.251 (IPv4) or FF02::FB (IPv6).\n# DNS-SD (RFC 6763) layers service discovery on top of mDNS using PTR, SRV,\n# and TXT records under the .local. pseudo-domain.\n#\n# Wire-format differences from unicast DNS:\n#\n#   id == 0x0000       Transaction ID is always zero in queries (and usually\n#                      zero in responses too — unlike unicast DNS).\n#   QU bit            Bit 15 of QCLASS in questions requests a unicast reply;\n#                     useful on networks where multicast is expensive or lossy.\n#   Cache-flush bit   Bit 15 of RRCLASS in answers asks receivers to flush\n#                     their cached records for that name/type/class tuple.\n#   AA always set     In responses, Authoritative Answer is always 1.\n#\n# DNS-SD well-known types (under _<service>._<proto>.local.):\n#   PTR   Service browse: maps service type to instance name\n#   SRV   Service resolve: instance name → host + port\n#   TXT   Service metadata: key=value pairs\n#   A     Address record for the resolved host\n#   AAAA  IPv6 address record\n#\n# Field detection strategy:\n#   Content rules capture the fixed bytes in the mDNS header that distinguish\n#   this protocol from arbitrary UDP. Port 5353 is added last as a fallback.\n#\n# Pieces of POSA used here:\n#\n#   dnsname              label-encoded name with 0xC0 compression pointers\n#   required TYPE name mask 0x7fff\n#                        mask out the top bit before storing the value\n#   bits srcfield n sh w extract a bitfield from an already-parsed field\n#   scope rdlength       bound rdata parsing to rdlength bytes (unknown types\n#                        still leave the walk in the right place)\n#   when type == N:      parse rdata according to the record type\n#   repeat count as item one subtree per record, count from a header field\n#   repeat until end as  loop until the enclosing scope is exhausted\n#   label \"...\" f, …     title each repeated subtree from its fields\n#   include Object       inline another object's fields into this one\n\n# ── mDNS Resource Record ─────────────────────────────────────────────────────\n# Defined before the main object so the three record sections can `include` it.\n# The cache-flush bit (bit 15 of RRCLASS) is extracted separately; the stored\n# class value is masked to 15 bits so comparisons against IN = 1 always work.\n\nObject<main> MDNS_RR\n    abbrev \"mdns\"\n\n    dnsname name \"Name\"\n    uint16 type \"Type\" defaults(1)\n        A     = 1\n        NS    = 2\n        CNAME = 5\n        SOA   = 6\n        PTR   = 12\n        HINFO = 13\n        MX    = 15\n        TXT   = 16\n        AAAA  = 28\n        SRV   = 33\n        OPT   = 41\n        NSEC  = 47\n        HTTPS = 65\n        ANY   = 255\n    uint16 class mask 0x7fff \"Class\" defaults(1)\n        IN  = 1\n        CH  = 3\n        ANY = 255\n    # Cache-flush: set by a responder to signal that all peer caches for this\n    # <name, type, class> tuple should be flushed before the new record is stored.\n    bits class flush 15 1 \"Cache flush\"\n        No flush = 0\n        Flush    = 1\n    uint32 ttl \"Time to live\" defaults(120)\n    uint16 rdlength \"Data length\" defaults(4)\n\n    scope rdlength\n        when type == 1:\n            ip4 rdata \"Address\"\n        when type == 28:\n            ip6 rdata \"Address\"\n        when type == 12:\n            # PTR: DNS-SD uses these to enumerate service instances.\n            # e.g. _http._tcp.local. PTR mydevice._http._tcp.local.\n            dnsname rdata \"Domain name\"\n        when type == 5:\n            dnsname rdata \"CNAME\"\n        when type == 2:\n            dnsname rdata \"Name server\"\n        when type == 15:\n            uint16 preference \"Preference\" defaults(10)\n            dnsname rdata \"Mail exchange\"\n        when type == 33:\n            # SRV: resolves a service instance to host + port.\n            # priority / weight guide client selection among multiple instances.\n            uint16 priority \"Priority\" defaults(0)\n            uint16 weight \"Weight\" defaults(0)\n            uint16 port \"Port\" defaults(0)\n            dnsname rdata \"Target\"\n        when type == 6:\n            dnsname rdata \"Primary name server\"\n            dnsname rname \"Responsible mailbox\"\n            uint32 serial \"Serial\" defaults(1)\n            uint32 refresh \"Refresh\" defaults(3600)\n            uint32 retry \"Retry\" defaults(600)\n            uint32 expire \"Expire\" defaults(604800)\n            uint32 minimum \"Minimum TTL\" defaults(300)\n        when type == 16:\n            # TXT: DNS-SD metadata — length-prefixed key=value strings with no\n            # count, so we loop until the rdata scope is exhausted.\n            repeat until end as txt\n                label \"%s\" rdata\n                uint8 txtlen \"Length\" defaults(0)\n                str[txtlen] rdata \"Text\"\n        when type == 41:\n            # OPT: EDNS0 options pseudo-RR; treat as opaque.\n            payload rdata \"Options\"\n        when type == 47:\n            dnsname rdata \"Next domain name\"\n        when type == 65:\n            # HTTPS / SVCB: treat rdata as raw bytes until a full parser exists.\n            payload rdata \"HTTPS parameters\"\n\n# ── mDNS message ─────────────────────────────────────────────────────────────\n\nObject<main> mDNS\n    abbrev \"mdns\"\n    col \"mDNS\"\n\n    # Header — identical to DNS.  In mDNS queries the id is always 0x0000;\n    # a non-zero id signals a legacy unicast DNS query tunnelled over port 5353.\n    uint16 id \"Transaction ID\" defaults(0)\n    uint16 flags \"Flags\" defaults(0)\n        bits flags qr      15 1 \"Response\"\n            Query    = 0\n            Response = 1\n        bits flags opcode  11 4 \"Opcode\"\n            QUERY  = 0\n            STATUS = 2\n            NOTIFY = 4\n            UPDATE = 5\n        bits flags aa      10 1 \"Authoritative\"\n            Non-authoritative = 0\n            Authoritative     = 1\n        bits flags tc       9 1 \"Truncated\"\n            Not truncated = 0\n            Truncated     = 1\n        bits flags rd       8 1 \"Recursion desired\"\n        bits flags ra       7 1 \"Recursion available\"\n        bits flags z        6 1 \"Reserved\"\n        bits flags ad       5 1 \"Answer authenticated\"\n        bits flags cd       4 1 \"Non-authenticated data OK\"\n        bits flags rcode    0 4 \"Reply code\"\n            NoError  = 0\n            FormErr  = 1\n            ServFail = 2\n            NXDomain = 3\n            NotImp   = 4\n            Refused  = 5\n    uint16 questions \"Questions\" defaults(1)\n    uint16 answers \"Answer RRs\" defaults(0)\n    uint16 authority \"Authority RRs\" defaults(0)\n    uint16 additional \"Additional RRs\" defaults(0)\n\n    # ── Questions ─────────────────────────────────────────────────────────────\n    # Each question carries a name, a type, and a class.  The QU bit (bit 15 of\n    # QCLASS) requests a unicast reply; it is separated from the class value by\n    # masking.  DNS-SD queries typically ask for PTR records of the form\n    # _<service>._tcp.local. or _<service>._udp.local.\n    repeat questions as query \"Queries\"\n        label \"%s: type %s, class %s\" qname, qtype, qclass\n        dnsname qname \"Name\"\n        uint16 qtype \"Type\" defaults(12)\n            A     = 1\n            NS    = 2\n            CNAME = 5\n            SOA   = 6\n            PTR   = 12\n            HINFO = 13\n            MX    = 15\n            TXT   = 16\n            AAAA  = 28\n            SRV   = 33\n            OPT   = 41\n            NSEC  = 47\n            HTTPS = 65\n            ANY   = 255\n        uint16 qclass mask 0x7fff \"Class\" defaults(1)\n            IN   = 1\n            CH   = 3\n            NONE = 254\n            ANY  = 255\n        # QU bit: if set the sender prefers a unicast reply (reduces multicast\n        # traffic when the answer is already in a peer's cache).\n        bits qclass qu 15 1 \"QU (unicast response preferred)\"\n            Multicast = 0\n            Unicast   = 1\n\n    # ── Answers ───────────────────────────────────────────────────────────────\n    # DNS-SD responses include PTR → SRV + TXT → A/AAAA, often in a single\n    # packet.  Each record is parsed via the shared MDNS_RR object.\n    repeat answers as answer \"Answers\"\n        label \"%s: type %s, class %s, %s\" name, type, class, rdata\n        include MDNS_RR\n\n    # ── Authority section ─────────────────────────────────────────────────────\n    repeat authority as authority_rr \"Authoritative nameservers\"\n        label \"%s: type %s, class %s, %s\" name, type, class, rdata\n        include MDNS_RR\n\n    # ── Additional records ────────────────────────────────────────────────────\n    # DNS-SD frequently piggybacks the SRV, TXT, and A records in the additional\n    # section alongside a PTR response, enabling one-shot service resolution.\n    repeat additional as additional_rr \"Additional records\"\n        label \"%s: type %s, class %s, %s\" name, type, class, rdata\n        include MDNS_RR\n\n    # Info column: show direction, the query type+name (from the questions\n    # section), and the answer type+data (from the answers section).  Fields from\n    # repeat blocks expand to the last value seen; absent fields expand to nothing\n    # so gaps close automatically, keeping the column readable.\n    info \"%s %s %s %s %s\" qr, qtype, qname, type, rdata\n\n# ── Content rules (primary — fire before port rules) ─────────────────────────\n# mDNS queries always start with a zero transaction ID (bytes 0-1 = 0x0000).\n# Pairing this with port 5353 gives high confidence with low overhead, but the\n# content rule alone is listed first so non-standard-port mDNS is still caught.\n#\n# The second rule looks four bytes deeper: zero flags (0x0000 = standard query)\n# plus a one-question count (0x0001) starting at offset 2 — a pattern that is\n# extremely common in mDNS probes and is vanishingly rare on other protocols.\n#\n# The third rule targets offset 4 only (question count = 1) as a lighter fallback.\n#\n# mDNS responses set the QR + AA bits (bytes 2-3 ≈ 0x8400); the fourth rule\n# catches those packets so the mDNS decoder is preferred over a plain DNS decode\n# on port 5353.\n\nweak rule udp.content \"\\x00\\x00\"            => mDNS\nrule udp.content@2 \"\\x00\\x00\\x00\\x01\" => mDNS\nweak rule udp.content@4 \"\\x00\\x01\"         => mDNS\nweak rule udp.content@2 \"\\x84\\x00\"         => mDNS\n\n# ── Port rule (secondary — fallback when content rules do not match) ──────────\nrule udp.port == 5353 => mDNS\n\n# ── Coloring rules (most specific first) ─────────────────────────────────────\n# Error responses: RCODE non-zero — always shown in alert palette.\ncolor mdns.rcode > 0  => yellow red\n\n# PTR records: DNS-SD service browsing (query or answer).  Yellow matches the\n# \"request / browse\" convention — the client is asking what is out there.\ncolor mdns.type == 12 => black lightyellow\n\n# SRV records: service instance resolution — the definitive answer that delivers\n# a hostname and port.  Green matches the \"response\" convention.\ncolor mdns.type == 33 => black lightgreen\n\n# A / AAAA records: address resolution, the final step of DNS-SD.  Blue signals\n# network-layer information distinct from the service-discovery layer above.\ncolor mdns.type == 1  => black lightblue\ncolor mdns.type == 28 => black lightblue\n\n# TXT records: application metadata (key=value pairs).  Gray de-emphasises these\n# relative to the name/address records that matter more for diagnosis.\ncolor mdns.type == 16 => black lightgray\n\n# Catch-all: any mDNS frame not matched by a type rule above.\ncolor mdns            => white darkblue\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# Wireshark dissects mDNS through its DNS dissector, so its field names all live\n# under the \"dns\" namespace.  The aliases below let display filters written for\n# Wireshark continue to work without modification.\n\n# Query fields\nalias dns.qry.name      => mdns.qname\nalias dns.qry.type      => mdns.qtype\nalias dns.qry.class     => mdns.qclass\n\n# Answer / response fields\nalias dns.resp.name     => mdns.name\nalias dns.a             => mdns.rdata\nalias dns.aaaa          => mdns.rdata\nalias dns.cname         => mdns.rdata\nalias dns.ptr.domain_name => mdns.rdata\nalias dns.ns            => mdns.rdata\n\n# SRV-specific sub-fields\nalias dns.srv.target    => mdns.rdata\nalias dns.srv.port      => mdns.port\nalias dns.srv.priority  => mdns.priority\nalias dns.srv.weight    => mdns.weight\n\n# TXT records\nalias dns.txt           => mdns.rdata\n\n# Record metadata\nalias dns.resp.type     => mdns.type\nalias dns.resp.ttl      => mdns.ttl\nalias dns.resp.len      => mdns.rdlength\nalias dns.resp.class    => mdns.class\n\n# Flags\nalias dns.flags.response  => mdns.qr\nalias dns.flags.opcode    => mdns.opcode\nalias dns.flags.aa        => mdns.aa\nalias dns.flags.tc        => mdns.tc\nalias dns.flags.rd        => mdns.rd\nalias dns.flags.ra        => mdns.ra\nalias dns.flags.rcode     => mdns.rcode\n\n# mDNS-specific\nalias dns.qry.qu          => mdns.qu\nalias mdns.qry.name       => mdns.qname\nalias mdns.resp.name      => mdns.name\nalias mdns.srv.target     => mdns.rdata\nalias mdns.txt            => mdns.rdata\nalias mdns.ptr.domain_name => mdns.rdata\n\n# Section counts\nalias dns.count.queries   => mdns.questions\nalias dns.count.answers   => mdns.answers\nalias dns.count.auth_rr   => mdns.authority\nalias dns.count.add_rr    => mdns.additional\n\n# Macro alias: filter for any mDNS service-discovery traffic\nalias mdns.dnssd          => mdns.type == 12 || mdns.type == 33 || mdns.type == 16\n",
    /* memcached.posa */
    "# memcached.posa — Memcached Text Protocol\n#\n# Memcached uses a line-oriented text protocol over TCP. Commands are\n# CRLF-terminated lines; the server replies with status keywords followed by\n# optional data.  A UDP framing header (8 bytes) precedes the text command\n# when Memcached is accessed over UDP.\n#\n# TCP text protocol:\n#   Client sends:  COMMAND <key> [flags] [expiry] [bytes] [noreply]\\r\\n\n#                  followed (for storage commands) by the value and \\r\\n\n#   Server replies: STATUS\\r\\n  or  VALUE <key> <flags> <bytes>\\r\\n<data>\\r\\n\n#\n# UDP framing (8 bytes before the text command):\n#   uint16_be request_id       — matches reply to request\n#   uint16_be sequence_number  — datagram sequence within a multi-datagram message\n#   uint16_be total_datagrams  — total datagrams for this message (must be ≠ 0)\n#   uint16_be reserved         — must be 0x0000\n#\n# Reference: https://github.com/memcached/memcached/blob/master/doc/protocol.txt\n#\n# Runs on TCP/UDP port 11211 (default).\n\nLookup MemcachedCommands\n    \"get\"     = \"Get\"\n    \"gets\"    = \"Get With CAS\"\n    \"set\"     = \"Set\"\n    \"add\"     = \"Add\"\n    \"replace\" = \"Replace\"\n    \"append\"  = \"Append\"\n    \"prepend\" = \"Prepend\"\n    \"cas\"     = \"Compare-And-Swap\"\n    \"delete\"  = \"Delete\"\n    \"incr\"    = \"Increment\"\n    \"decr\"    = \"Decrement\"\n    \"touch\"   = \"Touch\"\n    \"gat\"     = \"Get And Touch\"\n    \"gats\"    = \"Get And Touch With CAS\"\n    \"stats\"   = \"Statistics\"\n    \"flush_all\" = \"Flush All\"\n    \"version\" = \"Version\"\n    \"quit\"    = \"Quit\"\n    \"STORED\"     = \"Stored\"\n    \"NOT_STORED\" = \"Not Stored\"\n    \"EXISTS\"     = \"Exists (CAS conflict)\"\n    \"NOT_FOUND\"  = \"Not Found\"\n    \"DELETED\"    = \"Deleted\"\n    \"TOUCHED\"    = \"Touched\"\n    \"ERROR\"      = \"Error\"\n    \"CLIENT_ERROR\" = \"Client Error\"\n    \"SERVER_ERROR\" = \"Server Error\"\n    \"END\"        = \"End of Data\"\n    \"VALUE\"      = \"Value Data\"\n    \"STAT\"       = \"Stat Entry\"\n    \"VERSION\"    = \"Version Reply\"\n\n# ── UDP framing header + text command ────────────────────────────────────────\nObject<MEMCACHED> MEMCACHED_UDP\n    abbrev \"memcached\"\n    col \"Memcached\"\n\n    uint16 request_id \"Request ID\" defaults(0)\n    uint16 sequence_number \"Sequence Number\" defaults(0)\n    uint16 total_datagrams \"Total Datagrams\" defaults(1)\n    uint16 reserved \"Reserved\" defaults(0)\n    string command until \" \\r\" lookup MemcachedCommands \"Command\" defaults(\"get\")\n    payload rest \"Rest\"\n\n    info \"%s\" command\n\n# ── TCP text stream ───────────────────────────────────────────────────────────\nObject<MEMCACHED> MEMCACHED_TCP\n    abbrev \"memcached\"\n    col \"Memcached\"\n    starts \"get \" \"gets \" \"set \" \"add \" \"replace \" \"append \" \"prepend \" \"cas \"\n           \"delete \" \"incr \" \"decr \" \"touch \" \"gat \" \"gats \" \"stats\" \"version\"\n           \"flush_all\" \"quit\\r\"\n           \"STORED\\r\" \"NOT_STORED\\r\" \"EXISTS\\r\" \"NOT_FOUND\\r\" \"DELETED\\r\"\n           \"TOUCHED\\r\" \"ERROR\\r\" \"CLIENT_ERROR \" \"SERVER_ERROR \" \"END\\r\"\n           \"VALUE \" \"STAT \" \"VERSION \"\n\n    string command until \" \\r\" lookup MemcachedCommands \"Command\" defaults(\"get\")\n    payload rest \"Rest\"\n\n    info \"%s\" command\n\n# ── Binary protocol ──────────────────────────────────────────────────────────\n# Magic 0x80 = request, 0x81 = response. 24-byte fixed header:\n#   uint8 magic, uint8 opcode, uint16_be key_length, uint8 extras_length,\n#   uint8 data_type (0=raw), uint16_be vbucket_id/status, uint32_be total_body_length,\n#   uint32_be opaque, uint64_be cas\nObject<MEMCACHED> MEMCACHED_BIN\n    abbrev \"memcached\"\n    col \"Memcached\"\n\n    uint8  magic hex \"Magic\" defaults(0x80)\n        0x80 = \"Request\"\n        0x81 = \"Response\"\n    uint8  opcode hex \"Opcode\" defaults(0)\n        0x00 = \"Get\"\n        0x01 = \"Set\"\n        0x02 = \"Add\"\n        0x03 = \"Replace\"\n        0x04 = \"Delete\"\n        0x05 = \"Increment\"\n        0x06 = \"Decrement\"\n        0x07 = \"Quit\"\n        0x08 = \"Flush\"\n        0x09 = \"Get Quietly\"\n        0x0A = \"No-op\"\n        0x0B = \"Version\"\n        0x0C = \"Get Key\"\n        0x0D = \"Get Key Quietly\"\n        0x0E = \"Append\"\n        0x0F = \"Prepend\"\n        0x10 = \"Statistics\"\n        0x11 = \"Set Quietly\"\n        0x12 = \"Add Quietly\"\n        0x13 = \"Replace Quietly\"\n        0x14 = \"Delete Quietly\"\n        0x15 = \"Increment Quietly\"\n        0x16 = \"Decrement Quietly\"\n        0x17 = \"Quit Quietly\"\n        0x18 = \"Flush Quietly\"\n        0x19 = \"Append Quietly\"\n        0x1A = \"Prepend Quietly\"\n    uint16 key_length \"Key Length\" defaults(0)\n    uint8  extras_length \"Extras Length\" defaults(0)\n    uint8  data_type \"Data Type\" defaults(0)\n    uint16 vbucket_status hex \"VBucket/Status\" defaults(0)\n        0x0000 = \"No error\"\n        0x0001 = \"Key not found\"\n        0x0002 = \"Key exists\"\n        0x0003 = \"Value too big\"\n        0x0004 = \"Invalid arguments\"\n        0x0005 = \"Item not stored\"\n        0x0081 = \"Unknown command\"\n        0x0082 = \"Out of memory\"\n    uint32 total_body        \"Total Body Length\" defaults(0)\n    uint32 opaque        hex \"Opaque\"            defaults(0)\n    uint64 cas           hex \"CAS\"               defaults(0)\n    payload body             \"Body\"\n\n    info \"Memcached binary %s op=0x%02x\" magic, opcode\n\nObject<GROUP> MEMCACHED\n\nrule tcp.port == 11211 => MEMCACHED\nrule udp.port == 11211 => MEMCACHED\n\nrule tcp.content \"set \"     => MEMCACHED\nrule tcp.content \"get \"     => MEMCACHED\nrule tcp.content \"delete \"  => MEMCACHED\nrule tcp.content \"STORED\\r\" => MEMCACHED\nrule tcp.content \"VALUE \"   => MEMCACHED\nrule tcp.content \"ERROR\\r\"  => MEMCACHED\nweak rule tcp.content \"\\x80\"     => MEMCACHED\nweak rule tcp.content \"\\x81\"     => MEMCACHED\n\ncolor memcached.command == \"set\" || memcached.command == \"add\" || memcached.command == \"replace\" => black lightyellow\ncolor memcached.command == \"get\" || memcached.command == \"gets\" => black lightblue\ncolor memcached.command == \"STORED\" => black lightgreen\ncolor memcached.command == \"NOT_FOUND\" || memcached.command == \"NOT_STORED\" => black lightgray\ncolor memcached.command == \"ERROR\" || memcached.command == \"CLIENT_ERROR\" || memcached.command == \"SERVER_ERROR\" => white red\ncolor memcached => white darkblue\n",
    /* modbus_tcp.posa */
    "# Modbus TCP (MBAP header + PDU) — Modbus Application Protocol v1.1b3,\n# Modbus Messaging on TCP/IP Implementation Guide v1.0b\n#\n# TCP port 502. protocol_id is always 0 for Modbus. unit_id identifies the slave\n# device behind a gateway (0xFF when the target is the TCP endpoint itself).\n#\n# Frame layout:\n#   MBAP header (7 bytes)   transaction_id, protocol_id, length, unit_id\n#   PDU                     function_code + a body whose shape depends on it\n#\n# `length` counts unit_id plus the PDU, so the `scope length` below ends the\n# object exactly where the MBAP header says it does — a short read leaves the\n# remaining bytes undecoded rather than running off the end.\n#\n# Request vs response\n# ───────────────────\n# Modbus carries no direction bit: a dissector tells a request from a response\n# by which side of TCP/502 the packet came from, and a posa guard cannot see\n# the ports. The bodies are distinguished by size instead — `remaining` inside\n# the scope is `length - 2`, the PDU body byte count, and for most function\n# codes the two directions have different body sizes.\n#\n# One case stays ambiguous: a READ_COILS / READ_DISCRETE_INPUTS response\n# carrying a 3-byte coil status (byte_count=3, body=4) is the same size as the\n# 4-byte request body, and is decoded as a request. Registers are always 2\n# bytes each, so function codes 3 and 4 are not affected.\n#\n# Error responses set the high bit of the function code (READ_HOLDING_REGISTERS\n# error = 0x83) and carry a single exception code.\n\nObject<main> ModbusTCP\n    col \"Modbus/TCP\"\n\n    uint16 transaction_id [\"Transaction Identifier\"] defaults(1)\n    uint16 protocol_id [\"Protocol Identifier\"] defaults(0)\n        Modbus = 0\n    uint16 length [\"Length\"] defaults(6)\n\n    scope length\n        uint8 unit_id [\"Unit Identifier\"] defaults(1)\n        uint8 function_code [\"Function Code\"] defaults(3)\n            READ_COILS               = 1\n            READ_DISCRETE_INPUTS     = 2\n            READ_HOLDING_REGISTERS   = 3\n            READ_INPUT_REGISTERS     = 4\n            WRITE_SINGLE_COIL        = 5\n            WRITE_SINGLE_REGISTER    = 6\n            READ_EXCEPTION_STATUS    = 7\n            DIAGNOSTICS              = 8\n            WRITE_MULTIPLE_COILS     = 15\n            WRITE_MULTIPLE_REGISTERS = 16\n            REPORT_SERVER_ID         = 17\n            READ_FILE_RECORD         = 20\n            WRITE_FILE_RECORD        = 21\n            MASK_WRITE_REGISTER      = 22\n            READ_WRITE_MULTIPLE      = 23\n            READ_FIFO_QUEUE          = 24\n            MEI_TRANSPORT            = 43\n\n        # ── Error response — function_code | 0x80, one exception code ─────────\n        when function_code >= 0x80:\n            uint8 exception_code [\"Exception Code\"] defaults(1)\n                ILLEGAL_FUNCTION                        = 1\n                ILLEGAL_DATA_ADDRESS                    = 2\n                ILLEGAL_DATA_VALUE                      = 3\n                SERVER_DEVICE_FAILURE                   = 4\n                ACKNOWLEDGE                             = 5\n                SERVER_DEVICE_BUSY                      = 6\n                NEGATIVE_ACKNOWLEDGE                    = 7\n                MEMORY_PARITY_ERROR                     = 8\n                GATEWAY_PATH_UNAVAILABLE                = 10\n                GATEWAY_TARGET_DEVICE_FAILED_TO_RESPOND = 11\n\n        # ── 0x01 READ_COILS, 0x02 READ_DISCRETE_INPUTS ────────────────────────\n        # Request  : starting address + quantity of bits   (4 bytes)\n        # Response : byte count + packed bit status        (1 + N bytes)\n        when function_code == 1 or function_code == 2:\n            when remaining == 4:\n                uint16 start_address [\"Starting Address\"] defaults(0)\n                uint16 quantity [\"Quantity of Bits\"] defaults(1)\n            else:\n                uint8 byte_count [\"Byte Count\"] defaults(1)\n                bytes[byte_count] bit_status [\"Status\"]\n\n        # ── 0x03 READ_HOLDING_REGISTERS, 0x04 READ_INPUT_REGISTERS ────────────\n        # Request  : starting address + quantity of registers  (4 bytes)\n        # Response : byte count + 2 bytes per register         (1 + 2N bytes)\n        when function_code == 3 or function_code == 4:\n            when remaining == 4:\n                uint16 start_address [\"Starting Address\"] defaults(0)\n                uint16 quantity [\"Quantity of Registers\"] defaults(1)\n            else:\n                uint8 byte_count [\"Byte Count\"] defaults(2)\n                repeat byte_count/2 as register [\"Registers\"]\n                    uint16 value [\"Register Value\"] defaults(0)\n\n        # ── 0x05 WRITE_SINGLE_COIL, 0x06 WRITE_SINGLE_REGISTER ────────────────\n        # The response echoes the request, so both directions share one layout.\n        when function_code == 5 or function_code == 6:\n            uint16 address [\"Output Address\"] defaults(0)\n            uint16 value [\"Output Value\"] defaults(0)\n\n        # ── 0x07 READ_EXCEPTION_STATUS ────────────────────────────────────────\n        # Request carries no body; the response is a single status byte.\n        when function_code == 7:\n            when remaining >= 1:\n                uint8 exception_status [\"Exception Status\"] defaults(0)\n\n        # ── 0x08 DIAGNOSTICS — sub-function plus its data, echoed in the reply ─\n        when function_code == 8:\n            uint16 sub_function [\"Sub-function\"] defaults(0)\n                RETURN_QUERY_DATA               = 0\n                RESTART_COMMUNICATIONS_OPTION   = 1\n                RETURN_DIAGNOSTIC_REGISTER      = 2\n                CHANGE_ASCII_INPUT_DELIMITER    = 3\n                FORCE_LISTEN_ONLY_MODE          = 4\n                CLEAR_COUNTERS_AND_DIAGNOSTIC   = 10\n                RETURN_BUS_MESSAGE_COUNT        = 11\n                RETURN_BUS_COMM_ERROR_COUNT     = 12\n                RETURN_BUS_EXCEPTION_ERROR_COUNT = 13\n                RETURN_SERVER_MESSAGE_COUNT     = 14\n                RETURN_SERVER_NO_RESPONSE_COUNT = 15\n            payload diagnostic_data [\"Diagnostic Data\"]\n\n        # ── 0x0F WRITE_MULTIPLE_COILS, 0x10 WRITE_MULTIPLE_REGISTERS ──────────\n        # Request  : address + quantity + byte count + data  (5 + N bytes)\n        # Response : address + quantity                      (4 bytes)\n        when function_code == 15 or function_code == 16:\n            uint16 start_address [\"Starting Address\"] defaults(0)\n            uint16 quantity [\"Quantity\"] defaults(1)\n            when remaining >= 1:\n                uint8 byte_count [\"Byte Count\"] defaults(2)\n                bytes[byte_count] write_data [\"Data\"]\n\n        # ── 0x11 REPORT_SERVER_ID — no request body; response is opaque ───────\n        when function_code == 17:\n            when remaining >= 1:\n                uint8 byte_count [\"Byte Count\"] defaults(1)\n                bytes[byte_count] server_id [\"Server ID\"]\n\n        # ── 0x16 MASK_WRITE_REGISTER — echoed in the reply ────────────────────\n        when function_code == 22:\n            uint16 reference_address [\"Reference Address\"] defaults(0)\n            uint16 and_mask [\"AND Mask\"] defaults(0xffff)\n            uint16 or_mask [\"OR Mask\"] defaults(0)\n\n        # ── 0x17 READ_WRITE_MULTIPLE_REGISTERS ────────────────────────────────\n        # Request  : read address/count + write address/count + byte count + data\n        # Response : byte count + the registers that were read\n        when function_code == 23:\n            when remaining >= 9:\n                uint16 read_start_address [\"Read Starting Address\"] defaults(0)\n                uint16 read_quantity [\"Quantity to Read\"] defaults(1)\n                uint16 write_start_address [\"Write Starting Address\"] defaults(0)\n                uint16 write_quantity [\"Quantity to Write\"] defaults(1)\n                uint8 byte_count [\"Write Byte Count\"] defaults(2)\n                bytes[byte_count] write_data [\"Write Data\"]\n            else:\n                uint8 byte_count [\"Byte Count\"] defaults(2)\n                repeat byte_count/2 as register [\"Registers\"]\n                    uint16 value [\"Register Value\"] defaults(0)\n\n        # ── 0x18 READ_FIFO_QUEUE ──────────────────────────────────────────────\n        # Request  : FIFO pointer address                      (2 bytes)\n        # Response : byte count + FIFO count + the queued words\n        when function_code == 24:\n            when remaining == 2:\n                uint16 fifo_pointer [\"FIFO Pointer Address\"] defaults(0)\n            else:\n                uint16 byte_count [\"Byte Count\"] defaults(2)\n                uint16 fifo_count [\"FIFO Count\"] defaults(1)\n                repeat fifo_count as fifo_value [\"FIFO Values\"]\n                    uint16 value [\"Value\"] defaults(0)\n\n        # ── 0x2B MEI TRANSPORT — encapsulated interface, type then its body ───\n        when function_code == 43:\n            uint8 mei_type [\"MEI Type\"] defaults(14)\n                CANOPEN_GENERAL_REFERENCE = 13\n                READ_DEVICE_IDENTIFICATION = 14\n            payload mei_data [\"MEI Data\"]\n\n        # ── 0x14 READ_FILE_RECORD, 0x15 WRITE_FILE_RECORD and anything else ───\n        else:\n            payload data [\"Data\"]\n\n    info \"%s  Trans=%u  Unit=%u\" function_code, transaction_id, unit_id\n",
    /* mongodb.posa */
    "# mongodb.posa — MongoDB Wire Protocol\n#\n# Every MongoDB message has a 16-byte little-endian header followed by an\n# operation-specific body. The opCode field identifies the message type;\n# OP_MSG (2013) is the modern unified opcode used by MongoDB 3.6+. Legacy\n# opcodes (OP_QUERY, OP_REPLY, …) are still seen in older drivers.\n#\n# Reference: https://www.mongodb.com/docs/manual/reference/mongodb-wire-protocol/\n#\n# Runs on TCP port 27017 (default).\n\nObject<main> MONGODB\n    abbrev \"mongodb\"\n    col \"MongoDB\"\n\n    le_uint32 message_length \"Message Length\" defaults(16)\n    le_uint32 request_id \"Request ID\" defaults(1)\n    le_uint32 response_to \"Response To\" defaults(0)\n    le_uint32 op_code \"Op Code\" defaults(2013)\n        1    = \"OP_REPLY (deprecated)\"\n        2001 = \"OP_UPDATE (deprecated)\"\n        2002 = \"OP_INSERT (deprecated)\"\n        2004 = \"OP_QUERY (deprecated)\"\n        2005 = \"OP_GET_MORE (deprecated)\"\n        2006 = \"OP_DELETE (deprecated)\"\n        2007 = \"OP_KILL_CURSORS (deprecated)\"\n        2010 = \"OP_COMMAND (deprecated)\"\n        2011 = \"OP_COMMANDREPLY (deprecated)\"\n        2012 = \"OP_COMPRESSED\"\n        2013 = \"OP_MSG\"\n\n    payload body \"Body\"\n\n    info \"op=%u reqid=%u\" op_code, request_id\n\nrule tcp.port == 27017 => MONGODB\nrule tcp.port == 27018 => MONGODB\nrule tcp.port == 27019 => MONGODB\n\ncolor mongodb.op_code == 2013 => black lightblue\ncolor mongodb.op_code == 1    => black lightgreen\ncolor mongodb => white darkblue\n\nalias mongodb.opcode => mongodb.op_code\n",
    /* mpls.posa */
    "# MPLS label stack entry (RFC 3032)\n# Single 32-bit word: label(20 bits) | exp/TC(3 bits) | S/bottom-of-stack(1 bit) | TTL(8 bits)\n# S=1 means this is the bottom label; the next header is IP or another MPLS label.\nObject<main> MPLS\n    uint32 label_exp_s_ttl defaults(0)\n",
    /* mqtt.posa */
    "# mqtt.posa — Message Queuing Telemetry Transport (OASIS MQTT 3.1.1 / 5.0)\n# Reference: https://docs.oasis-open.org/mqtt/mqtt/v5.0/\n#            https://docs.oasis-open.org/mqtt/mqtt/v3.1.1/\n#\n# MQTT is a lightweight publish/subscribe protocol designed for constrained\n# devices and low-bandwidth, high-latency networks (IoT, M2M).  Every frame\n# starts with a 1-byte fixed header whose high nibble selects the message type\n# and low nibble carries per-type flags (DUP, QoS, RETAIN).  A variable-length\n# remaining-length field (1–4 bytes, bit 7 = continuation) follows.\n#\n# Message types decoded here:\n#   1  CONNECT     — client requests a session with the broker\n#   2  CONNACK     — broker responds with a return code (0 = accepted)\n#   3  PUBLISH     — either party delivers an application message\n#   4  PUBACK      — QoS 1 delivery acknowledgement\n#   5  PUBREC      — QoS 2 phase 1 acknowledgement\n#   6  PUBREL      — QoS 2 phase 2 release\n#   7  PUBCOMP     — QoS 2 phase 3 completion\n#   8  SUBSCRIBE   — client requests one or more topic subscriptions\n#   9  SUBACK      — broker grants (or refuses) subscriptions\n#   10 UNSUBSCRIBE — client removes topic subscriptions\n#   11 UNSUBACK    — broker confirms unsubscription\n#   12 PINGREQ     — keepalive probe (client → broker)\n#   13 PINGRESP    — keepalive reply (broker → client)\n#   14 DISCONNECT  — clean session teardown\n#\n# Detection: CONNECT (0x10) is the mandatory first packet on every new MQTT TCP\n# connection; CONNACK (0x20) is always the broker's first reply.  These two bytes\n# together cover both directions without relying on port numbers.  Additional\n# content rules anchor SUBSCRIBE (0x82 — spec-mandated reserved bits), the most\n# common PUBLISH byte (0x30), and the zero-payload keepalive pair (0xC0/0xD0).\n#\n# Transport: TCP/1883 (plaintext), TCP/8883 (TLS — port rule only; content rules\n# fire only on the unencrypted path).\n\nObject<main> MQTT\n    abbrev \"mqtt\"\n    col \"MQTT\"\n\n    # ── Fixed Header — byte 0 ─────────────────────────────────────────────────\n    uint8 type_flags \"Fixed Header\" defaults(0x10)\n\n    bits type_flags msg_type 4 4 \"Message Type\"\n        RESERVED    = 0\n        CONNECT     = 1\n        CONNACK     = 2\n        PUBLISH     = 3\n        PUBACK      = 4\n        PUBREC      = 5\n        PUBREL      = 6\n        PUBCOMP     = 7\n        SUBSCRIBE   = 8\n        SUBACK      = 9\n        UNSUBSCRIBE = 10\n        UNSUBACK    = 11\n        PINGREQ     = 12\n        PINGRESP    = 13\n        DISCONNECT  = 14\n        AUTH        = 15\n\n    bits type_flags dup    3 1 \"DUP Flag\"\n        Not Duplicate = 0\n        Duplicate     = 1\n\n    bits type_flags qos    1 2 \"QoS Level\"\n        At Most Once  = 0\n        At Least Once = 1\n        Exactly Once  = 2\n\n    bits type_flags retain 0 1 \"RETAIN\"\n        Do Not Retain = 0\n        Retain        = 1\n\n    # Remaining Length: MQTT uses variable-length encoding (bit 7 = more bytes\n    # follow; max 4 bytes encodes up to 268,435,455 bytes).  The single-byte\n    # form covers payloads up to 127 bytes, which is the common case for IoT.\n    # Multi-byte remaining-length packets are not further decoded beyond this byte.\n    uint8 remaining_length \"Remaining Length\" defaults(0)\n\n    # All variable-header and payload fields are bounded to remaining_length bytes.\n    scope remaining_length\n\n    # ── CONNECT ───────────────────────────────────────────────────────────────\n    # Mandatory first packet from the client to the broker.  The variable header\n    # carries the protocol name (UTF-8 length-prefixed string), version byte,\n    # connect flags, and keepalive interval.  The payload begins with the\n    # mandatory UTF-8 Client ID; Will topic/message, username, and password\n    # follow conditionally on the connect_flags bits (not decoded here).\n    when msg_type == 1:\n        uint16 proto_name_len \"Protocol Name Length\" defaults(4)\n        str[proto_name_len] proto_name \"Protocol Name\" defaults(\"MQTT\")\n        uint8 protocol_level \"Protocol Level\" defaults(4)\n            MQTT 3.1   = 3\n            MQTT 3.1.1 = 4\n            MQTT 5.0   = 5\n        uint8 connect_flags \"Connect Flags\" defaults(0x02)\n        bits connect_flags username_flag 7 1 \"User Name Flag\"\n        bits connect_flags password_flag 6 1 \"Password Flag\"\n        bits connect_flags will_retain   5 1 \"Will RETAIN\"\n        bits connect_flags will_qos      3 2 \"Will QoS\"\n        bits connect_flags will_flag     2 1 \"Will Flag\"\n        bits connect_flags clean_session 1 1 \"Clean Session\"\n        uint16 keep_alive \"Keep Alive (seconds)\" defaults(60)\n        # Payload — Client ID is mandatory; conditional fields are not decoded here\n        uint16 client_id_len \"Client ID Length\" defaults(0)\n        str[client_id_len] client_id \"Client ID\"\n\n    # ── CONNACK ───────────────────────────────────────────────────────────────\n    # Broker's response to CONNECT.  A non-zero return code means the broker\n    # refused the connection; the spec requires the TCP session to be closed\n    # by the broker after sending a non-zero CONNACK.\n    when msg_type == 2:\n        uint8 connack_flags \"Connect Acknowledge Flags\" defaults(0)\n        bits connack_flags session_present 0 1 \"Session Present\"\n        uint8 return_code \"Connect Return Code\" defaults(0)\n            Accepted              = 0\n            Unacceptable Protocol = 1\n            Identifier Rejected   = 2\n            Server Unavailable    = 3\n            Bad Credentials       = 4\n            Not Authorized        = 5\n\n    # ── PUBLISH ───────────────────────────────────────────────────────────────\n    # Carries one application message between client and broker (bidirectional).\n    # The Topic Name is always present.  QoS > 0 messages include a Packet\n    # Identifier in the variable header to correlate acknowledgements.\n    when msg_type == 3:\n        uint16 topic_len \"Topic Name Length\" defaults(0)\n        str[topic_len] topic \"Topic Name\"\n        when qos == 1:\n            uint16 packet_id \"Packet Identifier\" defaults(1)\n        when qos == 2:\n            uint16 packet_id \"Packet Identifier\" defaults(1)\n        payload message_data \"Message Payload\"\n\n    # ── QoS acknowledgement flow ──────────────────────────────────────────────\n    # PUBACK  (4): QoS 1 — broker or client confirms delivery\n    # PUBREC  (5): QoS 2 phase 1 — receiver confirms receipt\n    # PUBREL  (6): QoS 2 phase 2 — sender releases the message\n    # PUBCOMP (7): QoS 2 phase 3 — receiver confirms completion\n    when msg_type == 4:\n        uint16 packet_id \"Packet Identifier\" defaults(1)\n\n    when msg_type == 5:\n        uint16 packet_id \"Packet Identifier\" defaults(1)\n\n    when msg_type == 6:\n        uint16 packet_id \"Packet Identifier\" defaults(1)\n\n    when msg_type == 7:\n        uint16 packet_id \"Packet Identifier\" defaults(1)\n\n    # ── SUBSCRIBE ─────────────────────────────────────────────────────────────\n    # Client requests one or more topic filter subscriptions.  The reserved\n    # bits in the fixed header are defined as 0b0010 by the specification,\n    # making the first byte always 0x82 — a reliable content-detection anchor.\n    when msg_type == 8:\n        uint16 packet_id \"Packet Identifier\" defaults(1)\n        repeat until end as topic_filter \"Topic Filter List\"\n            uint16 filter_len \"Topic Filter Length\" defaults(0)\n            str[filter_len] filter_topic \"Topic Filter\"\n            uint8 requested_qos \"Requested QoS\" defaults(0)\n                QoS 0 = 0\n                QoS 1 = 1\n                QoS 2 = 2\n\n    # ── SUBACK ────────────────────────────────────────────────────────────────\n    # Broker acknowledges a SUBSCRIBE, one return code per requested topic filter.\n    # 0x80 (128) means the broker refused that particular filter.\n    when msg_type == 9:\n        uint16 packet_id \"Packet Identifier\" defaults(1)\n        repeat until end as granted_qos \"Granted QoS List\"\n            uint8 sub_return_code \"Return Code\" defaults(0)\n                Granted QoS 0 = 0\n                Granted QoS 1 = 1\n                Granted QoS 2 = 2\n                Failure       = 128\n\n    # ── UNSUBSCRIBE ───────────────────────────────────────────────────────────\n    # Client removes one or more topic filter subscriptions.  Reserved bits\n    # are 0b0010 (same as SUBSCRIBE), making the first byte always 0xA2.\n    when msg_type == 10:\n        uint16 packet_id \"Packet Identifier\" defaults(1)\n        repeat until end as unsub_filter \"Topic Filter List\"\n            uint16 unsub_len \"Topic Filter Length\" defaults(0)\n            str[unsub_len] unsub_topic \"Topic Filter\"\n\n    # ── UNSUBACK ──────────────────────────────────────────────────────────────\n    when msg_type == 11:\n        uint16 packet_id \"Packet Identifier\" defaults(1)\n\n    # PINGREQ  (12): no variable header or payload; exactly 2 bytes: 0xC0 0x00\n    # PINGRESP (13): no variable header or payload; exactly 2 bytes: 0xD0 0x00\n    # DISCONNECT (14): no variable header or payload in MQTT 3.x\n    #                  (MQTT 5.0 adds a reason code and properties — not decoded here)\n\n    else:\n        payload msg_body \"Message Body\"\n\n    info \"%s %s%s\" msg_type, topic, client_id\n\n\n# ── Content Detection Rules (primary — port-independent) ──────────────────────\n#\n# CONNECT (0x10): mandatory first byte of every new MQTT TCP session.\n# The type=1 / flags=0 combination is unique to MQTT CONNECT; no other protocol\n# uses this byte as its first octet in the same context.\nweak rule tcp.content \"\\x10\" => MQTT\n\n# CONNACK (0x20): broker's mandatory first reply; covers the server→client\n# direction so both halves of a TCP stream are anchored.\nweak rule tcp.content \"\\x20\" => MQTT\n\n# PUBLISH QoS-0 no-DUP no-RETAIN (0x30): the most common IoT telemetry pattern.\n# Sensors publishing periodic readings almost universally use QoS 0.\nweak rule tcp.content \"\\x30\" => MQTT\n\n# SUBSCRIBE (0x82): spec-mandated reserved bits make this value unambiguous.\nweak rule tcp.content \"\\x82\" => MQTT\n\n# PINGREQ / PINGRESP: fixed 2-byte packets; first byte is unambiguous per type.\nweak rule tcp.content \"\\xc0\" => MQTT\nweak rule tcp.content \"\\xd0\" => MQTT\n\n# DISCONNECT (0xE0): clean session teardown, distinct from any other protocol.\nweak rule tcp.content \"\\xe0\" => MQTT\n\n# ── Port Rules (secondary hints) ──────────────────────────────────────────────\n# Listed after content rules; content detection fires first on non-standard ports.\n# TCP/8883 carries MQTT over TLS: content rules cannot see inside TLS, so the\n# port rule is the only trigger for the encrypted path.\nrule tcp.port == 1883 => MQTT\nrule tcp.port == 8883 => MQTT\n\n\n# ── Coloring Rules ────────────────────────────────────────────────────────────\n# Most-specific rules first; first match wins.\n\n# Connection refused — red: authentication failure or protocol mismatch\ncolor mqtt.msg_type == 2 && mqtt.return_code > 0 => white red\n\n# Connection setup — yellow: security-relevant, session initiation\ncolor mqtt.msg_type == 1 => black lightyellow\n\n# Successful connection acknowledgement — green: broker confirmed the session\ncolor mqtt.msg_type == 2 => black lightgreen\n\n# Application data — green: PUBLISH and the full QoS acknowledgement flow\ncolor mqtt.msg_type == 3 => black lightgreen\ncolor mqtt.msg_type == 4 => black lightgreen\ncolor mqtt.msg_type == 5 => black lightgreen\ncolor mqtt.msg_type == 6 => black lightgreen\ncolor mqtt.msg_type == 7 => black lightgreen\n\n# Subscription requests — yellow: client-initiated control plane changes\ncolor mqtt.msg_type == 8  => black lightyellow\ncolor mqtt.msg_type == 10 => black lightyellow\n\n# Subscription acknowledgements — green: broker confirmations\ncolor mqtt.msg_type == 9  => black lightgreen\ncolor mqtt.msg_type == 11 => black lightgreen\n\n# Keepalives — gray: background traffic, not application data\ncolor mqtt.msg_type == 12 => black lightgray\ncolor mqtt.msg_type == 13 => black lightgray\n\n# Disconnect — dark blue: session teardown (channel closing)\ncolor mqtt.msg_type == 14 => white darkblue\n\n# Protocol catch-all — dark green: any MQTT frame not matched above\ncolor mqtt => white darkgreen\n\n\n# ── Wireshark-Compatible Aliases ──────────────────────────────────────────────\n# Map Wireshark's mqtt dissector field names to POSA abbrevs so existing display\n# filters continue to work unchanged against captures analysed with this decoder.\n\nalias mqtt.msgtype        => mqtt.msg_type\nalias mqtt.hdrflags       => mqtt.type_flags\nalias mqtt.dupflag        => mqtt.dup\nalias mqtt.qos            => mqtt.qos\nalias mqtt.retain         => mqtt.retain\nalias mqtt.len            => mqtt.remaining_length\nalias mqtt.proto_name     => mqtt.proto_name\nalias mqtt.ver            => mqtt.protocol_level\nalias mqtt.conflags       => mqtt.connect_flags\nalias mqtt.cleansess      => mqtt.clean_session\nalias mqtt.willflag       => mqtt.will_flag\nalias mqtt.willqos        => mqtt.will_qos\nalias mqtt.willretain     => mqtt.will_retain\nalias mqtt.usernameflag   => mqtt.username_flag\nalias mqtt.passwordflag   => mqtt.password_flag\nalias mqtt.kalive         => mqtt.keep_alive\nalias mqtt.clientid       => mqtt.client_id\nalias mqtt.connack        => mqtt.return_code\nalias mqtt.connreturncode => mqtt.return_code\nalias mqtt.sessPresent    => mqtt.session_present\nalias mqtt.topic          => mqtt.topic\nalias mqtt.msgid          => mqtt.packet_id\n",
    /* mssql_tds.posa */
    "# mssql_tds.posa — Microsoft SQL Server Tabular Data Stream (TDS)\n#\n# TDS is the application-layer protocol used by Microsoft SQL Server (and its\n# clients: ODBC, JDBC, FreeTDS, etc.). Every TDS message begins with an 8-byte\n# header identifying the packet type, status, total length, channel, packet\n# number, and a reserved window byte (always 0x00).  The payload follows and\n# is parsed by the server or client according to the packet type.\n#\n# Reference: https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-tds\n#\n# Runs on TCP port 1433 (standard).\n\nObject<main> MSSQL_TDS\n    abbrev \"tds\"\n    col \"MSSQL TDS\"\n\n    uint8 type \"Type\" defaults(1)\n        1  = \"SQL Batch\"\n        2  = \"Pre-TDS7 Login\"\n        3  = \"RPC\"\n        4  = \"Tabular Result\"\n        6  = \"Attention\"\n        7  = \"Bulk Load BCP\"\n        8  = \"Federated Authentication Token\"\n        14 = \"Transaction Manager Request\"\n        15 = \"TDS7 Login\"\n        16 = \"SSPI\"\n        17 = \"Pre-Login\"\n        18 = \"Pre-Login Response / Fed Auth Info\"\n\n    uint8 status hex \"Status\" defaults(0x01)\n        0x00 = \"Normal\"\n        0x01 = \"End of Message\"\n        0x02 = \"Ignore Event\"\n        0x04 = \"Reset Connection\"\n        0x08 = \"Reset Connection Skip Transaction\"\n        0x10 = \"Re-use Connection\"\n\n    uint16 length \"Total Length\" defaults(8)\n    uint16 channel \"Channel\" defaults(0)\n    uint8 packet_number \"Packet Number\" defaults(1)\n    uint8 window \"Window\" defaults(0)\n\n    payload body \"Body\"\n\n    info \"TDS type=%u len=%u pkt=%u\" type, length, packet_number\n\nrule tcp.port == 1433 => MSSQL_TDS\n\nrule tcp.content \"\\x12\\x01\\x00\" => MSSQL_TDS\nrule tcp.content \"\\x10\\x01\\x00\" => MSSQL_TDS\nrule tcp.content \"\\x01\\x01\\x00\" => MSSQL_TDS\n\ncolor tds.type == 15 || tds.type == 16 || tds.type == 17 => black lightyellow\ncolor tds.type == 4  => black lightgreen\ncolor tds.type == 6  => black lightyellow\ncolor tds.type == 14 => black lightblue\ncolor tds => white darkblue\n\nalias mssql.type   => tds.type\nalias mssql.status => tds.status\n",
    /* mumble.posa */
    "# mumble.posa — Mumble Voice Protocol (Murmur TCP control channel)\n#\n# Mumble uses TCP for control messages and (optionally) UDP for voice.\n# The TCP control channel frames every message with a 6-byte header:\n#\n#   uint16_be type    — message type (see enum below)\n#   uint32_be length  — length of the protobuf payload\n#   bytes[length]     — protobuf-encoded message body\n#\n# UDP voice packets are encrypted (OCB-AES128) so only the TCP control\n# channel is decodable here.\n#\n# The connection opens with the client sending a Version message (type 0),\n# then Authenticate (type 2). The server replies with CryptSetup (type 15),\n# CodecVersion (type 21), ChannelState (type 7), UserState (type 9), and\n# ServerSync (type 5) to complete the session setup.\n#\n# Reference: https://mumble-protocol.readthedocs.io/\n#            https://github.com/mumble-voip/mumble/blob/master/src/Mumble.proto\n#\n# Runs on TCP port 64738 (default).\n\nObject<main> MUMBLE\n    abbrev \"mumble\"\n    col \"Mumble\"\n\n    uint16 type \"Message Type\" defaults(0)\n        0  = \"Version\"\n        1  = \"UDPTunnel\"\n        2  = \"Authenticate\"\n        3  = \"Ping\"\n        4  = \"Reject\"\n        5  = \"ServerSync\"\n        6  = \"ChannelRemove\"\n        7  = \"ChannelState\"\n        8  = \"UserRemove\"\n        9  = \"UserState\"\n        10 = \"BanList\"\n        11 = \"TextMessage\"\n        12 = \"PermissionDenied\"\n        13 = \"ACL\"\n        14 = \"QueryUsers\"\n        15 = \"CryptSetup\"\n        16 = \"ContextActionModify\"\n        17 = \"ContextAction\"\n        18 = \"UserList\"\n        19 = \"VoiceTarget\"\n        20 = \"PermissionQuery\"\n        21 = \"CodecVersion\"\n        22 = \"UserStats\"\n        23 = \"RequestBlob\"\n        24 = \"ServerConfig\"\n        25 = \"SuggestConfig\"\n\n    uint32 length \"Payload Length\" defaults(0)\n    bytes[length] body \"Protobuf Body\"\n\n    info \"%s len=%u\" type, length\n\nrule tcp.port == 64738 => MUMBLE\n\n# The version handshake starts with uint16=0 (Version) then uint32 length then\n# the protobuf; the first 2 bytes are always \\x00\\x00 for the very first message.\nweak rule tcp.content \"\\x00\\x00\" => MUMBLE\n\ncolor mumble.type == 11 => black lightgreen\ncolor mumble.type == 2  => black lightyellow\ncolor mumble.type == 5  => black lightblue\ncolor mumble.type == 4  => white red\ncolor mumble.type == 3  => black lightgray\ncolor mumble.type == 1  => black lightblue\ncolor mumble => white darkblue\n\nalias mumble.msg_type  => mumble.type\nalias mumble.msg_size  => mumble.length\n",
    /* mysql.posa */
    "# MySQL / MariaDB client command packet (best-effort, per-packet).\n# The robust way to sniff queries across TCP segments is the stream script\n# scripts/mysql-queries.lua; this decoder labels single client-command packets.\nprotocol MySQL\n    col \"MySQL\"\n    bytes<3> length\n    uint8    seq defaults(0)\n    uint8    command \"Command\" defaults(3)\n        COM_QUIT     = 1\n        COM_INIT_DB  = 2\n        COM_QUERY    = 3\n        COM_PING     = 14\n    payload  query \"Query\"\nrule tcp.port == 3306 => MySQL\n",
    /* nat_pmp.posa */
    "# nat_pmp.posa — NAT Port Mapping Protocol (NAT-PMP / PCP)\n#\n# NAT-PMP (RFC 6886) allows a host behind NAT to learn its external IP and\n# request port mappings. Superseded by PCP (Port Control Protocol, RFC 6887)\n# which shares the same UDP port.\n#\n# NAT-PMP (RFC 6886):\n#   Client sends on UDP 5351:\n#     uint8  version   — 0\n#     uint8  opcode    — 0=external addr, 1=map UDP, 2=map TCP\n#     uint16 reserved  — 0\n#     uint16 int_port  — internal port (map requests)\n#     uint16 ext_port  — suggested external port\n#     uint32 lifetime  — seconds (0=delete)\n#\n#   Server response: version=0, opcode = 128+request_opcode, result_code\n#\n# PCP (RFC 6887) uses version=2 with a 24-byte common header:\n#     uint8  version   — 2\n#     uint8  r_opcode  — bit 7: response flag; bits 6-0: opcode\n#     uint16 reserved\n#     uint32 lifetime\n#     uint8[16] client_ip\n#\n# Default UDP port: 5351 (NAT-PMP and PCP share the same port)\n\nObject<NAT_PMP> NAT_PMP_REQUEST\n    abbrev \"nat_pmp\"\n    col \"NAT-PMP\"\n\n    uint8  version \"Version\" defaults(0)\n        0 = \"NAT-PMP v0\"\n    uint8  opcode \"Opcode\" defaults(1)\n        0 = \"External Address\"\n        1 = \"Map UDP\"\n        2 = \"Map TCP\"\n    uint16 reserved \"Reserved\" defaults(0)\n    when opcode != 0:\n        uint16 int_port \"Internal Port\" defaults(0)\n        uint16 ext_port \"External Port\" defaults(0)\n        uint32 lifetime \"Lifetime (seconds)\" defaults(7200)\n    else:\n        payload rest \"Data\"\n\n    info \"NAT-PMP req op=%u\" opcode\n\nObject<NAT_PMP> NAT_PMP_RESPONSE\n    abbrev \"nat_pmp\"\n    col \"NAT-PMP\"\n\n    uint8  version \"Version\" defaults(0)\n        0 = \"NAT-PMP v0\"\n    uint8  opcode \"Opcode\" defaults(128)\n        128 = \"External Address Response\"\n        129 = \"Map UDP Response\"\n        130 = \"Map TCP Response\"\n    uint16 result_code \"Result Code\" defaults(0)\n        0 = \"Success\"\n        1 = \"Unsupported Version\"\n        2 = \"Not Authorized\"\n        3 = \"Network Failure\"\n        4 = \"Out of Resources\"\n        5 = \"Unsupported Opcode\"\n    uint32 seconds_since_epoch \"Epoch Time\" defaults(0)\n    when opcode == 128:\n        ip4 ext_addr \"External Address\"\n    else:\n        uint16 int_port \"Internal Port\" defaults(0)\n        uint16 ext_port \"External Port\" defaults(0)\n        uint32 lifetime \"Lifetime (seconds)\" defaults(7200)\n\n    info \"NAT-PMP resp op=%u result=%u\" opcode, result_code\n\nObject<NAT_PMP> PCP_REQUEST\n    abbrev \"nat_pmp\"\n    col \"PCP\"\n\n    uint8  version \"Version\" defaults(2)\n        2 = \"PCP v2\"\n    uint8  r_opcode  hex \"R/Opcode\" defaults(0x01)\n        0x00 = \"Announce\"\n        0x01 = \"Map\"\n        0x02 = \"Peer\"\n    uint16 reserved \"Reserved\" defaults(0)\n    uint32 lifetime \"Requested Lifetime\" defaults(7200)\n    bytes<16> client_ip \"Client IP Address\"\n    payload pcp_options \"PCP Options\"\n\n    info \"PCP req op=%u\" r_opcode\n\nObject<GROUP> NAT_PMP\n\nrule udp.port == 5351 => NAT_PMP\n\n# NAT-PMP request starts with version=0\nrule udp.content \"\\x00\\x00\\x00\\x00\" => NAT_PMP\nrule udp.content \"\\x00\\x01\\x00\\x00\" => NAT_PMP\nrule udp.content \"\\x00\\x02\\x00\\x00\" => NAT_PMP\n# NAT-PMP response (opcode 128-130)\nrule udp.content \"\\x00\\x80\\x00\\x00\" => NAT_PMP\nrule udp.content \"\\x00\\x81\\x00\\x00\" => NAT_PMP\nrule udp.content \"\\x00\\x82\\x00\\x00\" => NAT_PMP\n# PCP starts with version=2\nrule udp.content \"\\x02\\x00\\x00\\x00\" => NAT_PMP\nrule udp.content \"\\x02\\x01\\x00\\x00\" => NAT_PMP\n\ncolor nat_pmp.result_code != 0 => white red\ncolor nat_pmp.opcode == 0 || nat_pmp.opcode == 128 => black lightyellow\ncolor nat_pmp.opcode == 1 || nat_pmp.opcode == 129 => black lightblue\ncolor nat_pmp.opcode == 2 || nat_pmp.opcode == 130 => black lightgreen\ncolor nat_pmp => white darkblue\n\nalias pcp.version   => nat_pmp.version\nalias pcp.opcode    => nat_pmp.r_opcode\nalias pcp.lifetime  => nat_pmp.lifetime\n",
    /* nats.posa */
    "# nats.posa — NATS Messaging Protocol\n#\n# NATS is a line-oriented text protocol: each message is a single line\n# terminated with \\r\\n. The first token on the line identifies the operation.\n#\n# Server-originated:  INFO {...}   MSG <subject> <sid> [reply] <#bytes>\\r\\n<payload>\\r\\n\n#                     PING         PONG         +OK         -ERR <desc>\n#\n# Client-originated:  CONNECT {...}   PUB <subject> [reply] <#bytes>\\r\\n<payload>\\r\\n\n#                     SUB <subject> [queue] <sid>      UNSUB <sid> [max]\n#                     PING         PONG\n#\n# Reference: https://docs.nats.io/reference/reference-protocols/nats\n#\n# Runs on TCP port 4222 (default).\n\nObject<NATS> NATS_SERVER\n    abbrev \"nats\"\n    col \"NATS\"\n    starts \"INFO \" \"PING\\r\" \"PONG\\r\" \"+OK\\r\" \"-ERR \"\n           \"MSG \" \"HMSG \"\n\n    string op until \" \\r\" \"Operation\" defaults(\"INFO\")\n    payload rest \"Rest\"\n\n    info \"%s\" op\n\nObject<NATS> NATS_CLIENT\n    abbrev \"nats\"\n    col \"NATS\"\n    starts \"CONNECT \" \"PUB \" \"SUB \" \"UNSUB \"\n           \"PING\\r\" \"PONG\\r\" \"HPUB \" \"HSUB \"\n\n    string op until \" \\r\" \"Operation\" defaults(\"CONNECT\")\n    payload rest \"Rest\"\n\n    info \"%s\" op\n\nObject<GROUP> NATS\n\nrule tcp.port == 4222 => NATS\nrule tcp.port == 6222 => NATS\n\nrule tcp.content \"INFO {\"   => NATS\nrule tcp.content \"CONNECT \" => NATS\nrule tcp.content \"PUB \"     => NATS\nrule tcp.content \"SUB \"     => NATS\nrule tcp.content \"MSG \"     => NATS\n\ncolor nats.op == \"MSG\"     => black lightgreen\ncolor nats.op == \"PUB\"     => black lightyellow\ncolor nats.op == \"SUB\"     => black lightblue\ncolor nats.op == \"+OK\"     => black lightgreen\ncolor nats.op == \"-ERR\"    => white red\ncolor nats.op == \"INFO\"    => black lightgray\ncolor nats.op == \"CONNECT\" => black lightyellow\ncolor nats => white darkblue\n",
    /* nbns.posa */
    "# nbns.posa — NetBIOS Name Service (RFC 1001 / RFC 1002)\n#\n# Transport: UDP port 137. Used in Windows networks for name registration,\n# name query, name release, and name conflict messages before DNS took over.\n#\n# Wire format is identical to DNS: a 12-byte fixed header followed by\n# question and resource-record sections. `dnsname` handles the DNS-style\n# label encoding (including 0xc0 compression pointers) transparently.\n#\n# Note on name display: NBNS applies a second-level encoding on top of the\n# DNS label format. The 16-byte padded NetBIOS name is encoded as a 32-byte\n# label where each nibble of each byte becomes an uppercase letter (nibble\n# + 0x41). The decoded label shown here is therefore the raw encoded form,\n# e.g. \"CKFJFJDPFPFPCACACACACACACACAAA\" for \"DESKTOP-ABC<20>\".  Full\n# decoding requires a post-processing step not expressible in POSA field\n# types, but the structure, IP addresses, and flags are fully decoded.\n#\n# NB resource record rdata (type 0x0020):\n#   Each address entry is 6 bytes: 2-byte NB flags + 4-byte IP address.\n#   A single NB record can list multiple addresses (rdlength / 6 entries).\n#   NB flag bits:\n#     bit 15  G   — group name (1) vs unique name (0)\n#     bits 14-13  ONT — owner node type: 0=B-node, 1=P-node, 2=M-node, 3=H-node\n#\n# NBSTAT rdata (type 0x0021) contains the adapter status response:\n#   1-byte name count, then count × (16-byte name + 2-byte name flags),\n#   then a 6-byte MAC address and 48 bytes of statistics. This is decoded\n#   as a raw payload here; a C dissector would be needed for full decode.\n\n# ── NB resource record (shared by answer / authority / additional) ───────────\nObject<main> NB_RR\n    abbrev \"nbns\"\n\n    dnsname name \"Name\"\n    uint16  type \"Type\" defaults(0x0020)\n        NB     = 0x0020\n        NBSTAT = 0x0021\n        A      = 0x0001\n        NS     = 0x0002\n        NULL   = 0x000a\n        ANY    = 0x00ff\n    uint16  class \"Class\" defaults(1)\n        IN  = 1\n        ANY = 255\n    uint32  ttl \"Time to live\" defaults(300)\n    uint16  rdlength \"Data length\" defaults(6)\n\n    scope rdlength\n        # NB record: one or more address entries, each 6 bytes\n        when type == 0x0020:\n            repeat until end as addr_entry \"NB Addresses\"\n                label \"%s (%s, %s)\" ip, group, ont\n                uint16 nb_flags \"NB Flags\" defaults(0)\n                    bits nb_flags group 15 1 \"Name type\"\n                        Unique = 0\n                        Group  = 1\n                    bits nb_flags ont   13 2 \"Owner node type\"\n                        B-node = 0\n                        P-node = 1\n                        M-node = 2\n                        H-node = 3\n                ip4 ip \"IP Address\"\n        # NBSTAT: adapter status — complex binary structure, surface as raw\n        when type == 0x0021:\n            payload rdata \"Adapter Status\"\n        when type != 0x0020:\n            payload rdata \"Rdata\"\n\n# ── Main NBNS frame ──────────────────────────────────────────────────────────\nObject<main> NBNS\n    abbrev \"nbns\"\n    col \"NBNS\"\n\n    uint16 transaction_id \"Transaction ID\" defaults(0)\n    uint16 flags \"Flags\" defaults(0x0110)\n        bits flags qr      15 1 \"Message type\"\n            Query    = 0\n            Response = 1\n        bits flags opcode  11 4 \"Opcode\"\n            Query        = 0\n            Registration = 5\n            Release      = 6\n            WACK         = 7\n            Refresh      = 8\n        bits flags aa      10 1 \"Authoritative answer\"\n        bits flags tc       9 1 \"Truncated\"\n        bits flags rd       8 1 \"Recursion desired\"\n        bits flags ra       7 1 \"Recursion available\"\n        bits flags b        4 1 \"Broadcast\"\n        bits flags rcode    0 4 \"Result code\"\n            No-error   = 0\n            Format-err = 1\n            Server-err = 2\n            Name-err   = 3\n            Unsupported= 4\n            Refused    = 5\n            Active-err = 6\n            Conflict   = 7\n\n    uint16 questions \"Questions\" defaults(1)\n    uint16 answers \"Answer RRs\" defaults(0)\n    uint16 authority \"Authority RRs\" defaults(0)\n    uint16 additional \"Additional RRs\" defaults(0)\n\n    # ── Questions ─────────────────────────────────────────────────────────────\n    repeat questions as query \"Queries\"\n        label \"%s  type %s\" qname, qtype\n        dnsname qname \"Name\"\n        uint16  qtype \"Type\" defaults(0x0020)\n            NB     = 0x0020\n            NBSTAT = 0x0021\n            ANY    = 0x00ff\n        uint16  qclass \"Class\" defaults(1)\n            IN  = 1\n            ANY = 255\n\n    # ── Resource-record sections ──────────────────────────────────────────────\n    repeat answers as answer \"Answers\"\n        label \"%s  %s  %s\" name, type, ip\n        include NB_RR\n\n    repeat authority as ns_rr \"Authority RRs\"\n        label \"%s  %s\" name, type\n        include NB_RR\n\n    repeat additional as extra_rr \"Additional RRs\"\n        label \"%s  %s\" name, type\n        include NB_RR\n\n    info \"%s 0x%04x %s %s %s\" qr, transaction_id, opcode, qname, ip\n\nrule udp.port == 137 => NBNS\n\ncolor nbns.rcode > 0  => yellow red\ncolor nbns.qr == 0    => black lightyellow\ncolor nbns            => white darkblue\n",
    /* netflow_v5.posa */
    "# NetFlow v5 export header (RFC 3954) — 24 bytes\n# UDP, typically port 2055 or 9995. Flow records (48 bytes each) follow the header.\n# count = number of flow records in this datagram (max 30 per UDP packet).\nObject<main> NetFlowV5\n    uint16 version defaults(5)\n    uint16 count defaults(0)\n    uint32 sys_uptime defaults(0)\n    uint32 unix_secs defaults(0)\n    uint32 unix_nsecs defaults(0)\n    uint32 flow_sequence defaults(0)\n    uint8  engine_type defaults(0)\n        RP  = 0\n        VIP = 1\n    uint8  engine_id defaults(0)\n    uint16 sampling_interval defaults(0)\n",
    /* nntp.posa */
    "# nntp.posa — Network News Transfer Protocol (NNTP / Usenet, RFC 3977)\n#\n# NNTP is a line-oriented text protocol used to distribute and retrieve\n# Usenet news articles. Commands are single-line ASCII; responses start with\n# a 3-digit status code followed by a space and a human-readable message.\n#\n# Client commands: CAPABILITIES, MODE READER, AUTHINFO USER/PASS, LIST,\n#   GROUP, ARTICLE, HEAD, BODY, STAT, NEXT, LAST, NEWGROUPS, NEWNEWS,\n#   OVER, HDR, IHAVE, POST, QUIT\n#\n# Server responses:\n#   1xx — Informational (100=help, 101=capabilities)\n#   2xx — Completed (200=ready, 215=list follows, 220=article, 221=head,\n#          222=body, 223=stat, 224=overview follows, 225=headers follow)\n#   3xx — Continue (335=send article, 381=password required)\n#   4xx — Transient errors (411=no such group, 420=no current article,\n#          430=no such article, 480=authentication required)\n#   5xx — Fatal errors (501=syntax error, 502=no permission)\n#\n# Reference: RFC 3977 (NNTP), RFC 4643 (AUTHINFO), RFC 4644 (STREAMING)\n# Default TCP port: 119 (NNTP), 563 (NNTPS/TLS), 433 (news readers)\n\nLookup NntpStatusCodes\n    \"100\" = \"Help text follows\"\n    \"101\" = \"Capability list follows\"\n    \"111\" = \"Server date and time\"\n    \"200\" = \"Service available, posting allowed\"\n    \"201\" = \"Service available, posting prohibited\"\n    \"202\" = \"Slave status noted\"\n    \"203\" = \"Streaming is OK\"\n    \"205\" = \"Connection closing\"\n    \"211\" = \"Group selected\"\n    \"215\" = \"List of newsgroups follows\"\n    \"220\" = \"Article follows\"\n    \"221\" = \"Article headers follow\"\n    \"222\" = \"Article body follows\"\n    \"223\" = \"Article exists\"\n    \"224\" = \"Overview information follows\"\n    \"225\" = \"Headers follow\"\n    \"230\" = \"List of new articles follows\"\n    \"231\" = \"List of new newsgroups follows\"\n    \"235\" = \"Article transferred OK\"\n    \"240\" = \"Article posted\"\n    \"250\" = \"Authorization accepted\"\n    \"281\" = \"Authentication accepted\"\n    \"283\" = \"Authentication accepted (SASL)\"\n    \"335\" = \"Send article to be transferred\"\n    \"340\" = \"Send article to be posted\"\n    \"381\" = \"Enter passphrase\"\n    \"383\" = \"Continue with SASL exchange\"\n    \"400\" = \"Service temporarily unavailable\"\n    \"401\" = \"Wrong MODE\"\n    \"411\" = \"No such newsgroup\"\n    \"412\" = \"No newsgroup selected\"\n    \"420\" = \"No current article\"\n    \"421\" = \"No next article\"\n    \"422\" = \"No previous article\"\n    \"423\" = \"No such article in group\"\n    \"430\" = \"No such article\"\n    \"431\" = \"Try sending it again later\"\n    \"435\" = \"Article not wanted\"\n    \"436\" = \"Transfer failed, try again later\"\n    \"437\" = \"Article rejected, do not retry\"\n    \"440\" = \"Posting not permitted\"\n    \"441\" = \"Posting failed\"\n    \"450\" = \"Authorization required\"\n    \"452\" = \"Authorization data rejected\"\n    \"480\" = \"Authentication required\"\n    \"481\" = \"Authentication failed\"\n    \"482\" = \"Authentication commands issued out of sequence\"\n    \"483\" = \"Operation only available after authentication\"\n    \"500\" = \"Unknown command\"\n    \"501\" = \"Syntax error in command\"\n    \"502\" = \"Access restriction or permission denied\"\n    \"503\" = \"Feature not supported\"\n    \"504\" = \"Base64 encoding error\"\n\nObject<NNTP> NNTP_RESPONSE\n    abbrev \"nntp\"\n    col \"NNTP\"\n    starts \"1\" \"2\" \"3\" \"4\" \"5\"\n\n    string status_code until \" \\r\" lookup NntpStatusCodes \"Status Code\" defaults(\"200\")\n    string message until \"\\r\" \"Message\" defaults(\"Service available\")\n    payload rest \"Data\"\n\n    info \"%s %s\" status_code, message\n\nObject<NNTP> NNTP_COMMAND\n    abbrev \"nntp\"\n    col \"NNTP\"\n    starts \"CAPABILITIES\" \"MODE\" \"AUTHINFO\" \"LIST\" \"GROUP\" \"ARTICLE\"\n           \"HEAD\" \"BODY\" \"STAT\" \"NEXT\" \"LAST\" \"NEWGROUPS\" \"NEWNEWS\"\n           \"OVER\" \"HDR\" \"IHAVE\" \"POST\" \"QUIT\" \"SLAVE\" \"CHECK\" \"TAKETHIS\"\n           \"DATE\" \"HELP\"\n\n    string command until \" \\r\" \"Command\" defaults(\"LIST\")\n    payload args \"Arguments\"\n\n    info \"%s\" command\n\nObject<GROUP> NNTP\n\nrule tcp.port == 119 => NNTP\nrule tcp.port == 433 => NNTP\n\ncolor nntp.status_code == \"220\" || nntp.status_code == \"221\" || nntp.status_code == \"222\" => black lightblue\ncolor nntp.status_code == \"235\" || nntp.status_code == \"240\" => black lightgreen\ncolor nntp.status_code == \"480\" || nntp.status_code == \"481\" || nntp.status_code == \"483\" => black lightyellow\ncolor nntp.status_code >= \"400\" => white red\ncolor nntp => white darkblue\n\nalias usenet.status => nntp.status_code\nalias news.command  => nntp.command\n",
    /* ntp.posa */
    "# ntp.posa — Network Time Protocol (RFC 5905)\n#\n# NTP carries clock synchronisation over UDP port 123. The minimum packet size\n# is 48 bytes; optional extension fields and an optional MAC follow.\n#\n# Byte 0 packs three sub-fields:\n#   bits 7-6  LI  — leap-second warning the current UTC day\n#   bits 5-3  VN  — protocol version (1–4; 3 and 4 in common use)\n#   bits 2-0  Mode — packet role: client (3), server (4), broadcast (5), …\n#\n# Root delay and root dispersion are NTP short format: 16-bit unsigned seconds\n# followed by 16-bit unsigned fraction (big-endian, total 32 bits each).\n# All timestamps are NTP 64-bit format: 32-bit unsigned seconds since\n# 1900-01-01 00:00:00 UTC concatenated with a 32-bit binary fraction.\n#\n# The Reference ID (ref_id) is interpreted differently by stratum:\n#   stratum 0  — four-character ASCII kiss code (e.g. \"DENY\", \"RATE\", \"STEP\")\n#   stratum 1  — four-character ASCII source ID (e.g. \"GPS\\0\", \"PPS\\0\", \"LOCL\")\n#   stratum 2+ — IPv4 address of the upstream reference (or MD5 hash for IPv6)\n#\n# Content-based detection is not used: byte 0 bit patterns for valid LI/VN/Mode\n# combinations (e.g. 0x1b, 0x23, 0x24) are too common in unrelated UDP traffic\n# to serve as a reliable signature. Port 123 is the canonical identifier.\n\nObject<main> NTP\n    abbrev \"ntp\"\n    col \"NTP\"\n\n    # ── Byte 0: LI + VN + Mode ──────────────────────────────────────────────────\n    uint8 li_vn_mode \"Flags\" defaults(0x23)\n        bits li_vn_mode li 6 2 \"Leap Indicator\"\n            No Warning = 0\n            Last Minute 61 Seconds = 1\n            Last Minute 59 Seconds = 2\n            Unsynchronized = 3\n        bits li_vn_mode vn 3 3 \"Version Number\"\n        bits li_vn_mode mode 0 3 \"Mode\"\n            Reserved = 0\n            Symmetric Active = 1\n            Symmetric Passive = 2\n            Client = 3\n            Server = 4\n            Broadcast = 5\n            Control = 6\n            Private = 7\n\n    # ── Bytes 1-3: Stratum, Poll, Precision ─────────────────────────────────────\n    # poll and precision are signed log2 values; stored as uint8 (two's-complement\n    # interpretation applies: e.g. precision 0xF6 = -10 means ~1 ms resolution).\n    uint8 stratum \"Stratum\" defaults(0)\n        Kiss of Death = 0\n        Primary Reference = 1\n        Stratum 2 = 2\n        Stratum 3 = 3\n        Stratum 4 = 4\n        Stratum 5 = 5\n        Stratum 6 = 6\n        Stratum 7 = 7\n        Stratum 8 = 8\n        Stratum 9 = 9\n        Stratum 10 = 10\n        Stratum 11 = 11\n        Stratum 12 = 12\n        Stratum 13 = 13\n        Stratum 14 = 14\n        Stratum 15 = 15\n    uint8 poll \"Poll Interval (log2 seconds)\" defaults(6)\n    uint8 precision \"Clock Precision (log2 seconds)\" defaults(0)\n\n    # ── Bytes 4-11: Root delay and dispersion (NTP short format) ─────────────────\n    uint32 root_delay \"Root Delay\" defaults(0)\n    uint32 root_dispersion \"Root Dispersion\" defaults(0)\n\n    # ── Bytes 12-15: Reference ID ─────────────────────────────────────────────────\n    bytes<4> ref_id \"Reference ID\"\n\n    # ── Bytes 16-47: Timestamps (NTP 64-bit format) ───────────────────────────────\n    bytes<8> ref_timestamp  \"Reference Timestamp\"\n    bytes<8> orig_timestamp \"Originate Timestamp (T1)\"\n    bytes<8> recv_timestamp \"Receive Timestamp (T2)\"\n    bytes<8> xmit_timestamp \"Transmit Timestamp (T3)\"\n\n    # ── Bytes 48+: Optional extension fields and MAC ──────────────────────────────\n    # Extension fields follow (RFC 7822): each is a 16-bit type, 16-bit length,\n    # then length bytes of value. A MAC appended after extensions is 4 bytes of\n    # key ID followed by 16 bytes (MD5) or 20 bytes (SHA-1) of digest.\n    # These are carried through undecoded until a dedicated extension decoder\n    # is added.\n    payload extensions_mac \"Extensions and MAC\"\n\n    info \"v%u %s %s\" vn, mode, stratum\n\n# ── Port binding ────────────────────────────────────────────────────────────────\n# Content detection is omitted (see header comment). Port 123 is the only rule.\nrule udp.port == 123 => NTP\n\n# ── Coloring ────────────────────────────────────────────────────────────────────\n# Kiss-of-Death packets (stratum 0): server is refusing or reporting an error.\ncolor ntp.stratum == 0                    => white red\n# Primary reference server (stratum 1): directly connected to a reference clock.\ncolor ntp.stratum == 1 && ntp.mode == 4  => black lightgreen\n# Server response (other strata): secondary synchronisation in progress.\ncolor ntp.mode == 4                       => black lightblue\n# Client request: outbound poll.\ncolor ntp.mode == 3                       => black lightyellow\n# Broadcast, symmetric, control, and private modes — catch-all.\ncolor ntp                                 => white darkblue\n\n# ── Wireshark-compatible field aliases ──────────────────────────────────────────\n# Wireshark's ntp dissector surfaces the byte-0 sub-fields under ntp.flags.*;\n# map those paths to the bits fields defined here so saved display filters work.\nalias ntp.flags.li   => ntp.li\nalias ntp.flags.vn   => ntp.vn\nalias ntp.flags.mode => ntp.mode\nalias ntp.stratum    => ntp.stratum\n",
    /* openflow.posa */
    "# OpenFlow header (OpenFlow 1.3, ONF spec) — 8 bytes\n# TCP port 6633 (legacy) or 6653 (IANA). xid is the transaction ID.\nObject<main> OpenFlow\n    uint8  version defaults(4)\n        OF10 = 1\n        OF13 = 4\n        OF14 = 5\n        OF15 = 6\n    uint8  type defaults(0)\n        HELLO             = 0\n        ERROR             = 1\n        ECHO_REQUEST      = 2\n        ECHO_REPLY        = 3\n        FEATURES_REQUEST  = 5\n        FEATURES_REPLY    = 6\n        GET_CONFIG_REQUEST = 7\n        GET_CONFIG_REPLY  = 8\n        SET_CONFIG        = 9\n        PACKET_IN         = 10\n        FLOW_REMOVED      = 11\n        PORT_STATUS       = 12\n        PACKET_OUT        = 13\n        FLOW_MOD          = 14\n        GROUP_MOD         = 15\n        PORT_MOD          = 16\n        TABLE_MOD         = 17\n        MULTIPART_REQUEST = 18\n        MULTIPART_REPLY   = 19\n        BARRIER_REQUEST   = 20\n        BARRIER_REPLY     = 21\n        METER_MOD         = 29\n    uint16 length defaults(8)\n    uint32 xid defaults(0)\n",
    /* openvpn.posa */
    "# openvpn.posa — OpenVPN packet decoder (RFC-compatible packet format, OpenVPN 2.x)\n#\n# OpenVPN runs over UDP (typically port 1194) or TCP (port 1194 or 443).\n# UDP packets start with a single opcode/key-id byte. TCP packets are framed with a\n# 2-byte big-endian length prefix before the same opcode byte.\n#\n# First byte layout:  bits 7-3 = opcode (5 bits), bits 2-0 = key_id (3 bits)\n#\n# Reference: OpenVPN 2.x protocol documentation and source (ssl_pkt.h)\n\nObject<main> OpenVPN\n    abbrev \"openvpn\"\n    col \"OpenVPN\"\n\n    uint8 opcode_keyid [\"Opcode/Key ID\"] defaults(0x38)\n\n    bits opcode_keyid opcode 3 5 [\"Opcode\"]\n        1  = P_CONTROL_HARD_RESET_CLIENT_V1\n        2  = P_CONTROL_HARD_RESET_SERVER_V1\n        3  = P_CONTROL_SOFT_RESET_V1\n        4  = P_CONTROL_V1\n        5  = P_ACK_V1\n        6  = P_DATA_V1\n        7  = P_CONTROL_HARD_RESET_CLIENT_V2\n        8  = P_CONTROL_HARD_RESET_SERVER_V2\n        9  = P_DATA_V2\n        10 = P_CONTROL_HARD_RESET_CLIENT_V3\n        11 = P_CONTROL_WKC_V1\n\n    bits opcode_keyid key_id 0 3 [\"Key ID\"]\n\n    bytes<8> session_id [\"Session ID\"]\n\n    payload data [\"Payload\"]\n\n    info \"%s key_id=%u\" opcode, key_id\n\n# ── content detection (primary — port-independent) ────────────────────────────\n# Detect by the opcode byte at offset 0. The most common initial handshake packet\n# (P_CONTROL_HARD_RESET_CLIENT_V2) has opcode/key_id byte 0x38 (key_id=0).\n# P_CONTROL_HARD_RESET_SERVER_V2 = 0x40, P_CONTROL_HARD_RESET_CLIENT_V1 = 0x08.\n# TCP framing: first 2 bytes = uint16 packet length, then opcode — 0x00 0x1c 0x38\n# matches a 28-byte reset packet (the minimum control-packet size).\nweak rule udp.content \"\\x38\" => OpenVPN\nweak rule udp.content \"\\x40\" => OpenVPN\nweak rule udp.content \"\\x08\" => OpenVPN\nrule tcp.content \"\\x00\\x1c\\x38\" => OpenVPN\n\n# ── port binding (secondary hints) ───────────────────────────────────────────\nrule udp.port == 1194 => OpenVPN\nrule tcp.port == 1194 => OpenVPN\nrule tcp.port == 443  => OpenVPN\n\n# ── coloring ──────────────────────────────────────────────────────────────────\n# Handshake / reset packets (connection setup — opcodes 1-3, 7-8, 10-11): yellow\ncolor openvpn.opcode == 7 || openvpn.opcode == 8 || openvpn.opcode == 1 || openvpn.opcode == 2 || openvpn.opcode == 3 || openvpn.opcode == 10 || openvpn.opcode == 11 => black lightyellow\n# Encrypted tunnel data (P_DATA_V1 / P_DATA_V2): dark blue\ncolor openvpn.opcode == 6 || openvpn.opcode == 9 => white darkblue\n# ACK-only packets: light gray (low visual weight, administrative)\ncolor openvpn.opcode == 5 => black lightgray\n# Control / TLS-record packets (P_CONTROL_V1): light green\ncolor openvpn.opcode == 4 => black lightgreen\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# The Wireshark OpenVPN dissector plugin uses openvpn.type, openvpn.keyid,\n# and openvpn.sessid; map those to the field names used above so display filters\n# written for Wireshark work unchanged here.\nalias openvpn.type   => openvpn.opcode\nalias openvpn.keyid  => openvpn.key_id\nalias openvpn.sessid => openvpn.session_id\n",
    /* openwire.posa */
    "# openwire.posa — OpenWire (ActiveMQ native protocol)\n# TCP 61616. Reference: Apache ActiveMQ OpenWire Protocol spec.\n#\n# OpenWire uses a length-prefixed binary framing. The first packet from the\n# broker is a WIREFORMAT_INFO command (type 1) that negotiates protocol options.\n# TCP content \"ActiveMQ\" (as part of the WIREFORMAT_INFO payload identifying the\n# broker) is the most reliable detection anchor.\n#\n# Frame header:\n#   4  data_length   — big-endian, length of the remaining frame data\n#   1  data_type     — command type\n#   4  command_id\n#   1  response_required (boolean)\n#   4  correlation_id\n\nLookup OpenWireCommandTypes\n    1   = \"WIREFORMAT_INFO\"\n    2   = \"BROKER_INFO\"\n    3   = \"CONNECTION_INFO\"\n    4   = \"SESSION_INFO\"\n    5   = \"PRODUCER_INFO\"\n    6   = \"CONSUMER_INFO\"\n    7   = \"CONNECTION_ERROR\"\n    8   = \"CONSUMER_CONTROL\"\n    9   = \"CIRCUIT_BREAKER_INFO\"\n    14  = \"REMOVE_INFO\"\n    15  = \"CONTROL_COMMAND\"\n    20  = \"FLUSH_COMMAND\"\n    21  = \"KEEP_ALIVE_INFO\"\n    22  = \"SHUTDOWN_INFO\"\n    23  = \"REMOVE_SUBSCRIPTION_INFO\"\n    24  = \"COMMAND_NULL\"\n    25  = \"CONSUMER_DISPATCH\"\n    26  = \"FLUSH_COMMAND\"\n    28  = \"RESPONSE\"\n    29  = \"EXCEPTION_RESPONSE\"\n    30  = \"DATA_RESPONSE\"\n    31  = \"DATA_ARRAY_RESPONSE\"\n    32  = \"INT_RESPONSE\"\n    33  = \"STRING_RESPONSE\"\n    34  = \"BROKER_SUBSCRIPTIONS\"\n    40  = \"MESSAGE_DISPATCH\"\n    41  = \"MESSAGE_ACK\"\n    42  = \"PRODUCER_ACK\"\n    43  = \"MESSAGE_PULL\"\n    61  = \"XACOMMIT\"\n    62  = \"XAPREPARE\"\n    63  = \"XAROLLBACK\"\n    72  = \"ACTIVEMQ_MESSAGE\"\n    73  = \"ACTIVEMQ_BYTES_MESSAGE\"\n    74  = \"ACTIVEMQ_MAP_MESSAGE\"\n    75  = \"ACTIVEMQ_OBJECT_MESSAGE\"\n    76  = \"ACTIVEMQ_STREAM_MESSAGE\"\n    77  = \"ACTIVEMQ_TEXT_MESSAGE\"\n    80  = \"ACTIVEMQ_BLOB_MESSAGE\"\n    90  = \"TRANSACTION_INFO\"\n    91  = \"DESTINATION_INFO\"\n    92  = \"REMOVE_SUBSCRIPTION_INFO\"\n    93  = \"KEEP_ALIVE_INFO\"\n    94  = \"CONTROL_COMMAND\"\n    95  = \"FLUSH_COMMAND\"\n    96  = \"CONNECTION_CONTROL\"\n    110 = \"JOURNAL_QUEUE_ACK\"\n    111 = \"JOURNAL_TRACE\"\n    112 = \"JOURNAL_TRANSACTION\"\n    113 = \"DURABLE_SUBSCRIPTION_INFO\"\n    114 = \"PARTIAL_COMMAND\"\n    115 = \"PARTIAL_LAST_COMMAND\"\n    116 = \"REPLAY\"\n\nObject<main> OpenWire\n    abbrev \"openwire\"\n    col \"OpenWire\"\n\n    uint32 data_length \"Data Length\" defaults(0)\n    uint8  data_type lookup OpenWireCommandTypes \"Command Type\" defaults(1)\n    uint32 command_id \"Command ID\" defaults(0)\n    uint8  response_required \"Response Required\" defaults(0)\n        False = 0\n        True  = 1\n    uint32 correlation_id \"Correlation ID\" defaults(0)\n\n    payload body \"Command Body\"\n\n    info \"OpenWire %s id=%u\" data_type, command_id\n\n\nrule tcp.port == 61616 => OpenWire\nrule tcp.content \"ActiveMQ\" => OpenWire\n\ncolor openwire.data_type == 7  => white red\ncolor openwire.data_type == 1  => black lightyellow\ncolor openwire.data_type == 28 => black lightgreen\ncolor openwire                 => white darkblue\n",
    /* ospf.posa */
    "# ospf.posa — Open Shortest Path First (RFC 2328 OSPFv2, RFC 5340 OSPFv3)\n#\n# OSPF is carried directly over IP as protocol 89 — there is no TCP or UDP\n# wrapping. Packets are sent to two well-known multicast groups:\n#   224.0.0.5  AllSPFRouters  — all OSPF-speaking routers on the segment\n#   224.0.0.6  AllDRouters    — only the Designated Router and Backup DR\n#\n# Common header layout (bytes 0-13, identical in both versions):\n#   [0]     version        1 byte  — 2=OSPFv2, 3=OSPFv3\n#   [1]     type           1 byte  — 1=Hello 2=DBD 3=LSR 4=LSU 5=LSACK\n#   [2-3]   packet_length  2 bytes — total length of this OSPF packet\n#   [4-7]   router_id      4 bytes — originating router (dotted-quad form)\n#   [8-11]  area_id        4 bytes — OSPF area (0.0.0.0 = backbone)\n#   [12-13] checksum       2 bytes — standard IP checksum over the OSPF packet\n#\n# Bytes 14-23 diverge by version:\n#   OSPFv2: au_type(2) + authentication(8)  — 10 bytes\n#   OSPFv3: instance_id(1) + reserved(1)    —  2 bytes; IPsec handles security\n#\n# Content detection uses the first two bytes: version (0x02 or 0x03) followed\n# immediately by the packet type (0x01-0x05). This combination is unique enough\n# to identify OSPF without relying on the IP protocol field, enabling detection\n# in tunnels or captures where the IP layer is unavailable.\n#\n# LSA common header (20 bytes, appears in DBD summaries, LSU bodies, LSACK):\n#   OSPFv2: age(2) + options(1) + type(1) + ls_id(4) + adv_router(4) + seq(4) + cksum(2) + len(2)\n#   OSPFv3: age(2) + type(2)              + ls_id(4) + adv_router(4) + seq(4) + cksum(2) + len(2)\n#   LSU uses `scope lsa_length - 20` to bound each LSA body so that the repeat\n#   cursor advances correctly regardless of LSA type.\n\nObject<main> OSPF\n    abbrev \"ospf\"\n    col \"OSPF\"\n\n    # ── Common header (bytes 0-13) ─────────────────────────────────────────\n    uint8 version \"Version\" defaults(2)\n        2 = \"OSPFv2\"\n        3 = \"OSPFv3\"\n\n    uint8 type \"Packet Type\" defaults(1)\n        1 = \"Hello\"\n        2 = \"Database Description\"\n        3 = \"Link State Request\"\n        4 = \"Link State Update\"\n        5 = \"LS Acknowledgment\"\n\n    uint16 packet_length \"Packet Length\" defaults(44)\n    ip4    router_id     \"Router ID\"\n    ip4    area_id       \"Area ID\"\n    uint16 checksum      \"Checksum\"      defaults(0)\n\n    # ── Bytes 14-23: version-specific ─────────────────────────────────────\n    when version == 2:\n        uint16   au_type \"Auth Type\" defaults(0)\n            0 = \"No Authentication\"\n            1 = \"Simple Password\"\n            2 = \"Cryptographic (MD5)\"\n        bytes<8> authentication \"Authentication Data\"\n\n    when version == 3:\n        uint8 instance_id \"Instance ID\" defaults(0)\n        uint8 v3_reserved \"Reserved\" defaults(0)\n        # OSPFv3 relies on IPsec (AH/ESP) for authentication and encryption;\n        # there is no per-packet authentication field in the OSPF header.\n\n    # ── Type 1: Hello — neighbour discovery and keepalive ─────────────────\n    # OSPFv2 and OSPFv3 Hello packets differ after the common header.\n    # OSPFv2 leads with the subnet mask of the outgoing interface.\n    # OSPFv3 leads with the outgoing interface ID and has a 3-byte options\n    # field; hello and dead intervals are 16-bit (not 32-bit as in OSPFv2).\n    when type == 1:\n        when version == 2:\n            ip4    network_mask   \"Network Mask\"\n            uint16 hello_interval \"Hello Interval (s)\" defaults(10)\n            uint8  options        \"Options\"            defaults(0x02)\n                bits options e_bit  1 1 \"E: External Routing Capability\"\n                bits options mc_bit 2 1 \"MC: Multicast Capability\"\n                bits options np_bit 3 1 \"N/P: NSSA Capability\"\n                bits options ea_bit 4 1 \"EA: External Attributes LSA\"\n                bits options dc_bit 5 1 \"DC: Demand Circuits\"\n                bits options o_bit  6 1 \"O: Opaque LSA Support\"\n            uint8  router_priority   \"Router Priority\"   defaults(1)\n            uint32 dead_interval     \"Dead Interval (s)\" defaults(40)\n            ip4    designated_router \"Designated Router\"\n            ip4    backup_dr         \"Backup Designated Router\"\n            repeat until end as neighbor \"Neighbours\"\n                label \"%s\" neighbor_addr\n                ip4 neighbor_addr \"Neighbour Address\"\n\n        when version == 3:\n            uint32 hello_interface_id \"Interface ID\" defaults(0)\n            uint8  router_priority \"Router Priority\" defaults(1)\n            uint24 v3_options \"Options\" defaults(0x43)\n                bits v3_options v3_v6_bit 0 1 \"V6: IPv6 Forwarding\"\n                bits v3_options v3_e_bit  1 1 \"E: External Routing Capability\"\n                bits v3_options v3_mc_bit 2 1 \"MC: Multicast Capability\"\n                bits v3_options v3_np_bit 3 1 \"N/P: NSSA Capability\"\n                bits v3_options v3_dc_bit 5 1 \"DC: Demand Circuits\"\n                bits v3_options v3_r_bit  6 1 \"R: Router Active\"\n            uint16 hello_interval    \"Hello Interval (s)\" defaults(10)\n            uint16 dead_interval     \"Dead Interval (s)\"  defaults(40)\n            ip4    designated_router \"Designated Router ID\"\n            ip4    backup_dr         \"Backup Designated Router ID\"\n            repeat until end as neighbor \"Neighbours\"\n                label \"%s\" neighbor_addr\n                ip4 neighbor_addr \"Neighbour Router ID\"\n\n    # ── Type 2: Database Description (DBD) — topology exchange ────────────\n    # Each DBD carries zero or more 20-byte LSA headers (no bodies) that let\n    # the receiver determine which LSAs it needs to request.\n    # OSPFv2 layout: MTU(2) + Options(1) + Flags(1) + Sequence(4) + LSA headers\n    # OSPFv3 layout: Reserved(1) + Options(3) + MTU(2) + Reserved(1) + Flags(1) + Sequence(4) + LSA headers\n    when type == 2:\n        when version == 2:\n            uint16 dbd_mtu \"Interface MTU\" defaults(1500)\n            uint8  dbd_options \"Options\" defaults(0x02)\n            uint8  dbd_flags \"DBD Flags\" defaults(0x07)\n                bits dbd_flags dbd_i  0 1 \"I: Initial (first DBD in exchange)\"\n                bits dbd_flags dbd_m  1 1 \"M: More DBDs to follow\"\n                bits dbd_flags dbd_ms 2 1 \"MS: This router is Master\"\n            uint32 dbd_sequence \"DD Sequence Number\" defaults(1)\n            repeat until end as dbd_lsa \"LSA Headers\"\n                label \"%s\" dbd_lsa_id\n                uint16 dbd_lsa_age \"LS Age\" defaults(0)\n                uint8  dbd_lsa_options \"LS Options\" defaults(0x02)\n                uint8  dbd_lsa_type \"LS Type\" defaults(1)\n                    1  = \"Router-LSA\"\n                    2  = \"Network-LSA\"\n                    3  = \"Summary-LSA (IP network)\"\n                    4  = \"Summary-LSA (ASBR)\"\n                    5  = \"AS-External-LSA\"\n                    7  = \"NSSA-AS-External-LSA\"\n                    9  = \"Opaque-LSA (link-local scope)\"\n                    10 = \"Opaque-LSA (area scope)\"\n                    11 = \"Opaque-LSA (AS scope)\"\n                ip4    dbd_lsa_id         \"Link State ID\"\n                ip4    dbd_lsa_adv_router \"Advertising Router\"\n                uint32 dbd_lsa_seq        \"LS Sequence Number\" defaults(0x80000001)\n                uint16 dbd_lsa_checksum   \"LS Checksum\"        defaults(0)\n                uint16 dbd_lsa_length     \"LS Length\"          defaults(20)\n\n        when version == 3:\n            uint8  v3_dbd_reserved \"Reserved\" defaults(0)\n            uint24 v3_dbd_options \"Options\" defaults(0x43)\n            uint16 dbd_mtu \"Interface MTU\" defaults(1500)\n            uint8  v3_dbd_reserved2 \"Reserved\" defaults(0)\n            uint8  dbd_flags \"DBD Flags\" defaults(0x07)\n                bits dbd_flags dbd_i  0 1 \"I: Initial (first DBD in exchange)\"\n                bits dbd_flags dbd_m  1 1 \"M: More DBDs to follow\"\n                bits dbd_flags dbd_ms 2 1 \"MS: This router is Master\"\n            uint32 dbd_sequence \"DD Sequence Number\" defaults(1)\n            repeat until end as dbd_lsa \"LSA Headers\"\n                label \"%s\" dbd_lsa_id\n                uint16 dbd_lsa_age \"LS Age\" defaults(0)\n                uint16 dbd_lsa_type \"LS Type\" defaults(1)\n                    0x2001 = \"Router-LSA\"\n                    0x2002 = \"Network-LSA\"\n                    0x2003 = \"Inter-Area-Prefix-LSA\"\n                    0x2004 = \"Inter-Area-Router-LSA\"\n                    0x4005 = \"AS-External-LSA\"\n                    0x2007 = \"NSSA-LSA\"\n                    0x0008 = \"Link-LSA\"\n                    0x2009 = \"Intra-Area-Prefix-LSA\"\n                ip4    dbd_lsa_id         \"Link State ID\"\n                ip4    dbd_lsa_adv_router \"Advertising Router\"\n                uint32 dbd_lsa_seq        \"LS Sequence Number\" defaults(0x80000001)\n                uint16 dbd_lsa_checksum   \"LS Checksum\"        defaults(0)\n                uint16 dbd_lsa_length     \"LS Length\"          defaults(20)\n\n    # ── Type 3: Link State Request — soliciting LSAs from a neighbour ──────\n    # Each 12-byte entry identifies one LSA the sender wants to receive.\n    # OSPFv2 encodes the type as a bare uint32; OSPFv3 uses reserved(16) +\n    # type(16) for the same total width, so the uint32 field works for both.\n    when type == 3:\n        repeat until end as lsr \"Link State Requests\"\n            label \"%s\" lsr_id\n            uint32 lsr_type       \"LS Type\" defaults(1)\n            ip4    lsr_id         \"Link State ID\"\n            ip4    lsr_adv_router \"Advertising Router\"\n\n    # ── Type 4: Link State Update — distributing LSAs ──────────────────────\n    # A single LSU packet can carry multiple complete LSAs. Each LSA begins\n    # with a 20-byte common header containing the lsa_length field, which\n    # covers both header and body. `scope lsu_lsa_length - 20` bounds the\n    # body payload so the repeat cursor advances by exactly lsu_lsa_length\n    # bytes per iteration regardless of LSA type.\n    when type == 4:\n        uint32 num_lsas \"Number of LSAs\" defaults(1)\n        repeat num_lsas as lsu_lsa \"LSAs\"\n            label \"%s\" lsu_lsa_id\n            uint16 lsu_lsa_age \"LS Age\" defaults(0)\n            when version == 2:\n                uint8 lsu_lsa_options \"LS Options\" defaults(0x02)\n                uint8 lsu_lsa_type \"LS Type\" defaults(1)\n                    1  = \"Router-LSA\"\n                    2  = \"Network-LSA\"\n                    3  = \"Summary-LSA (IP network)\"\n                    4  = \"Summary-LSA (ASBR)\"\n                    5  = \"AS-External-LSA\"\n                    7  = \"NSSA-AS-External-LSA\"\n                    9  = \"Opaque-LSA (link-local scope)\"\n                    10 = \"Opaque-LSA (area scope)\"\n                    11 = \"Opaque-LSA (AS scope)\"\n            when version == 3:\n                uint16 lsu_lsa_type \"LS Type\" defaults(1)\n                    0x2001 = \"Router-LSA\"\n                    0x2002 = \"Network-LSA\"\n                    0x2003 = \"Inter-Area-Prefix-LSA\"\n                    0x2004 = \"Inter-Area-Router-LSA\"\n                    0x4005 = \"AS-External-LSA\"\n                    0x2007 = \"NSSA-LSA\"\n                    0x0008 = \"Link-LSA\"\n                    0x2009 = \"Intra-Area-Prefix-LSA\"\n            ip4    lsu_lsa_id         \"Link State ID\"\n            ip4    lsu_lsa_adv_router \"Advertising Router\"\n            uint32 lsu_lsa_seq        \"LS Sequence Number\" defaults(0x80000001)\n            uint16 lsu_lsa_checksum   \"LS Checksum\"        defaults(0)\n            uint16 lsu_lsa_length     \"LS Length\"          defaults(20)\n            scope lsu_lsa_length - 20\n                payload lsu_lsa_body \"LSA Body\"\n\n    # ── Type 5: LS Acknowledgment — confirming receipt of LSAs ────────────\n    # An LSACK carries one or more bare 20-byte LSA common headers with no\n    # body. The layout matches the DBD LSA header entries exactly.\n    # OSPFv2: age(2) + options(1) + type(1) + id(4) + adv(4) + seq(4) + cksum(2) + len(2)\n    # OSPFv3: age(2) + type(2)              + id(4) + adv(4) + seq(4) + cksum(2) + len(2)\n    when type == 5:\n        repeat until end as lsack \"Acknowledged LSAs\"\n            label \"%s\" lsack_lsa_id\n            uint16 lsack_lsa_age \"LS Age\" defaults(0)\n            when version == 2:\n                uint8 lsack_lsa_options \"LS Options\" defaults(0x02)\n                uint8 lsack_lsa_type \"LS Type\" defaults(1)\n                    1  = \"Router-LSA\"\n                    2  = \"Network-LSA\"\n                    3  = \"Summary-LSA (IP network)\"\n                    4  = \"Summary-LSA (ASBR)\"\n                    5  = \"AS-External-LSA\"\n                    7  = \"NSSA-AS-External-LSA\"\n                    9  = \"Opaque-LSA (link-local scope)\"\n                    10 = \"Opaque-LSA (area scope)\"\n                    11 = \"Opaque-LSA (AS scope)\"\n            when version == 3:\n                uint16 lsack_lsa_type \"LS Type\" defaults(1)\n                    0x2001 = \"Router-LSA\"\n                    0x2002 = \"Network-LSA\"\n                    0x2003 = \"Inter-Area-Prefix-LSA\"\n                    0x2004 = \"Inter-Area-Router-LSA\"\n                    0x4005 = \"AS-External-LSA\"\n                    0x2007 = \"NSSA-LSA\"\n                    0x0008 = \"Link-LSA\"\n                    0x2009 = \"Intra-Area-Prefix-LSA\"\n            ip4    lsack_lsa_id         \"Link State ID\"\n            ip4    lsack_lsa_adv_router \"Advertising Router\"\n            uint32 lsack_lsa_seq        \"LS Sequence Number\" defaults(0x80000001)\n            uint16 lsack_lsa_checksum   \"LS Checksum\"        defaults(0)\n            uint16 lsack_lsa_length     \"LS Length\"          defaults(20)\n\n    info \"%s  Router=%s  Area=%s\" type, router_id, area_id\n\n\n# ── Content detection (primary — works independent of the IP layer) ───────────\n# The first two bytes of every OSPF packet are the version (0x02 or 0x03) and\n# the packet type (0x01-0x05). This combination does not appear in unrelated\n# traffic and allows OSPF to be recognised inside tunnels or raw captures.\n#\n# OSPFv2 (version byte 0x02):\nweak rule content \"\\x02\\x01\" => OSPF\nweak rule content \"\\x02\\x02\" => OSPF\nweak rule content \"\\x02\\x03\" => OSPF\nweak rule content \"\\x02\\x04\" => OSPF\nweak rule content \"\\x02\\x05\" => OSPF\n# OSPFv3 (version byte 0x03):\nweak rule content \"\\x03\\x01\" => OSPF\nweak rule content \"\\x03\\x02\" => OSPF\nweak rule content \"\\x03\\x03\" => OSPF\nweak rule content \"\\x03\\x04\" => OSPF\nweak rule content \"\\x03\\x05\" => OSPF\n\n# ── Protocol binding (secondary — canonical IP protocol number) ───────────────\n# Note: ip.proto == 89 is also declared in 00-builtin-rules.posa for the\n# built-in C dissector; keeping it here makes this .posa file self-contained.\nrule ip.proto == 89 => OSPF\n\n\n# ── Coloring rules ────────────────────────────────────────────────────────────\n# Hello packets: neighbour discovery and DR/BDR election keepalives — green\ncolor ospf.type == 1 => black lightgreen\n\n# Database Description: topology synchronisation exchange — yellow\ncolor ospf.type == 2 => black lightyellow\n\n# Link State Request and Link State Update: active topology distribution — blue\ncolor ospf.type == 3 || ospf.type == 4 => white darkblue\n\n# LS Acknowledgment: routine low-value ack traffic — gray\ncolor ospf.type == 5 => black lightgray\n\n# Unrecognised version byte (not 2 or 3): probable corruption or future version\ncolor ospf.version != 2 && ospf.version != 3 => white red\n\n# Protocol catch-all — any OSPF packet not matched by the rules above\ncolor ospf => white darkgray\n\n\n# ── Wireshark-compatible field aliases ────────────────────────────────────────\n# Wireshark's ospf dissector surfaces fields under paths that differ from the\n# names used here. These aliases allow existing display-filter profiles,\n# coloring rules, and io-stats expressions to keep working without modification.\n\n# Common header\nalias ospf.ospf_version                   => ospf.version\nalias ospf.msg.type                       => ospf.type\nalias ospf.packet_length                  => ospf.packet_length\nalias ospf.srcrouter                      => ospf.router_id\nalias ospf.area_id                        => ospf.area_id\nalias ospf.checksum                       => ospf.checksum\nalias ospf.auth.type                      => ospf.au_type\n\n# Hello — OSPFv2\nalias ospf.hello.network_mask             => ospf.network_mask\nalias ospf.hello.hello_interval           => ospf.hello_interval\nalias ospf.hello.options                  => ospf.options\nalias ospf.hello.router_priority          => ospf.router_priority\nalias ospf.hello.dead_interval            => ospf.dead_interval\nalias ospf.hello.designated_router        => ospf.designated_router\nalias ospf.hello.backup_designated_router => ospf.backup_dr\nalias ospf.hello.active_neighbor          => ospf.neighbor_addr\n\n# Hello — OSPFv3\nalias ospf.v3.hello.interface_id          => ospf.hello_interface_id\nalias ospf.v3.instance_id                 => ospf.instance_id\n\n# Database Description\nalias ospf.dbd.interface_mtu              => ospf.dbd_mtu\nalias ospf.dbd.db_desc_options            => ospf.dbd_options\nalias ospf.dbd.db_desc_flags              => ospf.dbd_flags\nalias ospf.dbd.db_desc_sequence           => ospf.dbd_sequence\n\n# LSA headers (aliases reference the LSU field names as the canonical set;\n# DBD and LSACK use the same header layout under different field-name prefixes)\nalias ospf.lsa.age                        => ospf.lsu_lsa_age\nalias ospf.lsa.type                       => ospf.lsu_lsa_type\nalias ospf.lsa.options                    => ospf.lsu_lsa_options\nalias ospf.lsa.id                         => ospf.lsu_lsa_id\nalias ospf.lsa.advertising_router         => ospf.lsu_lsa_adv_router\nalias ospf.lsa.seqnum                     => ospf.lsu_lsa_seq\nalias ospf.lsa.chksum                     => ospf.lsu_lsa_checksum\nalias ospf.lsa.length                     => ospf.lsu_lsa_length\n\n# LSR\nalias ospf.v2.lsr.ls_type                 => ospf.lsr_type\nalias ospf.v2.lsr.ls_id                   => ospf.lsr_id\nalias ospf.v2.lsr.adv_router              => ospf.lsr_adv_router\n",
    /* pim.posa */
    "# PIM base header (RFC 7761, PIM-SM) — 4 bytes\n# IP protocol 103, multicast 224.0.0.13. ver_type: ver(4)=2 | type(4).\n# Type-specific fields follow; HELLO carries option TLVs, JOIN_PRUNE carries group records.\nObject<main> PIM\n    uint8  ver_type defaults(0x20)\n        HELLO         = 0x20\n        REGISTER      = 0x21\n        REGISTER_STOP = 0x22\n        JOIN_PRUNE    = 0x23\n        BOOTSTRAP     = 0x24\n        ASSERT        = 0x25\n        GRAFT         = 0x26\n        GRAFT_ACK     = 0x27\n        CANDIDATE_RP  = 0x28\n        STATE_REFRESH = 0x29\n        DF_ELECTION   = 0x2A\n    uint8  reserved defaults(0)\n    uint16 checksum defaults(0)\n",
    /* pop3.posa */
    "# pop3.posa — Post Office Protocol version 3  (RFC 1939)\n#\n# POP3 is a line-based, plaintext mail retrieval protocol.  Each PDU is a single\n# CR LF-terminated ASCII line.  Two parties share one TCP connection:\n#\n#   Server → Client  \"+OK [message]\\r\\n\"    — positive response\n#                    \"-ERR [reason]\\r\\n\"    — negative response\n#   Client → Server  \"COMMAND [arg]\\r\\n\"   — command (argument optional)\n#\n# Multi-line data (RETR, LIST, UIDL, TOP, CAPA responses) is terminated by a\n# lone \".\" on its own line (\".\\r\\n\").  The decoder handles the first line of each\n# exchange; the continuation data appears as a payload field.\n#\n# Greeting: the very first bytes from the server are always \"+OK\", which makes an\n# offset-0 content rule reliable without needing port 110.  \"USER \" and \"PASS \"\n# catch client-side traffic that arrives before any server response is seen.\n#\n# TCP port 110 — plain-text POP3.\n# TCP port 995 — POP3S (implicit TLS); the POP3 handshake runs inside TLS, so\n#                content rules will not fire on 995; the port rule is the primary\n#                hint there.\n\nObject<main> POP3\n    abbrev \"pop3\"\n    col \"POP3\"\n\n    # First token: \"+OK\", \"-ERR\", or an uppercase command keyword such as \"USER\".\n    # \"string until\" stops at the first space; for argument-free commands (STAT,\n    # NOOP, RSET, QUIT, CAPA, STLS) it stops at \\r and the CRLF is left in the\n    # buffer to be consumed by whichever \"until \\r\\n\" field follows.\n    string keyword until \" \" [\"Command/Status\"] defaults(\"+OK\")\n\n    # ── Server responses ──────────────────────────────────────────────────────\n    when keyword == \"+OK\":\n        string response until \"\\r\\n\" [\"Server Response\"] defaults(\"POP3 server ready\")\n        payload response_body        [\"Response Body\"]\n\n    when keyword == \"-ERR\":\n        string error_detail until \"\\r\\n\" [\"Error Detail\"]\n\n    # ── Authentication commands ───────────────────────────────────────────────\n    when keyword == \"USER\":\n        string username until \"\\r\\n\" [\"Username\"]\n\n    when keyword == \"PASS\":\n        string password until \"\\r\\n\" [\"Password\"]\n\n    when keyword == \"AUTH\":\n        string auth_mech until \"\\r\\n\" [\"Auth Mechanism\"]\n\n    # ── Retrieval and listing commands ────────────────────────────────────────\n    when keyword == \"RETR\":\n        string retr_num until \"\\r\\n\" [\"Message Number\"]\n\n    when keyword == \"TOP\":\n        string top_args until \"\\r\\n\" [\"Msg Num / Lines\"]\n\n    when keyword == \"DELE\":\n        string dele_num until \"\\r\\n\" [\"Message Number\"]\n\n    when keyword == \"LIST\":\n        string list_arg until \"\\r\\n\" [\"Message Number\"]\n\n    when keyword == \"UIDL\":\n        string uidl_arg until \"\\r\\n\" [\"Message Number\"]\n\n    # ── Catch-all: STAT NOOP RSET QUIT CAPA STLS and any unrecognised line ───\n    else:\n        string argument until \"\\r\\n\" [\"Argument\"]\n\n    # Info column: keyword then whichever detail field is populated.\n    # Only one conditional field is set per line; missing fields shrink to\n    # nothing, so the result is always \"KEYWORD detail\" with no extra spaces.\n    info \"%s %s%s%s%s%s%s%s%s%s%s\" keyword, response, error_detail, \\\n         username, password, auth_mech, retr_num, top_args, dele_num, \\\n         list_arg, argument\n\n# ── Content detection rules (primary — port-independent) ─────────────────────\n# Anchored at offset 0: the server greeting \"+OK\" is always the first payload\n# byte of a new connection, making this a low-false-positive rule.\nrule tcp.content@0 \"+OK\"  => POP3\n# Non-anchored fallbacks cover mid-session captures and client-originated lines.\nrule tcp.content \"+OK\"    => POP3\nrule tcp.content \"-ERR\"   => POP3\nrule tcp.content \"USER \"  => POP3\nrule tcp.content \"PASS \"  => POP3\n\n# ── Port bindings (secondary) ─────────────────────────────────────────────────\nrule tcp.port == 110 => POP3\nrule tcp.port == 995 => POP3\n\n# ── Coloring rules (highest specificity first) ────────────────────────────────\n# Server errors — white on red, maximum visibility.\ncolor pop3.keyword == \"-ERR\"                                            => white red\n\n# Positive server responses — black on light green.\ncolor pop3.keyword == \"+OK\"                                             => black lightgreen\n\n# Authentication and TLS upgrade commands — yellow, security-relevant traffic.\ncolor pop3.keyword == \"USER\" || pop3.keyword == \"PASS\" || \\\n      pop3.keyword == \"AUTH\" || pop3.keyword == \"STLS\"                  => black lightyellow\n\n# Message retrieval commands — dark blue (bulk / data-plane direction).\ncolor pop3.keyword == \"RETR\" || pop3.keyword == \"TOP\"                   => white darkblue\n\n# Catch-all for any remaining POP3 frames on standard ports.\ncolor tcp.port == 110 || tcp.port == 995                                => black lightgray\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# Wireshark's built-in \"pop\" dissector uses the field prefix \"pop\", not \"pop3\".\n# These aliases let display filters written for Wireshark continue to work.\nalias pop.response.indicator => pop3.keyword\nalias pop.response           => pop3.response\nalias pop.request.command    => pop3.keyword\nalias pop.request.parameter  => pop3.argument\nalias pop3.command           => pop3.keyword\nalias pop3.status            => pop3.keyword\nalias pop3.response          => pop3.response\n",
    /* postgresql.posa */
    "# postgresql.posa — PostgreSQL Frontend/Backend Protocol v3\n# Reference: https://www.postgresql.org/docs/current/protocol-message-formats.html\n#\n# Two objects in the PostgreSQL group:\n#   Startup  — the initial client message (uint32 length + uint32 version, no type byte)\n#   Message  — all subsequent messages from both sides (uint8 type + uint32 length + body)\n#\n# Detection: the protocol version magic 0x00030000 always sits at byte offset 4 of the\n# startup message, so `tcp.content@4` fires on any port without needing port 5432.\n# The port rule is a secondary hint for sessions where the startup is not captured.\n#\n# The startup length field encodes the total message size including itself (4 bytes), so\n# the body is (startup_length - 8) bytes. The same applies to regular msg_length fields:\n# the body is (msg_length - 4) bytes. Scope is not applied here because POSA scopes are\n# absolute byte counts from the current position, not offsets.\n\n# ── Startup / SSL negotiation ─────────────────────────────────────────────────────────\n# Client sends this first. No type byte precedes it. The length field's high bytes are\n# always 0x00 for any realistic message size, making `starts \"\\x00\\x00\"` a safe\n# discriminator against the regular message format (which starts with a printable ASCII\n# type byte, 0x31–0x74).\n\nObject<PostgreSQL> Startup\n    abbrev \"postgresql\"\n    col \"PGSQL\"\n    starts \"\\x00\\x00\"\n\n    uint32 startup_length \"Message Length\" defaults(0)\n    uint32 protocol \"Protocol Version\" defaults(0x00030000)\n        PostgreSQL v3 = 0x00030000\n        SSL Request = 0x04D2162F\n        GSS Enc Request = 0x04D21630\n\n    # Startup parameters: a sequence of cstring key/value pairs, terminated by \\0.\n    # Only present for the main v3 startup, not for SSL/GSS negotiation messages.\n    when protocol == 0x00030000:\n        repeat until \"\\x00\" as startup_param \"Startup Parameters\"\n            label \"%s=%s\" param_key, param_value\n            cstring param_key \"Parameter Name\"\n            cstring param_value \"Parameter Value\"\n\n    info \"Startup 0x%x\" protocol\n\n\n# ── Regular typed messages (after startup, both directions) ───────────────────────────\n# Every message begins with a single-byte ASCII type tag, then a uint32 length that\n# counts itself but not the type byte. The body fields decoded below are the most common\n# ones; everything else falls through to the payload catch-all.\n\nObject<PostgreSQL> Message\n    abbrev \"postgresql\"\n    col \"PGSQL\"\n\n    uint8 msg_type \"Message Type\" defaults(81)\n        # Server → Client\n        Authentication = 82          # 'R'\n        ParameterStatus = 83         # 'S'  (conflicts with client Sync, server wins here)\n        BackendKeyData = 75          # 'K'\n        ReadyForQuery = 90           # 'Z'\n        RowDescription = 84          # 'T'\n        DataRow = 68                 # 'D'  (conflicts with client Describe)\n        CommandComplete = 67         # 'C'  (conflicts with client Close)\n        ErrorResponse = 69           # 'E'  (conflicts with client Execute)\n        NoticeResponse = 78          # 'N'\n        NotificationResponse = 65    # 'A'\n        EmptyQueryResponse = 73      # 'I'\n        CopyInResponse = 71          # 'G'\n        CopyOutResponse = 72         # 'H'\n        CopyBothResponse = 87        # 'W'\n        FunctionCallResponse = 86    # 'V'\n        ParseComplete = 49           # '1'\n        BindComplete = 50            # '2'\n        CloseComplete = 51           # '3'\n        NoData = 110                 # 'n'\n        PortalSuspended = 115        # 's'\n        ParameterDescription = 116   # 't'\n        # Client → Server (after startup)\n        SimpleQuery = 81             # 'Q'\n        Parse = 80                   # 'P'\n        Bind = 66                    # 'B'\n        Execute = 69                 # 'E'  — same byte as ErrorResponse; server/client context differs\n        Sync = 83                    # 'S'  — same byte as ParameterStatus\n        Flush = 72                   # 'H'  — same byte as CopyOutResponse\n        Terminate = 88               # 'X'\n        PasswordMessage = 112        # 'p'\n        CopyData = 100               # 'd'\n        CopyDone = 99                # 'c'\n        CopyFail = 102               # 'f'\n\n    uint32 msg_length \"Message Length\" defaults(0)\n\n    # ── Authentication request (server → client, msg_type == 'R' == 82) ──────────────\n    when msg_type == 82:\n        uint32 auth_type \"Authentication Type\" defaults(0)\n            Authentication OK = 0\n            Kerberos V5 = 2\n            Cleartext Password = 3\n            MD5 Password = 5\n            SCM Credential = 6\n            GSSAPI = 7\n            GSSAPI Continue = 8\n            SSPI = 9\n            SASL = 10\n            SASL Continue = 11\n            SASL Final = 12\n        payload auth_data \"Authentication Data\"\n\n    # ── ReadyForQuery (server → client, msg_type == 'Z' == 90) ───────────────────────\n    when msg_type == 90:\n        uint8 tx_status \"Transaction Status\" defaults(0)\n            Idle = 73                # 'I'\n            In Transaction = 84     # 'T'\n            In Failed Transaction = 69  # 'E'\n\n    # ── SimpleQuery (client → server, msg_type == 'Q' == 81) ─────────────────────────\n    when msg_type == 81:\n        cstring query \"Query String\" defaults(\"SELECT 1\")\n\n    # ── CommandComplete (server → client, msg_type == 'C' == 67) ─────────────────────\n    when msg_type == 67:\n        cstring command_tag \"Command Tag\"\n\n    # ── BackendKeyData (server → client, msg_type == 'K' == 75) ─────────────────────\n    when msg_type == 75:\n        uint32 backend_pid \"Backend PID\" defaults(0)\n        uint32 secret_key \"Secret Key\" defaults(0)\n\n    # ── ParameterStatus (server → client, msg_type == 'S' == 83) ────────────────────\n    when msg_type == 83:\n        cstring param_name \"Parameter Name\"\n        cstring param_value \"Parameter Value\"\n\n    # ── ErrorResponse (server → client, msg_type == 'E' == 69) ──────────────────────\n    # Fields are (uint8 code, cstring value) pairs terminated by a single 0x00 byte.\n    # `repeat until \"\\x00\"` stops before reading a null field_code, leaving the list\n    # terminator unread (it will be absorbed by the next-message boundary).\n    when msg_type == 69:\n        repeat until \"\\x00\" as error_field \"Error Fields\"\n            label \"%s\" field_value\n            uint8 field_code \"Field Code\" defaults(0)\n                Severity = 83            # 'S'\n                Severity Nonloc = 86     # 'V'\n                SQL State = 67           # 'C'\n                Message = 77             # 'M'\n                Detail = 68              # 'D'\n                Hint = 72                # 'H'\n                Position = 80            # 'P'\n                Internal Position = 112  # 'p'\n                Internal Query = 113     # 'q'\n                Where = 87               # 'W'\n                Schema = 115             # 's'\n                Table = 116              # 't'\n                Column = 99              # 'c'\n                Data Type = 100          # 'd'\n                Constraint = 110         # 'n'\n                File = 70                # 'F'\n                Line = 76                # 'L'\n                Routine = 82             # 'R'\n            cstring field_value \"Field Value\"\n\n    # ── RowDescription (server → client, msg_type == 'T' == 84) ─────────────────────\n    when msg_type == 84:\n        uint16 field_count \"Field Count\" defaults(1)\n        repeat field_count as row_field \"Row Fields\"\n            label \"%s\" field_name\n            cstring field_name \"Field Name\"\n            uint32 table_oid \"Table OID\" defaults(0)\n            uint16 column_attr \"Column Attribute\" defaults(0)\n            uint32 type_oid \"Type OID\" defaults(0)\n            uint16 type_size \"Type Size\" defaults(0)\n            uint32 type_modifier \"Type Modifier\" defaults(0)\n            uint16 format_code \"Format Code\" defaults(0)\n                Text = 0\n                Binary = 1\n\n    # ── DataRow (server → client, msg_type == 'D' == 68) ─────────────────────────────\n    when msg_type == 68:\n        uint16 field_count \"Field Count\" defaults(1)\n        payload row_data \"Row Data\"\n\n    # ── PasswordMessage / SASL response (client → server, msg_type == 'p' == 112) ────\n    when msg_type == 112:\n        payload password_data \"Password Data\"\n\n    else:\n        payload msg_body \"Message Body\"\n\n    info \"%s\" msg_type\n\n\n# ── Detection rules ───────────────────────────────────────────────────────────────────\n# Content rule first: the startup protocol version magic at byte offset 4.\n# Also add the SSL request magic (80877103 = 0x04D2162F) at offset 4.\nrule tcp.content@4 \"\\x00\\x03\\x00\\x00\" => PostgreSQL\nrule tcp.content@4 \"\\x04\\xd2\\x16\\x2f\" => PostgreSQL\n# Port fallback: handles sessions where the startup is not captured.\nrule tcp.port == 5432 => PostgreSQL\n\n# ── Coloring rules ────────────────────────────────────────────────────────────────────\n# Errors — white on red\ncolor postgresql.msg_type == 69 => white red\n\n# Notices — black on yellow\ncolor postgresql.msg_type == 78 => black lightyellow\n\n# ReadyForQuery — green: session is idle and accepting commands\ncolor postgresql.msg_type == 90 => black lightgreen\n\n# Authentication negotiation — yellow (security-relevant)\ncolor postgresql.msg_type == 82 => black lightyellow\n\n# Client queries — light yellow (outbound command)\ncolor postgresql.msg_type == 81 => black lightyellow\n\n# Completed commands — light green (successful response)\ncolor postgresql.msg_type == 67 => black lightgreen\n\n# Data rows and row descriptions — light gray (bulk data)\ncolor postgresql.msg_type == 84 => black lightgray\ncolor postgresql.msg_type == 68 => black lightgray\n\n# SSL / GSS encryption requests — dark blue (tunneled path)\ncolor postgresql.protocol == 0x04D2162F => white darkblue\ncolor postgresql.protocol == 0x04D21630 => white darkblue\n\n# Catch-all — unclassified PostgreSQL traffic\ncolor postgresql => white darkgray\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────────────\nalias pgsql.startup.protocol => postgresql.protocol\nalias pgsql.type             => postgresql.msg_type\nalias pgsql.query            => postgresql.query\nalias pgsql.authtype         => postgresql.auth_type\n",
    /* ppp.posa */
    "# PPP header (RFC 1661) — 2 bytes (protocol field only; address 0xFF + control 0x03 omitted when ACFC negotiated)\n# Carried over serial links, L2TP, PPTP, and PPPoE (after the PPPoE session header).\nObject<main> PPP\n    uint16 proto defaults(0x0021)\n        IP       = 0x0021\n        IPV6     = 0x0057\n        IPX      = 0x002B\n        MPLS_UC  = 0x0281\n        MPLS_MC  = 0x0283\n        LCP      = 0xC021\n        PAP      = 0xC023\n        CHAP     = 0xC223\n        IPCP     = 0x8021\n        IPV6CP   = 0x8057\n",
    /* pppoe.posa */
    "# PPPoE header (RFC 2516)\n# EtherType 0x8863 (discovery) or 0x8864 (session).\n# ver_type is always 0x11 (version=1, type=1).\nObject<main> PPPoE\n    uint8  ver_type defaults(0x11)\n    uint8  code defaults(0x00)\n        SESSION_DATA = 0x00\n        PADI         = 0x09\n        PADO         = 0x07\n        PADR         = 0x19\n        PADS         = 0x65\n        PADT         = 0xA7\n    uint16 session_id defaults(0)\n    uint16 payload_len defaults(0)\n",
    /* pptp.posa */
    "# pptp.posa — Point-to-Point Tunneling Protocol (RFC 2637)\n#\n# PPTP uses a TCP control connection on port 1723 for session management.\n# All control messages carry a fixed 12-byte header followed by type-specific\n# attributes. A mandatory 4-byte magic cookie (0x1A2B3C4D) sits at offset 4\n# in every control message and serves as the primary content fingerprint,\n# enabling decoding on non-standard ports without relying on the port number.\n#\n# GRE data tunnels carry PPP frames using Enhanced GRE (IP protocol 47,\n# GRE key type 0x880B); those frames are not decoded here.\n\nObject<main> PPTP\n    abbrev \"pptp\"\n    col \"PPTP\"\n\n    uint16 length \"Length\" defaults(156)\n    uint16 pptp_message_type \"PPTP Message Type\" defaults(1)\n        Control    = 1\n        Management = 2\n    uint32 magic_cookie hex \"Magic Cookie\" defaults(0x1A2B3C4D)\n    uint16 control_message_type \"Control Message Type\" defaults(1)\n        Start-Control-Connection-Request = 1\n        Start-Control-Connection-Reply   = 2\n        Stop-Control-Connection-Request  = 3\n        Stop-Control-Connection-Reply    = 4\n        Echo-Request                     = 5\n        Echo-Reply                       = 6\n        Outgoing-Call-Request            = 7\n        Outgoing-Call-Reply              = 8\n        Incoming-Call-Request            = 9\n        Incoming-Call-Reply              = 10\n        Incoming-Call-Connected          = 11\n        Call-Clear-Request               = 12\n        Call-Disconnect-Notify           = 13\n        WAN-Error-Notify                 = 14\n        Set-Link-Info                    = 15\n    uint16 reserved \"Reserved\" defaults(0)\n    payload\n\n    info \"%s\" control_message_type\n\n# ── Content rule (primary — port-independent) ─────────────────────────────────\n# The magic cookie 0x1A2B3C4D is mandatory in every PPTP control message and\n# sits at a fixed byte offset of 4, making it a reliable content fingerprint.\n# This rule fires before the port rule so PPTP is recognised on any port.\n\nrule tcp.content@4 \"\\x1a\\x2b\\x3c\\x4d\" => PPTP\n\n# ── Port rule (secondary — hint when content rule is insufficient) ────────────\n\nrule tcp.port == 1723 => PPTP\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# Map the Wireshark PPTP dissector field names to the abbrevs emitted above so\n# that display filters written for Wireshark continue to work unchanged.\n\nalias pptp.msg_type => pptp.control_message_type\nalias pptp.magic    => pptp.magic_cookie\nalias pptp.length   => pptp.length\n\n# ── Coloring rules (most specific first) ──────────────────────────────────────\n\n# Connection setup — Start-Control-Connection Request (1) and Reply (2): yellow.\ncolor pptp.control_message_type == 1 => black lightyellow\ncolor pptp.control_message_type == 2 => black lightyellow\n\n# Connection teardown — Stop-Control-Connection (3, 4) and\n# Call-Clear / Call-Disconnect (12, 13): red.\ncolor pptp.control_message_type == 3  => white red\ncolor pptp.control_message_type == 4  => white red\ncolor pptp.control_message_type == 12 => white red\ncolor pptp.control_message_type == 13 => white red\n\n# Data call signalling — Outgoing-Call (7, 8), Incoming-Call (9, 10, 11): green.\ncolor pptp.control_message_type == 7  => black lightgreen\ncolor pptp.control_message_type == 8  => black lightgreen\ncolor pptp.control_message_type == 9  => black lightgreen\ncolor pptp.control_message_type == 10 => black lightgreen\ncolor pptp.control_message_type == 11 => black lightgreen\n\n# Keepalive — Echo-Request (5) and Echo-Reply (6): neutral gray.\ncolor pptp.control_message_type == 5  => black lightgray\ncolor pptp.control_message_type == 6  => black lightgray\n\n# Error / management — WAN-Error-Notify (14): red; Set-Link-Info (15): gray.\ncolor pptp.control_message_type == 14 => white red\ncolor pptp.control_message_type == 15 => black lightgray\n\n# Catch-all: any PPTP frame not matched above (e.g. unknown future message types).\ncolor pptp => white darkblue\n",
    /* ptpv2.posa */
    "# PTPv2 header (IEEE 1588-2008) — 34 bytes\n# Over Ethernet (EtherType 0x88F7), UDP port 319 (event) / 320 (general), or raw IPv4/IPv6.\n# ts_msg_type: transportSpecific(4) | messageType(4). version is always 2.\n# correction_field is in nanoseconds scaled by 2^16 (subnanosecond precision).\nObject<main> PTPv2\n    uint8     ts_msg_type defaults(0x00)\n        SYNC              = 0x00\n        DELAY_REQ         = 0x01\n        PDELAY_REQ        = 0x02\n        PDELAY_RESP       = 0x03\n        FOLLOW_UP         = 0x08\n        DELAY_RESP        = 0x09\n        PDELAY_RESP_FUP   = 0x0A\n        ANNOUNCE          = 0x0B\n        SIGNALING         = 0x0C\n        MANAGEMENT        = 0x0D\n    uint8     version defaults(0x02)\n    uint16    message_len defaults(44)\n    uint8     domain_num defaults(0)\n    uint8     minor_sdoid defaults(0)\n    uint16    flags defaults(0)\n    bytes<8>  correction_field\n    uint32    msg_type_specific defaults(0)\n    bytes<8>  src_clock_id\n    uint16    src_port_num defaults(1)\n    uint16    seq_id defaults(0)\n    uint8     control defaults(0)\n    uint8     log_interval defaults(0x7F)\n",
    /* quic.posa */
    "# quic.posa — QUIC (Quick UDP Internet Connections) RFC 9000 / RFC 9369\n#\n# QUIC multiplexes streams over UDP with mandatory TLS 1.3 encryption.\n# Two header forms are distinguished by the high bit of the first byte:\n#\n#   QUIC_LONG  (bit 7 = 1, bit 6 = 1 fixed) — used during the handshake and\n#              for 0-RTT data.  Carries explicit version, destination CID and\n#              source CID.  Four long-header packet types (bits 5-4):\n#                  00 = Initial   (key exchange, always unencrypted header)\n#                  01 = 0-RTT     (early data before handshake completes)\n#                  10 = Handshake (crypto frames after Initial exchange)\n#                  11 = Retry     (server requests address validation)\n#\n#   QUIC_SHORT (bit 7 = 0, bit 6 = 1 fixed) — 1-RTT data packets after the\n#              handshake.  Destination CID length is inferred from context\n#              (negotiated during handshake), so the payload is opaque here.\n#\n# QUIC uses variable-length integers (varints) for token_length, packet\n# length, and similar fields after the SCID.  POSA has no native varint type,\n# so those trailing bytes are captured as a single payload field.\n#\n# Known version values:\n#   0x00000000  Version Negotiation (not a real version; signals negotiation)\n#   0x00000001  QUICv1 (RFC 9000)\n#   0x6b3343cf  QUICv2 (RFC 9369)\n#   0x51303434  Google QUIC Q044\n#   0x51303530  Google QUIC Q050\n#   0xff000000 | N  IETF draft version N\n\nObject<GROUP> QUIC\n\n# ─── Long Header ─────────────────────────────────────────────────────────────\n# First byte: 0xC0-0xCF = Initial, 0xD0-0xDF = 0-RTT,\n#             0xE0-0xEF = Handshake, 0xF0-0xFF = Retry\n# The reserved bits (3-2) must be 00 per RFC 9000; bits 1-0 encode pn_length,\n# so valid values per type are: 0, 1, 2, 3 → xC0-xC3, xD0-xD3, xE0-xE3, xF0-xF3.\n# Additional values (reserved bits set) are listed for resilience.\nObject<QUIC> QUIC_LONG\n    abbrev \"quic\"\n    col \"QUIC\"\n    starts \"\\xc0\" \"\\xc1\" \"\\xc2\" \"\\xc3\"\n           \"\\xd0\" \"\\xd1\" \"\\xd2\" \"\\xd3\"\n           \"\\xe0\" \"\\xe1\" \"\\xe2\" \"\\xe3\"\n           \"\\xf0\" \"\\xf1\" \"\\xf2\" \"\\xf3\"\n\n    uint8 first_byte \"First Byte\" defaults(0)\n    bits first_byte header_form 7 1 \"Header Form\"\n    bits first_byte fixed_bit 6 1 \"Fixed Bit\"\n    bits first_byte long_packet_type 4 2 \"Long Packet Type\"\n        0 = Initial\n        1 = 0-RTT\n        2 = Handshake\n        3 = Retry\n    bits first_byte type_specific_bits 0 4 \"Type Specific Bits\"\n\n    uint32 version hex \"Version\" defaults(0x00000001)\n        0x00000000 = Version Negotiation\n        0x00000001 = QUIC v1\n        0x6b3343cf = QUIC v2\n        0x51303434 = Google QUIC Q044\n        0x51303530 = Google QUIC Q050\n\n    uint8 dcil \"Destination Connection ID Length\" defaults(8)\n    bytes[dcil] dcid \"Destination Connection ID\"\n    uint8 scil \"Source Connection ID Length\" defaults(0)\n    bytes[scil] scid \"Source Connection ID\"\n\n    # Remaining bytes hold type-specific varint-encoded fields:\n    #   Initial:   token_length (varint), token (bytes), length (varint),\n    #              packet_number (1-4 bytes), crypto/STREAM frames\n    #   0-RTT:     length (varint), packet_number, encrypted frames\n    #   Handshake: length (varint), packet_number, crypto frames\n    #   Retry:     retry_token (variable), retry_integrity_tag (16 bytes)\n    payload remaining \"Remaining (Token / Length / PN / Payload)\"\n\n    info \"QUIC Long %s ver=%s dcid=%s scid=%s\" long_packet_type, version, dcid, scid\n\n# ─── Short Header (1-RTT) ────────────────────────────────────────────────────\n# First byte: bit 7=0 (short), bit 6=1 (fixed), bit 5=spin, bits 4-3=reserved,\n#             bit 2=key_phase, bits 1-0=pn_length.\n# Destination CID length is opaque here (negotiated during handshake).\n# Listed starts: spin=0 kp=0,1 and spin=1 kp=0,1, all four pn_lengths each.\nObject<QUIC> QUIC_SHORT\n    abbrev \"quic\"\n    col \"QUIC\"\n    starts \"\\x40\" \"\\x41\" \"\\x42\" \"\\x43\"\n           \"\\x44\" \"\\x45\" \"\\x46\" \"\\x47\"\n           \"\\x60\" \"\\x61\" \"\\x62\" \"\\x63\"\n           \"\\x64\" \"\\x65\" \"\\x66\" \"\\x67\"\n\n    uint8 first_byte \"First Byte\" defaults(0)\n    bits first_byte header_form 7 1 \"Header Form\"\n    bits first_byte fixed_bit 6 1 \"Fixed Bit\"\n    bits first_byte spin_bit 5 1 \"Spin Bit\"\n    bits first_byte reserved_bits 3 2 \"Reserved Bits\"\n    bits first_byte key_phase 2 1 \"Key Phase\"\n    bits first_byte pn_length 0 2 \"Packet Number Length\"\n\n    # DCID length not encoded in-packet for Short Header; treat remainder as opaque\n    payload dcid_and_payload \"Destination Connection ID + Encrypted Payload\"\n\n    info \"QUIC Short (1-RTT) spin=%u key_phase=%u\" spin_bit, key_phase\n\n# ─── Content detection (primary — port-independent) ──────────────────────────\n# Most specific first: full 5-byte QUICv1 Initial prefix fires before the\n# single-byte fallbacks.  Ordering ensures the longest match wins.\nrule udp.content \"\\xc0\\x00\\x00\\x00\\x01\" => QUIC   # QUICv1 Initial (RFC 9000)\nrule udp.content \"\\xc0\\x6b\\x33\\x43\\xcf\" => QUIC   # QUICv2 Initial (RFC 9369)\nrule udp.content \"\\xc0\\x00\\x00\\x00\\x00\" => QUIC   # Version Negotiation\nweak rule udp.content \"\\xc0\" => QUIC                    # Long Header Initial\nweak rule udp.content \"\\xd0\" => QUIC                    # Long Header 0-RTT\nweak rule udp.content \"\\xe0\" => QUIC                    # Long Header Handshake\nweak rule udp.content \"\\xf0\" => QUIC                    # Long Header Retry\n\n# ─── Port bindings (secondary — hints when content prefix is ambiguous) ───────\nrule udp.port == 443 => QUIC\nrule udp.port == 80  => QUIC\n\n# ─── Coloring ─────────────────────────────────────────────────────────────────\n# Long Header types ordered most-to-least specific; catch-all covers 1-RTT data.\ncolor quic.long_packet_type == 0 => black lightyellow    # Initial (key exchange)\ncolor quic.long_packet_type == 1 => white darkblue       # 0-RTT (early encrypted data)\ncolor quic.long_packet_type == 2 => black lightgreen     # Handshake (crypto completion)\ncolor quic.long_packet_type == 3 => white red            # Retry (address validation)\ncolor quic                       => white darkblue        # Short Header 1-RTT (encrypted)\n\n# ─── Wireshark-compatible field aliases ───────────────────────────────────────\n# Wireshark's quic dissector uses these names; map them to our abbrev fields\n# so display filters written against Wireshark captures continue to work.\nalias quic.version          => quic.version\nalias quic.dcid             => quic.dcid\nalias quic.scid             => quic.scid\nalias quic.packet_type      => quic.long_packet_type\nalias quic.long.packet_type => quic.long_packet_type\nalias quic.header_form      => quic.header_form\nalias quic.fixed_bit        => quic.fixed_bit\nalias quic.spin_bit         => quic.spin_bit\nalias quic.key_phase        => quic.key_phase\nalias quic.dcil             => quic.dcil\nalias quic.scil             => quic.scil\n",
    /* radius.posa */
    "# ── RADIUS attribute types (RFC 2865 + RFC 2866 + RFC 3576 common subset) ────\nLookup RadiusAttributeTypes\n    1   = \"User-Name\"\n    2   = \"User-Password\"\n    3   = \"CHAP-Password\"\n    4   = \"NAS-IP-Address\"\n    5   = \"NAS-Port\"\n    6   = \"Service-Type\"\n    7   = \"Framed-Protocol\"\n    8   = \"Framed-IP-Address\"\n    9   = \"Framed-IP-Netmask\"\n    10  = \"Framed-Routing\"\n    11  = \"Filter-Id\"\n    12  = \"Framed-MTU\"\n    13  = \"Framed-Compression\"\n    14  = \"Login-IP-Host\"\n    15  = \"Login-Service\"\n    16  = \"Login-TCP-Port\"\n    18  = \"Reply-Message\"\n    19  = \"Callback-Number\"\n    20  = \"Callback-Id\"\n    22  = \"Framed-Route\"\n    23  = \"Framed-IPX-Network\"\n    24  = \"State\"\n    25  = \"Class\"\n    26  = \"Vendor-Specific\"\n    27  = \"Session-Timeout\"\n    28  = \"Idle-Timeout\"\n    29  = \"Termination-Action\"\n    30  = \"Called-Station-Id\"\n    31  = \"Calling-Station-Id\"\n    32  = \"NAS-Identifier\"\n    33  = \"Proxy-State\"\n    34  = \"Login-LAT-Service\"\n    35  = \"Login-LAT-Node\"\n    36  = \"Login-LAT-Group\"\n    37  = \"Framed-AppleTalk-Link\"\n    38  = \"Framed-AppleTalk-Network\"\n    39  = \"Framed-AppleTalk-Zone\"\n    40  = \"Acct-Status-Type\"\n    41  = \"Acct-Delay-Time\"\n    42  = \"Acct-Input-Octets\"\n    43  = \"Acct-Output-Octets\"\n    44  = \"Acct-Session-Id\"\n    45  = \"Acct-Authentic\"\n    46  = \"Acct-Session-Time\"\n    47  = \"Acct-Input-Packets\"\n    48  = \"Acct-Output-Packets\"\n    49  = \"Acct-Terminate-Cause\"\n    50  = \"Acct-Multi-Session-Id\"\n    51  = \"Acct-Link-Count\"\n    52  = \"Acct-Input-Gigawords\"\n    53  = \"Acct-Output-Gigawords\"\n    55  = \"Event-Timestamp\"\n    60  = \"CHAP-Challenge\"\n    61  = \"NAS-Port-Type\"\n    62  = \"Port-Limit\"\n    63  = \"Login-LAT-Port\"\n    64  = \"Tunnel-Type\"\n    65  = \"Tunnel-Medium-Type\"\n    66  = \"Tunnel-Client-Endpoint\"\n    67  = \"Tunnel-Server-Endpoint\"\n    68  = \"Acct-Tunnel-Connection\"\n    69  = \"Tunnel-Password\"\n    70  = \"ARAP-Password\"\n    71  = \"ARAP-Features\"\n    72  = \"ARAP-Zone-Access\"\n    73  = \"ARAP-Security\"\n    74  = \"ARAP-Security-Data\"\n    75  = \"Password-Retry\"\n    76  = \"Prompt\"\n    77  = \"Connect-Info\"\n    78  = \"Configuration-Token\"\n    79  = \"EAP-Message\"\n    80  = \"Message-Authenticator\"\n    81  = \"Tunnel-Private-Group-Id\"\n    82  = \"Tunnel-Assignment-Id\"\n    83  = \"Tunnel-Preference\"\n    84  = \"ARAP-Challenge-Response\"\n    85  = \"Acct-Interim-Interval\"\n    86  = \"Acct-Tunnel-Packets-Lost\"\n    87  = \"NAS-Port-Id\"\n    88  = \"Framed-Pool\"\n    90  = \"Tunnel-Client-Auth-Id\"\n    91  = \"Tunnel-Server-Auth-Id\"\n    95  = \"NAS-IPv6-Address\"\n    96  = \"Framed-Interface-Id\"\n    97  = \"Framed-IPv6-Prefix\"\n    98  = \"Login-IPv6-Host\"\n    99  = \"Framed-IPv6-Route\"\n    100 = \"Framed-IPv6-Pool\"\n\n# RADIUS — Remote Authentication Dial-In User Service (RFC 2865 / RFC 2866)\n# Authentication port: UDP 1812 (legacy 1645)\n# Accounting port:     UDP 1813 (legacy 1646)\n#\n# Every RADIUS packet has a fixed 20-byte header followed by zero or more\n# Type-Length-Value attributes. The Length field in each attribute encodes\n# the total attribute size including the type and length bytes themselves,\n# so the value payload is (attr_length - 2) bytes.\n#\n# The Authenticator field is 16 bytes: in Access-Request it is a random\n# nonce; in responses it is an MD5 hash over (code + id + length + req-auth + attrs + secret).\n#\n# RFC 3576 (Dynamic Authorization) adds Disconnect and CoA message types,\n# also carried on UDP 3799 in that RFC but vendors commonly reuse 1812/1813.\n\nObject<main> RADIUS\n    abbrev \"radius\"\n    col \"RADIUS\"\n\n    uint8 code \"Code\" defaults(1)\n        1   = \"Access-Request\"\n        2   = \"Access-Accept\"\n        3   = \"Access-Reject\"\n        4   = \"Accounting-Request\"\n        5   = \"Accounting-Response\"\n        6   = \"Accounting-Status\"\n        7   = \"Password-Request\"\n        8   = \"Password-Ack\"\n        9   = \"Password-Reject\"\n        10  = \"Accounting-Message\"\n        11  = \"Access-Challenge\"\n        12  = \"Status-Server\"\n        13  = \"Status-Client\"\n        21  = \"Resource-Free-Request\"\n        22  = \"Resource-Free-Response\"\n        23  = \"Resource-Query-Request\"\n        24  = \"Resource-Query-Response\"\n        25  = \"Alternate-Resource-Reclaim-Request\"\n        26  = \"NAS-Reboot-Request\"\n        27  = \"NAS-Reboot-Response\"\n        29  = \"Next-Passcode\"\n        30  = \"New-Pin\"\n        31  = \"Terminate-Session\"\n        32  = \"Password-Expired\"\n        33  = \"Event-Request\"\n        34  = \"Event-Response\"\n        40  = \"Disconnect-Request\"\n        41  = \"Disconnect-ACK\"\n        42  = \"Disconnect-NAK\"\n        43  = \"CoA-Request\"\n        44  = \"CoA-ACK\"\n        45  = \"CoA-NAK\"\n        255 = \"Reserved\"\n\n    uint8 identifier \"Identifier\" defaults(1)\n    uint16 length \"Length\" defaults(20)\n    bytes<16> authenticator \"Authenticator\"\n\n    repeat until end as attribute \"Attributes\"\n        label \"%s\" attr_type\n        uint8 attr_type lookup RadiusAttributeTypes \"Type\" defaults(1)\n        uint8 attr_length \"Length\" defaults(2)\n        scope attr_length - 2\n            payload value \"Value\"\n\n    info \"%s id=%u\" code, identifier\n\n\n# ── Detection rules ───────────────────────────────────────────────────────────\n# Content rule first: the high byte of the 16-bit Length field at offset 2 is\n# always 0x00 for any normal RADIUS packet (max 4096 bytes, so high byte <= 0x0F;\n# common packets are well under 256 bytes). This catches RADIUS on non-standard ports.\nweak rule udp.content@2 \"\\x00\" => RADIUS\n# Standard authentication and accounting ports (RFC 2865 / RFC 2866).\nrule udp.port == 1812 => RADIUS\nrule udp.port == 1813 => RADIUS\n# Legacy ports originally registered to livingstn-passwd / livingstn-billing.\nrule udp.port == 1645 => RADIUS\nrule udp.port == 1646 => RADIUS\n\n# ── Coloring rules ────────────────────────────────────────────────────────────\n# Access-Request — client credential submission (yellow: outbound auth attempt)\ncolor radius.code == 1  => black lightyellow\n\n# Access-Accept — authentication granted (green: success)\ncolor radius.code == 2  => black lightgreen\n\n# Access-Reject — authentication denied (red: failure / alert)\ncolor radius.code == 3  => white red\n\n# Accounting-Request / Accounting-Response — session accounting (blue: data plane)\ncolor radius.code == 4  => white darkblue\ncolor radius.code == 5  => white darkblue\n\n# Access-Challenge — server requires further interaction (yellow: pending)\ncolor radius.code == 11 => black lightyellow\n\n# Status-Server / Status-Client — keepalive probes (gray: management)\ncolor radius.code == 12 => black lightgray\ncolor radius.code == 13 => black lightgray\n\n# Disconnect-Request / CoA-Request — dynamic authorization (yellow: outbound command)\ncolor radius.code == 40 => black lightyellow\ncolor radius.code == 43 => black lightyellow\n\n# Disconnect-ACK / CoA-ACK — command accepted (green: success)\ncolor radius.code == 41 => black lightgreen\ncolor radius.code == 44 => black lightgreen\n\n# Disconnect-NAK / CoA-NAK — command refused (red: failure)\ncolor radius.code == 42 => white red\ncolor radius.code == 45 => white red\n\n# Catch-all — unclassified RADIUS traffic\ncolor radius => white darkgray\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\nalias radius.code         => radius.code\nalias radius.id           => radius.identifier\nalias radius.User_Name    => radius.attribute.attr_type == 1\nalias radius.NAS_IP_Address => radius.attribute.attr_type == 4\n",
    /* raknet.posa */
    "# raknet.posa — RakNet / Minecraft Bedrock Edition network protocol\n# UDP (any port, but Minecraft Bedrock uses 19132/19133 by default).\n# Reference: https://wiki.vg/Raknet_Protocol\n#\n# RakNet offline messages carry a 16-byte magic sequence at offset 1:\n#   \\x00\\xff\\xff\\x00\\xfe\\xfe\\xfe\\xfe\\xfd\\xfd\\xfd\\xfd\\x12\\x34\\x56\\x78\n#\n# Offline message IDs (first byte):\n#   0x01  UNCONNECTED_PING\n#   0x02  UNCONNECTED_PING_OPEN_CONNECTIONS\n#   0x1C  UNCONNECTED_PONG\n#   0x05  OPEN_CONNECTION_REQUEST_1\n#   0x06  OPEN_CONNECTION_REPLY_1\n#   0x07  OPEN_CONNECTION_REQUEST_2\n#   0x08  OPEN_CONNECTION_REPLY_2\n#   0x09  CONNECTION_REQUEST (online — no magic)\n#   0x10  CONNECTION_REQUEST_ACCEPTED\n#   0x13  NEW_INCOMING_CONNECTION\n#   0x15  DISCONNECT\n#   0x19  CONNECTION_ATTEMPT_FAILED\n#   0x1A  ALREADY_CONNECTED\n#   0x1B  NEW_INCOMING_CONNECTION\n#   0x1D  INCOMPATIBLE_PROTOCOL_VERSION\n#\n# Connected (online) packets use a different framing and are not decoded here.\n\nObject<RAKNET> RAKNET_OFFLINE\n    abbrev \"raknet\"\n    col \"RakNet\"\n\n    uint8  message_id \"Message ID\" defaults(0x01)\n        UNCONNECTED_PING              = 0x01\n        UNCONNECTED_PING_OPEN         = 0x02\n        UNCONNECTED_PONG              = 0x1C\n        OPEN_CONNECTION_REQUEST_1     = 0x05\n        OPEN_CONNECTION_REPLY_1       = 0x06\n        OPEN_CONNECTION_REQUEST_2     = 0x07\n        OPEN_CONNECTION_REPLY_2       = 0x08\n        INCOMPATIBLE_PROTOCOL_VERSION = 0x1D\n\n    when message_id == 0x01:\n        uint64 send_ping_time \"Ping Time\" defaults(0)\n        bytes<16> offline_magic hex \"Offline Magic\" defaults(\"\\x00\\xff\\xff\\x00\\xfe\\xfe\\xfe\\xfe\\xfd\\xfd\\xfd\\xfd\\x12\\x34\\x56\\x78\")\n\n    when message_id == 0x1C:\n        uint64 send_ping_time \"Ping Time\" defaults(0)\n        uint64 server_guid \"Server GUID\" defaults(0)\n        bytes<16> offline_magic hex \"Offline Magic\" defaults(\"\\x00\\xff\\xff\\x00\\xfe\\xfe\\xfe\\xfe\\xfd\\xfd\\xfd\\xfd\\x12\\x34\\x56\\x78\")\n        uint16 server_info_len \"Server Info Length\" defaults(0)\n        str[server_info_len] server_info \"Server Info\"\n\n    when message_id == 0x05:\n        bytes<16> offline_magic hex \"Offline Magic\" defaults(\"\\x00\\xff\\xff\\x00\\xfe\\xfe\\xfe\\xfe\\xfd\\xfd\\xfd\\xfd\\x12\\x34\\x56\\x78\")\n        uint8  raknet_protocol_version \"RakNet Protocol Version\" defaults(11)\n        payload  null_padding \"Null Padding (MTU)\"\n\n    when message_id == 0x06:\n        bytes<16> offline_magic hex \"Offline Magic\" defaults(\"\\x00\\xff\\xff\\x00\\xfe\\xfe\\xfe\\xfe\\xfd\\xfd\\xfd\\xfd\\x12\\x34\\x56\\x78\")\n        uint64 server_guid \"Server GUID\" defaults(0)\n        uint8  use_security \"Use Security\" defaults(0)\n        uint16 mtu \"MTU Size\" defaults(1400)\n\n    when message_id == 0x07:\n        bytes<16> offline_magic hex \"Offline Magic\" defaults(\"\\x00\\xff\\xff\\x00\\xfe\\xfe\\xfe\\xfe\\xfd\\xfd\\xfd\\xfd\\x12\\x34\\x56\\x78\")\n        uint64 client_guid          \"Client GUID\"   defaults(0)\n        payload  body               \"Body\"\n\n    when message_id == 0x08:\n        bytes<16> offline_magic hex \"Offline Magic\" defaults(\"\\x00\\xff\\xff\\x00\\xfe\\xfe\\xfe\\xfe\\xfd\\xfd\\xfd\\xfd\\x12\\x34\\x56\\x78\")\n        uint64 server_guid          \"Server GUID\"   defaults(0)\n        uint16 mtu                  \"MTU Size\"      defaults(1400)\n        payload  body               \"Body\"\n\n    else:\n        payload body \"Body\"\n\n    info \"RakNet %s\" message_id\n\nObject<GROUP> RAKNET\n\n# RakNet offline magic at offset 1\nrule udp.content@1 \"\\x00\\xff\\xff\\x00\\xfe\\xfe\\xfe\\xfe\\xfd\\xfd\\xfd\\xfd\\x12\\x34\\x56\\x78\" => RAKNET\n\n# Minecraft Bedrock ports\nrule udp.port == 19132 => RAKNET\nrule udp.port == 19133 => RAKNET\n\ncolor raknet.message_id == 0x05 => black lightyellow\ncolor raknet.message_id == 0x06 => black lightgreen\ncolor raknet.message_id == 0x07 => black lightyellow\ncolor raknet.message_id == 0x08 => black lightgreen\ncolor raknet.message_id == 0x1C => black lightblue\ncolor raknet                    => white darkblue\n",
    /* rdp.posa */
    "# rdp.posa — Remote Desktop Protocol over TPKT / ISO-8073 (COTP), fully in posa.\n# Demonstrates the extended grammar: layered sub-protocols, length scopes,\n# conditional layout, a delimiter field, a derived Info string, and a port rule.\n\nprotocol TPKT\n    col \"RDP\"\n    abbrev \"rdp\"\n    uint8  version defaults(3)\n    uint8  reserved defaults(0)\n    uint16 length defaults(19)\n    layer   cotp    COTP\n\nprotocol COTP\n    abbrev \"cotp\"\n    uint8  li defaults(6)\n    uint8  pdu_type defaults(0xe0)\n        CR = 0xe0\n        CC = 0xd0\n        DT = 0xf0\n    scope   li\n        when pdu_type & 0xf0 == 0xe0:\n            uint16 dst_ref defaults(0)\n            uint16 src_ref defaults(0)\n            uint8  class defaults(0)\n            layer neg RDP_NEGOTIATION\n        when pdu_type & 0xf0 == 0xd0:\n            uint16 dst_ref defaults(0)\n            uint16 src_ref defaults(0)\n            uint8  class defaults(0)\n            layer neg RDP_NEGOTIATION\n        when pdu_type & 0xf0 == 0xf0:\n            uint8  eot defaults(0x80)\n    info \"COTP %s\" pdu_type\n\nprotocol RDP_NEGOTIATION\n    abbrev \"rdp\"\n    optional string cookie until \"\\r\\n\"\n    when remaining >= 8:\n        uint8 type defaults(1)\n            Request  = 1\n            Response = 2\n            Failure  = 3\n        uint8     flags defaults(0)\n        le_uint16 length defaults(19)\n        le_uint32 protocols defaults(0x1)\n            TLS     = 0x1\n            CredSSP = 0x3\n    info \"%s Negotiate %s\" cookie, type\n\nrule tcp.port == 3389 => TPKT\n\n# Coloring — carcal paints a matching packet with these colors, first match wins.\n# Declared here so the decoder ships its own look: drop this file into protos/\n# and RDP is both decoded and colored, with no rebuild. Consulted before\n# carcal's generic tcp rule, so they win over it.\ncolor rdp.type == 3 => yellow red      # negotiation Failure\ncolor rdp           => black lightcyan\n",
    /* redis.posa */
    "# redis.posa — Redis RESP Protocol (REdis Serialization Protocol)\n# Covers RESP2 (RFC-style, original) and RESP3 (introduced in Redis 6).\n# Spec: https://redis.io/docs/reference/protocol-spec/\n#\n# Wire shape: each message begins with a single ASCII type byte, then either\n# an inline value terminated by \\r\\n, or a length/count followed by \\r\\n and\n# the payload bytes.  RESP is inherently line-oriented, so fields are read as\n# strings until the \\r\\n delimiter rather than length-prefixed integers.\n#\n# What this decoder exposes:\n#   resp.type_byte  — the leading type character (+ - : $ * _ , # ( ~ | > %)\n#   resp.value      — inline text for scalar types (Simple String, Error,\n#                     Integer, Double, Boolean, Big Number, Null marker)\n#   resp.count      — element count for container types (Array, Set, Map,\n#                     Push, Attribute)\n#   resp.length     — declared byte length for Bulk String\n#   resp.data       — raw payload following the header line(s)\n#\n# The nested elements of Array / Set / Map / Push are surfaced as resp.data\n# (payload) because RESP nesting requires recursive parsing; a second pass on\n# that payload with the same decoder reveals the child frames.\n\nObject<main> REDIS\n    abbrev \"resp\"\n    col \"REDIS\"\n\n    # One ASCII byte selects the message type.  Enum labels appear in the\n    # Type column and in the %s expansion of the info line below.\n    uint8 type_byte \"Type\" defaults(0x2A)\n        0x2B = Simple String\n        0x2D = Error\n        0x3A = Integer\n        0x24 = Bulk String\n        0x2A = Array\n        0x5F = Null\n        0x2C = Double\n        0x23 = Boolean\n        0x28 = Big Number\n        0x7E = Set\n        0x7C = Attribute\n        0x3E = Push\n        0x25 = Map\n\n    # ── RESP2 scalar types ────────────────────────────────────────────────────\n\n    # +<text>\\r\\n  — server confirms success or sends a status word (e.g. \"OK\")\n    when type_byte == 0x2B:\n        string value until \"\\r\\n\" \"Simple String\"\n\n    # -<message>\\r\\n  — server signals an error (\"ERR unknown command …\")\n    when type_byte == 0x2D:\n        string value until \"\\r\\n\" \"Error Message\"\n\n    # :<number>\\r\\n  — server returns a signed 64-bit integer as decimal text\n    when type_byte == 0x3A:\n        string value until \"\\r\\n\" \"Integer\"\n\n    # $<bytecount>\\r\\n<data>\\r\\n  — a binary-safe byte string\n    # The declared byte count is read here; the data follows as a payload so\n    # that binary content (images, serialised objects) is not mis-parsed.\n    when type_byte == 0x24:\n        string length until \"\\r\\n\" \"Bulk Length\"\n        payload data \"Bulk Data\"\n\n    # *<count>\\r\\n  followed by <count> RESP elements (most client commands)\n    when type_byte == 0x2A:\n        string count until \"\\r\\n\" \"Element Count\"\n        payload data \"Array Elements\"\n\n    # ── RESP3 types (Redis 6+) ────────────────────────────────────────────────\n\n    # _\\r\\n  — typed null (replaces the $-1 / *-1 null sentinels of RESP2)\n    when type_byte == 0x5F:\n        string value until \"\\r\\n\" \"Null Marker\"\n\n    # ,<double>\\r\\n  — IEEE 754 double as decimal text, or \"inf\" / \"-inf\"\n    when type_byte == 0x2C:\n        string value until \"\\r\\n\" \"Double\"\n\n    # #<t|f>\\r\\n  — boolean: literal 't' or 'f'\n    when type_byte == 0x23:\n        string value until \"\\r\\n\" \"Boolean\"\n\n    # (<digits>\\r\\n  — arbitrary-precision signed integer as decimal text\n    when type_byte == 0x28:\n        string value until \"\\r\\n\" \"Big Number\"\n\n    # ~<count>\\r\\n  — unordered set of <count> RESP3 elements\n    when type_byte == 0x7E:\n        string count until \"\\r\\n\" \"Set Size\"\n        payload data \"Set Elements\"\n\n    # |<count>\\r\\n  — attribute / metadata map of <count> key-value pairs\n    when type_byte == 0x7C:\n        string count until \"\\r\\n\" \"Attribute Count\"\n        payload data \"Attribute Data\"\n\n    # ><count>\\r\\n  — server-initiated push (Pub/Sub, keyspace notifications)\n    when type_byte == 0x3E:\n        string count until \"\\r\\n\" \"Push Count\"\n        payload data \"Push Data\"\n\n    # %<count>\\r\\n  — map of <count> key-value pairs (RESP3 equivalent of Hash)\n    when type_byte == 0x25:\n        string count until \"\\r\\n\" \"Map Size\"\n        payload data \"Map Entries\"\n\n    else:\n        payload data \"Data\"\n\n    # Info column: type enum label, then whichever of value / count / length is\n    # present for this message.  Missing fields collapse to nothing automatically\n    # so the three trailing %s slots produce at most one visible token.\n    info \"%s %s%s%s\" type_byte, value, count, length\n\n# ── Detection rules ───────────────────────────────────────────────────────────\n# Content rules are checked first (port-independent) so the protocol is\n# recognised on any port, including Redis Cluster bus ports and TLS proxies.\n# The leading-byte signatures are unambiguous because RESP uses only printable\n# ASCII type bytes that do not start other common protocols.\n\nweak rule tcp.content \"*\" => REDIS\nweak rule tcp.content \"+\" => REDIS\nweak rule tcp.content \"$\" => REDIS\nweak rule tcp.content \":\" => REDIS\nweak rule tcp.content \"-\" => REDIS\nweak rule tcp.content \"_\" => REDIS\nweak rule tcp.content \"%\" => REDIS\n\n# Port rule is a secondary hint — active only when content rules do not match.\nrule tcp.port == 6379 => REDIS\n\n# ── Coloring rules ────────────────────────────────────────────────────────────\n# Most specific first; the catch-all at the end paints anything the engine\n# identified as REDIS but that did not match a more targeted filter above it.\n\n# Error replies stand out with the classic error palette.\ncolor resp.type_byte == 0x2D => white red\n\n# Push messages (Pub/Sub, keyspace events) share the \"tunneled/async\" palette.\ncolor resp.type_byte == 0x3E => white darkblue\n\n# Successful scalar responses — green to match Wireshark's HTTP-200 convention.\ncolor resp.type_byte == 0x2B => black lightgreen\ncolor resp.type_byte == 0x3A => black lightgreen\ncolor resp.type_byte == 0x2C => black lightgreen\ncolor resp.type_byte == 0x23 => black lightgreen\ncolor resp.type_byte == 0x28 => black lightgreen\n\n# Client commands and bulk data — yellow to signal request/send direction.\ncolor resp.type_byte == 0x2A => black lightyellow\ncolor resp.type_byte == 0x24 => black lightyellow\ncolor resp.type_byte == 0x7E => black lightyellow\ncolor resp.type_byte == 0x25 => black lightyellow\ncolor resp.type_byte == 0x7C => black lightyellow\n\n# Null and unknown — neutral gray.\ncolor resp.type_byte == 0x5F => black lightgray\n\n# Catch-all: any Redis frame not coloured above.\ncolor resp => white darkgray\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# Map the field names a Wireshark dissector would emit so that display filters\n# written for Wireshark's redis dissector continue to work here.\n\nalias redis.type   => resp.type_byte\nalias redis.data   => resp.data\nalias redis.value  => resp.value\nalias redis.count  => resp.count\nalias redis.length => resp.length\n\n# Convenience macro aliases for common filter expressions.\nalias redis.error   => resp.type_byte == 0x2D\nalias redis.command => resp.type_byte == 0x2A\nalias redis.ok      => resp.type_byte == 0x2B\n",
    /* ripv2.posa */
    "# RIPv2 header (RFC 2453)\n# UDP port 520. Header is 4 bytes; route entries follow (20 bytes each).\nObject<main> RIPv2\n    uint8  command defaults(2)\n        REQUEST  = 1\n        RESPONSE = 2\n    uint8  version defaults(2)\n    uint16 zero defaults(0)\n",
    /* rsvp.posa */
    "# RSVP header (RFC 2205) — 8 bytes\n# IP protocol 46. version_flags: Ver(4)=1 | Flags(4). Objects follow the header.\n# send_ttl copies the IP TTL so intermediate nodes can detect routing loops.\nObject<main> RSVP\n    uint8  version_flags defaults(0x10)\n    uint8  msg_type defaults(1)\n        PATH      = 1\n        RESV      = 2\n        PATHERR   = 3\n        RESVERR   = 4\n        PATHTEAR  = 5\n        RESVTEAR  = 6\n        RESVCONF  = 7\n        BUNDLE    = 12\n        ACK       = 13\n        SREFRESH  = 15\n        HELLO     = 20\n    uint16 checksum defaults(0)\n    uint8  send_ttl defaults(64)\n    uint8  reserved defaults(0)\n    uint16 rsvp_length defaults(0)\n",
    /* rsync.posa */
    "# rsync.posa — rsync wire protocol (TCP 873)\n# Reference: https://rsync.samba.org/how-rsync-works.html\n#            https://github.com/WayneD/rsync (protocol.c)\n#\n# The rsync protocol begins with a plain-text greeting on both sides:\n#   Server sends: \"@RSYNCD: <major>.<minor>\\n\"\n#   Client echoes: \"@RSYNCD: <major>.<minor>\\n\"\n#   Client sends module name: \"<module>\\n\" (empty for listing)\n#   Server sends: \"@RSYNCD: OK\\n\" or \"@RSYNCD: EXIT\\n\" or \"@ERROR: ...\\n\"\n#\n# After the handshake the session switches to a binary protocol with\n# a variable-length multiplexed data stream (not decoded here — the\n# binary format is version-dependent and complex). We decode the handshake.\n\nObject<RSYNC> RSYNC_SERVER\n    abbrev \"rsync\"\n    col \"rsync\"\n    starts \"@RSYNCD: \"\n\n    string greeting until \"\\n\" \"Greeting\" defaults(\"@RSYNCD: 31.0\")\n    info \"%s\" greeting\n\nObject<RSYNC> RSYNC_OK\n    abbrev \"rsync\"\n    col \"rsync\"\n    starts \"@RSYNCD: OK\"\n\n    string status until \"\\n\" \"Status\" defaults(\"@RSYNCD: OK\")\n    info \"%s\" status\n\nObject<RSYNC> RSYNC_EXIT\n    abbrev \"rsync\"\n    col \"rsync\"\n    starts \"@RSYNCD: EXIT\"\n\n    string status until \"\\n\" \"Status\" defaults(\"@RSYNCD: EXIT\")\n    info \"%s\" status\n\nObject<RSYNC> RSYNC_ERROR\n    abbrev \"rsync\"\n    col \"rsync\"\n    starts \"@ERROR\"\n\n    string error_msg until \"\\n\" \"Error\" defaults(\"@ERROR: unknown module\")\n    info \"%s\" error_msg\n\nObject<GROUP> RSYNC\n\nrule tcp.port == 873 => RSYNC\nrule tcp.content \"@RSYNCD:\" => RSYNC\n\ncolor rsync.error_msg => white red\ncolor rsync           => black lightcyan\n",
    /* rtmp.posa */
    "# rtmp.posa — Real-Time Messaging Protocol (Adobe RTMP)\n#\n# RTMP runs over TCP and opens with a three-way handshake:\n#\n#   C0/S0  1 byte   — version byte, must be 0x03 (0x06 for RTMPE)\n#   C1/S1  1536 bytes — uint32_be timestamp + uint32_be zeros + 1528 random bytes\n#   C2/S2  1536 bytes — echo of the peer's C1/S1\n#\n# After the handshake both sides exchange RTMP chunks. Each chunk has:\n#\n#   Basic Header  1-3 bytes:\n#     bits 7-6: fmt  (0=full, 1=delta+type, 2=timestamp-delta only, 3=identical)\n#     bits 5-0: cs_id (chunk stream ID; 0 and 1 add 1 or 2 extension bytes)\n#\n#   Message Header (fmt=0, 11 bytes):\n#     uint24_be timestamp + uint24_be msg_length + uint8 msg_type_id\n#     + uint32_le msg_stream_id\n#\n#   Message Header (fmt=1, 7 bytes): timestamp_delta + msg_length + msg_type_id\n#   Message Header (fmt=2, 3 bytes): timestamp_delta only\n#   Message Header (fmt=3, 0 bytes): identical to previous chunk on this cs_id\n#\n# If any timestamp field is 0xFFFFFF an extended 4-byte timestamp follows.\n#\n# Reference: https://rtmp.veriskope.com/docs/spec/  (Adobe RTMP specification)\n#\n# Runs on TCP port 1935. RTMPS (TLS-wrapped) uses TCP port 443.\n\nObject<RTMP> RTMP_HANDSHAKE\n    abbrev \"rtmp\"\n    col \"RTMP\"\n    starts \"\\x03\"\n\n    uint8    version \"Version\" defaults(3)\n        3 = \"RTMP\"\n        6 = \"RTMPE (encrypted)\"\n    uint32   timestamp \"C1 Timestamp\" defaults(0)\n    uint32   zeros     \"C1 Zeros\"     defaults(0)\n    payload  random    \"C1 Random Data\"\n\n    info \"RTMP handshake version=%u\" version\n\nObject<RTMP> RTMP_CHUNK\n    abbrev \"rtmp\"\n    col \"RTMP\"\n\n    uint8 basic_header hex \"Basic Header\" defaults(0)\n    bits basic_header fmt 6 2 \"Format\"\n        0 = \"Type 0 — New stream\"\n        1 = \"Type 1 — Same stream, new type\"\n        2 = \"Type 2 — Same stream and type\"\n        3 = \"Type 3 — Identical to previous\"\n    bits basic_header cs_id 0 6 \"Chunk Stream ID\"\n        2 = \"RTMP Control\"\n        3 = \"AMF Command\"\n        4 = \"User Control\"\n        5 = \"Video\"\n        6 = \"Audio\"\n\n    when fmt == 0:\n        uint24 timestamp \"Timestamp\" defaults(0)\n        uint24 msg_length \"Message Length\" defaults(0)\n        uint8  msg_type_id \"Message Type\" defaults(20)\n            1  = \"Set Chunk Size\"\n            2  = \"Abort\"\n            3  = \"Acknowledgement\"\n            4  = \"User Control\"\n            5  = \"Window Acknowledgement Size\"\n            6  = \"Set Peer Bandwidth\"\n            8  = \"Audio Data\"\n            9  = \"Video Data\"\n            15 = \"AMF3 Data\"\n            16 = \"AMF3 Shared Object\"\n            17 = \"AMF3 Command\"\n            18 = \"AMF0 Data\"\n            19 = \"AMF0 Shared Object\"\n            20 = \"AMF0 Command\"\n            22 = \"Aggregate\"\n        le_uint32 msg_stream_id \"Message Stream ID\" defaults(0)\n        payload chunk_data \"Chunk Data\"\n    else:\n        payload rest \"Chunk Data\"\n\n    info \"RTMP fmt=%u cs_id=%u type=%u\" fmt, cs_id, msg_type_id\n\nObject<GROUP> RTMP\n\nrule tcp.port == 1935 => RTMP\nrule tcp.content \"\\x03\\x00\\x00\\x00\\x00\\x00\\x00\\x00\" => RTMP\n\ncolor rtmp.msg_type_id == 9  => black lightblue\ncolor rtmp.msg_type_id == 8  => black lightgreen\ncolor rtmp.msg_type_id == 20 || rtmp.msg_type_id == 17 => black lightyellow\ncolor rtmp.msg_type_id == 1  => black lightgray\ncolor rtmp => white darkblue\n\nalias rtmp.audio     => rtmp.msg_type_id == 8\nalias rtmp.video     => rtmp.msg_type_id == 9\nalias rtmp.command   => rtmp.msg_type_id == 20 || rtmp.msg_type_id == 17\n",
    /* rtp.posa */
    "# rtp.posa — Real-time Transport Protocol (RFC 3550)\n#\n# RTP carries audio and video over UDP using dynamically negotiated ports.\n# The most common allocation range is 16384–32767 (even ports for media, odd\n# for the companion RTCP stream), though any ephemeral port pair may be used\n# when negotiated via SDP, SIP, or RTSP.  IANA also registers port 5004 for\n# RTP and port 5005 for RTCP as conventional defaults.\n#\n# Fixed 12-byte header, then optional CSRC list and extension:\n#\n#   Byte  0  : V(2) P(1) X(1) CC(4)   — version, padding, extension, CSRC count\n#   Byte  1  : M(1) PT(7)              — marker, payload type\n#   Bytes 2–3: sequence_number         — increments by 1 per packet sent\n#   Bytes 4–7: timestamp               — sampling instant; clock rate is PT-dependent\n#   Bytes 8–11: ssrc                   — synchronisation source identifier\n#   Bytes 12..12+CC×4–1: csrc list     — contributing source identifiers (0–15 entries)\n#   If X=1:\n#     +0..1  ext_profile               — extension profile (app-defined or IETF)\n#     +2..3  ext_length                — extension length in 32-bit words\n#     +4..   ext_data                  — ext_length × 4 bytes of extension data\n#   Remainder: media payload\n#\n# Content detection: any byte value in the range 0x80–0xBF in position 0 means\n# V=2 (bits 7–6 = 10).  The most commonly seen values are listed below; less\n# common combinations (padding+extension, CSRC counts > 4) are caught by the\n# port rules.\n\nObject<main> RTP\n    abbrev \"rtp\"\n    col \"RTP\"\n\n    # ── Byte 0: version, padding, extension, CSRC count ─────────────────────────\n    uint8 first_byte \"Flags\" defaults(0x80)\n        bits first_byte version    6 2 \"Version\"\n        bits first_byte padding    5 1 \"Padding\"\n        bits first_byte extension  4 1 \"Extension\"\n        bits first_byte csrc_count 0 4 \"CC (CSRC Count)\"\n\n    # ── Byte 1: marker bit and payload type ──────────────────────────────────────\n    uint8 second_byte \"Marker / Payload Type\" defaults(0)\n        bits second_byte marker       7 1 \"Marker\"\n        bits second_byte payload_type 0 7 \"Payload Type\"\n            PCMU (G.711 µ-law)          =  0\n            GSM                         =  3\n            G.723                       =  4\n            DVI4 (8000 Hz)              =  5\n            DVI4 (16000 Hz)             =  6\n            LPC                         =  7\n            PCMA (G.711 A-law)          =  8\n            G.722                       =  9\n            L16 (Mono)                  = 10\n            L16 (Stereo)                = 11\n            QCELP                       = 12\n            CN (Comfort Noise)          = 13\n            MPA (MPEG Audio)            = 14\n            G.728                       = 15\n            DVI4 (11025 Hz)             = 16\n            DVI4 (22050 Hz)             = 17\n            G.729                       = 18\n            CelB                        = 25\n            JPEG                        = 26\n            nv                          = 28\n            H.261                       = 31\n            MPV (MPEG-1/2 Video)        = 32\n            MP2T (MPEG-2 Transport)     = 33\n            H.263                       = 34\n            Dynamic (96)                = 96\n            Dynamic (97)                = 97\n            Dynamic (98)                = 98\n            Dynamic (99)                = 99\n            Dynamic (100)               = 100\n            Dynamic (101)               = 101\n            Dynamic (102)               = 102\n            Dynamic (103)               = 103\n            Dynamic (104)               = 104\n            Dynamic (105)               = 105\n            Dynamic (106)               = 106\n            Dynamic (107)               = 107\n            Dynamic (108)               = 108\n            Dynamic (109)               = 109\n            Dynamic (110)               = 110\n            Dynamic (111)               = 111\n            Dynamic (112)               = 112\n            Dynamic (113)               = 113\n            Dynamic (114)               = 114\n            Dynamic (115)               = 115\n            Dynamic (116)               = 116\n            Dynamic (117)               = 117\n            Dynamic (118)               = 118\n            Dynamic (119)               = 119\n            Dynamic (120)               = 120\n            Dynamic (121)               = 121\n            Dynamic (122)               = 122\n            Dynamic (123)               = 123\n            Dynamic (124)               = 124\n            Dynamic (125)               = 125\n            Dynamic (126)               = 126\n            Dynamic (127)               = 127\n\n    # ── Bytes 2–11: sequence, timestamp, SSRC ────────────────────────────────────\n    uint16 sequence_number \"Sequence Number\" defaults(0)\n    uint32 timestamp \"Timestamp\" defaults(0)\n    uint32 ssrc hex \"SSRC\" defaults(0)\n\n    # ── Bytes 12+: CSRC list (CC entries, 4 bytes each) ──────────────────────────\n    repeat csrc_count as csrc \"CSRC List\"\n        label \"CSRC 0x%x\" csrc_id\n        uint32 csrc_id hex \"CSRC\" defaults(0)\n\n    # ── Optional header extension (present when X bit = 1) ───────────────────────\n    # ext_length counts 32-bit words; actual extension data is ext_length × 4 bytes.\n    # The scope reads ext_length bytes — a known limitation when no arithmetic\n    # expression is available in the length specifier; add a manual extension decoder\n    # for profiles that pad to exact word boundaries (RFC 5285 one-byte/two-byte headers).\n    when extension == 1:\n        uint16 ext_profile hex \"Extension Profile\" defaults(0)\n        uint16 ext_length \"Extension Length (words)\" defaults(0)\n        bytes[ext_length] ext_data \"Extension Data\"\n\n    # ── Media payload ─────────────────────────────────────────────────────────────\n    # Payload type 31 is the one video format RFC 3551 assigns a static number\n    # to and that has a decoder here, so it can be dispatched without SDP.\n    # Everything else — including H.264, H.265 and H.263+, which always use a\n    # dynamic type negotiated in SDP — is left as bytes; add a branch here with\n    # the negotiated number to decode those in a given capture.\n    when payload_type == 31:\n        layer h261 H261\n    else:\n        payload media_payload \"Media Payload\"\n\n    info \"PT=%s Seq=%u SSRC=0x%x\" payload_type, sequence_number, ssrc\n\n# ── Content detection (primary — port-independent) ────────────────────────────\n# V=2 requires first_byte & 0xC0 == 0x80, giving 0x80–0xBF.  The most common\n# values are enumerated; CC=0 (0x80/0x90/0xa0) and CC=1 (0x81/0x91) cover the\n# overwhelming majority of real captures.  Port rules below catch the rest.\nweak rule udp.content \"\\x80\" => RTP\nweak rule udp.content \"\\x81\" => RTP\nweak rule udp.content \"\\x82\" => RTP\nweak rule udp.content \"\\x83\" => RTP\nweak rule udp.content \"\\x84\" => RTP\nweak rule udp.content \"\\x85\" => RTP\nweak rule udp.content \"\\x86\" => RTP\nweak rule udp.content \"\\x87\" => RTP\nweak rule udp.content \"\\x88\" => RTP\nweak rule udp.content \"\\x89\" => RTP\nweak rule udp.content \"\\x8a\" => RTP\nweak rule udp.content \"\\x8b\" => RTP\nweak rule udp.content \"\\x8c\" => RTP\nweak rule udp.content \"\\x8d\" => RTP\nweak rule udp.content \"\\x8e\" => RTP\nweak rule udp.content \"\\x8f\" => RTP\n# X=1 (with header extension), CC=0–3\nweak rule udp.content \"\\x90\" => RTP\nweak rule udp.content \"\\x91\" => RTP\nweak rule udp.content \"\\x92\" => RTP\nweak rule udp.content \"\\x93\" => RTP\n# P=1 (with padding), CC=0–1\nweak rule udp.content \"\\xa0\" => RTP\nweak rule udp.content \"\\xa1\" => RTP\n# P=1, X=1\nweak rule udp.content \"\\xb0\" => RTP\nweak rule udp.content \"\\xb1\" => RTP\n\n# ── Port bindings (secondary hint) ────────────────────────────────────────────\nrule udp.port == 5004 => RTP\nrule udp.port == 5005 => RTP\n\n# ── Coloring rules (most specific first) ──────────────────────────────────────\n# Static video payload types: JPEG, H.261, MPEG-1/2 video, MPEG-2 TS, H.263\ncolor rtp.payload_type == 26 || rtp.payload_type == 31 || rtp.payload_type == 32 || rtp.payload_type == 33 || rtp.payload_type == 34 => white darkblue\n\n# Dynamic payload types 96–127: H.264, H.265, VP8, VP9, Opus, etc. (SDP-negotiated)\ncolor rtp.payload_type >= 96 => black lightblue\n\n# Static audio payload types: PCMU, GSM, G.723, LPC, PCMA, G.722, L16, QCELP,\n# CN, MPA, G.728, G.729\ncolor rtp.payload_type == 0 || rtp.payload_type == 3 || rtp.payload_type == 4 || rtp.payload_type == 7 || rtp.payload_type == 8 || rtp.payload_type == 9 || rtp.payload_type == 10 || rtp.payload_type == 11 || rtp.payload_type == 12 || rtp.payload_type == 13 || rtp.payload_type == 14 || rtp.payload_type == 15 || rtp.payload_type == 18 => black lightgreen\n\n# Marker bit set: end of talk-spurt or last packet of a video frame boundary\ncolor rtp.marker == 1 => black lightyellow\n\n# Protocol catch-all\ncolor rtp => black lightgray\n\n# ── Wireshark-compatible field aliases ────────────────────────────────────────\n# Map Wireshark's rtp dissector field names to the abbrevs defined above so\n# that display filters written for Wireshark work without modification.\nalias rtp.version     => rtp.version\nalias rtp.padding     => rtp.padding\nalias rtp.extension   => rtp.extension\nalias rtp.cc          => rtp.csrc_count\nalias rtp.marker      => rtp.marker\nalias rtp.p_type      => rtp.payload_type\nalias rtp.seq         => rtp.sequence_number\nalias rtp.timestamp   => rtp.timestamp\nalias rtp.ssrc        => rtp.ssrc\nalias rtp.csrc.item   => rtp.csrc.csrc_id\nalias rtp.ext.profile => rtp.ext_profile\nalias rtp.ext.len     => rtp.ext_length\n",
    /* rtps.posa */
    "# rtps.posa — Real-Time Publish Subscribe Protocol (DDS / RTPS 2.x)\n#\n# RTPS (Real-Time Publish Subscribe) is the wire protocol for DDS (Data\n# Distribution Service), used extensively in robotics (ROS 2), simulation,\n# and distributed real-time systems.\n#\n# Every RTPS message starts with a 20-byte header:\n#\n#   char[4]  magic         — \"RTPS\" (0x52545053)\n#   uint8    protocol_ver_major\n#   uint8    protocol_ver_minor\n#   uint8    vendor_id[2]  — vendor identifier (e.g. 0x0101 = eProsima)\n#   uint8    guid_prefix[12] — GUID prefix of the message sender\n#\n# Followed by one or more SubMessages, each with a 4-byte subheader:\n#\n#   uint8    submsg_id     — submessage type\n#   uint8    flags         — flags byte; bit 0 = Endianness flag (E: 0=BE, 1=LE)\n#   uint16   submsg_length — octets from the END of this header to end of submessage\n#\n# Standard ports: 7400-7401 UDP (multicast discovery), 7410+ (unicast)\n#\n# Reference: OMG DDS-RTPS 2.3 specification (formal/2019-04-03)\n\nLookup RtpsSubmessageTypes\n    0x01 = \"PAD\"\n    0x06 = \"ACKNACK\"\n    0x07 = \"HEARTBEAT\"\n    0x08 = \"GAP\"\n    0x09 = \"INFO_TS\"\n    0x0B = \"INFO_SRC\"\n    0x0C = \"INFO_REPLY_IP4\"\n    0x0D = \"INFO_DST\"\n    0x0E = \"INFO_REPLY\"\n    0x12 = \"NACK_FRAG\"\n    0x13 = \"HEARTBEAT_FRAG\"\n    0x15 = \"DATA\"\n    0x16 = \"DATA_FRAG\"\n\nObject<main> RTPS\n    abbrev \"rtps\"\n    col \"RTPS\"\n    starts \"RTPS\"\n\n    str<4>  magic \"Magic\" defaults(\"RTPS\")\n    uint8   version_major \"Protocol Version (major)\" defaults(2)\n    uint8   version_minor \"Protocol Version (minor)\" defaults(1)\n    uint8   vendor_id_major hex \"Vendor ID (major)\" defaults(0x01)\n        0x01 = \"OMG\"\n    uint8   vendor_id_minor hex \"Vendor ID (minor)\" defaults(0x01)\n        0x01 = \"eProsima FastRTPS\"\n        0x02 = \"RTI Connext DDS\"\n        0x03 = \"OpenSplice DDS\"\n        0x04 = \"OpenDDS\"\n        0x05 = \"MilSoft\"\n        0x06 = \"Gallium Visual Systems\"\n        0x07 = \"TOC-CDF\"\n        0x08 = \"Marti Software\"\n        0x09 = \"PRISMTECH\"\n        0x0A = \"TWIN OAKS COMPUTING\"\n        0x0B = \"Lakota Technical Solutions\"\n        0x0C = \"ICOUP Consulting\"\n        0x0D = \"ETRI Electronics\"\n        0x0E = \"Real-Time Innovations\"\n        0x10 = \"PrismTech\"\n    bytes<12> guid_prefix \"GUID Prefix\"\n\n    # First submessage header (more may follow but are in the payload)\n    uint8   submsg_id       lookup RtpsSubmessageTypes \"Submessage ID\"     defaults(0x15)\n    uint8   submsg_flags    hex                        \"Flags\"             defaults(0x01)\n    uint16  submsg_length                              \"Submessage Length\" defaults(0)\n    payload submsg_body                                \"Submessage Body + Remaining\"\n\n    info \"RTPS v%u.%u %s len=%u\" version_major, version_minor, submsg_id, submsg_length\n\nrule udp.port == 7400 => RTPS\nrule udp.port == 7401 => RTPS\n\nrule udp.content \"RTPS\" => RTPS\n\ncolor rtps.submsg_id == 0x15 || rtps.submsg_id == 0x16 => black lightblue\ncolor rtps.submsg_id == 0x07 || rtps.submsg_id == 0x06 => black lightgreen\ncolor rtps.submsg_id == 0x09 => black lightgray\ncolor rtps => white darkblue\n\nalias dds.magic       => rtps.magic\nalias dds.version     => rtps.version_major\nalias dds.guid_prefix => rtps.guid_prefix\n",
    /* rtsp.posa */
    "# rtsp.posa — Real Time Streaming Protocol (RFC 2326)\n#\n# RTSP is an HTTP-shaped text protocol used to initiate and control real-time\n# media streams; it shares the line-based header syntax of HTTP but has its own\n# method set and version string (\"RTSP/1.0\" rather than \"HTTP/1.x\").\n#\n# Two message types share the Object<RTSP> group:\n#\n#   RTSP_RESPONSE — status line \"RTSP/1.0 <code> <reason>\\r\\n\" followed by\n#                   CRLF-terminated headers and an optional body.\n#   RTSP_REQUEST  — request line \"<METHOD> <URI> RTSP/1.0\\r\\n\" likewise.\n#\n# The group is dispatched on the first field: a response begins with the four\n# bytes \"RTSP\" (0x52545350), so RTSP_RESPONSE claims those packets; everything\n# else — any recognised method prefix — falls to RTSP_REQUEST via `starts`.\n# A `seek 0` rewinds after the magic check so the full status line is then\n# re-read as the human-readable text it actually is.\n#\n# Headers are parsed as raw CRLF-terminated lines exactly as HTTP does; the\n# blank CRLF that ends the header section is captured as a separate field so\n# the parser does not loop past the boundary into the body.\n\n# ── RTSP status codes (RFC 2326 §7.1.1, plus the HTTP codes it inherits) ─────\nLookup RtspStatusCodes\n    \"100\" = \"Continue\"\n    \"200\" = \"OK\"\n    \"201\" = \"Created\"\n    \"250\" = \"Low on Storage Space\"\n    \"300\" = \"Multiple Choices\"\n    \"301\" = \"Moved Permanently\"\n    \"302\" = \"Moved Temporarily\"\n    \"303\" = \"See Other\"\n    \"304\" = \"Not Modified\"\n    \"305\" = \"Use Proxy\"\n    \"400\" = \"Bad Request\"\n    \"401\" = \"Unauthorized\"\n    \"402\" = \"Payment Required\"\n    \"403\" = \"Forbidden\"\n    \"404\" = \"Not Found\"\n    \"405\" = \"Method Not Allowed\"\n    \"406\" = \"Not Acceptable\"\n    \"407\" = \"Proxy Authentication Required\"\n    \"408\" = \"Request Timeout\"\n    \"410\" = \"Gone\"\n    \"411\" = \"Length Required\"\n    \"412\" = \"Precondition Failed\"\n    \"413\" = \"Request Entity Too Large\"\n    \"414\" = \"Request-URI Too Long\"\n    \"415\" = \"Unsupported Media Type\"\n    \"451\" = \"Parameter Not Understood\"\n    \"452\" = \"Conference Not Found\"\n    \"453\" = \"Not Enough Bandwidth\"\n    \"454\" = \"Session Not Found\"\n    \"455\" = \"Method Not Valid in This State\"\n    \"456\" = \"Header Field Not Valid for Resource\"\n    \"457\" = \"Invalid Range\"\n    \"458\" = \"Parameter Is Read-Only\"\n    \"459\" = \"Aggregate Operation Not Allowed\"\n    \"460\" = \"Only Aggregate Operation Allowed\"\n    \"461\" = \"Unsupported Transport\"\n    \"462\" = \"Destination Unreachable\"\n    \"500\" = \"Internal Server Error\"\n    \"501\" = \"Not Implemented\"\n    \"502\" = \"Bad Gateway\"\n    \"503\" = \"Service Unavailable\"\n    \"504\" = \"Gateway Timeout\"\n    \"505\" = \"RTSP Version Not Supported\"\n    \"551\" = \"Option Not Supported\"\n\n# ── Response object ───────────────────────────────────────────────────────────\n\nObject<RTSP> RTSP_RESPONSE\n    abbrev \"rtsp\"\n    col \"RTSP\"\n\n    # Dispatch: the first four bytes of a response are \"RTSP\" = 0x52545350.\n    # After the check passes, seek 0 rewinds so the status line is read as text.\n    uint32 magic hex \"Magic\" defaults(0x52545350)\n    seek 0\n\n    string version     until \" \" \"Version\" defaults(\"RTSP/1.0\")\n    string status_code until \" \" lookup RtspStatusCodes \"Status Code\" defaults(\"200\")\n    string reason      until \"\\r\\n\" \"Reason Phrase\" defaults(\"OK\")\n\n    kvblock headers \"Headers\"\n    payload body \"Body\"\n\n    info \"%s %s\" status_code, reason\n\n# ── Request object ────────────────────────────────────────────────────────────\n\nObject<RTSP> RTSP_REQUEST\n    abbrev \"rtsp\"\n    col \"RTSP\"\n\n    # The `starts` prefixes include a trailing space so method-like tokens inside\n    # URIs or user-agent strings cannot trigger a false decode. OPTIONS is the one\n    # method whose request-URI may legally be \"*\" rather than an rtsp:// absolute\n    # URI, so only its method name (plus space) is listed; all other methods\n    # require an absolute URI and the content rules already confirm that.\n    starts \"OPTIONS \" \"DESCRIBE \" \"ANNOUNCE \" \"SETUP \" \"PLAY \" \"PAUSE \"\n           \"TEARDOWN \" \"GET_PARAMETER \" \"SET_PARAMETER \" \"RECORD \" \"REDIRECT \"\n\n    string method  until \" \" \"Method\" defaults(\"DESCRIBE\")\n    string uri     until \" \" \"Request URI\" defaults(\"rtsp://example.com/stream\")\n    string version until \"\\r\\n\" \"Version\" defaults(\"RTSP/1.0\")\n\n    kvblock headers \"Headers\"\n    payload body \"Body\"\n\n    info \"%s %s\" method, uri\n\n# ── Port rules (secondary — active only when content rules do not match) ──────\n\nrule tcp.port == 554  => RTSP\nrule tcp.port == 8554 => RTSP\n\n# ── Content rules (primary — port-independent) ────────────────────────────────\n# \"RTSP/1.0\" identifies responses; method + \"rtsp://\" avoids false positives\n# on mid-stream body bytes that happen to begin with a short word. GET_PARAMETER\n# and SET_PARAMETER use slightly longer prefixes because they share a prefix\n# with the two-word tokens above them.\n\nrule tcp.content \"RTSP/1.0\"              => RTSP\nrule tcp.content \"OPTIONS rtsp://\"       => RTSP\nrule tcp.content \"OPTIONS *\"             => RTSP\nrule tcp.content \"DESCRIBE rtsp://\"      => RTSP\nrule tcp.content \"ANNOUNCE rtsp://\"      => RTSP\nrule tcp.content \"SETUP rtsp://\"         => RTSP\nrule tcp.content \"PLAY rtsp://\"          => RTSP\nrule tcp.content \"PAUSE rtsp://\"         => RTSP\nrule tcp.content \"TEARDOWN rtsp://\"      => RTSP\nrule tcp.content \"GET_PARAMETER rtsp://\" => RTSP\nrule tcp.content \"SET_PARAMETER rtsp://\" => RTSP\nrule tcp.content \"RECORD rtsp://\"        => RTSP\nrule tcp.content \"REDIRECT rtsp://\"      => RTSP\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# Map Wireshark RTSP dissector field names to the abbrevs emitted here so that\n# display filters written for Wireshark continue to work without modification.\n\nalias rtsp.request        => rtsp.method\nalias rtsp.request.method => rtsp.method\nalias rtsp.request.uri    => rtsp.uri\nalias rtsp.response       => rtsp.status_code\nalias rtsp.response.code  => rtsp.status_code\nalias rtsp.response.phrase => rtsp.reason\n\n# Macro aliases: boolean expressions usable as a single filter term.\nalias rtsp.success        => rtsp.response.code >= 200 && rtsp.response.code < 300\nalias rtsp.error          => rtsp.response.code >= 400\n\n# ── Coloring rules (most specific first) ──────────────────────────────────────\n# RTSP 2xx — stream playing or session established; match Wireshark HTTP-200 green.\ncolor tcp.content \"RTSP/1.0 2\" => black lightgreen\n\n# RTSP 3xx — redirect; share the request palette since the client must retry.\ncolor tcp.content \"RTSP/1.0 3\" => black lightyellow\n\n# RTSP 4xx / 5xx — errors; classic alert palette.\ncolor tcp.content \"RTSP/1.0 4\" => white red\ncolor tcp.content \"RTSP/1.0 5\" => white red\n\n# RTSP requests — yellow to signal client-to-server direction.\ncolor rtsp.method => black lightyellow\n\n# Catch-all: any RTSP frame not matched above (interleaved binary data, etc.).\ncolor rtsp => white darkblue\n",
    /* s7comm.posa */
    "# s7comm.posa — Siemens S7 Communication Protocol\n# Encapsulation stack: RFC 1006 TPKT -> ISO 8073 COTP -> S7comm\n#\n# Deployed on Siemens SIMATIC PLCs: S7-300, S7-400, S7-1200, S7-1500.\n# Primary ICS/SCADA protocol for Siemens factory automation and process control.\n#\n# Layer structure:\n#   TPKT (RFC 1006)  : 4-byte transport header (version=3, length field)\n#   COTP (ISO 8073)  : ISO class-0 transport; PDU types 0xE0 CR, 0xD0 CC, 0xF0 DT\n#   S7comm           : Siemens proprietary request/response protocol (magic 0x32)\n#\n# S7comm message types:\n#   0x01 = Job Request   (PLC read/write/control commands from HMI or SCADA)\n#   0x02 = Ack           (intermediate acknowledge)\n#   0x03 = Ack-Data      (response carrying data back to the requester)\n#   0x07 = Userdata      (diagnostics, block-list, SZL reads, time sync)\n#\n# Common function codes (parameters[0] of the S7 payload):\n#   0x04 Read Variable, 0x05 Write Variable\n#   0x1A Request Download, 0x1B Download Block, 0x1C Download Ended\n#   0x1D Start Upload, 0x1E Upload, 0x1F End Upload\n#   0x28 PLC Control, 0x29 PLC Stop\n#\n# Detection:\n#   Primary:  TPKT version byte 0x03 at offset 0 (covers all ISO-TSAP traffic)\n#             COTP DT Data unit 0x02 0xF0 0x80 at offset 4 (S7comm DT frames only)\n#   Secondary: TCP port 102 (ISO-TSAP standard port)\n\nObject<main> S7comm\n    abbrev \"s7comm\"\n    col \"S7COMM\"\n\n    # ── TPKT Header (RFC 1006) — 4 bytes ─────────────────────────────────────\n    # version is always 3.  reserved is always 0.\n    # tpkt_length covers the entire PDU including this 4-byte header.\n    uint8  version \"TPKT Version\" defaults(3)\n    uint8  tpkt_reserved \"TPKT Reserved\" defaults(0)\n    uint16 tpkt_length \"TPKT Length\" defaults(0)\n\n    # ── COTP Header (ISO 8073 / RFC 905) ─────────────────────────────────────\n    # cotp_length = header byte count minus 1 (a value of 2 means 3 header bytes).\n    # pdu_type selects the COTP PDU class; only DT Data (0xF0) carries S7comm.\n    uint8  cotp_length \"COTP Header Length\" defaults(2)\n    uint8  pdu_type \"COTP PDU Type\" defaults(0xf0)\n        0xe0 = \"CR Connection Request\"\n        0xd0 = \"CC Connection Confirm\"\n        0xf0 = \"DT Data\"\n\n    # last_data_unit byte: bit 7 = end-of-transmission flag, bits 6-0 = seq number.\n    # For S7comm DT frames this byte is always 0x80 (last unit, seq 0).\n    uint8  last_data_unit \"Last Data Unit Byte\" defaults(0x80)\n    bits last_data_unit last_unit  7 1 \"Last Data Unit (EOT)\"\n    bits last_data_unit eot_seq    0 7 \"EOT Sequence Number\"\n\n    # ── S7comm Payload — present only when COTP PDU Type = DT Data (0xF0) ───\n    # The S7 protocol_id magic byte 0x32 is always the first byte of the payload.\n    # message_type selects the direction and class of the PDU.\n    # seq_num correlates Job Requests with their Ack-Data responses.\n    # data_length = byte count of the parameter block that starts at offset +10.\n    # error_length = byte count of the data block that follows the parameter block.\n    # function_code = parameters[0]; determines the operation being requested.\n    when pdu_type == 0xf0:\n        uint8  protocol_id \"S7 Protocol ID\" defaults(0x32)\n        uint8  message_type \"Message Type\" defaults(0x01)\n            0x01 = \"Job Request\"\n            0x02 = \"Ack\"\n            0x03 = \"Ack-Data\"\n            0x07 = \"Userdata\"\n        uint16 s7_reserved \"Reserved\" defaults(0)\n        uint16 seq_num \"Sequence Number\" defaults(1)\n        uint16 data_length \"Parameter Length\" defaults(0)\n        uint16 error_length \"Data Length\" defaults(0)\n        uint8  function_code \"Function Code\" defaults(0x04)\n            0x04 = \"Read Variable\"\n            0x05 = \"Write Variable\"\n            0x1a = \"Request Download\"\n            0x1b = \"Download Block\"\n            0x1c = \"Download Ended\"\n            0x1d = \"Start Upload\"\n            0x1e = \"Upload\"\n            0x1f = \"End Upload\"\n            0x28 = \"PLC Control\"\n            0x29 = \"PLC Stop\"\n        payload s7_data \"S7 Payload\"\n    else:\n        payload cotp_data \"COTP Data\"\n\n    info \"%s  %s  %s  seq=%u\" pdu_type, message_type, function_code, seq_num\n\n\n# ── Content Detection Rules (primary — port-independent) ──────────────────────\n# Listed before port rules; content rules fire first on any TCP stream.\n#\n# Rule 1: TPKT version byte 0x03 followed by reserved byte 0x00.\n#         Unique to RFC 1006 TPKT framing; covers all ISO-TSAP traffic on any port.\nweak rule tcp.content \"\\x03\\x00\" => S7comm\n\n# Rule 2: COTP DT Data PDU at offset 4 within a TPKT frame.\n#         Bytes: cotp_length=0x02, pdu_type=0xF0, last_data_unit=0x80 (EOT, seq 0).\n#         Precisely identifies S7comm data frames; avoids matching CR/CC frames only.\nrule tcp.content@4 \"\\x02\\xf0\\x80\" => S7comm\n\n# ── Port Rule (secondary — fallback hint) ─────────────────────────────────────\n# TCP 102 is the IANA-assigned port for ISO-TSAP; listed after content rules.\nrule tcp.port == 102 => S7comm\n\n\n# ── Coloring Rules ────────────────────────────────────────────────────────────\n# Most specific (highest operational priority) first.\n\n# PLC Stop — critical: halts the PLC; highest severity, white on red\ncolor s7comm.function_code == 0x29 => white red\n\n# PLC Control — high severity: start/stop sequences, mode changes\ncolor s7comm.function_code == 0x28 => black orange\n\n# Write Variable — data modification; yellow signals writes (caution)\ncolor s7comm.function_code == 0x05 => black lightyellow\n\n# Read Variable — most common operation; blue for reads (informational)\ncolor s7comm.function_code == 0x04 => black lightblue\n\n# Upload operations (reading PLC program blocks from PLC to master)\ncolor s7comm.function_code == 0x1d => black lightblue\ncolor s7comm.function_code == 0x1e => black lightblue\ncolor s7comm.function_code == 0x1f => black lightblue\n\n# Download operations (writing PLC program blocks from master to PLC)\ncolor s7comm.function_code == 0x1a => black lightyellow\ncolor s7comm.function_code == 0x1b => black lightyellow\ncolor s7comm.function_code == 0x1c => black lightyellow\n\n# COTP connection setup — green: session establishment\ncolor s7comm.pdu_type == 0xe0 => black lightgreen\ncolor s7comm.pdu_type == 0xd0 => black lightgreen\n\n# Protocol catch-all — dark blue: any S7comm frame not matched above\ncolor s7comm => white darkblue\n\n\n# ── Wireshark-Compatible Aliases ──────────────────────────────────────────────\n# Map Wireshark s7comm dissector field names to POSA abbrevs so that existing\n# display filters and coloring rules continue to work without modification.\n\nalias s7comm.pdu_type     => s7comm.pdu_type\nalias s7comm.message_type => s7comm.message_type\nalias s7comm.function     => s7comm.function_code\nalias s7comm.seq_num      => s7comm.seq_num\n",
    /* sctp.posa */
    "# SCTP common header (RFC 4960) — 12 bytes\n# IP protocol 132. Chunks follow the common header; each chunk has its own type/flags/length.\n# checksum uses CRC-32c (not IP checksum).\nObject<main> SCTP\n    uint16 sport defaults(0)\n    uint16 dport defaults(0)\n    uint32 vtag defaults(0)\n    uint32 checksum defaults(0)\n",
    /* sflow.posa */
    "# sflow.posa — sFlow Network Traffic Sampling (RFC 3176)\n#\n# sFlow is a UDP-based protocol where network devices (switches, routers) send\n# sampled packets and interface counters to a collector. Each sFlow datagram\n# starts with a common header:\n#\n#   uint32 version         — sFlow version: 2, 4, or 5\n#   uint32 agent_addr_type — 1=IPv4, 2=IPv6\n#   [ip4 or ip6 agent_addr]\n#   uint32 sub_agent_id    — agent sub-id (v5 only; zero in v2/v4)\n#   uint32 sequence_num    — datagram sequence number\n#   uint32 uptime          — milliseconds since agent boot\n#   uint32 num_samples     — number of sample records following\n#\n# sFlow sample records are XDR-encoded and variable length. Only the fixed\n# header is decoded here; the sample records need a more complex parser.\n#\n# Reference: RFC 3176 (sFlow v4), sFlow v5: https://sflow.org/sflow_version_5.txt\n# Default UDP port: 6343\n\nObject<SFLOW> SFLOW_V5\n    abbrev \"sflow\"\n    col \"sFlow\"\n\n    uint32 version \"Version\" defaults(5)\n        2 = \"sFlow v2\"\n        4 = \"sFlow v4\"\n        5 = \"sFlow v5\"\n    uint32 agent_addr_type \"Agent Address Type\" defaults(1)\n        1 = \"IPv4\"\n        2 = \"IPv6\"\n    ip4    agent_address \"Agent IPv4 Address\"\n    uint32 sub_agent_id  \"Sub-Agent ID\"       defaults(0)\n    uint32 sequence_num  \"Sequence Number\"    defaults(1)\n    uint32 uptime        \"System Uptime (ms)\" defaults(0)\n    uint32 num_samples   \"Number of Samples\"  defaults(1)\n    payload samples      \"Sample Records\"\n\n    info \"sFlow v%u from %s samples=%u\" version, agent_address, num_samples\n\nObject<SFLOW> SFLOW_V2\n    abbrev \"sflow\"\n    col \"sFlow\"\n\n    uint32 version \"Version\" defaults(2)\n        2 = \"sFlow v2\"\n        4 = \"sFlow v4\"\n    uint32 agent_addr_type \"Agent Address Type\" defaults(1)\n        1 = \"IPv4\"\n        2 = \"IPv6\"\n    ip4    agent_address \"Agent IPv4 Address\"\n    uint32 sequence_num  \"Sequence Number\"    defaults(1)\n    uint32 uptime        \"System Uptime (ms)\" defaults(0)\n    uint32 num_samples   \"Number of Samples\"  defaults(1)\n    payload samples      \"Sample Records\"\n\n    info \"sFlow v%u from %s samples=%u\" version, agent_address, num_samples\n\nObject<GROUP> SFLOW\n\nrule udp.port == 6343 => SFLOW\n\n# sFlow v5 starts with uint32 = 5, sFlow v2 with 2, v4 with 4\nrule udp.content \"\\x00\\x00\\x00\\x05\" => SFLOW\nrule udp.content \"\\x00\\x00\\x00\\x04\" => SFLOW\nrule udp.content \"\\x00\\x00\\x00\\x02\" => SFLOW\n\ncolor sflow.version == 5 => black lightblue\ncolor sflow.version == 4 => black lightgray\ncolor sflow => white darkblue\n\nalias sflow.agent => sflow.agent_address\nalias sflow.count => sflow.num_samples\n",
    /* sip.posa */
    "# sip.posa — Session Initiation Protocol (RFC 3261)\n#\n# SIP is a line-oriented text protocol sharing HTTP's \"method URI version\\r\\n\"\n# header-block body shape.  Two object variants form the SIP group:\n#\n#   SIP_RESPONSE — starts with \"SIP/\" (magic 0x5349502F); seek 0 then parse\n#                  \"SIP/2.0 STATUS REASON\\r\\n\" as text fields.\n#   SIP_REQUEST  — disambiguated by `starts` on the method keyword; first line\n#                  is \"METHOD sip:URI SIP/2.0\\r\\n\".\n#\n# Headers are captured by `kvblock headers` which parses the MIME-style\n# \"Key: Value\\r\\n...\\r\\n\\r\\n\" block into named child fields so that any\n# extension header automatically appears in the tree without pre-declaration.\n#\n# Runs over UDP and TCP; standard port 5060, TLS-wrapped variant on TCP 5061.\n# Content rules fire first so SIP on non-standard ports (e.g. 5080, 5090) is\n# still recognised.\n\n# ── SIP response status codes (RFC 3261, §21) ────────────────────────────────\n# Declared as a standalone Lookup so it can hold all 46 entries without hitting\n# the 32-enum-per-field inline limit, and so the table can be shared if other\n# objects ever need it.\nLookup SipStatusCodes\n    \"100\" = \"Trying\"\n    \"180\" = \"Ringing\"\n    \"181\" = \"Call Is Being Forwarded\"\n    \"182\" = \"Queued\"\n    \"183\" = \"Session Progress\"\n    \"200\" = \"OK\"\n    \"202\" = \"Accepted\"\n    \"301\" = \"Moved Permanently\"\n    \"302\" = \"Moved Temporarily\"\n    \"305\" = \"Use Proxy\"\n    \"380\" = \"Alternative Service\"\n    \"400\" = \"Bad Request\"\n    \"401\" = \"Unauthorized\"\n    \"403\" = \"Forbidden\"\n    \"404\" = \"Not Found\"\n    \"405\" = \"Method Not Allowed\"\n    \"406\" = \"Not Acceptable\"\n    \"407\" = \"Proxy Authentication Required\"\n    \"408\" = \"Request Timeout\"\n    \"410\" = \"Gone\"\n    \"413\" = \"Request Entity Too Large\"\n    \"414\" = \"Request-URI Too Long\"\n    \"415\" = \"Unsupported Media Type\"\n    \"420\" = \"Bad Extension\"\n    \"480\" = \"Temporarily Unavailable\"\n    \"481\" = \"Call/Transaction Does Not Exist\"\n    \"482\" = \"Loop Detected\"\n    \"483\" = \"Too Many Hops\"\n    \"484\" = \"Address Incomplete\"\n    \"485\" = \"Ambiguous\"\n    \"486\" = \"Busy Here\"\n    \"487\" = \"Request Terminated\"\n    \"488\" = \"Not Acceptable Here\"\n    \"491\" = \"Request Pending\"\n    \"494\" = \"Security Agreement Required\"\n    \"500\" = \"Internal Server Error\"\n    \"501\" = \"Not Implemented\"\n    \"502\" = \"Bad Gateway\"\n    \"503\" = \"Service Unavailable\"\n    \"504\" = \"Server Timeout\"\n    \"505\" = \"Version Not Supported\"\n    \"513\" = \"Message Too Large\"\n    \"600\" = \"Busy Everywhere\"\n    \"603\" = \"Decline\"\n    \"604\" = \"Does Not Exist Anywhere\"\n    \"606\" = \"Not Acceptable\"\n    \"607\" = \"Unwanted\"\n    \"608\" = \"Rejected\"\n    \"300\" = \"Multiple Choices\"\n    \"204\" = \"No Notification\"\n    \"412\" = \"Conditional Request Failed\"\n    \"416\" = \"Unsupported URI Scheme\"\n    \"421\" = \"Extension Required\"\n    \"422\" = \"Session Timer Too Small\"\n    \"423\" = \"Interval Too Brief\"\n    \"428\" = \"Use Identity Header\"\n    \"429\" = \"Provide Referrer Identity\"\n    \"430\" = \"Flow Failed\"\n    \"433\" = \"Anonymity Disallowed\"\n    \"436\" = \"Bad Identity-Info\"\n    \"437\" = \"Unsupported Certificate\"\n    \"438\" = \"Invalid Identity Header\"\n    \"439\" = \"First Hop Lacks Outbound Support\"\n    \"440\" = \"Max-Breadth Exceeded\"\n    \"470\" = \"Consent Needed\"\n    \"489\" = \"Bad Event\"\n    \"493\" = \"Undecipherable\"\n\nObject<SIP> SIP_RESPONSE\n    abbrev \"sip\"\n    col \"SIP\"\n\n    # The group dispatcher reads the first four bytes to tell a response\n    # (\"SIP/\") from a request (method keyword).  0x5349502F = big-endian \"SIP/\"\n    # After matching, seek 0 rewinds so the full status line can be read as text.\n    uint32 magic hex \"Magic\" defaults(0x5349502F)\n    seek 0\n\n    string version until \" \" \"SIP-Version\" defaults(\"SIP/2.0\")\n\n    string status_code until \" \" lookup SipStatusCodes \"Status-Code\" defaults(\"200\")\n\n    string reason until \"\\r\\n\" \"Reason-Phrase\" defaults(\"OK\")\n\n    kvblock headers \"Headers\"\n\n    payload body \"Message Body\"\n\n    info \"%s %s [%s]\" status_code, reason, call_id\n\nObject<SIP> SIP_REQUEST\n    abbrev \"sip\"\n    col \"SIP\"\n\n    # All fourteen RFC 3261 / RFC 3265 / RFC 3515 methods, each followed by a\n    # space to prevent false matches on words that start with a method name.\n    starts \"INVITE \" \"ACK \" \"BYE \" \"CANCEL \" \"OPTIONS \" \"REGISTER \" \"REFER \" \\\n           \"SUBSCRIBE \" \"NOTIFY \" \"INFO \" \"PRACK \" \"UPDATE \" \"MESSAGE \" \"PUBLISH \"\n\n    string method until \" \" \"Method\" defaults(\"INVITE\")\n        # String identity enums removed — these were dead code (enum_name() only\n        # does uint64 comparison and is never called for string fields).\n\n    string request_uri until \" \" \"Request-URI\" defaults(\"sip:user@example.com\")\n    string version until \"\\r\\n\" \"SIP-Version\" defaults(\"SIP/2.0\")\n\n    kvblock headers \"Headers\"\n\n    payload body \"Message Body\"\n\n    info \"%s %s [%s]\" method, request_uri, call_id\n\n# ── Content-based detection (primary — fires on ANY port) ────────────────────\n# Method + \"sip:\" is specific enough to avoid false positives.  \"SIP/2.0 \" on\n# a bare response covers servers that reply on a non-standard source port.\n# UDP rules mirror TCP so SIP over UDP is caught equally.\n\nrule tcp.content \"INVITE sip:\"    => SIP\nrule tcp.content \"REGISTER sip:\"  => SIP\nrule tcp.content \"BYE sip:\"       => SIP\nrule tcp.content \"ACK sip:\"       => SIP\nrule tcp.content \"CANCEL sip:\"    => SIP\nrule tcp.content \"OPTIONS sip:\"   => SIP\nrule tcp.content \"REFER sip:\"     => SIP\nrule tcp.content \"SUBSCRIBE sip:\" => SIP\nrule tcp.content \"NOTIFY sip:\"    => SIP\nrule tcp.content \"MESSAGE sip:\"   => SIP\nrule tcp.content \"PUBLISH sip:\"   => SIP\nrule tcp.content \"SIP/2.0 \"       => SIP\n\nrule udp.content \"INVITE sip:\"    => SIP\nrule udp.content \"REGISTER sip:\"  => SIP\nrule udp.content \"BYE sip:\"       => SIP\nrule udp.content \"ACK sip:\"       => SIP\nrule udp.content \"CANCEL sip:\"    => SIP\nrule udp.content \"OPTIONS sip:\"   => SIP\nrule udp.content \"SIP/2.0 \"       => SIP\n\n# ── Port-based detection (secondary) ─────────────────────────────────────────\n# Tried only when no content rule matched; keeps a cheap fast-path for\n# standard deployments while non-standard-port SIP is still detected above.\n\nrule udp.port == 5060 => SIP\nrule tcp.port == 5060 => SIP\nrule tcp.port == 5061 => SIP   # SIPS — SIP over TLS\n\n# ── Coloring rules ────────────────────────────────────────────────────────────\n# Ordered most-specific first (first match wins).\n\n# Session teardown and cancellation — yellow-on-red (orange) signals urgency.\ncolor sip.method == \"BYE\" || sip.method == \"CANCEL\" => yellow red\n\n# INVITE — yellow background, session-setup highlight.\ncolor sip.method == \"INVITE\" => black lightyellow\n\n# Other request methods (REGISTER, OPTIONS, REFER, …) — request tint.\ncolor sip.method => black lightyellow\n\n# 4xx / 5xx / 6xx — error palette.\ncolor sip.status_code == \"400\" || sip.status_code == \"401\" || sip.status_code == \"403\" || sip.status_code == \"404\" || sip.status_code == \"405\" || sip.status_code == \"407\" || sip.status_code == \"408\" => white red\ncolor sip.status_code == \"480\" || sip.status_code == \"481\" || sip.status_code == \"486\" || sip.status_code == \"487\" || sip.status_code == \"488\" || sip.status_code == \"491\" => white red\ncolor sip.status_code == \"500\" || sip.status_code == \"501\" || sip.status_code == \"502\" || sip.status_code == \"503\" || sip.status_code == \"504\" || sip.status_code == \"513\" => white red\ncolor sip.status_code == \"600\" || sip.status_code == \"603\" || sip.status_code == \"604\" || sip.status_code == \"606\" => white red\n\n# 2xx — success.\ncolor sip.status_code == \"200\" || sip.status_code == \"202\" => black lightgreen\n\n# 3xx — redirect.\ncolor sip.status_code == \"301\" || sip.status_code == \"302\" || sip.status_code == \"305\" || sip.status_code == \"380\" => black lightgray\n\n# 1xx — informational / provisional.\ncolor sip.status_code == \"100\" || sip.status_code == \"180\" || sip.status_code == \"181\" || sip.status_code == \"182\" || sip.status_code == \"183\" => black lightblue\n\n# Catch-all — any SIP frame not matched above (e.g. unknown status class).\ncolor sip => white darkblue\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# Map Wireshark's sip dissector field names so existing display filters work.\n\nalias sip.Method             => sip.method\nalias sip.request.method     => sip.method\nalias sip.request-line       => sip.request_uri\nalias sip.Status-Code        => sip.status_code\nalias sip.response.code      => sip.status_code\nalias sip.reason-phrase      => sip.reason\nalias sip.from               => sip.headers.from\nalias sip.to                 => sip.headers.to\nalias sip.Call-ID            => sip.headers.call_id\nalias sip.CSeq               => sip.headers.cseq\nalias sip.Via                => sip.headers.via\nalias sip.Contact            => sip.headers.contact\nalias sip.User-Agent         => sip.headers.user_agent\nalias sip.Authorization      => sip.headers.authorization\nalias sip.WWW-Authenticate   => sip.headers.www_authenticate\nalias sip.Content-Type       => sip.headers.content_type\nalias sip.Content-Length     => sip.headers.content_length\nalias sip.Max-Forwards       => sip.headers.max_forwards\nalias sip.Expires            => sip.headers.expires\nalias sip.Event              => sip.headers.event\nalias sip.Refer-To           => sip.headers.refer_to\nalias sip.Record-Route       => sip.headers.record_route\nalias sip.Route              => sip.headers.route\nalias sip.Allow              => sip.headers.allow\nalias sip.Supported          => sip.headers.supported\n\n# Convenience macro aliases for common filter expressions.\nalias sip.is_request         => sip.method\nalias sip.is_response        => sip.status_code\nalias sip.is_invite          => sip.method == \"INVITE\"\nalias sip.is_register        => sip.method == \"REGISTER\"\nalias sip.is_bye             => sip.method == \"BYE\"\nalias sip.error              => sip.status_code >= 400\nalias sip.success            => sip.status_code >= 200 && sip.status_code < 300\nalias sip.provisional        => sip.status_code >= 100 && sip.status_code < 200\n",
    /* smb.posa */
    "# smb.posa — SMB2/3 over NetBIOS (TCP 445), plus enough SMB1 to read the\n# negotiate that usually opens a session. MS-SMB2, §2.2.\n#\n# Layout of the file:\n#\n#   NBSS   the 4-byte NetBIOS framing carcal sees first on TCP/445\n#   SMB    an Object<> group: the header's magic picks SMB1 or SMB2\n#   SMB2   the 64-byte header, then a body chosen by (command, direction)\n#   SMB1   header only — enough to name the command\n#\n# Two posa constructs carry most of the weight here:\n#\n#   bits flags response 0 1     the direction lives in a header flag bit, and\n#                               every command has a different request/response\n#                               body — so the body is picked by `when command`\n#                               nested inside `when response`.\n#   seek <field>                SMB2 does not lay its variable parts out in\n#                               order: it puts an *offset from the start of the\n#                               SMB2 header* in the fixed part, and the bytes\n#                               somewhere after it. `seek` follows that offset,\n#                               which is why a share path or file name can be\n#                               shown at all.\n#\n# Not covered yet (add it here, no rebuild needed): compounded requests\n# (NextCommand chaining), the GSS-API/NTLMSSP inside the security blob, and the\n# info-level structures of QueryInfo/SetInfo. The blobs are shown as bytes.\n\n# ── NetBIOS session service ──────────────────────────────────────────────────\nObject<main> NBSS\n    abbrev \"nbss\"\n    col \"SMB\"\n\n    uint8 msg_type \"Message Type\" defaults(0x00)\n        Session message = 0x00\n        Session request = 0x81\n        Positive session response = 0x82\n        Negative session response = 0x83\n    uint24 length \"Length\" defaults(0)\n    scope length\n        layer smb SMB\n\n# ── SMB2 ─────────────────────────────────────────────────────────────────────\n# Object<SMB>: the group's members are told apart by their first field, so the\n# 0xFE534D42 / 0xFF534D42 magic selects the dialect with no code to do it.\nObject<SMB> SMB2\n    abbrev \"smb2\"\n    col \"SMB2\"\n\n    uint32 protocol_id hex \"Protocol ID\" defaults(0xFE534D42)\n    le_uint16 header_length \"Header Length\" defaults(64)\n    le_uint16 credit_charge \"Credit Charge\" defaults(1)\n    le_uint32 status \"Status\" defaults(0)\n        STATUS_SUCCESS = 0x00000000\n        STATUS_PENDING = 0x00000103\n        STATUS_NOTIFY_CLEANUP = 0x0000010b\n        STATUS_MORE_PROCESSING_REQUIRED = 0xc0000016\n        STATUS_ACCESS_DENIED = 0xc0000022\n        STATUS_OBJECT_NAME_NOT_FOUND = 0xc0000034\n        STATUS_OBJECT_NAME_COLLISION = 0xc0000035\n        STATUS_OBJECT_PATH_NOT_FOUND = 0xc000003a\n        STATUS_SHARING_VIOLATION = 0xc0000043\n        STATUS_LOGON_FAILURE = 0xc000006d\n        STATUS_BAD_NETWORK_NAME = 0xc00000cc\n        STATUS_NOT_SUPPORTED = 0xc00000bb\n        STATUS_FILE_CLOSED = 0xc0000128\n        STATUS_NO_MORE_FILES = 0x80000006\n    le_uint16 command \"Command\" defaults(0)\n        Negotiate = 0\n        Session Setup = 1\n        Logoff = 2\n        Tree Connect = 3\n        Tree Disconnect = 4\n        Create = 5\n        Close = 6\n        Flush = 7\n        Read = 8\n        Write = 9\n        Lock = 10\n        Ioctl = 11\n        Cancel = 12\n        Echo = 13\n        Query Directory = 14\n        Change Notify = 15\n        Query Info = 16\n        Set Info = 17\n        Oplock Break = 18\n    le_uint16 credits \"Credits\" defaults(1)\n    le_uint32 flags hex \"Flags\" defaults(0)\n        bits flags response 0 1 \"Direction\"\n            Request = 0\n            Response = 1\n        bits flags async 1 1 \"Async command\"\n        bits flags related 2 1 \"Related operation\"\n        bits flags signing 3 1 \"Signed\"\n    le_uint32 next_command \"Next Command\" defaults(0)\n    le_uint64 message_id \"Message ID\" defaults(0)\n    le_uint32 reserved \"Reserved\" defaults(0)\n    le_uint32 tree_id \"Tree ID\" defaults(0)\n    le_uint64 session_id hex \"Session ID\" defaults(0)\n    bytes<16> signature \"Signature\"\n\n    # ── Negotiate ────────────────────────────────────────────────────────────\n    when command == 0:\n        when response == 0:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            le_uint16 dialect_count \"Dialect Count\" defaults(1)\n            le_uint16 security_mode \"Security Mode\" defaults(1)\n                Signing enabled = 1\n                Signing required = 2\n            le_uint16 reserved2 \"Reserved\" defaults(0)\n            le_uint32 capabilities hex \"Capabilities\" defaults(0)\n            bytes<16> client_guid \"Client GUID\"\n            bytes<8> client_start_time \"Client Start Time\"\n            repeat dialect_count as dialect \"Dialects\"\n                label \"Dialect: %s\" dialect_rev\n                le_uint16 dialect_rev \"Dialect\" defaults(0x0311)\n                    SMB 2.0.2 = 0x0202\n                    SMB 2.1 = 0x0210\n                    SMB 3.0 = 0x0300\n                    SMB 3.0.2 = 0x0302\n                    SMB 3.1.1 = 0x0311\n                    SMB 2.??? = 0x02ff\n        when response == 1:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            le_uint16 security_mode \"Security Mode\" defaults(1)\n            le_uint16 dialect_rev \"Dialect\" defaults(0x0311)\n                SMB 2.0.2 = 0x0202\n                SMB 2.1 = 0x0210\n                SMB 3.0 = 0x0300\n                SMB 3.0.2 = 0x0302\n                SMB 3.1.1 = 0x0311\n            le_uint16 negotiate_context_count \"Negotiate Context Count\" defaults(0)\n            bytes<16> server_guid \"Server GUID\"\n            le_uint32 capabilities hex \"Capabilities\" defaults(0)\n            le_uint32 max_transact_size \"Max Transaction Size\" defaults(1048576)\n            le_uint32 max_read_size \"Max Read Size\" defaults(1048576)\n            le_uint32 max_write_size \"Max Write Size\" defaults(1048576)\n            bytes<8> system_time \"System Time\"\n            bytes<8> server_start_time \"Server Start Time\"\n            le_uint16 sec_offset \"Security Buffer Offset\" defaults(128)\n            le_uint16 sec_length \"Security Buffer Length\" defaults(0)\n            seek sec_offset\n            bytes[sec_length] security_blob \"Security Blob (GSS-API)\"\n\n    # ── Session Setup ────────────────────────────────────────────────────────\n    when command == 1:\n        when response == 0:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            uint8 session_flags \"Flags\" defaults(0)\n            uint8 security_mode \"Security Mode\" defaults(1)\n            le_uint32 capabilities hex \"Capabilities\" defaults(0)\n            le_uint32 channel \"Channel\" defaults(0)\n            le_uint16 sec_offset \"Security Buffer Offset\" defaults(128)\n            le_uint16 sec_length \"Security Buffer Length\" defaults(0)\n            le_uint64 previous_session_id \"Previous Session ID\" defaults(0)\n            seek sec_offset\n            bytes[sec_length] security_blob \"Security Blob (NTLMSSP/Kerberos)\"\n        when response == 1:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            le_uint16 session_flags \"Session Flags\" defaults(0)\n                Guest = 1\n                Null = 2\n                Encrypt data = 4\n            le_uint16 sec_offset \"Security Buffer Offset\" defaults(128)\n            le_uint16 sec_length \"Security Buffer Length\" defaults(0)\n            seek sec_offset\n            bytes[sec_length] security_blob \"Security Blob (NTLMSSP/Kerberos)\"\n\n    # ── Tree Connect ─────────────────────────────────────────────────────────\n    when command == 3:\n        when response == 0:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            le_uint16 reserved3 \"Reserved\" defaults(0)\n            le_uint16 path_offset \"Path Offset\" defaults(72)\n            le_uint16 path_length \"Path Length\" defaults(0)\n            seek path_offset\n            utf16[path_length] path \"Tree\"\n        when response == 1:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            uint8 share_type \"Share Type\" defaults(1)\n                Physical disk = 1\n                Named pipe = 2\n                Printer = 3\n            uint8 reserved4 \"Reserved\" defaults(0)\n            le_uint32 share_flags \"Share Flags\" defaults(0)\n            le_uint32 capabilities hex \"Capabilities\" defaults(0)\n            le_uint32 maximal_access \"Maximal Access\" defaults(0)\n\n    # ── Create ───────────────────────────────────────────────────────────────\n    when command == 5:\n        when response == 0:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            uint8 security_flags \"Security Flags\" defaults(0)\n            uint8 oplock \"Requested Oplock Level\" defaults(0)\n            le_uint32 impersonation \"Impersonation Level\" defaults(2)\n            bytes<8> create_flags \"SMB Create Flags\"\n            bytes<8> reserved5 \"Reserved\"\n            le_uint32 desired_access hex \"Desired Access\" defaults(0x00100081)\n            le_uint32 file_attributes \"File Attributes\" defaults(0)\n            le_uint32 share_access \"Share Access\" defaults(1)\n            le_uint32 create_disposition \"Create Disposition\" defaults(1)\n                Supersede = 0\n                Open = 1\n                Create = 2\n                Open if = 3\n                Overwrite = 4\n                Overwrite if = 5\n            le_uint32 create_options \"Create Options\" defaults(0)\n            le_uint16 name_offset \"Filename Offset\" defaults(120)\n            le_uint16 name_length \"Filename Length\" defaults(0)\n            le_uint32 cc_offset \"Create Contexts Offset\" defaults(0)\n            le_uint32 cc_length \"Create Contexts Length\" defaults(0)\n            seek name_offset\n            utf16[name_length] filename \"Filename\"\n        when response == 1:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            uint8 oplock \"Oplock Level\" defaults(0)\n            uint8 create_flags2 \"Flags\" defaults(0)\n            le_uint32 create_action \"Create Action\" defaults(1)\n                Superseded = 0\n                Opened = 1\n                Created = 2\n                Overwritten = 3\n            bytes<8> creation_time \"Created\"\n            bytes<8> last_access \"Last Access\"\n            bytes<8> last_write \"Last Write\"\n            bytes<8> change_time \"Last Change\"\n            le_uint64 allocation_size \"Allocation Size\" defaults(0)\n            le_uint64 end_of_file \"End of File\" defaults(0)\n            le_uint32 file_attributes \"File Attributes\" defaults(0)\n            le_uint32 reserved6 \"Reserved\" defaults(0)\n            bytes<16> file_id \"File ID\"\n\n    # ── Close ────────────────────────────────────────────────────────────────\n    when command == 6:\n        when response == 0:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            le_uint16 close_flags \"Flags\" defaults(0)\n            le_uint32 reserved7 \"Reserved\" defaults(0)\n            bytes<16> file_id \"File ID\"\n\n    # ── Read ─────────────────────────────────────────────────────────────────\n    when command == 8:\n        when response == 0:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            uint8 padding \"Padding\" defaults(0)\n            uint8 read_flags \"Flags\" defaults(0)\n            le_uint32 read_length \"Length\" defaults(0)\n            le_uint64 file_offset \"File Offset\" defaults(0)\n            bytes<16> file_id \"File ID\"\n            le_uint32 minimum_count \"Minimum Count\" defaults(0)\n        when response == 1:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            uint8 data_offset \"Data Offset\" defaults(0)\n            uint8 reserved8 \"Reserved\" defaults(0)\n            le_uint32 data_length \"Data Length\" defaults(0)\n            le_uint32 data_remaining \"Data Remaining\" defaults(0)\n            seek data_offset\n            bytes[data_length] data \"Data\"\n\n    # ── Write ────────────────────────────────────────────────────────────────\n    when command == 9:\n        when response == 0:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            le_uint16 data_offset \"Data Offset\" defaults(0)\n            le_uint32 write_length \"Length\" defaults(0)\n            le_uint64 file_offset \"File Offset\" defaults(0)\n            bytes<16> file_id \"File ID\"\n            seek data_offset\n            bytes[write_length] data \"Data\"\n        when response == 1:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            le_uint16 reserved9 \"Reserved\" defaults(0)\n            le_uint32 count \"Count\" defaults(0)\n\n    # ── Query Directory ──────────────────────────────────────────────────────\n    when command == 14:\n        when response == 0:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            uint8 info_class \"File Information Class\" defaults(0)\n            uint8 qd_flags \"Flags\" defaults(0)\n            le_uint32 file_index \"File Index\" defaults(0)\n            bytes<16> file_id \"File ID\"\n            le_uint16 name_offset \"Search Pattern Offset\" defaults(120)\n            le_uint16 name_length \"Search Pattern Length\" defaults(0)\n            le_uint32 output_length \"Output Buffer Length\" defaults(0)\n            seek name_offset\n            utf16[name_length] filename \"Search Pattern\"\n\n    # ── Query Info ───────────────────────────────────────────────────────────\n    when command == 16:\n        when response == 0:\n            le_uint16 structure_size \"Structure Size\" defaults(36)\n            uint8 info_type \"Info Type\" defaults(1)\n                File = 1\n                Filesystem = 2\n                Security = 3\n                Quota = 4\n            uint8 file_info_class \"File Info Class\" defaults(0)\n            le_uint32 output_length \"Output Buffer Length\" defaults(0)\n            le_uint16 input_offset \"Input Buffer Offset\" defaults(0)\n            le_uint16 reserved10 \"Reserved\" defaults(0)\n            le_uint32 input_length \"Input Buffer Length\" defaults(0)\n            le_uint32 additional_info \"Additional Information\" defaults(0)\n            le_uint32 qi_flags \"Flags\" defaults(0)\n            bytes<16> file_id \"File ID\"\n\n    info \"%s %s %s%s %s\" command, response, path, filename, dialect_rev\n\n# ── SMB1 — header only, so the negotiate that opens a session is readable ────\nObject<SMB> SMB1\n    abbrev \"smb\"\n    col \"SMB\"\n\n    uint32 protocol_id hex \"Protocol ID\" defaults(0xFF534D42)\n    uint8 smb_command \"Command\" defaults(0x72)\n        Create Directory   = 0x00\n        Delete Directory   = 0x01\n        Open               = 0x02\n        Create             = 0x03\n        Close              = 0x04\n        Flush              = 0x05\n        Delete             = 0x06\n        Rename             = 0x07\n        Query Information  = 0x08\n        Set Information    = 0x09\n        Read               = 0x0A\n        Write              = 0x0B\n        Lock Byte Range    = 0x0C\n        Unlock Byte Range  = 0x0D\n        Create Temp        = 0x0E\n        Check Directory    = 0x10\n        Process Exit       = 0x11\n        Seek               = 0x12\n        Lock And Read      = 0x13\n        Write And Unlock   = 0x14\n        Read Raw           = 0x1A\n        Write Raw          = 0x1D\n        Write Complete     = 0x20\n        Set Information2   = 0x22\n        Query Information2 = 0x23\n        Locking AndX       = 0x24\n        Transaction        = 0x25\n        Transaction Secondary = 0x26\n        Echo               = 0x2B\n        Open AndX          = 0x2D\n        Read AndX          = 0x2E\n        Write AndX         = 0x2F\n        Trans2             = 0x32\n        Trans2 Secondary   = 0x33\n        Find Close2        = 0x34\n        Find Notify Close  = 0x35\n        Tree Connect       = 0x70\n        Tree Disconnect    = 0x71\n        Negotiate Protocol = 0x72\n        Session Setup AndX = 0x73\n        Logoff AndX        = 0x74\n        Tree Connect AndX  = 0x75\n        Query Info Disk    = 0x80\n        Search             = 0x81\n        Find               = 0x82\n        Find Unique        = 0x83\n        Find Close         = 0x84\n        NT Transact        = 0xA0\n        NT Transact Secondary = 0xA1\n        NT Create AndX     = 0xA2\n        NT Cancel          = 0xA4\n        NT Rename          = 0xA5\n        Open Print File    = 0xC0\n        Write Print File   = 0xC1\n        Close Print File   = 0xC2\n        Get Print Queue    = 0xC3\n        No AndX Command    = 0xFF\n    le_uint32 nt_status \"NT Status\" defaults(0)\n    uint8 smb_flags \"Flags\" defaults(0x18)\n    le_uint16 smb_flags2 \"Flags2\" defaults(0xC803)\n    le_uint16 pid_high \"PID High\" defaults(0)\n    bytes<8> security_features \"Signature\"\n    le_uint16 reserved \"Reserved\" defaults(0)\n    le_uint16 tid \"Tree ID\" defaults(0)\n    le_uint16 pid \"Process ID\" defaults(0xFEFF)\n    le_uint16 uid \"User ID\" defaults(0)\n    le_uint16 mid \"Multiplex ID\" defaults(0)\n\n    info \"SMB1 %s\" smb_command\n\nrule tcp.port == 445 => NBSS\n\ncolor smb2.status > 0 => yellow red\ncolor smb2           => black lightcyan\ncolor nbss           => black lightcyan\n\n# Content signatures — SMB carries a 4-byte NetBIOS session header, then the\n# SMB magic (\\xfeSMB for SMB2/3, \\xffSMB for SMB1) at offset 4. Recognise it on\n# any port, not only 445.\nrule tcp.content@4 \"\\xfeSMB\" => NBSS\nrule tcp.content@4 \"\\xffSMB\" => NBSS\n",
    /* smpp.posa */
    "# smpp.posa — Short Message Peer-to-Peer Protocol (SMPP 3.4 / 5.0)\n#\n# SMPP is a TCP protocol used between SMS service providers and aggregators\n# for exchanging short messages (SMS). Each PDU has a fixed 16-byte header:\n#\n#   uint32_be command_length  — total length of the PDU including the header\n#   uint32_be command_id      — operation code\n#   uint32_be command_status  — status code (0 = success in request PDUs)\n#   uint32_be sequence_number — unique PDU sequence number\n#\n# Followed by the command-specific body (variable length).\n#\n# Reference: SMPP v3.4 and v5.0 specification (SMPP Developers Forum)\n# Default TCP port: 2775\n\nLookup SmppCommandIds\n    0x00000001 = \"bind_receiver\"\n    0x00000002 = \"bind_transmitter\"\n    0x00000003 = \"query_sm\"\n    0x00000004 = \"submit_sm\"\n    0x00000005 = \"deliver_sm\"\n    0x00000006 = \"unbind\"\n    0x00000007 = \"replace_sm\"\n    0x00000008 = \"cancel_sm\"\n    0x00000009 = \"bind_transceiver\"\n    0x0000000B = \"outbind\"\n    0x00000015 = \"enquire_link\"\n    0x00000021 = \"submit_multi\"\n    0x00000102 = \"alert_notification\"\n    0x00000103 = \"data_sm\"\n    0x80000001 = \"bind_receiver_resp\"\n    0x80000002 = \"bind_transmitter_resp\"\n    0x80000003 = \"query_sm_resp\"\n    0x80000004 = \"submit_sm_resp\"\n    0x80000005 = \"deliver_sm_resp\"\n    0x80000006 = \"unbind_resp\"\n    0x80000007 = \"replace_sm_resp\"\n    0x80000008 = \"cancel_sm_resp\"\n    0x80000009 = \"bind_transceiver_resp\"\n    0x80000015 = \"enquire_link_resp\"\n    0x80000021 = \"submit_multi_resp\"\n    0x80000103 = \"data_sm_resp\"\n    0x80000000 = \"generic_nack\"\n\nObject<main> SMPP\n    abbrev \"smpp\"\n    col \"SMPP\"\n\n    uint32 command_length \"Command Length\" defaults(16)\n    uint32 command_id       hex lookup SmppCommandIds \"Command ID\" defaults(0x00000009)\n    uint32 command_status   hex \"Command Status\" defaults(0)\n        0x00000000 = \"ESME_ROK (Success)\"\n        0x00000001 = \"ESME_RINVMSGLEN\"\n        0x00000002 = \"ESME_RINVCMDLEN\"\n        0x00000003 = \"ESME_RINVCMDID\"\n        0x00000004 = \"ESME_RINVBNDSTS\"\n        0x00000005 = \"ESME_RALYBND\"\n        0x00000006 = \"ESME_RINVPRTFLG\"\n        0x00000007 = \"ESME_RINVREGDLVFLG\"\n        0x00000008 = \"ESME_RSYSERR\"\n        0x0000000A = \"ESME_RINVSRCADR\"\n        0x0000000B = \"ESME_RINVDSTADR\"\n        0x0000000C = \"ESME_RINVMSGID\"\n        0x0000000D = \"ESME_RBINDFAIL\"\n        0x0000000E = \"ESME_RINVPASWD\"\n        0x0000000F = \"ESME_RINVSYSID\"\n        0x00000011 = \"ESME_RCANCELFAIL\"\n        0x00000013 = \"ESME_RREPLACEFAIL\"\n        0x00000061 = \"ESME_RMSGQFUL\"\n        0x00000063 = \"ESME_RINVSERTYP\"\n        0x00000064 = \"ESME_RINVNUMDESTS\"\n        0x00000065 = \"ESME_RINVDLNAME\"\n        0x00000073 = \"ESME_RINVDESTFLAG\"\n        0x00000075 = \"ESME_RINVSUBREP\"\n        0x00000076 = \"ESME_RINVESMCLASS\"\n        0x00000077 = \"ESME_RCNTSUBDL\"\n        0x00000078 = \"ESME_RSUBMITFAIL\"\n    uint32 sequence_number \"Sequence Number\" defaults(1)\n    payload body           \"PDU Body\"\n\n    info \"SMPP %s seq=%u\" command_id, sequence_number\n\nrule tcp.port == 2775 => SMPP\n\n# SMPP has no fixed magic; use port rule only.\n# command_length is at least 16 for the header alone; try common first byte.\n\ncolor smpp.command_id == 0x00000004 || smpp.command_id == 0x00000005 => black lightyellow\ncolor smpp.command_id == 0x80000004 || smpp.command_id == 0x80000005 => black lightgreen\ncolor smpp.command_id == 0x80000000 => white red\ncolor smpp.command_status != 0x00000000 => white red\ncolor smpp => white darkblue\n\nalias smpp.submit_sm   => smpp.command_id == 0x00000004\nalias smpp.deliver_sm  => smpp.command_id == 0x00000005\n",
    /* smtp.posa */
    "# SMTP — Simple Mail Transfer Protocol\n# RFC 5321 (relay, port 25) · RFC 6409 (submission, port 587) · RFC 8314 (implicit TLS, port 465)\n#\n# Wire format is line-oriented ASCII (CRLF-terminated):\n#   Server response (final):        NNN SP  text CRLF\n#   Server response (continuation): NNN \"-\" text CRLF\n#   Client command (with args):     VERB SP args CRLF\n#   Client command (no args):       VERB CRLF\n#\n# Detection strategy:\n#   Primary   — content rules (protocol visible on any port, including relay chains)\n#   Secondary — port rules    (fallback when first packet is not yet seen)\n\n# ── Main Protocol Object ───────────────────────────────────────────────────────\n\nObject<main> SMTP\n    abbrev \"smtp\"\n    col \"SMTP\"\n\n# ── Server Responses ──────────────────────────────────────────────────────────\n# bytes<3> reads exactly three ASCII code digits; bytes<1> captures the\n# separator — SP (final line) or \"-\" (multi-line continuation).  Both variants\n# are listed in starts so that full multi-line exchanges are captured.\n\nObject<SMTP> RESPONSE\n    abbrev \"smtp.response\"\n    starts \"220 \" \"220-\"\n           \"221 \" \"221-\"\n           \"235 \" \"235-\"\n           \"250 \" \"250-\"\n           \"251 \" \"251-\"\n           \"334 \" \"334-\"\n           \"354 \"\n           \"421 \" \"421-\"\n           \"450 \" \"450-\"\n           \"451 \" \"451-\"\n           \"452 \"\n           \"500 \" \"501 \" \"502 \" \"503 \" \"504 \"\n           \"535 \"\n           \"550 \" \"551 \" \"552 \" \"553 \" \"554 \"\n    bytes<3> code \"Code\" defaults(\"220\")\n        220 = \"Service Ready\"\n        221 = \"Service Closing Transmission Channel\"\n        235 = \"Authentication Successful\"\n        250 = \"Requested Mail Action OK\"\n        251 = \"User Not Local; Will Forward\"\n        334 = \"Server AUTH Challenge\"\n        354 = \"Start Mail Input; End With CRLF.CRLF\"\n        421 = \"Service Not Available; Closing Transmission Channel\"\n        450 = \"Mailbox Busy; Try Again Later\"\n        451 = \"Requested Action Aborted: Local Error in Processing\"\n        452 = \"Requested Action Not Taken: Insufficient System Storage\"\n        500 = \"Syntax Error: Command Unrecognized\"\n        501 = \"Syntax Error in Parameters or Arguments\"\n        502 = \"Command Not Implemented\"\n        503 = \"Bad Sequence of Commands\"\n        504 = \"Command Parameter Not Implemented\"\n        535 = \"Authentication Credentials Invalid\"\n        550 = \"Requested Action Not Taken: Mailbox Unavailable\"\n        551 = \"User Not Local; Please Try Forwarding\"\n        552 = \"Requested Mail Action Aborted: Exceeded Storage Allocation\"\n        553 = \"Requested Action Not Taken: Mailbox Name Not Allowed\"\n        554 = \"Transaction Failed; No SMTP Service Here\"\n    bytes<1> separator \"Sep\" defaults(\" \")\n    string message until \"\\r\\n\" \"Message\" defaults(\"example.com ESMTP\")\n    info \"%s %s\" code, message\n\n# ── Client Commands ────────────────────────────────────────────────────────────\n# string verb until \" \" stops at the first space.  For no-argument commands\n# (DATA, QUIT, RSET, NOOP, STARTTLS) there is no space — the parser reaches\n# CRLF and the args field is absent; info collapses the gap automatically.\n\nObject<SMTP> COMMAND\n    abbrev \"smtp.command\"\n    starts \"EHLO \" \"HELO \"\n           \"MAIL \" \"RCPT \"\n           \"DATA\\r\" \"QUIT\\r\" \"RSET\\r\"\n           \"VRFY \" \"EXPN \"\n           \"NOOP\\r\" \"NOOP \"\n           \"AUTH \"\n           \"STARTTLS\\r\"\n    string verb until \" \" \"Command\" defaults(\"EHLO\")\n        EHLO     = \"Extended Hello (ESMTP Negotiation)\"\n        HELO     = \"Hello (Basic SMTP)\"\n        MAIL     = \"Mail (Begin Transfer)\"\n        RCPT     = \"Recipient\"\n        DATA     = \"Data (Begin Message Body)\"\n        QUIT     = \"Quit\"\n        RSET     = \"Reset (Abort Transaction)\"\n        VRFY     = \"Verify Address\"\n        EXPN     = \"Expand Mailing List\"\n        NOOP     = \"No Operation\"\n        AUTH     = \"Authenticate (SASL)\"\n        STARTTLS = \"Start TLS Negotiation\"\n    string args until \"\\r\\n\" \"Arguments\" defaults(\"example.com\")\n    info \"%s %s\" verb, args\n\n# ── Content Detection Rules (primary) ─────────────────────────────────────────\n# Ordered by specificity; server greeting and envelope commands are the most\n# unambiguous signals.  Port rules below serve only as secondary hints.\n\nrule tcp.content \"220 \"       => SMTP\nrule tcp.content \"EHLO \"      => SMTP\nrule tcp.content \"HELO \"      => SMTP\nrule tcp.content \"MAIL FROM:\" => SMTP\nrule tcp.content \"RCPT TO:\"   => SMTP\nrule tcp.content \"250 OK\"     => SMTP\nrule tcp.content \"354 \"       => SMTP\n\n# ── Port Binding Rules (secondary) ────────────────────────────────────────────\n\nrule tcp.port == 25  => SMTP\nrule tcp.port == 587 => SMTP\nrule tcp.port == 465 => SMTP\n\n# ── Coloring Rules ────────────────────────────────────────────────────────────\n# Most-specific first.  Security events (auth failure, permanent error) take\n# priority over category bands; AUTH exchange uses orange throughout so the\n# full challenge/response pair is visible as a unit.\n\n# AUTH failure — red (security event, higher priority than generic 5xx band)\ncolor smtp.response.code == \"535\" => white red\n\n# Permanent failures 5xx — red\ncolor smtp.response.code == \"500\" or smtp.response.code == \"501\" or smtp.response.code == \"502\" or smtp.response.code == \"503\" or smtp.response.code == \"504\" => white red\ncolor smtp.response.code == \"550\" or smtp.response.code == \"551\" or smtp.response.code == \"552\" or smtp.response.code == \"553\" or smtp.response.code == \"554\" => white red\n\n# Temporary failures 4xx — yellow/amber\ncolor smtp.response.code == \"421\" or smtp.response.code == \"450\" or smtp.response.code == \"451\" or smtp.response.code == \"452\" => black lightyellow\n\n# AUTH exchange — orange (server challenge and client AUTH command paired)\ncolor smtp.response.code == \"334\" => black orange\ncolor smtp.command.verb == \"AUTH\" => black orange\n\n# STARTTLS — dark blue (encryption transition)\ncolor smtp.command.verb == \"STARTTLS\" => white darkblue\n\n# DATA boundary — cyan (marks envelope-to-body transition)\ncolor smtp.response.code == \"354\" => black lightcyan\n\n# Success responses 2xx — green\ncolor smtp.response.code == \"220\" or smtp.response.code == \"221\" or smtp.response.code == \"235\" or smtp.response.code == \"250\" or smtp.response.code == \"251\" => black lightgreen\n\n# Client commands (general) — light yellow\ncolor smtp.command => black lightyellow\n\n# Catch-all for any SMTP traffic not matched above\ncolor smtp => white darkgray\n\n# ── Wireshark Compatibility Aliases ───────────────────────────────────────────\n# Maps Wireshark smtp dissector field names to POSA equivalents so that\n# existing display filters and column expressions continue to work.\n\nalias smtp.code               => smtp.response.code\nalias smtp.response.parameter => smtp.response.message\n\nalias smtp.req.command        => smtp.command.verb\nalias smtp.req.parameter      => smtp.command.args\n\n# Envelope address convenience aliases.\n# args holds the raw parameter string (\"FROM:<addr>\" or \"TO:<addr>\"); these\n# aliases let analysts write smtp.sender or smtp.recipient in filters without\n# knowing the internal field path.\nalias smtp.sender    => smtp.command.args\nalias smtp.recipient => smtp.command.args\n",
    /* snap.posa */
    "# SNAP header (RFC 1042) — 5 bytes\n# Follows an LLC header with DSAP=SSAP=0xAA. oui=0x000000 means the etype field is\n# interpreted as a standard IEEE EtherType (same values as Ethernet II).\nObject<main> SNAP\n    bytes<3> oui\n    uint16   etype defaults(0x0800)\n        IPV4       = 0x0800\n        IPV6       = 0x86DD\n        ARP        = 0x0806\n        APPLETALK  = 0x809B\n        IPX        = 0x8137\n        MPLS       = 0x8847\n",
    /* snmp.posa */
    "# snmp.posa — SNMP Simple Network Management Protocol\n# RFC 1157 (SNMPv1), RFC 1901 / RFC 3416 (SNMPv2c), RFC 3412 (SNMPv3)\n#\n# Encoding: BER/ASN.1 over UDP.  The outer wrapper is always a SEQUENCE\n# (universal tag 0x30), so byte 0 is a reliable content signature.\n#\n# Detection: byte 0 == 0x30 (primary, catches non-standard ports); UDP 161\n# (agent) and UDP 162 (trap manager) are secondary disambiguation rules.\n#\n# Simplified BER decode strategy:\n#   outer SEQUENCE tag/length\n#   → version INTEGER  (tag 0x02, len 0x01, value 0/1/3)\n#   → v1/v2c: community OCTET STRING (tag 0x04, len, string)\n#             PDU type (application-class tag 0xA0–0xA8)\n#             PDU length\n#             request-id INTEGER  (variable 1–4 bytes)\n#             error-status INTEGER\n#             error-index INTEGER\n#             VarBindList payload\n#   → v3:    msgGlobalData SEQUENCE + security params + msgData; captured as\n#             a single payload because the inner structure differs significantly.\n#\n# Missing fields in the info line (v3 path) expand to nothing automatically.\n\nObject<main> SNMP\n    abbrev \"snmp\"\n    col \"SNMP\"\n\n    # ── Outer BER SEQUENCE ────────────────────────────────────────────────────\n    uint8 sequence_tag defaults(0x30)\n        0x30 = \"SEQUENCE\"\n\n    uint8 sequence_length \"Sequence Length\" defaults(0)\n\n    # ── version INTEGER ───────────────────────────────────────────────────────\n    uint8 version_tag defaults(0x02)\n        0x02 = \"INTEGER\"\n\n    uint8 version_tag_length \"Version Length\" defaults(1)\n\n    uint8 version \"Version\" defaults(1)\n        0 = \"SNMPv1\"\n        1 = \"SNMPv2c\"\n        3 = \"SNMPv3\"\n\n    # ── Version-specific structure ─────────────────────────────────────────────\n    when version == 3:\n        # SNMPv3: msgGlobalData SEQUENCE + msgSecurityParameters OCTET STRING\n        # + msgData CHOICE follow — too many nested layers for a flat decode.\n        payload v3_message \"SNMPv3 Message\"\n    else:\n        # SNMPv1 / SNMPv2c: community OCTET STRING (tag 0x04)\n        uint8 community_tag defaults(0x04)\n            0x04 = \"OCTET STRING\"\n\n        uint8 community_length \"Community Length\" defaults(6)\n\n        str[community_length] community_string \"Community String\" defaults(\"public\")\n\n        # PDU type — context-class constructed tag (0xA0–0xA8)\n        uint8 pdu_type \"PDU Type\" defaults(0xA0)\n            0xA0 = \"GetRequest\"\n            0xA1 = \"GetNextRequest\"\n            0xA2 = \"Response\"\n            0xA3 = \"SetRequest\"\n            0xA4 = \"Trap\"\n            0xA5 = \"GetBulkRequest\"\n            0xA6 = \"InformRequest\"\n            0xA7 = \"SNMPv2-Trap\"\n            0xA8 = \"Report\"\n\n        uint8 pdu_length \"PDU Length\" defaults(0)\n\n        # request-id INTEGER (tag 0x02, variable length 1–4 bytes)\n        uint8 req_id_tag defaults(0x02)\n            0x02 = \"INTEGER\"\n\n        uint8 req_id_length \"Request-ID Length\" defaults(4)\n\n        bytes[req_id_length] request_id \"Request-ID\"\n\n        # error-status INTEGER\n        uint8 error_status_tag defaults(0x02)\n            0x02 = \"INTEGER\"\n\n        uint8 error_status_length \"Error-Status Length\" defaults(1)\n\n        uint8 error_status \"Error-Status\" defaults(0)\n            0  = \"noError\"\n            1  = \"tooBig\"\n            2  = \"noSuchName\"\n            3  = \"badValue\"\n            4  = \"readOnly\"\n            5  = \"genErr\"\n            6  = \"noAccess\"\n            7  = \"wrongType\"\n            8  = \"wrongLength\"\n            9  = \"wrongEncoding\"\n            10 = \"wrongValue\"\n            11 = \"noCreation\"\n            12 = \"inconsistentValue\"\n            13 = \"resourceUnavailable\"\n            14 = \"commitFailed\"\n            15 = \"undoFailed\"\n            16 = \"authorizationError\"\n            17 = \"notWritable\"\n            18 = \"inconsistentName\"\n\n        # error-index INTEGER\n        uint8 error_index_tag defaults(0x02)\n            0x02 = \"INTEGER\"\n\n        uint8 error_index_length \"Error-Index Length\" defaults(1)\n\n        uint8 error_index \"Error-Index\" defaults(0)\n\n        # Remaining bytes: VarBindList SEQUENCE OF VarBind\n        payload varbinds \"Variable Bindings\"\n\n    info \"%s [%s] community=%s err=%s\" version, pdu_type, community_string, error_status\n\n# ── Detection rules ───────────────────────────────────────────────────────────\n# Content rule first: SEQUENCE tag 0x30 at byte 0 catches non-standard ports.\n# Note: 0x30 is also used by TLS, LDAP, and other BER protocols — the port\n# rules below narrow the match when multiple BER protocols are on the wire.\nweak rule udp.content@0 \"\\x30\" => SNMP\nrule udp.port == 161 => SNMP\nrule udp.port == 162 => SNMP\n\n# ── Coloring ──────────────────────────────────────────────────────────────────\n# First match wins; severity rules come before direction rules.\ncolor snmp.error_status != 0  => white red          # Application-level error\ncolor snmp.pdu_type == 0xA4   => black yellow       # v1 Trap (alert)\ncolor snmp.pdu_type == 0xA7   => black yellow       # SNMPv2-Trap (alert)\ncolor snmp.pdu_type == 0xA8   => white red          # Report (v3 error / alert)\ncolor snmp.pdu_type == 0xA2   => black lightgreen   # Response\ncolor snmp.pdu_type == 0xA0   => black lightyellow  # GetRequest\ncolor snmp.pdu_type == 0xA1   => black lightyellow  # GetNextRequest\ncolor snmp.pdu_type == 0xA3   => black lightyellow  # SetRequest\ncolor snmp.pdu_type == 0xA5   => black lightyellow  # GetBulkRequest\ncolor snmp.pdu_type == 0xA6   => black lightyellow  # InformRequest\ncolor snmp                    => white darkblue      # SNMP catch-all (v3 + uncolored)\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\nalias snmp.version       => snmp.version\nalias snmp.community     => snmp.community_string\nalias snmp.pdu_type      => snmp.pdu_type\nalias snmp.error_status  => snmp.error_status\nalias snmp.error_index   => snmp.error_index\nalias snmp.request_id    => snmp.request_id\n",
    /* socks5.posa */
    "# SOCKS5 (RFC 1928) — client/server handshake and CONNECT request\n# Default port 1080. Used by proxies, SOCKS tunnels, EarthWorm C2 pivots.\n# Three message types:\n#   SOCKS5_HELLO   — client auth method negotiation (3 bytes)\n#   SOCKS5_SERVER  — server selected method (2 bytes)\n#   SOCKS5_CONNECT — client CONNECT request with IPv4 dst (10 bytes)\n#   SOCKS5_REPLY   — server response with IPv4 bind addr (10 bytes)\nObject<main> SOCKS5_HELLO\n    uint8 version defaults(5)\n    uint8 nmethods defaults(1)\n    uint8 method defaults(0x00)\n        NO_AUTH    = 0x00\n        GSSAPI     = 0x01\n        USERPASS   = 0x02\n        NO_METHODS = 0xFF\n\nObject<main> SOCKS5_SERVER\n    uint8 version defaults(5)\n    uint8 method defaults(0x00)\n        NO_AUTH    = 0x00\n        GSSAPI     = 0x01\n        USERPASS   = 0x02\n        NO_METHODS = 0xFF\n\nObject<main> SOCKS5_CONNECT\n    uint8  version defaults(5)\n    uint8  cmd defaults(1)\n        CONNECT   = 1\n        BIND      = 2\n        UDP_ASSOC = 3\n    uint8  rsv defaults(0)\n    uint8  atyp defaults(1)\n        IPv4       = 1\n        DOMAINNAME = 3\n        IPv6       = 4\n    ip4    dst_addr defaults(0.0.0.0)\n    uint16 dst_port defaults(0)\n\nObject<main> SOCKS5_REPLY\n    uint8  version defaults(5)\n    uint8  rep defaults(0)\n        SUCCESS              = 0\n        GENERAL_FAILURE      = 1\n        CONN_NOT_ALLOWED     = 2\n        NETWORK_UNREACHABLE  = 3\n        HOST_UNREACHABLE     = 4\n        CONN_REFUSED         = 5\n        TTL_EXPIRED          = 6\n        CMD_NOT_SUPPORTED    = 7\n        ATYP_NOT_SUPPORTED   = 8\n    uint8  rsv defaults(0)\n    uint8  atyp defaults(1)\n    ip4    bnd_addr defaults(0.0.0.0)\n    uint16 bnd_port defaults(0)\n",
    /* ssdp.posa */
    "# ssdp.posa — Simple Service Discovery Protocol (UPnP, IETF draft / RFC 2616-style)\n#\n# SSDP is a text protocol modelled on HTTP, carried over UDP (almost always)\n# and occasionally over TCP.  Three message shapes share the wire:\n#\n#   SSDP_RESPONSE — unicast reply to an M-SEARCH; starts with \"HTTP/1.1 200 OK\\r\\n\".\n#                   Dispatched by the group on the four-byte \"HTTP\" magic (0x48545450),\n#                   then seek 0 rewinds so the full status line is read as text.\n#\n#   SSDP_SEARCH   — client discovery broadcast; starts with \"M-SEARCH * HTTP/1.1\\r\\n\".\n#                   Dispatched by `starts \"M-SEARCH \"`.\n#\n#   SSDP_NOTIFY   — device advertisement; starts with \"NOTIFY * HTTP/1.1\\r\\n\".\n#                   Carries NTS: ssdp:alive (device up) or ssdp:byebye (device down).\n#                   Dispatched by `starts \"NOTIFY \"`.\n#\n# Key headers (MAN, ST, NT, NTS, LOCATION, USN, SERVER, …) are declared as named\n# string fields BEFORE the catch-all repeat block; the text-protocol-aware runtime\n# extracts them by matching the declared label against \"Header-Name: value\\r\\n\" lines.\n# Remaining or unrecognised headers accumulate in the generic repeat.\n#\n# Standard transport: UDP port 1900, multicast group 239.255.255.250.\n# Content rules fire first so SSDP on non-standard ports is still recognised.\n\nObject<GROUP> SSDP\n\n# ── HTTP/1.1 200 OK: unicast reply to M-SEARCH ───────────────────────────────\n#\n# Device responds directly to the querying host; contains the same headers as\n# ssdp:alive but is a unicast HTTP response rather than a multicast NOTIFY.\n\nObject<SSDP> SSDP_RESPONSE\n    abbrev \"ssdp\"\n    col \"SSDP\"\n\n    # The group dispatcher reads the first four bytes to distinguish a response\n    # (\"HTTP\") from the request types above.  0x48545450 = big-endian \"HTTP\".\n    # seek 0 rewinds so the full status line is re-read as human-readable text.\n    uint32 magic hex \"Magic\" defaults(0x48545450)\n    seek 0\n\n    string version     until \" \" \"Version\" defaults(\"HTTP/1.1\")\n    string status_code until \" \" \"Status-Code\" defaults(\"200\")\n    string reason      until \"\\r\\n\" \"Reason-Phrase\" defaults(\"OK\")\n\n    kvblock headers \"Headers\"\n\n    payload body \"Message Body\"\n\n    info \"%s %s  ST: %s\" status_code, reason, st\n\n# ── M-SEARCH: client broadcasts a discovery request ──────────────────────────\n#\n# Sent to the multicast group 239.255.255.250:1900; all devices on the local\n# link receive it and respond if the ST header matches their type or USN.\n\nObject<SSDP> SSDP_SEARCH\n    abbrev \"ssdp\"\n    col \"SSDP\"\n\n    # Trailing space prevents a false match on \"M-SEARCH\" appearing inside a body\n    # or a URI path of an unrelated protocol.\n    starts \"M-SEARCH \"\n\n    string method         until \" \" \"Method\" defaults(\"M-SEARCH\")\n    string request_target until \" \" \"Request-Target\" defaults(\"*\")\n    string version        until \"\\r\\n\" \"Version\" defaults(\"HTTP/1.1\")\n\n    kvblock headers \"Headers\"\n\n    payload body \"Message Body\"\n\n    info \"M-SEARCH  ST: %s\" st\n\n# ── NOTIFY: device announces presence or departure ────────────────────────────\n#\n# Sent to the multicast group; NTS header distinguishes alive from byebye.\n# ssdp:alive — device is up and advertising its services.\n# ssdp:byebye — device is shutting down; listeners should remove its entries.\n\nObject<SSDP> SSDP_NOTIFY\n    abbrev \"ssdp\"\n    col \"SSDP\"\n\n    # Trailing space prevents a false match on \"NOTIFY\" inside a SIP NOTIFY body\n    # or any other mid-stream text that happens to start with the word.\n    starts \"NOTIFY \"\n\n    string method         until \" \" \"Method\" defaults(\"NOTIFY\")\n    string request_target until \" \" \"Request-Target\" defaults(\"*\")\n    string version        until \"\\r\\n\" \"Version\" defaults(\"HTTP/1.1\")\n\n    kvblock headers \"Headers\"\n\n    payload body \"Message Body\"\n\n    info \"NOTIFY  NTS: %s  NT: %s\" nts, nt\n\n# ── Content detection (primary — catches SSDP on any port) ───────────────────\n# UDP rules cover the multicast channel; the TCP rule catches tunnelled or\n# proxied M-SEARCH traffic on non-standard ports.  Content rules always fire\n# before port rules so the protocol is recognised even on ephemeral ports.\n\nrule udp.content \"M-SEARCH * HTTP\" => SSDP\nrule udp.content \"NOTIFY * HTTP\"   => SSDP\nrule udp.content \"HTTP/1.1 200 OK\" => SSDP\nrule tcp.content \"M-SEARCH * HTTP\" => SSDP\n\n# ── Port binding (secondary — hint when content rules did not match) ──────────\nrule udp.port == 1900 => SSDP\n\n# ── Coloring rules (most specific first; first match wins) ────────────────────\n\n# ssdp:byebye — device leaving the network; alert palette signals the event.\ncolor ssdp.nts == \"ssdp:byebye\" => white red\n\n# ssdp:alive — device announcing presence; green signals a healthy advertisement.\ncolor ssdp.nts == \"ssdp:alive\" => black lightgreen\n\n# M-SEARCH discovery requests — yellow to signal client-originated direction.\ncolor ssdp.method == \"M-SEARCH\" => black lightyellow\n\n# 200 OK unicast responses — blue for informational server replies.\ncolor ssdp.status_code == \"200\" => black lightblue\n\n# Catch-all: any SSDP frame not matched above (unrecognised NTS, etc.).\ncolor ssdp => white darkblue\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# Map short header-name forms and Wireshark field-naming conventions to the\n# descriptive POSA field names so that existing display filters continue to work.\n\nalias ssdp.st            => ssdp.headers.st\nalias ssdp.nt            => ssdp.headers.nt\nalias ssdp.location      => ssdp.headers.location\nalias ssdp.nts           => ssdp.headers.nts\nalias ssdp.usn           => ssdp.headers.usn\nalias ssdp.server        => ssdp.headers.server\nalias ssdp.man           => ssdp.headers.man\nalias ssdp.mx            => ssdp.headers.mx\nalias ssdp.cache.control => ssdp.headers.cache_control\nalias ssdp.search_target => ssdp.headers.st\nalias ssdp.notification_type => ssdp.headers.nt\n\n# Convenience macro aliases for common filter expressions.\nalias ssdp.is_search  => ssdp.method == \"M-SEARCH\"\nalias ssdp.is_notify  => ssdp.method == \"NOTIFY\"\nalias ssdp.is_alive   => ssdp.nts == \"ssdp:alive\"\nalias ssdp.is_byebye  => ssdp.nts == \"ssdp:byebye\"\n",
    /* ssh.posa */
    "# SSH (Secure Shell, RFC 4253) — banner exchange and binary packet framing\n\nObject<main> SSH\n    abbrev \"ssh\"\n    col \"SSH\"\n    seek 4\n    string version until \"-\" [\"Protocol Version\"] defaults(\"SSH\")\n    string software until \"\\r\\n\" [\"Software Version\"] defaults(\"2.0-OpenSSH_9.6\")\n    info \"%s %s\" version, software\n\nObject SSH_Packet\n    abbrev \"ssh.pkt\"\n    col \"SSH\"\n    uint32 packet_length [\"Packet Length\"] defaults(0)\n    uint8  padding_length [\"Padding Length\"] defaults(0)\n    uint8  msg_type [\"Message Type\"] defaults(20)\n        DISCONNECT           = 1\n        IGNORE               = 2\n        UNIMPLEMENTED        = 3\n        DEBUG                = 4\n        SERVICE_REQUEST      = 5\n        SERVICE_ACCEPT       = 6\n        KEXINIT              = 20\n        NEWKEYS              = 21\n        KEXECDH_INIT         = 30\n        KEXECDH_REPLY        = 31\n        USERAUTH_REQUEST     = 50\n        USERAUTH_FAILURE     = 51\n        USERAUTH_SUCCESS     = 52\n        USERAUTH_PK_OK       = 60\n        CHANNEL_OPEN         = 80\n        CHANNEL_OPEN_CONFIRM = 81\n        CHANNEL_OPEN_FAILURE = 82\n        CHANNEL_DATA         = 90\n        CHANNEL_REQUEST      = 96\n        CHANNEL_SUCCESS      = 97\n    payload [\"Payload\"]\n    info \"%s\" msg_type\n\nrule tcp.content@0 \"SSH-\" => SSH\nrule tcp.port == 22       => SSH\n\ncolor ssh     => black lightyellow\ncolor ssh.pkt => white darkblue\n\nalias ssh.version  => ssh.version\nalias ssh.software => ssh.software\n",
    /* steam.posa */
    "# steam.posa — Steam Protocol (Discovery + In-Home Streaming)\n#\n# Steam uses several UDP and TCP protocols. The most reliably structured\n# for detection are:\n#\n# 1) Steam In-Home Streaming (IHS) Discovery — UDP broadcast, port 27036:\n#    Client sends a discovery query; servers respond with streaming capability.\n#    Starts with a fixed 8-byte LE magic: FF FF FF FF 21 4C 5F A0\n#    (uint64_le 0xA05F4C21FFFFFFFF).\n#    Followed by a uint32_le message_type and uint32_le payload_length,\n#    then a protobuf-encoded payload.\n#\n# 2) Steam Friends / Community (CM) — TCP, port 27017/27036/443/27015:\n#    Fixed header: uint32_le payload_length + uint32_le magic (0x31305456 = \"VT01\")\n#    Followed by a GZip-compressed protobuf payload.\n#    This is also used for the Steam client-to-CM server channel.\n#\n# 3) Steam Datagram Relay (SDR) / GameNetworkingSockets — UDP:\n#    Variable structure; first byte is the connection type discriminator.\n#    Not statically decodable without context.\n#\n# Reference: https://github.com/nickcoutsos/steamcontroller/wiki/Steam-IHS-Discovery\n#            https://github.com/ValveSoftware/GameNetworkingSockets\n#\n# Ports: UDP 27036 (IHS discovery), TCP 27017/27036 (CM), UDP/TCP 27015 (game server queries)\n\n# ── In-Home Streaming Discovery ───────────────────────────────────────────────\nObject<STEAM> STEAM_IHS_DISCOVERY\n    abbrev \"steam\"\n    col \"Steam\"\n\n    bytes<4> magic1 hex \"Magic1 (0xFFFFFFFF)\" defaults(\"\\xff\\xff\\xff\\xff\")\n    bytes<4> magic2 hex \"Magic2 (0xA05F4C21)\" defaults(\"\\xa0\\x5f\\x4c\\x21\")\n    le_uint32 message_type \"Message Type\" defaults(1)\n        1 = \"Discovery Request\"\n        2 = \"Discovery Response\"\n        3 = \"Streaming Request\"\n        4 = \"Streaming Response\"\n    le_uint32 payload_length \"Payload Length\" defaults(0)\n    bytes[payload_length] body \"Protobuf Payload\"\n\n    info \"Steam IHS type=%u len=%u\" message_type, payload_length\n\n# ── Steam CM (Community Manager) ─────────────────────────────────────────────\nObject<STEAM> STEAM_CM\n    abbrev \"steam\"\n    col \"Steam\"\n\n    le_uint32 payload_length \"Payload Length\" defaults(0)\n    le_uint32 magic hex \"Magic\" defaults(0x31305456)\n        0x31305456 = \"VT01 (Steam CM)\"\n        0x564E4F51 = \"QNOV (GC)\"\n    bytes[payload_length] body \"Protobuf Payload\"\n\n    info \"Steam CM magic=0x%08x len=%u\" magic, payload_length\n\nObject<GROUP> STEAM\n\nrule udp.port == 27036 => STEAM\nrule tcp.port == 27036 => STEAM\nrule tcp.port == 27017 => STEAM\n\n# IHS magic: \\xFF\\xFF\\xFF\\xFF\\x21\\x4C\\x5F\\xA0\nrule udp.content \"\\xff\\xff\\xff\\xff\\x21\\x4c\\x5f\\xa0\" => STEAM\n\n# Steam CM magic \"VT01\" at offset 4 is not directly expressible as a content rule,\n# but the 4-byte zero payload_length check is not reliable; use port rule only for CM.\n\ncolor steam.message_type == 1 => black lightyellow\ncolor steam.message_type == 2 => black lightgreen\ncolor steam.magic == 0x31305456 => black lightblue\ncolor steam => white darkblue\n\nalias steam.ihs    => steam.message_type\nalias steam.cm     => steam.magic\n",
    /* stomp.posa */
    "# stomp.posa — Simple (or Streaming) Text Oriented Messaging Protocol\n#\n# STOMP is a text-framed protocol modelled on HTTP. A frame is:\n#   COMMAND\\n\n#   header1:value1\\n\n#   header2:value2\\n\n#   \\n\n#   body bytes\n#   \\0\n#\n# Client commands: STOMP, CONNECT, SEND, SUBSCRIBE, UNSUBSCRIBE, ACK, NACK,\n#                  BEGIN, COMMIT, ABORT, DISCONNECT.\n# Server frames:   CONNECTED, MESSAGE, RECEIPT, ERROR.\n#\n# Reference: https://stomp.github.io/stomp-specification-1.2.html\n#\n# Runs on TCP port 61613 (ActiveMQ default), 61614 (WebSocket variant).\n\nObject<STOMP> STOMP_CLIENT\n    abbrev \"stomp\"\n    col \"STOMP\"\n    starts \"STOMP\\n\" \"CONNECT\\n\" \"SEND\\n\" \"SUBSCRIBE\\n\" \"UNSUBSCRIBE\\n\"\n           \"ACK\\n\" \"NACK\\n\" \"BEGIN\\n\" \"COMMIT\\n\" \"ABORT\\n\" \"DISCONNECT\\n\"\n\n    string command until \"\\n\" \"Command\" defaults(\"CONNECT\")\n    kvblock headers sep \":\" \"Headers\"\n    payload body \"Body\"\n\n    info \"%s\" command\n\nObject<STOMP> STOMP_SERVER\n    abbrev \"stomp\"\n    col \"STOMP\"\n    starts \"CONNECTED\\n\" \"MESSAGE\\n\" \"RECEIPT\\n\" \"ERROR\\n\"\n\n    string command until \"\\n\" \"Command\" defaults(\"CONNECTED\")\n    kvblock headers sep \":\" \"Headers\"\n    payload body \"Body\"\n\n    info \"%s\" command\n\nObject<GROUP> STOMP\n\nrule tcp.port == 61613 => STOMP\nrule tcp.port == 61614 => STOMP\n\nrule tcp.content \"STOMP\\n\"     => STOMP\nrule tcp.content \"CONNECT\\n\"   => STOMP\nrule tcp.content \"CONNECTED\\n\" => STOMP\nrule tcp.content \"SEND\\n\"      => STOMP\nrule tcp.content \"MESSAGE\\n\"   => STOMP\nrule tcp.content \"ERROR\\n\"     => STOMP\n\ncolor stomp.command == \"MESSAGE\"   => black lightgreen\ncolor stomp.command == \"SEND\"      => black lightyellow\ncolor stomp.command == \"CONNECTED\" => black lightblue\ncolor stomp.command == \"ERROR\"     => white red\ncolor stomp.command == \"SUBSCRIBE\" || stomp.command == \"UNSUBSCRIBE\" => black lightblue\ncolor stomp => white darkblue\n",
    /* stp.posa */
    "# STP / RSTP BPDU (IEEE 802.1D)\n# Sent to multicast 01:80:C2:00:00:00, LLC DSAP/SSAP 0x42.\n# max_age/hello_time/forward_delay are in 1/256 seconds (e.g. 5120 = 20s, 512 = 2s, 3840 = 15s).\nObject<main> STP\n    uint16   protocol_id defaults(0)\n    uint8    version defaults(0)\n        STP  = 0\n        RSTP = 2\n        MSTP = 3\n    uint8    bpdu_type defaults(0)\n        CONFIG     = 0x00\n        TCN        = 0x80\n        RAPID_PVST = 0x02\n    uint8    flags defaults(0)\n    bytes<8> root_id\n    uint32   root_path_cost defaults(0)\n    bytes<8> bridge_id\n    uint16   port_id defaults(0)\n    uint16   message_age defaults(0)\n    uint16   max_age defaults(5120)\n    uint16   hello_time defaults(512)\n    uint16   forward_delay defaults(3840)\n",
    /* stun.posa */
    "# STUN — Session Traversal Utilities for NAT (RFC 5389 / RFC 8489)\n# Ports: UDP/TCP 3478 (plain STUN/TURN), 5349 (STUN/TURN over TLS/DTLS).\n# The magic cookie 0x2112A442 at bytes 4-7 uniquely identifies STUN on any\n# port, making content-based detection the primary and most reliable trigger.\n#\n# Message structure (20-byte fixed header + variable TLV attributes):\n#   [0-1]   message_type   — 16-bit; bits 15-14 always 0; class in bits 8 (C1)\n#                            and 4 (C0); method in remaining bits (non-contiguous\n#                            groups 13-9, 7-5, 3-0). Class encoding:\n#                            00=Request, 01=Indication, 10=Success, 11=Error.\n#   [2-3]   message_length — byte count of attributes only (excludes 20-byte header)\n#   [4-7]   magic_cookie   — MUST be 0x2112A442 (used for XOR-MAPPED-ADDRESS too)\n#   [8-19]  transaction_id — 96-bit random value correlating requests to responses\n#   [20..]  attributes     — TLV list: uint16 type + uint16 length + value bytes;\n#                            the length field counts value bytes only; value is\n#                            zero-padded to the next 4-byte boundary (padding not\n#                            counted in length).\n#\n# Primary use cases: WebRTC ICE connectivity checks, SIP NAT traversal, TURN\n# relay allocation. Commonly multiplexed on non-standard ports alongside DTLS\n# and SRTP (RFC 5764), making the magic cookie the only reliable discriminator.\n\nObject<main> STUN\n    abbrev \"stun\"\n    col \"STUN\"\n\n    # [0-1] Message Type\n    # Bits 15-14 are always 0 (framing marker distinguishing STUN from TURN\n    # ChannelData, which uses bits 15-14 = 01).\n    # Class bits: C1 at bit 8, C0 at bit 4 (interleaved with method bits).\n    # Method bits: 13-9 (M11-M7), 7-5 (M6-M4), 3-0 (M3-M0) — non-contiguous.\n    # The enumerated values below combine method and class for all common types.\n    uint16 message_type \"Message Type\" defaults(0x0001)\n        # ── Binding (method 0x001) — ICE connectivity checks ─────────────────\n        0x0001 = \"Binding Request\"\n        0x0011 = \"Binding Indication\"\n        0x0101 = \"Binding Success Response\"\n        0x0111 = \"Binding Error Response\"\n        # ── Allocate (method 0x003, TURN RFC 5766) — relay allocation ─────────\n        0x0003 = \"Allocate Request\"\n        0x0103 = \"Allocate Success Response\"\n        0x0113 = \"Allocate Error Response\"\n        # ── Refresh (method 0x004, TURN) — keep allocation alive ─────────────\n        0x0004 = \"Refresh Request\"\n        0x0104 = \"Refresh Success Response\"\n        0x0114 = \"Refresh Error Response\"\n        # ── Send / Data (methods 0x006 / 0x007, TURN data relay) ─────────────\n        0x0016 = \"Send Indication\"\n        0x0017 = \"Data Indication\"\n        # ── CreatePermission (method 0x008, TURN) — install peer permission ───\n        0x0008 = \"CreatePermission Request\"\n        0x0108 = \"CreatePermission Success Response\"\n        0x0118 = \"CreatePermission Error Response\"\n        # ── ChannelBind (method 0x009, TURN) — bind a channel number ──────────\n        0x0009 = \"ChannelBind Request\"\n        0x0109 = \"ChannelBind Success Response\"\n        0x0119 = \"ChannelBind Error Response\"\n\n    # [2-3] Attribute payload length (bytes after the fixed 20-byte header)\n    uint16 message_length \"Message Length\" defaults(0)\n\n    # [4-7] Magic cookie — always 0x2112A442; XOR'd into XOR-MAPPED-ADDRESS etc.\n    uint32 magic_cookie \"Magic Cookie\" defaults(0x2112A442)\n\n    # [8-19] Transaction ID — 96-bit random opaque value\n    bytes<12> transaction_id \"Transaction ID\"\n\n    # [20..] TLV attribute list\n    # Each attribute: 2-byte type + 2-byte length (value only) + value bytes.\n    # Value is zero-padded to the next 4-byte boundary; padding bytes are not\n    # counted in attr_length. Comprehension-required types (< 0x8000) MUST be\n    # understood by the receiver; comprehension-optional (>= 0x8000) may be\n    # silently ignored.\n    repeat until end as attribute \"Attributes\"\n        label \"%s\" attr_type\n        uint16 attr_type \"Attribute Type\" defaults(0x0001)\n            # ── Comprehension-required (0x0000-0x7FFF) ────────────────────────\n            0x0001 = \"MAPPED-ADDRESS\"\n            0x0006 = \"USERNAME\"\n            0x0008 = \"MESSAGE-INTEGRITY\"\n            0x0009 = \"ERROR-CODE\"\n            0x000C = \"CHANNEL-NUMBER\"\n            0x000D = \"LIFETIME\"\n            0x0012 = \"XOR-PEER-ADDRESS\"\n            0x0013 = \"DATA\"\n            0x0014 = \"REALM\"\n            0x0015 = \"NONCE\"\n            0x0016 = \"XOR-RELAYED-ADDRESS\"\n            0x0018 = \"EVEN-PORT\"\n            0x0019 = \"REQUESTED-TRANSPORT\"\n            0x001A = \"DONT-FRAGMENT\"\n            0x0020 = \"XOR-MAPPED-ADDRESS\"\n            0x0022 = \"RESERVATION-TOKEN\"\n            0x0024 = \"PRIORITY\"\n            0x0025 = \"USE-CANDIDATE\"\n            0x0054 = \"ICMP\"\n            # ── Comprehension-optional (0x8000-0xFFFF) ────────────────────────\n            0x8022 = \"SOFTWARE\"\n            0x8023 = \"ALTERNATE-SERVER\"\n            0x8025 = \"TRANSACTION-TRANSMIT-COUNTER\"\n            0x8028 = \"FINGERPRINT\"\n            0x8029 = \"ICE-CONTROLLED\"\n            0x802A = \"ICE-CONTROLLING\"\n            0x802B = \"RESPONSE-ORIGIN\"\n            0x802C = \"OTHER-ADDRESS\"\n            0x802E = \"MESSAGE-INTEGRITY-SHA256\"\n            0x802F = \"PASSWORD-ALGORITHM\"\n            0x8030 = \"USERHASH\"\n        uint16 attr_length \"Attribute Length\" defaults(0)\n        bytes[attr_length] attr_value \"Value\"\n\n    info \"%s\" message_type\n\n\n# ── Detection rules ────────────────────────────────────────────────────────────\n# Content rules first: magic cookie 0x2112A442 at byte offset 4 uniquely\n# identifies STUN/TURN on any port. This is the PRIMARY trigger — it covers\n# WebRTC multiplexing and non-standard deployments without relying on ports.\nrule udp.content@4 \"\\x21\\x12\\xa4\\x42\" => STUN\nrule tcp.content@4 \"\\x21\\x12\\xa4\\x42\" => STUN\n# Standard STUN/TURN port (RFC 5389 §9 / RFC 5766 §4)\nrule udp.port == 3478 => STUN\nrule tcp.port == 3478 => STUN\n# STUN/TURN over TLS (STUNS) and DTLS (RFC 5389 §9 / RFC 7350)\nrule udp.port == 5349 => STUN\nrule tcp.port == 5349 => STUN\n\n\n# ── Coloring rules ─────────────────────────────────────────────────────────────\n# Requests (class C1=0, C0=0): outbound NAT probe or TURN allocation — yellow\ncolor stun.message_type == 0x0001 => black lightyellow\ncolor stun.message_type == 0x0003 => black lightyellow\ncolor stun.message_type == 0x0004 => black lightyellow\ncolor stun.message_type == 0x0008 => black lightyellow\ncolor stun.message_type == 0x0009 => black lightyellow\n\n# Indications (class C1=0, C0=1): one-way Send/Data relay messages — blue\ncolor stun.message_type == 0x0011 => white darkblue\ncolor stun.message_type == 0x0016 => white darkblue\ncolor stun.message_type == 0x0017 => white darkblue\n\n# Success Responses (class C1=1, C0=0): NAT mapping or TURN allocation OK — green\ncolor stun.message_type == 0x0101 => black lightgreen\ncolor stun.message_type == 0x0103 => black lightgreen\ncolor stun.message_type == 0x0104 => black lightgreen\ncolor stun.message_type == 0x0108 => black lightgreen\ncolor stun.message_type == 0x0109 => black lightgreen\n\n# Error Responses (class C1=1, C0=1): auth failure, quota, unknown attribute — red\ncolor stun.message_type == 0x0111 => white red\ncolor stun.message_type == 0x0113 => white red\ncolor stun.message_type == 0x0114 => white red\ncolor stun.message_type == 0x0118 => white red\ncolor stun.message_type == 0x0119 => white red\n\n# Catch-all — unrecognised or future STUN method/class combinations\ncolor stun => black lightgray\n\n\n# ── Wireshark-compatible field aliases ─────────────────────────────────────────\n# These map Wireshark STUN dissector field paths (packet-stun.c) to POSA names\n# so that saved display filters continue to work unchanged.\nalias stun.type                   => stun.message_type\nalias stun.length                 => stun.message_length\nalias stun.cookie                 => stun.magic_cookie\nalias stun.id                     => stun.transaction_id\nalias stun.att.type               => stun.attribute.attr_type\nalias stun.att.length             => stun.attribute.attr_length\nalias stun.att.value              => stun.attribute.attr_value\nalias stun.att.username           => stun.attribute.attr_type == 0x0006\nalias stun.att.software           => stun.attribute.attr_type == 0x8022\nalias stun.att.realm              => stun.attribute.attr_type == 0x0014\nalias stun.att.nonce              => stun.attribute.attr_type == 0x0015\nalias stun.att.mapped_address     => stun.attribute.attr_type == 0x0001\nalias stun.att.xor_mapped_address => stun.attribute.attr_type == 0x0020\nalias stun.att.fingerprint        => stun.attribute.attr_type == 0x8028\nalias stun.att.ice_controlled     => stun.attribute.attr_type == 0x8029\nalias stun.att.ice_controlling    => stun.attribute.attr_type == 0x802A\nalias stun.att.priority           => stun.attribute.attr_type == 0x0024\nalias stun.att.use_candidate      => stun.attribute.attr_type == 0x0025\n",
    /* syslog.posa */
    "# syslog.posa — Syslog Protocol (RFC 3164 BSD Syslog / RFC 5424 IETF Structured Syslog)\n#\n# Two formats share the Syslog group:\n#\n#   SYSLOG_5424 (RFC 5424):\n#     <PRI>VERSION SP TIMESTAMP SP HOSTNAME SP APP-NAME SP PROCID SP MSGID SP SD MSG\n#     VERSION is always the ASCII digit '1'; the two bytes \"1 \" immediately after\n#     the closing '>' distinguish this format from RFC 3164 (which has a letter there).\n#     Example: <165>1 2003-10-11T22:14:15.003Z mymachine.example.com evntslog - ID47\n#              [exampleSDID@32473 iut=\"3\"] BOMAn application event log entry...\n#\n#   SYSLOG_3164 (RFC 3164 — BSD Syslog):\n#     <PRI>Mmm DD HH:MM:SS HOSTNAME MSG\n#     Catch-all for any message whose byte after '>' is not '1'.\n#     Example: <34>Oct 11 22:14:15 mymachine su: 'su root' failed for lonvick on /dev/pts/8\n#\n# Priority encoding — PRI is a decimal integer (0-191) in ASCII between '<' and '>':\n#   Facility = integer(PRI) >> 3        range 0-23, identifies the subsystem\n#   Severity = integer(PRI) & 0x07     range 0-7,  indicates urgency\n#\n# The runtime derives syslog.facility and syslog.severity from the text value of\n# syslog.pri_raw (integer(pri_raw) >> 3 and & 7 respectively).  Color rules and\n# the info line reference these derived integer fields with their enum names.\n#\n# Facility values (0-23):\n#   kern=0  user=1  mail=2  daemon=3  auth=4  syslog=5  lpr=6  news=7\n#   uucp=8  cron=9  authpriv=10  ftp=11  ntp=12  logaudit=13  logalert=14\n#   cron2=15  local0=16..local7=23\n#\n# Severity values (0-7):\n#   emergency=0  alert=1  critical=2  error=3  warning=4  notice=5  info=6  debug=7\n#\n# Ports:  UDP/514 (RFC 5426), TCP/514 (BSD plain),\n#         TCP/601 (RFC 3195 reliable delivery), TCP/6514 (RFC 5425 TLS)\n# Content: all syslog messages begin with '<' (0x3C)\n\nObject<GROUP> Syslog\n\n# ── RFC 5424: IETF Structured Syslog ─────────────────────────────────────────\n#\n# Tried first by the GROUP engine.  The two bytes \"1 \" (version digit + mandatory\n# SP) immediately after the closing '>' act as the discriminator; RFC 3164 has an\n# upper-case letter in that position instead (start of a month abbreviation).\n# If the required field check fails the GROUP falls through to SYSLOG_3164.\n\nObject<Syslog> SYSLOG_5424\n    abbrev \"syslog\"\n    col \"Syslog\"\n    starts \"<\"\n\n    # Priority block: <FACILITY*8 + SEVERITY>\n    # After the closing '>' the runtime computes:\n    #   syslog.facility = integer(pri_raw) >> 3   (Facility enum, 0-23)\n    #   syslog.severity = integer(pri_raw) & 0x07 (Severity enum, 0-7)\n    string pri_raw until \">\" \"Priority\" defaults(\"<34\")\n\n    # Version byte '1' (0x31) + mandatory SP (0x20).\n    # Fails and causes GROUP fallthrough for any RFC 3164 message.\n    bytes<2> version_sp \"Version\" defaults(\"1 \")\n\n    # TIMESTAMP: ISO 8601 extended, e.g. \"2003-10-11T22:14:15.003Z\" or \"-\"\n    string timestamp until \" \" \"Timestamp\" defaults(\"2003-10-11T22:14:15.003Z\")\n\n    # HOSTNAME, APP-NAME, PROCID, MSGID — each NILVALUE-able as \"-\"\n    string hostname until \" \" \"Hostname\" defaults(\"mymachine\")\n    string appname  until \" \" \"App-Name\" defaults(\"su\")\n    string procid   until \" \" \"ProcID\" defaults(\"-\")\n    string msgid    until \" \" \"MsgID\" defaults(\"ID47\")\n\n    # Structured Data: NILVALUE '-' or one-or-more '[SD-ID PARAM-LIST]' elements.\n    # 'until \" \"' correctly handles the NILVALUE case and single-element SD whose\n    # parameters contain no internal spaces.  For SD elements with quoted spaces\n    # (e.g. [exampleSDID@32473 iut=\"3\" eventSource=\"Application\"]) the value will\n    # be truncated at the first space inside the element; multi-element SD is\n    # captured up to the first ']' + space only.  Full SD tokenisation requires\n    # a custom implementation extension.\n    string sd_raw until \" \" \"Structured Data\" defaults(\"-\")\n\n    # MSG: optional UTF-8 payload (may be preceded by a UTF-8 BOM, EF BB BF)\n    payload message \"Message\"\n\n    info \"%s.%s %s %s %s: %s\" facility, severity, hostname, appname, msgid, message\n\n# ── RFC 3164: BSD Syslog ──────────────────────────────────────────────────────\n#\n# Catch-all — matches anything that SYSLOG_5424 did not claim.\n# The HEADER timestamp \"Mmm DD HH:MM:SS\" is always exactly 15 bytes; single-digit\n# days are space-padded (e.g. \"Oct  6 22:14:15\" has two spaces before the '6').\n\nObject<Syslog> SYSLOG_3164\n    abbrev \"syslog\"\n    col \"Syslog\"\n    starts \"<\"\n\n    # Priority block: same encoding as RFC 5424.\n    # Runtime computes syslog.facility and syslog.severity as above.\n    string pri_raw until \">\" \"Priority\" defaults(\"<34\")\n\n    # HEADER: fixed 15-byte timestamp \"Mmm DD HH:MM:SS\"\n    bytes<15> timestamp \"Timestamp\" defaults(\"Oct 11 22:14:15\")\n    # Separator SP between HEADER and HOSTNAME\n    bytes<1>  ts_sp \"SP\" defaults(\" \")\n\n    # HOSTNAME is up to 255 chars; in practice a short DNS name or IP literal\n    string hostname until \" \" \"Hostname\" defaults(\"mymachine\")\n\n    # MSG optionally begins with a TAG field \"program[pid]: \" — parsed as payload\n    payload message \"Message\"\n\n    info \"%s.%s %s: %s\" facility, severity, hostname, message\n\n# ── Content detection (primary — fires before port rules) ────────────────────\n# '<' (0x3C) is the required first byte of every syslog message.  The content\n# rules cover non-standard ports; port rules below are secondary refinements.\nweak rule udp.content \"<\" => Syslog\nweak rule tcp.content \"<\" => Syslog\n\n# ── Port bindings (secondary hints) ──────────────────────────────────────────\nrule udp.port == 514  => Syslog      # RFC 5426  UDP syslog\nrule tcp.port == 514  => Syslog      # BSD plain TCP syslog\nrule tcp.port == 601  => Syslog      # RFC 3195  reliable delivery\nrule tcp.port == 6514 => Syslog      # RFC 5425  TLS syslog\n\n# ── Severity-based coloring ───────────────────────────────────────────────────\n# References syslog.severity — the runtime-derived integer (integer(pri_raw) & 7).\n# Rules are ordered most-severe first; the first match wins.\n\n# 0=emergency, 1=alert, 2=critical — highest urgency: white on red\ncolor syslog.severity <= 2 => white red\n\n# 3=error — serious but not immediate: yellow on red\ncolor syslog.severity == 3 => yellow red\n\n# 4=warning — potential problem: black on yellow\ncolor syslog.severity == 4 => black yellow\n\n# 5=notice, 6=info — normal operational messages: black on light gray\ncolor syslog.severity == 5 => black lightgray\ncolor syslog.severity == 6 => black lightgray\n\n# 7=debug — verbose tracing: black on light blue\ncolor syslog.severity == 7 => black lightblue\n\n# ── Wireshark-compatible aliases ──────────────────────────────────────────────\n# Map the Wireshark syslog dissector field names to their POSA equivalents so\n# that display filters written for Wireshark captures continue to work.\n\nalias syslog.facility  => syslog.facility     # derived integer field, matches WS name\nalias syslog.severity  => syslog.severity     # derived integer field, matches WS name\nalias syslog.hostname  => syslog.hostname     # field name matches\nalias syslog.appname   => syslog.appname      # field name matches\nalias syslog.procid    => syslog.procid       # field name matches\nalias syslog.msgid     => syslog.msgid        # field name matches\nalias syslog.msg       => syslog.message      # Wireshark uses \"msg\" not \"message\"\nalias syslog.time      => syslog.timestamp    # Wireshark uses \"time\" not \"timestamp\"\nalias syslog.data      => syslog.sd_raw       # Wireshark uses \"data\" for structured data\n\n# Convenience macro aliases for common filter expressions\nalias syslog.is_emergency => syslog.severity == 0\nalias syslog.is_critical  => syslog.severity <= 2\nalias syslog.is_error     => syslog.severity <= 3\nalias syslog.is_warning   => syslog.severity <= 4\nalias syslog.kern         => syslog.facility == 0\nalias syslog.user         => syslog.facility == 1\nalias syslog.daemon       => syslog.facility == 3\nalias syslog.auth         => syslog.facility == 4\nalias syslog.local0       => syslog.facility == 16\nalias syslog.local1       => syslog.facility == 17\nalias syslog.local2       => syslog.facility == 18\nalias syslog.local3       => syslog.facility == 19\nalias syslog.local4       => syslog.facility == 20\nalias syslog.local5       => syslog.facility == 21\nalias syslog.local6       => syslog.facility == 22\nalias syslog.local7       => syslog.facility == 23\n",
    /* teamspeak.posa */
    "# teamspeak.posa — TeamSpeak 3 Protocol\n#\n# TeamSpeak 3 uses two wire formats depending on transport:\n#\n#   UDP TS3INIT1 — session handshake; begins with the ASCII literal \"TS3INIT1\"\n#   (8 bytes), then uint8 reserved (0x65), uint16_be version, uint8 command\n#   (0x00 = initiation, 0x01 = reply), uint32_be random, uint32_be cookie.\n#\n#   TCP legacy — management/query connection; begins with a uint32_be magic:\n#   0xF4BE0100 (client greeting), 0xF4BE0200 (server greeting), 0xF4BE0300\n#   (data frame). After the magic: uint32_be length, then the payload.\n#\n# TeamSpeak 3 voice traffic uses encrypted UDP packets after the handshake;\n# only the handshake is decodable.\n#\n# Default port: UDP/TCP 9987 (voice + control).\n\nObject<TS> TEAMSPEAK_UDP\n    abbrev \"ts\"\n    col \"TeamSpeak\"\n    starts \"TS3INIT1\"\n\n    str<8>   magic \"Magic\" defaults(\"TS3INIT1\")\n    uint8    reserved \"Reserved\" defaults(0)\n    uint16   ts_version \"TS Version\" defaults(0)\n    uint8    command \"Command\" defaults(0)\n        0    = \"Initiation\"\n        1    = \"Reply\"\n    uint32   random      hex \"Random\" defaults(0)\n    uint32   cookie      hex \"Cookie\" defaults(0)\n    payload  rest            \"Payload\"\n\n    info \"TS3INIT1 cmd=%u\" command\n\nObject<TS> TEAMSPEAK_TCP\n    abbrev \"ts\"\n    col \"TeamSpeak\"\n\n    uint32 magic hex \"Magic\" defaults(0xF4BE0100)\n        0xF4BE0100 = \"Client Greeting\"\n        0xF4BE0200 = \"Server Greeting\"\n        0xF4BE0300 = \"Data Frame\"\n\n    uint32 length \"Length\" defaults(0)\n    payload body  \"Body\"\n\n    info \"TeamSpeak TCP magic=0x%08x\" magic\n\nObject<GROUP> TS\n\nrule tcp.port == 9987 => TS\nrule udp.port == 9987 => TS\nrule tcp.port == 10011 => TS\nrule tcp.port == 30033 => TS\n\nrule udp.content \"TS3INIT1\"   => TS\nrule tcp.content \"\\xf4\\xbe\\x01\\x00\" => TS\nrule tcp.content \"\\xf4\\xbe\\x02\\x00\" => TS\n\ncolor ts.command == 0 => black lightyellow\ncolor ts.command == 1 => black lightgreen\ncolor ts.magic == 0xF4BE0100 => black lightyellow\ncolor ts.magic == 0xF4BE0200 => black lightgreen\ncolor ts => white darkblue\n\nalias teamspeak.magic   => ts.magic\nalias teamspeak.command => ts.command\n",
    /* telegram.posa */
    "# telegram.posa — Telegram MTProto\n#\n# Telegram's wire protocol (MTProto) cannot be decoded from packet content:\n#  - TCP transport: no fixed magic; Telegram uses obfuscation where the first\n#    56 bytes contain a 32-byte random key followed by an 8-byte magic check\n#    inside the encrypted envelope. Detection in nDPI is done by IP address only.\n#  - UDP transport: detection is a variable-position scan for a run of 0xFF bytes\n#    — no fixed offset, not expressible as a posa content rule.\n#\n# This file registers Telegram's datacenter IP blocks so that traffic to/from\n# those addresses is labeled as Telegram even without payload inspection.\n#\n# Telegram datacenter IPv4 blocks (official, as of 2024):\n#   149.154.160.0/20   — DCs 1-5 (main production cluster, Netherlands/USA)\n#   91.108.4.0/22      — DC1 / additional\n#   91.108.8.0/22      — DC2\n#   91.108.56.0/22     — DC4\n#   91.108.0.0/22      — alternative range\n#   91.105.192.0/23    — media servers\n#   185.76.151.0/24    — voice relays\n#   95.161.64.0/20     — Russia-specific DC\n#\n# In display filters, CIDR notation works directly:\n#   ip.addr == 149.154.160.0/20\n#   ip.addr in { 149.154.160.0/20 91.108.4.0/22 91.108.8.0/22 }\n#\n# Ports vary: TCP 80, 443, 5222 are all used. UDP uses dynamic ports.\n\nObject<main> TELEGRAM\n    abbrev \"telegram\"\n    col \"Telegram\"\n\n    payload data \"MTProto Data (encrypted)\"\n\n    info \"Telegram MTProto (encrypted)\"\n\nrule ip4.addr in 149.154.160.0/20 => TELEGRAM\nrule ip4.addr in 91.108.4.0/22    => TELEGRAM\nrule ip4.addr in 91.108.8.0/22    => TELEGRAM\nrule ip4.addr in 91.108.56.0/22   => TELEGRAM\nrule ip4.addr in 91.108.0.0/22    => TELEGRAM\nrule ip4.addr in 91.105.192.0/23  => TELEGRAM\nrule ip4.addr in 185.76.151.0/24  => TELEGRAM\nrule ip4.addr in 95.161.64.0/20   => TELEGRAM\n\ncolor telegram => white darkblue\n",
    /* telnet.posa */
    "# telnet.posa — Telnet (RFC 854)\n#\n# Telnet carries two kinds of traffic: raw terminal data and IAC (Interpret As\n# Command) control sequences. Every IAC sequence starts with byte 0xFF, followed\n# by a command byte, and — for negotiation commands (WILL/WONT/DO/DONT/SB) — an\n# option byte and, for SB, an option-specific subnegotiation payload.\n#\n# Structure per record:\n#   0xFF cmd          — simple command (NOP, Break, GA, AYT, …)\n#   0xFF cmd option   — negotiation (WILL/WONT/DO/DONT) or subnegotiation start (SB)\n#   0xFF 0xFA option payload — subnegotiation: option byte + data until IAC SE\n#   <data>            — raw terminal bytes (non-IAC)\n#\n# Content detection fires on the IAC byte at offset 0, so the decoder activates\n# on any port whose session opens with a negotiation exchange (very common).\n# TCP 23 is the standard fallback for sessions that begin with raw data.\n\nObject<main> Telnet\n    abbrev \"telnet\"\n    col \"TELNET\"\n\n    # First byte: 0xFF marks an IAC sequence; anything else is raw terminal data.\n    uint8 iac \"IAC Marker\" defaults(0xFF)\n\n    when iac == 0xFF:\n        uint8 cmd \"Command\" defaults(0xFB)\n            NOP                  = 0xF1\n            Data Mark            = 0xF2\n            Break                = 0xF3\n            Interrupt Process    = 0xF4\n            Abort Output         = 0xF5\n            Are You There        = 0xF6\n            Erase Character      = 0xF7\n            Erase Line           = 0xF8\n            Go Ahead             = 0xF9\n            Subnegotiation Begin = 0xFA\n            WILL                 = 0xFB\n            WONT                 = 0xFC\n            DO                   = 0xFD\n            DONT                 = 0xFE\n            IAC                  = 0xFF\n\n        # SB (0xFA) and negotiation commands 0xFB–0xFE all carry an option byte.\n        # Simple commands (NOP, Break, GA, AYT, …) end after the command byte.\n        when cmd >= 0xFA:\n            uint8 option \"Option\" defaults(1)\n                Binary Transmission    = 0\n                Echo                   = 1\n                Suppress Go Ahead      = 3\n                Status                 = 5\n                Timing Mark            = 6\n                Terminal Type          = 24\n                Window Size            = 31\n                Terminal Speed         = 32\n                Remote Flow Control    = 33\n                Linemode               = 34\n                X Display Location     = 35\n                Environment Variables  = 36\n                Encryption Option      = 38\n                New Environment Option = 39\n\n            # SB carries option-specific payload until the peer sends IAC SE (0xFF 0xF0).\n            when cmd == 0xFA:\n                payload subneg \"Subnegotiation Data\"\n\n    else:\n        # Non-IAC: raw terminal data. iac holds the first data byte; the rest follows.\n        payload data \"Data\"\n\n    # cmd and option are absent for raw-data records — missing fields collapse,\n    # so the info line reads \"IAC NOP\", \"IAC WILL Echo\", \"IAC DO Terminal Type\", etc.\n    info \"IAC %s %s\" cmd, option\n\n# Content detection — IAC byte at the start of the stream. Most telnet sessions\n# open with a WILL/DO negotiation exchange, making this the primary trigger.\n# Listed first so the decoder fires on non-standard ports without relying on port 23.\nweak rule tcp.content \"\\xff\" => Telnet\n\n# Port 23 is the standard telnet assignment and catches sessions that open with\n# raw data (no leading IAC to trigger the content rule above).\nrule tcp.port == 23 => Telnet\n\n# Coloring — negotiation commands by direction; raw data is neutral grey.\n#   DO / WILL  (enabling a capability)  → blue\n#   DONT / WONT (refusing a capability) → yellow\n#   SB (subnegotiation exchange)        → cyan\n#   Raw terminal data / catch-all       → light grey\ncolor telnet.cmd == 0xFD or telnet.cmd == 0xFB => black lightblue\ncolor telnet.cmd == 0xFE or telnet.cmd == 0xFC => black lightyellow\ncolor telnet.cmd == 0xFA                        => black lightcyan\ncolor telnet                                    => black lightgray\n\n# Wireshark-compatible filter aliases.\n# Wireshark exposes the command byte as \"telnet.command\"; our abbrev uses \"telnet.cmd\".\nalias telnet.command => telnet.cmd\n",
    /* teredo.posa */
    "# teredo.posa — Teredo IPv6-over-UDP Tunneling (RFC 4380)\n#\n# Teredo tunnels IPv6 packets inside UDP to allow IPv6 connectivity for hosts\n# behind IPv4 NAT devices. All Teredo traffic runs over UDP port 3544.\n#\n# There are three packet types, distinguished by the first two bytes:\n#\n# 1) IPv6 packet: starts with 0x60-0x6F (IPv6 version nibble = 6) in first byte\n#\n# 2) Teredo Authentication header (indicator = 0x0001):\n#   uint16  indicator    = 0x0001\n#   uint8   id_len       — length of client identifier\n#   uint8   au_len       — length of authentication value\n#   bytes[id_len] id     — client identifier\n#   bytes[au_len] au     — authentication value\n#   bytes<8> nonce       — 8-byte nonce\n#   uint8   confirmation — 0=OK, 1=failure\n#\n# 3) Teredo Origin Indication header (indicator = 0x0000):\n#   uint16  indicator    = 0x0000\n#   uint16  obfuscated_port — bitwise NOT of client port\n#   uint32  obfuscated_ip   — bitwise NOT of client IPv4 address\n#\n# Reference: RFC 4380, RFC 5991 (security updates)\n# Default UDP port: 3544\n\nObject<TEREDO> TEREDO_AUTH\n    abbrev \"teredo\"\n    col \"Teredo\"\n\n    uint16 indicator     hex \"Indicator\" defaults(0x0001)\n        0x0001 = \"Authentication\"\n    uint8  id_len \"Client ID Length\" defaults(0)\n    uint8  au_len \"Authentication Length\" defaults(0)\n    bytes[id_len] client_id \"Client Identifier\"\n    bytes[au_len] auth_value \"Authentication Value\"\n    bytes<8> nonce       hex \"Nonce\"\n    uint8  confirmation \"Confirmation\" defaults(0)\n        0 = \"OK\"\n        1 = \"Failure\"\n    payload ipv6_packet \"IPv6 Packet\"\n\n    info \"Teredo Auth confirm=%u\" confirmation\n\nObject<TEREDO> TEREDO_ORIGIN\n    abbrev \"teredo\"\n    col \"Teredo\"\n\n    uint16 indicator     hex \"Indicator\" defaults(0x0000)\n        0x0000 = \"Origin Indication\"\n    uint16 obf_port      hex \"Obfuscated Port (NOT of client port)\"    defaults(0)\n    uint32 obf_addr      hex \"Obfuscated Address (NOT of client IPv4)\" defaults(0)\n    payload ipv6_packet      \"IPv6 Packet\"\n\n    info \"Teredo Origin\"\n\nObject<TEREDO> TEREDO_IPV6\n    abbrev \"teredo\"\n    col \"Teredo\"\n    starts \"\\x60\" \"\\x61\" \"\\x62\" \"\\x63\" \"\\x64\" \"\\x65\" \"\\x66\" \"\\x67\"\n           \"\\x68\" \"\\x69\" \"\\x6a\" \"\\x6b\" \"\\x6c\" \"\\x6d\" \"\\x6e\" \"\\x6f\"\n\n    bytes<1> version_tc hex \"Version/Traffic Class\"\n    payload ipv6_payload \"IPv6 Packet\"\n\n    info \"Teredo encapsulated IPv6\"\n\nObject<GROUP> TEREDO\n\nrule udp.port == 3544 => TEREDO\n\nweak rule udp.content \"\\x00\\x01\" => TEREDO\nweak rule udp.content \"\\x00\\x00\" => TEREDO\n\ncolor teredo.indicator == 0x0001 => black lightyellow\ncolor teredo.indicator == 0x0000 => black lightblue\ncolor teredo => white darkblue\n",
    /* tftp.posa */
    "# tftp.posa — TFTP (RFC 1350), dispatched by opcode.\n#\n# Load is automatic (carcal reads its protos/ dir at startup) and the `rule`\n# line at the bottom binds it to the TFTP port, so it decodes out of the box.\n# To bind it somewhere else too:  Analyze ▸ Decode As…\n#\n# `abbrev \"tftp\"` gives every field a Wireshark-style tftp.* name, so you can\n# filter on `tftp`, `tftp.opcode == 1`, `tftp.filename` — without it the fields\n# would be named after each Object (TFTP_RRQ.opcode), which nobody wants to type.\n\nObject<TFTP> TFTP_RRQ\n    abbrev \"tftp\"\n    uint16  opcode defaults(1)\n        RRQ = 1\n    cstring filename defaults(\"file.txt\")\n    cstring mode defaults(\"octet\")\n\nObject<TFTP> TFTP_WRQ\n    abbrev \"tftp\"\n    uint16  opcode defaults(2)\n        WRQ = 2\n    cstring filename defaults(\"file.txt\")\n    cstring mode defaults(\"octet\")\n\nObject<TFTP> TFTP_DATA\n    abbrev \"tftp\"\n    uint16  opcode defaults(3)\n        DATA = 3\n    uint16  block defaults(1)\n    payload data\n\nObject<TFTP> TFTP_ACK\n    abbrev \"tftp\"\n    uint16 opcode defaults(4)\n        ACK = 4\n    uint16 block defaults(0)\n\nObject<TFTP> TFTP_ERROR\n    abbrev \"tftp\"\n    uint16  opcode defaults(5)\n        ERROR = 5\n    uint16  code defaults(0)\n    cstring msg\n\nrule udp.port == 69 => TFTP\n\n# Coloring — carcal paints a matching packet with these colors, first match wins.\n# Declared here so the decoder ships its own look: drop this file into protos/\n# and TFTP is both decoded and colored, with no rebuild. These are consulted\n# before carcal's generic udp rule, so they win over it.\ncolor tftp.opcode == 5 => yellow red\ncolor tftp             => black lightgreen\n",
    /* thrift.posa */
    "# thrift.posa — Apache Thrift Binary Protocol\n#\n# Apache Thrift is an RPC framework. Several wire protocols exist; the most\n# common is the \"Binary Protocol\" (strict message framing):\n#\n#   uint32  frame_length  — length of the following message (optional, when framed)\n#\n#   Binary Protocol message envelope:\n#     uint32  version_type — high 16 bits: 0x8001 (version), low 16 bits: message type\n#                            OR (non-strict) uint8 field_type = 12 (start of struct)\n#     uint32  method_len   — length of the method name (strict form)\n#     str[method_len] method\n#     uint32  seq_id       — sequence/correlation ID\n#\n# Message types: 1=Call, 2=Reply, 3=Exception, 4=Oneway\n#\n# Compact Protocol (type 2, 0x82) starts with a 2-byte header:\n#   uint8  protocol_id   — 0x82 (compact protocol identifier)\n#   uint8  version_type  — bits 7-5: message type, bits 4-0: version (must be 1)\n#   varint seq_id        — zigzag-encoded sequence ID\n#   string method_name\n#\n# Reference: https://github.com/apache/thrift/blob/master/doc/specs/thrift-binary-protocol.md\n# Default TCP port: 9090 (common default)\n\nObject<THRIFT> THRIFT_BINARY\n    abbrev \"thrift\"\n    col \"Thrift\"\n    starts \"\\x80\\x01\"\n\n    uint16 version      hex \"Version\" defaults(0x8001)\n        0x8001 = \"Binary Protocol v1\"\n    uint16 message_type \"Message Type\" defaults(1)\n        1 = \"Call\"\n        2 = \"Reply\"\n        3 = \"Exception\"\n        4 = \"Oneway\"\n    uint32 method_len      \"Method Name Length\" defaults(0)\n    str[method_len] method \"Method Name\"\n    uint32 seq_id          \"Sequence ID\"        defaults(1)\n    payload body           \"Message Body\"\n\n    info \"Thrift %s %s seq=%u\" message_type, method, seq_id\n\nObject<THRIFT> THRIFT_COMPACT\n    abbrev \"thrift\"\n    col \"Thrift\"\n    starts \"\\x82\"\n\n    uint8  protocol_id  hex \"Protocol ID\" defaults(0x82)\n        0x82 = \"Compact Protocol\"\n    uint8  version_type hex \"Version/Type\" defaults(0x21)\n    payload rest            \"Method + Body\"\n\n    info \"Thrift compact\"\n\nObject<THRIFT> THRIFT_FRAMED\n    abbrev \"thrift\"\n    col \"Thrift\"\n\n    uint32 frame_length \"Frame Length\" defaults(0)\n    starts \"\\x80\\x01\" \"\\x82\"\n    payload frame \"Framed Message\"\n\n    info \"Thrift framed len=%u\" frame_length\n\nObject<GROUP> THRIFT\n\nrule tcp.port == 9090 => THRIFT\n\nrule tcp.content \"\\x80\\x01\\x00\\x01\" => THRIFT\nrule tcp.content \"\\x80\\x01\\x00\\x02\" => THRIFT\nrule tcp.content \"\\x80\\x01\\x00\\x04\" => THRIFT\nweak rule tcp.content \"\\x82\"             => THRIFT\n\ncolor thrift.message_type == 1 => black lightyellow\ncolor thrift.message_type == 2 => black lightgreen\ncolor thrift.message_type == 3 => white red\ncolor thrift => white darkblue\n\nalias thrift.call    => thrift.message_type == 1\nalias thrift.reply   => thrift.message_type == 2\nalias thrift.exception => thrift.message_type == 3\n",
    /* tls.posa */
    "# tls.posa — TLS/SSL record layer + the handshake messages worth reading.\n#\n# A TLS payload is a run of records, each with its own length, and a handshake\n# record carries a message with *another* length inside it. So this file is\n# mostly `scope`s inside `repeat`s:\n#\n#   repeat until end as record     one record per iteration, no count anywhere\n#       scope length               the record body is `length` bytes, and the\n#                                  scope closes there even if we decode nothing\n#                                  inside it — an unknown record type still\n#                                  leaves the next record correctly aligned\n#\n# The Client Hello's extensions are the same shape one level deeper, which is\n# how the server name (SNI) is reached: extensions repeat until the extension\n# block ends, each is scoped to its own length, and extension 0 is the name.\n#\n# Not covered (add it here, no rebuild): decryption of application data,\n# certificate parsing (shown as bytes), and TLS 1.3 encrypted extensions.\n\nObject<main> TLS\n    abbrev \"tls\"\n    col \"TLS\"\n\n    repeat until end as record \"Records\"\n        # a record that is not a handshake has no handshake type — the missing\n        # argument expands to nothing and the gap it leaves is closed\n        label \"%s %s\" content_type, handshake_type\n        uint8 content_type \"Content Type\" defaults(22)\n            Change Cipher Spec = 20\n            Alert = 21\n            Handshake = 22\n            Application Data = 23\n        uint16 version hex \"Version\" defaults(0x0303)\n            SSL 3.0 = 0x0300\n            TLS 1.0 = 0x0301\n            TLS 1.1 = 0x0302\n            TLS 1.2 = 0x0303\n            TLS 1.3 = 0x0304\n        uint16 length \"Length\" defaults(0)\n\n        scope length\n            when content_type == 22:\n                uint8 handshake_type \"Handshake Type\" defaults(1)\n                    Hello Request = 0\n                    Client Hello = 1\n                    Server Hello = 2\n                    New Session Ticket = 4\n                    Certificate = 11\n                    Server Key Exchange = 12\n                    Certificate Request = 13\n                    Server Hello Done = 14\n                    Certificate Verify = 15\n                    Client Key Exchange = 16\n                    Finished = 20\n                uint24 hs_length \"Handshake Length\" defaults(0)\n\n                scope hs_length\n                    # ── Client Hello ────────────────────────────────────────\n                    when handshake_type == 1:\n                        uint16 hs_version hex \"Version\" defaults(0x0303)\n                            TLS 1.0 = 0x0301\n                            TLS 1.1 = 0x0302\n                            TLS 1.2 = 0x0303\n                        bytes<32> random \"Random\"\n                        uint8 session_id_len \"Session ID Length\" defaults(0)\n                        bytes[session_id_len] session_id \"Session ID\"\n                        uint16 cipher_len \"Cipher Suites Length\" defaults(0)\n                        scope cipher_len\n                            repeat until end as cipher \"Cipher Suites\"\n                                label \"Cipher Suite: %s\" cipher_suite\n                                uint16 cipher_suite hex \"Cipher Suite\" defaults(0x1301)\n                                    TLS_AES_128_GCM_SHA256 = 0x1301\n                                    TLS_AES_256_GCM_SHA384 = 0x1302\n                                    TLS_CHACHA20_POLY1305_SHA256 = 0x1303\n                                    TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256 = 0xc02f\n                                    TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384 = 0xc030\n                        uint8 comp_len \"Compression Methods Length\" defaults(1)\n                        bytes[comp_len] comp_methods \"Compression Methods\"\n                        uint16 ext_len \"Extensions Length\" defaults(0)\n                        scope ext_len\n                            repeat until end as extension \"Extensions\"\n                                label \"%s\" ext_type\n                                uint16 ext_type \"Type\" defaults(0)\n                                    server_name = 0\n                                    supported_groups = 10\n                                    ec_point_formats = 11\n                                    signature_algorithms = 13\n                                    application_layer_protocol_negotiation = 16\n                                    supported_versions = 43\n                                    key_share = 51\n                                uint16 ext_data_len \"Length\" defaults(0)\n                                scope ext_data_len\n                                    when ext_type == 0:\n                                        uint16 sni_list_len \"Server Name List Length\" defaults(0)\n                                        uint8 sni_type \"Server Name Type\" defaults(0)\n                                        uint16 sni_len \"Server Name Length\" defaults(0)\n                                        str[sni_len] server_name \"Server Name\"\n                                    else:\n                                        payload ext_data \"Data\"\n\n                    # ── Server Hello ────────────────────────────────────────\n                    when handshake_type == 2:\n                        uint16 hs_version hex \"Version\" defaults(0x0303)\n                        bytes<32> random \"Random\"\n                        uint8 session_id_len \"Session ID Length\" defaults(0)\n                        bytes[session_id_len] session_id \"Session ID\"\n                        uint16 cipher_suite hex \"Cipher Suite\" defaults(0x1301)\n                            TLS_AES_128_GCM_SHA256 = 0x1301\n                            TLS_AES_256_GCM_SHA384 = 0x1302\n                            TLS_CHACHA20_POLY1305_SHA256 = 0x1303\n                            TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256 = 0xc02f\n                            TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384 = 0xc030\n                        uint8 comp_method \"Compression Method\" defaults(0)\n\n            when content_type == 21:\n                uint8 alert_level \"Level\" defaults(1)\n                    Warning = 1\n                    Fatal = 2\n                uint8 alert_desc \"Description\" defaults(0)\n                    Close notify = 0\n                    Handshake failure = 40\n                    Bad certificate = 42\n                    Certificate expired = 45\n                    Unknown CA = 48\n                    Protocol version = 70\n\n            when content_type == 23:\n                payload app_data \"Encrypted Application Data\"\n\n    info \"%s %s %s\" content_type, handshake_type, server_name\n\nrule tcp.port == 443  => TLS\nrule tcp.port == 8443 => TLS\n\n# Content signature — a TLS record on any port. 0x16 = Handshake, 0x03 0x0N =\n# SSL 3.0 / TLS 1.x version, so this fires on the ClientHello/ServerHello that\n# open a session on a non-standard port. (Encrypted app-data records, 0x17, are\n# left to the port rule.)\nweak rule tcp.content \"\\x16\\x03\" => TLS\n\ncolor tls.alert_level => yellow red\ncolor tls             => black lightmagenta\n",
    /* vnc.posa */
    "# vnc.posa — VNC Remote Framebuffer Protocol (RFB)\n#\n# Both client and server open a VNC session by exchanging the same 12-byte\n# ASCII banner:\n#\n#   \"RFB \" (4 bytes) + major version (3 digits) + \".\" + minor version (3 digits) + LF\n#\n# After the banner exchange the server sends a list of supported security types\n# (or for RFB 3.3 a fixed uint32_be security type), the client picks one, and\n# authentication follows. The post-handshake stream is largely opaque.\n#\n# Common version strings: \"RFB 003.003\\n\", \"RFB 003.007\\n\", \"RFB 003.008\\n\".\n#\n# Reference: https://www.rfc-editor.org/rfc/rfc6143 (RFB Protocol)\n#\n# Runs on TCP port 5900 (display 0), 5901 (display 1), etc.\n\nObject<main> VNC\n    abbrev \"vnc\"\n    col \"VNC\"\n\n    str<4> magic \"Magic\" defaults(\"RFB \")\n    str<3> version_major \"Version Major\" defaults(\"003\")\n    str<1> separator \"Separator\" defaults(\".\")\n    str<3> version_minor \"Version Minor\" defaults(\"008\")\n    str<1> terminator \"Terminator\" defaults(\"\\n\")\n\n    payload rest \"Post-Banner Data\"\n\n    info \"RFB %s.%s\" version_major, version_minor\n\nrule tcp.port == 5900 => VNC\nrule tcp.port == 5901 => VNC\nrule tcp.port == 5902 => VNC\nrule tcp.port == 5903 => VNC\n\nrule tcp.content \"RFB 003.003\\n\" => VNC\nrule tcp.content \"RFB 003.007\\n\" => VNC\nrule tcp.content \"RFB 003.008\\n\" => VNC\nrule tcp.content \"RFB 004.001\\n\" => VNC\n\ncolor vnc => black lightyellow\n\nalias rfb.version => vnc.version_major\n",
    /* vrrp.posa */
    "# VRRPv2 header (RFC 3768) — 8 bytes (plus count_ip x 4-byte virtual IP addresses)\n# IP protocol 112, multicast 224.0.0.18. ver_type: ver(4)=2 | type(4)=1 (ADVERTISEMENT).\n# adver_int in seconds; priority 255 = owner, 100 = default, 0 = release mastership.\nObject<main> VRRP\n    uint8  ver_type defaults(0x21)\n    uint8  vrid defaults(1)\n    uint8  priority defaults(100)\n    uint8  count_ip defaults(1)\n    uint8  auth_type defaults(0)\n        NO_AUTH     = 0\n        SIMPLE_TEXT = 1\n        IP_AUTH_HDR = 2\n    uint8  adver_int defaults(1)\n    uint16 checksum defaults(0)\n",
    /* vxlan.posa */
    "# vxlan.posa — Virtual eXtensible Local Area Network (RFC 7348)\n#\n# VXLAN encapsulates Ethernet frames in UDP to provide Layer 2 overlay networks\n# across Layer 3 boundaries. Each VXLAN packet has an 8-byte header:\n#\n#   uint8  flags     — bits 7-4 reserved (0), bit 3 = I (VNI valid), bits 2-0 reserved\n#   uint24 reserved1 — must be 0\n#   uint24 vni       — 24-bit VXLAN Network Identifier\n#   uint8  reserved2 — must be 0\n#\n# When the I-flag is set the VNI field is valid. A VXLAN frame carrying a\n# Ethernet frame follows immediately after the 8-byte header.\n#\n# VXLAN-GPE (Generic Protocol Extension, RFC 8926) extends the flags to add\n# a next-protocol field, but the UDP port is the same and the base 8-byte\n# header shape is compatible.\n#\n# Reference: RFC 7348, RFC 8926 (VXLAN-GPE)\n# Default UDP port: 4789 (IANA), 8472 (Linux legacy)\n\nObject<main> VXLAN\n    abbrev \"vxlan\"\n    col \"VXLAN\"\n\n    uint8  flags    hex \"Flags\" defaults(0x08)\n        0x08 = \"VNI Valid (I-flag set)\"\n    uint24 reserved hex \"Reserved\" defaults(0)\n    uint24 vni \"VNI (VXLAN Network Identifier)\" defaults(0)\n    uint8  reserved2 \"Reserved2\" defaults(0)\n    payload inner_frame \"Inner Ethernet Frame\"\n\n    info \"VXLAN VNI=%u\" vni\n\nrule udp.port == 4789  => VXLAN\nrule udp.port == 8472  => VXLAN\n\n# VXLAN header always has flags=0x08 (VNI valid); reserved must be 0.\nrule udp.content \"\\x08\\x00\\x00\\x00\" => VXLAN\n\ncolor vxlan => white darkblue\n\nalias vxlan.network_id => vxlan.vni\n",
    /* websocket.posa */
    "# WebSocket protocol decoder — RFC 6455\n# Detects WebSocket frames by their first-byte patterns (FIN + opcode).\n# WebSocket is negotiated via HTTP Upgrade; data frames follow on the same TCP connection.\n# Client-to-server frames are masked (MASK bit set); server-to-client frames are unmasked.\n\nObject<main> WebSocket\n    abbrev \"ws\"\n    col \"WebSocket\"\n\n    # Byte 0: FIN flag, reserved bits, and opcode\n    uint8 flags_opcode \"Flags/Opcode\" defaults(0x81)\n    bits flags_opcode fin      7 1 \"FIN\"\n    bits flags_opcode rsv1     6 1 \"RSV1\"\n    bits flags_opcode rsv2     5 1 \"RSV2\"\n    bits flags_opcode rsv3     4 1 \"RSV3\"\n    bits flags_opcode opcode   0 4 \"Opcode\"\n        CONTINUATION = 0\n        TEXT         = 1\n        BINARY       = 2\n        CLOSE        = 8\n        PING         = 9\n        PONG         = 10\n\n    # Byte 1: MASK flag and 7-bit base payload length\n    uint8 mask_len_byte \"Mask/Length\" defaults(0x80)\n    bits mask_len_byte mask        7 1 \"Mask\"\n    bits mask_len_byte payload_len 0 7 \"Payload Length\"\n\n    # Extended payload length — only present when base payload_len indicates it\n    when payload_len == 126:\n        uint16 ext_payload_len \"Extended Payload Length\" defaults(0)\n    when payload_len == 127:\n        uint64 ext_payload_len_64 \"Extended Payload Length (64-bit)\" defaults(0)\n\n    # Masking key — 4 bytes, present only when MASK bit is set (client frames)\n    when mask == 1:\n        bytes<4> masking_key \"Masking Key\"\n\n    # Payload data (XOR-masked when MASK bit is set)\n    payload\n\n    info \"%s len=%u\" opcode, payload_len\n\n# ── Content detection (PRIMARY — port-independent) ──────────────────────────\n# Match the most common WebSocket frame first bytes: FIN(1) + opcode\n# 0x81 = FIN + TEXT, 0x82 = FIN + BINARY\n# 0x88 = FIN + CLOSE, 0x89 = FIN + PING, 0x8A = FIN + PONG\nweak rule tcp.content \"\\x81\" => WebSocket\nweak rule tcp.content \"\\x82\" => WebSocket\nweak rule tcp.content \"\\x88\" => WebSocket\nweak rule tcp.content \"\\x89\" => WebSocket\nweak rule tcp.content \"\\x8A\" => WebSocket\n\n# ── No port bindings, deliberately ───────────────────────────────────────────\n# WebSocket has no port of its own. It begins life as an HTTP request carrying\n# `Upgrade: websocket`, and only then do frames start flowing — over whatever\n# port that HTTP connection was already using, which is 80 or 443 almost every\n# time. Claiming those ports here would take them from HTTP and TLS, and a port\n# binding outranks the weak signatures above: a TLS ClientHello on 443 would be\n# decoded as a WebSocket frame (0x16 reads as a valid opcode) and its SNI would\n# never be seen. The signatures are what recognise WebSocket, and they are asked\n# after the port bindings, which is the right way round for a protocol that\n# borrows someone else's port.\n\n# ── Coloring rules ───────────────────────────────────────────────────────────\n# Text frames — green\ncolor ws.opcode == 1  => black lightgreen\n# Binary frames — blue\ncolor ws.opcode == 2  => black lightblue\n# Continuation frames — light gray\ncolor ws.opcode == 0  => black lightgray\n# Control frames (close, ping, pong) — gray\ncolor ws.opcode == 8  => black lightgray\ncolor ws.opcode == 9  => black lightgray\ncolor ws.opcode == 10 => black lightgray\n\n# ── Wireshark-compatible aliases ─────────────────────────────────────────────\nalias websocket.opcode      => ws.opcode\nalias websocket.payload_len => ws.payload_len\nalias websocket.mask        => ws.mask\n",
    /* whois.posa */
    "# whois.posa — WHOIS Protocol (RFC 3912)\n#\n# WHOIS is a simple request/response TCP protocol. The client sends a single\n# line query (domain name or IP address or ASN) terminated by CRLF. The server\n# responds with free-form text and closes the connection.\n#\n# There is no structured binary header — the entire exchange is plain text.\n# RDAP (RFC 7482) is the JSON-based successor, served over HTTPS; it is not\n# decodable here without HTTP dissection.\n#\n# Reference: RFC 3912 (WHOIS), RFC 7482 (RDAP)\n# Default TCP port: 43\n\nObject<WHOIS> WHOIS_REQUEST\n    abbrev \"whois\"\n    col \"WHOIS\"\n    starts \"domain \" \"Domain \" \"nic \" \"AS\" \"ARIN \" \"RIPE \" \"-T \"\n\n    string query until \"\\r\\n\" \"Query\" defaults(\"example.com\")\n    payload rest \"Data\"\n\n    info \"WHOIS query: %s\" query\n\nObject<WHOIS> WHOIS_RESPONSE\n    abbrev \"whois\"\n    col \"WHOIS\"\n    starts \"%\" \"#\" \"Domain Name\" \"domain\" \"inetnum\" \"NetRange\" \"route\"\n           \"OrgName\" \"OrgID\" \"NetName\" \"CIDR\" \"NetHandle\" \"aut-num\"\n           \"descr\" \"person\" \"role\" \"organisation\" \"mnt-by\" \"Registrar\"\n           \"Registry Domain ID\" \"Registrant\"\n\n    string first_line until \"\\r\\n\" \"First Line\"\n    payload body \"Response Body\"\n\n    info \"WHOIS response\"\n\nObject<GROUP> WHOIS\n\nrule tcp.port == 43 => WHOIS\n\ncolor whois => white darkblue\n",
    /* wireguard.posa */
    "# wireguard.posa — WireGuard VPN Protocol\n# https://www.wireguard.com/papers/wireguard.pdf\n#\n# WireGuard encapsulates IP traffic inside ChaCha20-Poly1305-encrypted UDP\n# datagrams using a Noise_IKpsk2 handshake.  Four message types share a common\n# 4-byte header: byte[0] is the type (1–4); bytes[1–3] are always zero (reserved).\n#\n# Message types and sizes:\n#   1  Handshake Initiation  — exactly  148 bytes\n#   2  Handshake Response    — exactly   92 bytes\n#   3  Cookie Reply          — exactly   64 bytes\n#   4  Transport Data        — minimum   32 bytes (16-byte header + encrypted payload)\n#\n# Detection fires on the 4-byte type+reserved prefix, which is unique enough to\n# identify WireGuard reliably on any UDP port.  The default port 51820 is listed\n# as a secondary hint only.\n#\n# Objects:\n#   WG_HandshakeInit   — Initiator → Responder; carries ephemeral key and encrypted static\n#   WG_HandshakeResp   — Responder → Initiator; completes the Noise handshake\n#   WG_CookieReply     — Either peer under load; relays a rate-limiting cookie\n#   WG_Transport       — Either peer; carries ChaCha20-Poly1305 encrypted IP packets\n\nObject<GROUP> WireGuard\n\n# ─── Message type 1: Handshake Initiation (exactly 148 bytes) ─────────────────\n# Sent by the initiator to start the Noise_IKpsk2 exchange.  The sender_index\n# is a random 32-bit value the initiator assigns to this session; the responder\n# must echo it back in its receiver_index field.\nObject<WireGuard> WG_HandshakeInit\n    abbrev \"wireguard\"\n    col    \"WireGuard\"\n    starts \"\\x01\\x00\\x00\\x00\"\n\n    uint8     msg_type              \"Message Type\" defaults(1)\n    bytes<3>  reserved              \"Reserved\"\n    le_uint32 sender_index          \"Sender Index\" defaults(0)\n    bytes<32> unencrypted_ephemeral \"Unencrypted Ephemeral (Curve25519)\"\n    bytes<48> encrypted_static      \"Encrypted Static Key\"\n    bytes<28> encrypted_timestamp   \"Encrypted Timestamp (TAI64N)\"\n    bytes<16> mac1                  \"MAC1\"\n    bytes<16> mac2                  \"MAC2\"\n\n    info \"Handshake Initiation  Sender=0x%x\" sender_index\n\n# ─── Message type 2: Handshake Response (exactly 92 bytes) ────────────────────\n# Sent by the responder after verifying the initiation message.  Both peers can\n# derive session keys once this message is authenticated.  encrypted_nothing is\n# an empty plaintext encrypted under the session key to prove key agreement.\nObject<WireGuard> WG_HandshakeResp\n    abbrev \"wireguard\"\n    col    \"WireGuard\"\n    starts \"\\x02\\x00\\x00\\x00\"\n\n    uint8     msg_type              \"Message Type\"   defaults(2)\n    bytes<3>  reserved              \"Reserved\"\n    le_uint32 sender_index          \"Sender Index\"   defaults(0)\n    le_uint32 receiver_index        \"Receiver Index\" defaults(0)\n    bytes<32> unencrypted_ephemeral \"Unencrypted Ephemeral (Curve25519)\"\n    bytes<16> encrypted_nothing     \"Encrypted Nothing\"\n    bytes<16> mac1                  \"MAC1\"\n    bytes<16> mac2                  \"MAC2\"\n\n    info \"Handshake Response  Sender=0x%x  Receiver=0x%x\" sender_index, receiver_index\n\n# ─── Message type 3: Cookie Reply (exactly 64 bytes) ──────────────────────────\n# Sent by a peer under load when it cannot process a handshake immediately.\n# The cookie is XChaCha20-Poly1305-encrypted under a key derived from the\n# responder's static key so only the legitimate initiator can read it.\nObject<WireGuard> WG_CookieReply\n    abbrev \"wireguard\"\n    col    \"WireGuard\"\n    starts \"\\x03\\x00\\x00\\x00\"\n\n    uint8     msg_type         \"Message Type\"   defaults(3)\n    bytes<3>  reserved         \"Reserved\"\n    le_uint32 receiver_index   \"Receiver Index\" defaults(0)\n    bytes<24> nonce            \"Nonce (XChaCha20)\"\n    bytes<32> encrypted_cookie \"Encrypted Cookie\"\n\n    info \"Cookie Reply  Receiver=0x%x\" receiver_index\n\n# ─── Message type 4: Transport Data (minimum 32 bytes) ────────────────────────\n# Carries one or more ChaCha20-Poly1305-encrypted IP packets.  The 64-bit\n# counter provides replay protection; the receiver maintains a sliding window.\n# The encrypted payload is at least 16 bytes (Poly1305 authentication tag only,\n# empty plaintext); normal packets are larger.\nObject<WireGuard> WG_Transport\n    abbrev \"wireguard\"\n    col    \"WireGuard\"\n    starts \"\\x04\\x00\\x00\\x00\"\n\n    uint8     msg_type                      \"Message Type\"                defaults(4)\n    bytes<3>  reserved                      \"Reserved\"\n    le_uint32 receiver_index                \"Receiver Index\"              defaults(0)\n    le_uint64 counter                       \"Counter (Replay Protection)\" defaults(0)\n    payload   encrypted_encapsulated_packet \"Encrypted Encapsulated Packet\"\n\n    info \"Transport Data  Receiver=0x%x  Counter=%u\" receiver_index, counter\n\n# ─── Content detection (primary — port-independent) ───────────────────────────\n# The 4-byte prefix uniquely identifies each WireGuard message type because\n# bytes[1–3] are required to be zero by the specification.  All four rules\n# must be listed so WireGuard is decoded on non-standard ports too.\nrule udp.content \"\\x01\\x00\\x00\\x00\" => WireGuard\nrule udp.content \"\\x02\\x00\\x00\\x00\" => WireGuard\nrule udp.content \"\\x03\\x00\\x00\\x00\" => WireGuard\nrule udp.content \"\\x04\\x00\\x00\\x00\" => WireGuard\n\n# ─── Port binding (secondary hint) ────────────────────────────────────────────\nrule udp.port == 51820 => WireGuard\n\n# ─── Coloring ──────────────────────────────────────────────────────────────────\n# Handshake Initiation — yellow (request/initiator)\ncolor wireguard.msg_type == 1 => black lightyellow\n# Handshake Response — green (response/responder)\ncolor wireguard.msg_type == 2 => black lightgreen\n# Cookie Reply — gray (out-of-band control, neither data nor handshake)\ncolor wireguard.msg_type == 3 => black lightgray\n# Transport Data — dark blue (encrypted tunneled payload)\ncolor wireguard.msg_type == 4 => white darkblue\n# Catch-all (e.g. future message types or partial frames)\ncolor wireguard => white darkgray\n\n# ─── Wireshark-compatible field aliases ───────────────────────────────────────\n# Map Wireshark's wg dissector field names to the abbrevs used here so that\n# display filters written against a Wireshark capture continue to work.\nalias wg.type             => wireguard.msg_type\nalias wg.sender           => wireguard.sender_index\nalias wg.receiver         => wireguard.receiver_index\nalias wg.ephemeral        => wireguard.unencrypted_ephemeral\nalias wg.static           => wireguard.encrypted_static\nalias wg.timestamp        => wireguard.encrypted_timestamp\nalias wg.mac1             => wireguard.mac1\nalias wg.mac2             => wireguard.mac2\nalias wg.nonce            => wireguard.nonce\nalias wg.encrypted_cookie => wireguard.encrypted_cookie\nalias wg.counter          => wireguard.counter\nalias wg.encrypted_packet => wireguard.encrypted_encapsulated_packet\n",
    /* wol.posa */
    "# Wake-on-LAN magic packet — 102 bytes\n# 6 bytes of 0xFF followed by the target MAC address repeated 16 times.\n# Typically sent as UDP broadcast to port 9 (or 7), or as a raw Ethernet frame.\n# Set sync to ff:ff:ff:ff:ff:ff and target to 16 repetitions of the MAC to wake.\nObject<main> WOL\n    bytes<6>  sync defaults(\"\\xff\\xff\\xff\\xff\\xff\\xff\")\n    bytes<96> target\n",
    /* xmpp.posa */
    "# xmpp.posa — Extensible Messaging and Presence Protocol (RFC 6120, RFC 7590)\n#\n# Covers three transport configurations:\n#   TCP 5222 — XMPP client-to-server (C2S), plain or STARTTLS\n#   TCP 5269 — XMPP server-to-server (S2S) federation\n#   TCP 5280 — BOSH HTTP-binding (XEP-0206), plain HTTP\n#   TCP 5281 — BOSH HTTP-binding (XEP-0206), over HTTPS / TLS\n#\n# XMPP is a streaming XML protocol.  Each TCP segment may carry one or more\n# complete XML stanzas, or a fragment of one.  This dissector makes a best-effort\n# pass over the leading element of each segment:\n#\n#   \"stanza\"  reads the opening element name up to the first space, giving the\n#             stanza type indicator: <?xml, <stream:stream, <iq, <message, etc.\n#\n#   \"attrs\"   reads everything from that space up to the closing \">\", capturing\n#             all XML attributes on one line.\n#\n#   \"body\"    holds the rest of the payload (further stanzas, TLS data, etc.)\n#             as an opaque blob for higher-level reassembly.\n#\n# Self-closing tags with no attribute space (e.g. <presence/>) and closing tags\n# (</stream:stream>) will not split cleanly at the space boundary; the full tag\n# text lands in \"stanza\" and \"attrs\" may be empty.  Content rules still identify\n# those segments as XMPP and the catch-all color rule applies.\n#\n# Stream opening (client to server):\n#   <?xml version='1.0'?>\n#   <stream:stream xmlns:stream='http://etherx.jabber.org/streams'\n#                  xmlns='jabber:client' to='example.com' version='1.0'>\n#\n# Common stanzas inside the stream:\n#   <iq type=\"get|set|result|error\" from=\"…\" to=\"…\" id=\"…\">   info/query\n#   <message type=\"chat|groupchat|error\" from=\"…\" to=\"…\">      instant messages\n#   <presence type=\"available|unavailable|subscribe|…\">         availability\n\nObject<main> XMPP\n    abbrev \"xmpp\"\n    col \"XMPP\"\n\n    # Element tag name — the stanza type indicator.\n    # Typical values: <?xml  <stream:stream  <iq  <message  <presence\n    # Closing tags:   </stream:stream>  (no space; full tag lands here)\n    string stanza until \" \" \"Stanza\" defaults(\"<message\")\n\n    # Remaining XML attributes up to the closing \">\" of the opening element.\n    # Example:  type=\"get\" from=\"user@example.com\" to=\"example.com\" id=\"aabbcc\">\n    string attrs  until \">\" \"Attributes\" defaults(\"type=\\\"chat\\\"\")\n\n    # Rest of the TCP segment — subsequent stanzas, stream body, or TLS data.\n    payload body \"Stream Body\"\n\n    info \"%s %s\" stanza, attrs\n\n# ── Content-based detection (PRIMARY — protocol recognised on any TCP port) ───\n# Ordered from most specific (stream-level) to stanza-level indicators.\nrule tcp.content \"<?xml\"          => XMPP\nrule tcp.content \"<stream:stream\" => XMPP\nrule tcp.content \"<iq \"           => XMPP\nrule tcp.content \"<message \"      => XMPP\nrule tcp.content \"<presence\"      => XMPP\n\n# ── Port-based rules (SECONDARY — fallback when stream start is not captured) ─\nrule tcp.port == 5222 => XMPP   # RFC 6120 — XMPP client-to-server\nrule tcp.port == 5269 => XMPP   # RFC 7590 — XMPP server-to-server federation\nrule tcp.port == 5280 => XMPP   # XEP-0206 — BOSH, plain HTTP\nrule tcp.port == 5281 => XMPP   # XEP-0206 — BOSH, HTTPS / TLS\n\n# ── Coloring rules (most specific first) ──────────────────────────────────────\n\n# Stream-level errors — white on red (highest priority)\ncolor xmpp.stanza == \"<stream:error\"       => white red\n\n# Stanza-level error attribute (iq/message/presence with type=\"error\")\ncolor tcp.content \"type=\\\"error\\\"\"         => white red\n\n# Presence stanzas — availability and subscription state — white on dark blue\ncolor xmpp.stanza == \"<presence\"           => white darkblue\n\n# Message stanzas — instant messages and groupchat — black on light green\ncolor xmpp.stanza == \"<message\"            => black lightgreen\n\n# IQ stanzas — info/query (get / set / result) — black on light yellow\ncolor xmpp.stanza == \"<iq\"                 => black lightyellow\n\n# Stream-level and negotiation frames — light gray (session management)\ncolor xmpp.stanza == \"<stream:stream\"      => black lightgray\ncolor xmpp.stanza == \"<stream:features\"    => black lightgray\ncolor xmpp.stanza == \"<?xml\"              => black lightgray\n\n# Catch-all — roster pushes, auth steps, and any unclassified XMPP frame\ncolor xmpp                                 => white darkgray\n\n# ── Wireshark-compatible aliases ───────────────────────────────────────────────\n# Maps Wireshark XMPP and legacy jabber.* display filter field names to the\n# POSA fields emitted by this dissector.  jabber.* names are used by Wireshark's\n# built-in XMPP dissector; xmpp.* aliases reflect modern filter conventions.\n# Note: xmpp.type / xmpp.from / xmpp.to are embedded within the attrs string\n# and cannot be individually extracted without a full XML attribute parser.\nalias xmpp.stanza       => xmpp.stanza   # opening element tag name\nalias xmpp.type         => xmpp.attrs    # type=\"…\" attribute (within attrs)\nalias xmpp.from         => xmpp.attrs    # from=\"…\" attribute (within attrs)\nalias xmpp.to           => xmpp.attrs    # to=\"…\"   attribute (within attrs)\nalias jabber.tag        => xmpp.stanza   # legacy Wireshark jabber.* namespace\nalias jabber.attrs      => xmpp.attrs\nalias jabber.type       => xmpp.attrs\nalias jabber.from       => xmpp.attrs\nalias jabber.to         => xmpp.attrs\n",
    /* zabbix.posa */
    "# zabbix.posa — Zabbix Monitoring Protocol\n#\n# Zabbix uses a custom binary TCP protocol between agents, proxies, and the\n# server. The framing header is 13 bytes:\n#\n#   char[4]  magic    — \"ZBXD\" (0x5A425844)\n#   uint8    flags    — protocol flags:\n#                        0x01 = ZABBIX_PROTOCOL (standard)\n#                        0x04 = COMPRESSION\n#                        0x08 = RESERVED\n#   uint32   data_length   — uncompressed payload size (LE)\n#   uint32   reserved      — compressed size or 0\n#\n# The payload is a JSON object. Common payload types (identified by the \"request\"\n# or \"response\" JSON key) include:\n#   agent data, active checks, config sync, host list, etc.\n#\n# Active agent (port 10050): server → agent connections for passive checks.\n# Passive agent (port 10051): agent → server connections for active checks.\n#\n# Reference: https://www.zabbix.com/documentation/current/en/manual/appendix/protocols\n# Default TCP ports: 10050 (Zabbix agent), 10051 (Zabbix trapper/server)\n\nObject<main> ZABBIX\n    abbrev \"zabbix\"\n    col \"Zabbix\"\n    starts \"ZBXD\"\n\n    str<4>  magic \"Magic\" defaults(\"ZBXD\")\n    uint8   flags           hex \"Flags\" defaults(0x01)\n        0x01 = \"Standard Protocol\"\n        0x04 = \"Compressed\"\n        0x05 = \"Compressed Protocol\"\n    le_uint32 data_length \"Data Length\"                defaults(0)\n    le_uint32 reserved    \"Reserved / Compressed Size\" defaults(0)\n    payload payload       \"JSON Payload\"\n\n    info \"Zabbix flags=0x%02x len=%u\" flags, data_length\n\nrule tcp.port == 10050 => ZABBIX\nrule tcp.port == 10051 => ZABBIX\n\nrule tcp.content \"ZBXD\" => ZABBIX\n\ncolor zabbix.flags == 0x04 || zabbix.flags == 0x05 => black lightyellow\ncolor zabbix => white darkblue\n\nalias zabbix.agent  => tcp.dstport == 10050\nalias zabbix.server => tcp.dstport == 10051\n",
    /* zeromq.posa */
    "# zeromq.posa — ZeroMQ Message Transport Protocol (ZMTP 3.x, RFC-level spec)\n# Reference: https://rfc.zeromq.org/spec/37/ (ZMTP 3.0)\n#            https://rfc.zeromq.org/spec/23/ (ZMTP 2.0)\n#\n# ZMTP 3.x greeting (64 bytes sent by both peers):\n#   [0]      0xFF               — signature byte\n#   [1-8]    0x00 0x00 0x00 0x00 0x00 0x00 0x00 0x00  (padding)\n#   [9]      0x7F               — signature byte (final)\n#   [10]     version_major      (3 for ZMTP 3.x)\n#   [11]     version_minor\n#   [12-31]  mechanism          (null-terminated string: \"NULL\", \"PLAIN\", \"CURVE\", \"GSSAPI\")\n#   [32]     as_server          (0 = client, 1 = server)\n#   [33-63]  filler (zeros)\n#\n# ZMTP 2.0 signature uses the same first 10 bytes but version byte is 2.\n\nObject<ZMTP> ZMTP3\n    abbrev \"zmtp\"\n    col \"ZMTP\"\n\n    uint8  sig_ff    hex \"Signature Start\" defaults(0xFF)\n    bytes<8> padding        hex \"Padding\"\n    uint8  sig_7f   hex \"Signature End\" defaults(0x7F)\n    uint8  version_major \"Version Major\" defaults(3)\n        ZMTP_2 = 2\n        ZMTP_3 = 3\n    uint8  version_minor \"Version Minor\" defaults(0)\n    bytes<20> mechanism \"Security Mechanism\" defaults(\"NULL\")\n    uint8  as_server \"Role\" defaults(0)\n        Client = 0\n        Server = 1\n    bytes<31> filler        hex \"Filler\"\n\n    payload frames \"Frames\"\n\n    info \"ZMTP %u.%u %s\" version_major, version_minor, mechanism\n\nObject<GROUP> ZMTP\n\n# ZMTP magic is the same regardless of port\nrule tcp.content \"\\xff\\x00\\x00\\x00\\x00\\x00\\x00\\x00\\x00\\x7f\" => ZMTP\n\n# Common ZeroMQ ports (not standardized, but widely used defaults)\nrule tcp.port == 5555 => ZMTP\nrule tcp.port == 5556 => ZMTP\n\ncolor zmtp.as_server == 1 => black lightcyan\ncolor zmtp.as_server == 0 => black lightyellow\ncolor zmtp                => white darkblue\n",
    /* zoom.posa */
    "# zoom.posa — Zoom Video Conferencing UDP media protocol\n#\n# Zoom's UDP media stream uses two layered headers:\n#\n#   SFU Encapsulation (zoom_sfu_enc, 8 bytes) — identifies the packet class and\n#   direction in Zoom's Selective Forwarding Unit architecture.\n#\n#   ZME header (zoom_media_enc, ~15 bytes) — identifies the media type (audio,\n#   video, screen share, RTCP) with sequence and timestamp, followed by the\n#   actual RTP/RTCP payload.\n#\n# SFU type 5 packets carry a nested ZME header after the outer SFU header.\n# SFU types 1 and 2 are identified by the first three bytes [0x01/0x02, 0x00,\n# 0x02/0x03] with no ZME header.  Types 3 and 4 are heuristically detected.\n#\n# P2P calls (type 0x1F) skip the SFU header entirely and start with a ZME header.\n#\n# References:\n#   https://github.com/Princeton-Cabernet/zoom-analysis\n#   https://citizenlab.ca/2020/04/move-fast-roll-your-own-crypto-a-quick-look-at-the-confidentiality-of-zoom-meetings/\n#\n# Runs on UDP ports 8801–8810 (Zoom standard) plus peer-to-peer on ephemeral ports.\n\nObject<GROUP> ZOOM\n\n# ── SFU encapsulated packets (types 1–5, ports 8801-8810) ────────────────────\nObject<ZOOM> ZOOM_SFU\n    abbrev \"zoom\"\n    col \"Zoom\"\n    starts \"\\x01\\x00\\x02\" \"\\x01\\x00\\x03\" \"\\x02\\x00\\x02\" \"\\x02\\x00\\x03\"\n           \"\\x03\" \"\\x04\" \"\\x05\"\n\n    uint8 sfu_type \"SFU Type\" defaults(1)\n        1 = \"Client→SFU\"\n        2 = \"SFU→Client\"\n        3 = \"Zoom Type 3\"\n        4 = \"Zoom Type 4\"\n        5 = \"SFU with ZME\"\n    uint16 sequence_num \"Sequence Number\" defaults(0)\n    uint32 unknown hex \"Unknown\" defaults(0)\n    uint8 direction \"Direction\" defaults(0)\n        0 = \"To Zoom\"\n        4 = \"From Zoom\"\n\n    when sfu_type == 5:\n        uint8 enc_type \"Media Type\" defaults(15)\n            13 = \"Screen Share\"\n            15 = \"Audio (RTP)\"\n            16 = \"Video (RTP)\"\n            30 = \"P2P Screen Share\"\n            33 = \"RTCP\"\n            34 = \"RTCP\"\n            35 = \"RTCP\"\n        uint32 unknown_1 hex \"Unknown 1\" defaults(0)\n        uint32 unknown_2 hex \"Unknown 2\" defaults(0)\n        uint16 media_seq \"Media Sequence\" defaults(0)\n        uint32 timestamp \"Timestamp\" defaults(0)\n        payload media_payload \"Media Payload\"\n    else:\n        payload payload \"Payload\"\n\n    info \"Zoom SFU type=%u dir=%u\" sfu_type, direction\n\n# ── P2P / ZME-only packets (type 0x1F, no SFU header) ────────────────────────\nObject<ZOOM> ZOOM_P2P\n    abbrev \"zoom\"\n    col \"Zoom\"\n    starts \"\\x1f\\x02\\x01\"\n\n    uint8 enc_type \"ZME Type\" defaults(0x1F)\n        0x1F = \"P2P Connection Setup\"\n    payload body \"Body\"\n\n    info \"Zoom P2P\"\n\nrule udp.port == 8801 => ZOOM\nrule udp.port == 8802 => ZOOM\nrule udp.port == 8803 => ZOOM\nrule udp.port == 8804 => ZOOM\nrule udp.port == 8805 => ZOOM\n\nrule udp.content \"\\x01\\x00\\x02\" => ZOOM\nrule udp.content \"\\x01\\x00\\x03\" => ZOOM\nrule udp.content \"\\x02\\x00\\x02\" => ZOOM\nrule udp.content \"\\x02\\x00\\x03\" => ZOOM\nrule udp.content \"\\x1f\\x02\\x01\" => ZOOM\n\ncolor zoom.sfu_type == 15 => black lightblue\ncolor zoom.sfu_type == 16 => black lightgreen\ncolor zoom.sfu_type == 13 || zoom.sfu_type == 30 => black lightyellow\ncolor zoom.enc_type == 33 || zoom.enc_type == 34 || zoom.enc_type == 35 => black lightgray\ncolor zoom => white darkblue\n",
    NULL
};

#endif /* BUILTIN_PROTOS_H */
